# Stage 1 Outputs — powerups.shop.ts

## Agent 01
1. comment — block comment (lines 1–7)
2. blank — line 8
3. import — `CATALOG` from `'./powerups.types.ts'`
4. import — type-only import of `PowerUpId`, `PowerUpCatalogEntry` from `'./powerups.types.ts'`
5. blank — line 11
6. bind name to function definition — `renderShop` (exported)

## Agent 02
1. comment — block comment spanning lines 1–7
2. blank — line 8
3. import — `CATALOG` from `'./powerups.types.ts'`
4. import — type-only import of `PowerUpId`, `PowerUpCatalogEntry` from `'./powerups.types.ts'`
5. blank — line 11
6. bind name to function definition — `renderShop` (exported)

## Agent 03
1. comment — block comment (lines 1–7)
2. blank — line 8
3. import — `CATALOG` from `./powerups.types.ts`
4. import — type-only import of `PowerUpId`, `PowerUpCatalogEntry` from `./powerups.types.ts`
5. blank — line 11
6. bind name to function definition — `renderShop`, exported in place

## Agent 04
1. comment — block comment (lines 1–7)
2. blank — line 8
3. import — `CATALOG` from `./powerups.types.ts` (line 9)
4. import — type-only import of `PowerUpId`, `PowerUpCatalogEntry` from `./powerups.types.ts` (line 10)
5. blank — line 11
6. bind name to function definition — `renderShop`, exported (lines 12–35)

## Agent 05
1. comment — block comment (lines 1–7)
2. blank (line 8)
3. import — `CATALOG` from `./powerups.types.ts`
4. import — type-only `PowerUpId`, `PowerUpCatalogEntry` from `./powerups.types.ts`
5. blank (line 11)
6. bind name to function definition — `renderShop`, exported
