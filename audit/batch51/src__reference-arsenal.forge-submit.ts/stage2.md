# Stage 2 Outputs — reference-arsenal.forge-submit.ts

## Agent 01

### _submitForge (line 20)

`_submitForge` is an `async` function that accepts a `ForgeFormState` object, a boolean `isEdit` flag, an optional `ArsenalReference` for edit mode, and an `onComplete` callback. Execution begins with a DOM lookup: `document.getElementById('forge-submit')` is cast to `HTMLButtonElement | null` and stored in `submitBtn`. If `submitBtn` is non-null, its `disabled` property is set to `true` and its `textContent` is set to either `'Saving...'` (if `isEdit` is truthy) or `'Forging...'` (if falsy). A `try` block then branches on `isEdit && editRef`: if both are truthy, the edit path executes — `editReference(editRef.id, { source_title, source_author, source_date, locator, claim_text, category })` is awaited; if the resolved `result.action === 'collision'`, `showToast` is called with an error message naming `result.existing_name` and the function returns early from inside the `try` block (the `finally` block still runs). If no collision, `showToast('Reference updated! ✏️', 'success')` is called and `onComplete(editRef.id)` fires, which may close the form and detach `submitBtn` from the DOM. If `isEdit` is false or `editRef` is undefined, the forge path executes — `forgeReference({ source_title, source_author, source_date, locator, claim_text, source_type, category, source_url: state.source_url || undefined })` is awaited; a collision on `result.action === 'collision'` triggers an error toast with a different message text and an early return. Otherwise `showToast('Reference forged! ⚔️', 'success')` fires and `onComplete(result.ref_id || '')` is called. If any awaited call or synchronous operation throws, the `catch` block extracts the message from the `Error` instance if possible, otherwise falls back to `'Edit failed'` or `'Forge failed'` depending on `isEdit`, then calls `showToast(msg, 'error')`. Regardless of which path was taken — success, collision early-return, or caught error — the `finally` block runs: if `submitBtn` is non-null, `submitBtn.disabled` is still `true`, and `submitBtn.isConnected` is `true` (i.e., the button is still attached to the DOM), then `disabled` is set back to `false` and `textContent` is restored to either `'✏️ Save Changes (10t)'` or `'⚔ Forge Weapon (50t)'`; if `submitBtn` has been detached by `onComplete`, the `isConnected` guard is `false` and the re-enable is skipped.

## Agent 02

### _submitForge (line 20)

When `_submitForge` is invoked, it first queries the DOM for an element with id `forge-submit`, coercing the result to `HTMLButtonElement | null`. If that element exists, it is immediately disabled and its label changed to `'Saving...'` or `'Forging...'` based on `isEdit`. The function then enters a `try` block and tests the compound condition `isEdit && editRef`. When this is truthy, an edit flow runs: `editReference` is awaited with `editRef.id` and a subset of `state` fields (`source_title`, `source_author`, `source_date`, `locator`, `claim_text`, `category` cast to `ReferenceCategory`); if the returned `result.action` is the string `'collision'`, `showToast` is called with an error-level message interpolating `result.existing_name`, and the function returns — this early return exits the `try` body, skipping `onComplete`, but the `finally` block executes unconditionally after the return. If no collision is detected in edit mode, a success toast `'Reference updated! ✏️'` is shown and `onComplete(editRef.id)` is called synchronously, potentially unmounting the submit button from the DOM. When `isEdit` is `false` or `editRef` is `undefined`, the forge flow runs: `forgeReference` is awaited with the full state payload, using `state.source_url || undefined` to coerce an empty string to `undefined` before the RPC call; a `'collision'` result triggers an error toast with a different message text and an early return from the `try` block. Otherwise a forge-success toast fires and `onComplete(result.ref_id || '')` is invoked, passing an empty string if `result.ref_id` is falsy. Any exception thrown by `editReference`, `forgeReference`, or any other expression in the `try` block is caught: the `catch` clause derives a message string by checking `err instanceof Error`, falling back to either `'Edit failed'` or `'Forge failed'` based on `isEdit`, then calls `showToast` with severity `'error'`. The `finally` block always runs last: the re-enable of `submitBtn` is conditional on three simultaneous truths — `submitBtn` is non-null, `submitBtn.disabled` is still `true`, and `submitBtn.isConnected` is `true` — only then are `disabled` reset to `false` and `textContent` restored to the appropriate label for the mode.

## Agent 03

### _submitForge (line 20)

`_submitForge` begins by attempting to locate the submit button in the live DOM via `document.getElementById('forge-submit')`; the result is narrowed to `HTMLButtonElement | null` and bound to `submitBtn`. Provided `submitBtn` is non-null, two mutations occur synchronously before any async work: `disabled` is set to `true` (preventing double-submission) and `textContent` is set to `'Saving...'` when `isEdit` is truthy or `'Forging...'` otherwise. Execution then enters a `try/catch/finally` triad. Inside `try`, a single conditional `if (isEdit && editRef)` splits the work into two branches. The edit branch awaits `editReference(editRef.id, partialState)` where `partialState` includes `source_title`, `source_author`, `source_date`, `locator`, `claim_text`, and `category` (cast); upon resolution, if `result.action === 'collision'` the function calls `showToast` with an error toast containing the existing reference name and then executes a `return` statement — this exits the `try` body early but guarantees the `finally` block still fires. If no collision, `showToast('Reference updated! ✏️', 'success')` is called and `onComplete(editRef.id)` executes, which may tear down the form container and disconnect `submitBtn` from the document tree. The forge branch (when `isEdit` is falsy or `editRef` is `undefined`) awaits `forgeReference(fullPayload)` where `source_url` is passed as `state.source_url || undefined` — converting an empty string to `undefined` before the RPC call; a `'collision'` result triggers a distinct error toast message and an early `return`; otherwise `showToast('Reference forged! ⚔️', 'success')` fires followed by `onComplete(result.ref_id || '')` where an empty string guards against a falsy `ref_id`. The `catch` block handles all thrown values: if `err` is an `Error` instance its `.message` is used; otherwise the fallback string is `'Edit failed'` or `'Forge failed'` depending on `isEdit`; `showToast(msg, 'error')` is called. In all code paths — normal completion, early collision return, or caught exception — the `finally` block executes and applies a three-way guard: `submitBtn` must be non-null, `submitBtn.disabled` must still be `true`, and `submitBtn.isConnected` must be `true`; all three must hold before `disabled` is cleared to `false` and `textContent` is restored to either `'✏️ Save Changes (10t)'` or `'⚔ Forge Weapon (50t)'`.

## Agent 04

### _submitForge (line 20)

`_submitForge` is an async function that orchestrates form submission for both new reference creation ("forge") and existing reference editing. On entry, it queries the DOM for an element with id `forge-submit`, casting it to `HTMLButtonElement | null`. If that element exists, it immediately sets `disabled = true` and sets its `textContent` to either `'Saving...'` or `'Forging...'` depending on the `isEdit` flag. The function then enters a `try/catch/finally` block. Inside `try`, it branches on `isEdit && editRef`: if both are truthy, it awaits `editReference(editRef.id, { source_title, source_author, source_date, locator, claim_text, category })` — a network RPC call. When that promise resolves, it checks `result.action === 'collision'`; if so, it calls `showToast(...)` with an error message that embeds `result.existing_name`, then returns early, falling through to `finally`. On a non-collision result, it calls `showToast('Reference updated! ✏️', 'success')` and then invokes `onComplete(editRef.id)`. If `isEdit` is false or `editRef` is undefined, the else branch runs: it awaits `forgeReference({ source_title, source_author, source_date, locator, claim_text, source_type, category, source_url: state.source_url || undefined })`. On resolution, it again checks `result.action === 'collision'` and toasts an error if true, returning early. On success, it toasts `'Reference forged! ⚔️'` and calls `onComplete(result.ref_id || '')`, where the `|| ''` guards against a nullish `ref_id`. If either awaited call throws, the `catch` block fires: it extracts `err.message` if `err` is an `Error` instance, otherwise falls back to `'Edit failed'` or `'Forge failed'` based on `isEdit`, then calls `showToast(msg, 'error')`. The `finally` block always runs last: it re-enables the submit button only if `submitBtn` is non-null, `submitBtn.disabled` is still `true`, and `submitBtn.isConnected` is `true`. If `onComplete` already closed the form and detached the button, `isConnected` will be false and the re-enable is skipped. If re-enabling, it sets `disabled = false` and restores `textContent` to either `'✏️ Save Changes (10t)'` or `'⚔ Forge Weapon (50t)'`.

## Agent 05

### _submitForge (line 20)

`_submitForge` begins by performing a DOM lookup via `document.getElementById('forge-submit')`, typed as `HTMLButtonElement | null`. If the button is found, two mutations occur immediately and synchronously: `disabled` is set to `true` (blocking further clicks) and `textContent` is set to `'Saving...'` for edit mode or `'Forging...'` for forge mode. Control then enters a `try` block. The first branch condition is `isEdit && editRef` — both must be truthy. When true, it makes an async call to `editReference` with `editRef.id` and a payload of six fields drawn from `state`. Execution suspends until the promise settles. On fulfillment, `result.action` is compared to the string `'collision'`; a match causes an error toast whose message interpolates `result.existing_name`, followed by an early `return` that skips `onComplete`. A non-collision path calls `showToast` with a success message and then invokes `onComplete(editRef.id)` — this callback is the caller's hook to close the form or navigate away, potentially detaching the submit button from the DOM as a side effect relevant to `finally`. When `isEdit` is false or `editRef` is falsy, the else branch runs `forgeReference` with eight fields including `source_url` where an empty string is converted to `undefined` via `|| undefined`. After awaiting, collision detection mirrors the edit path but with a distinct user-facing message. On success, `showToast` fires and `onComplete` receives `result.ref_id || ''` — the empty-string fallback handles a missing or null `ref_id` from the server. Any thrown exception is caught: if the thrown value is an `Error`, its `.message` is used; otherwise the fallback message is chosen by `isEdit`. Either way, `showToast(msg, 'error')` is called. The `finally` block executes unconditionally after the try or catch path completes. It evaluates three conditions: `submitBtn` must be non-null, `submitBtn.disabled` must still be `true`, and `submitBtn.isConnected` must be `true` — if `onComplete` already removed the form from the DOM, `isConnected` will be `false` and the button state restoration is silently skipped. If all three are satisfied, `disabled` is set to `false` and `textContent` is restored to `'✏️ Save Changes (10t)'` or `'⚔ Forge Weapon (50t)'` depending on `isEdit`.
