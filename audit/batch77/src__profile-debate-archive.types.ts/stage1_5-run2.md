# Anchor List — profile-debate-archive.types.ts

(No function definitions found)

## Resolution notes

Two exported interface bindings (ArchiveEntry, RecentDebate) — interface declarations are type-only, not callable functions. No candidates to include.
