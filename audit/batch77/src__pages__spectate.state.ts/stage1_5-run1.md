# Anchor List — spectate.state.ts

(No function definitions found)

## Resolution notes

This file contains zero function definitions. It consists entirely of:
- Block comments (lines 1-3, 5-7)
- Type imports (lines 9-10)
- A single exported object literal binding named `state` (lines 12-28)

The `state` object is a data structure (object literal), not a callable function definition, and therefore does not meet the inclusion criteria.
