# Anchor List — arena-room-end-after-effects.ts

1. renderAfterEffects  (line 9)

## Resolution notes

- renderChain (line 24): excluded — inner function declaration defined inside the body of `renderAfterEffects`, not a top-level binding.
- renderInventoryEvent (line 43): excluded — inner function declaration defined inside the body of `renderAfterEffects`, not a top-level binding.
