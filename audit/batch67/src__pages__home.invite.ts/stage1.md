# Stage 1 Outputs — home.invite.ts

## Agent 01
1. comment — block comment (lines 1–7)
2. blank (line 8)
3. import — `safeRpc` from `../auth.ts` (line 9)
4. import — type-only import of `InviteStats` from `./home.invite-types.ts` (line 10)
5. import — `renderInvite` from `./home.invite-render.ts` (line 11)
6. import — `openClaimSheet` from `./home.invite-sheet.ts` (line 12)
7. blank (line 13)
8. comment — section comment `// ── Module state ───…` (line 14)
9. blank (line 15)
10. bind name to value — `_sheetCleanup` (line 16)
11. blank (line 17)
12. comment — section comment `// ── Entry points ───…` (line 18)
13. blank (line 19)
14. bind name to function definition — `loadInviteScreen`, exported (lines 20–41)
15. blank (line 42)
16. bind name to function definition — `cleanupInviteScreen`, exported (lines 43–45)

## Agent 02
1. comment — block comment (lines 1–7)
2. blank — (line 8)
3. import — `safeRpc` from `'../auth.ts'` (line 9)
4. import — type-only import of `InviteStats` from `'./home.invite-types.ts'` (line 10)
5. import — `renderInvite` from `'./home.invite-render.ts'` (line 11)
6. import — `openClaimSheet` from `'./home.invite-sheet.ts'` (line 12)
7. blank — (line 13)
8. comment — section comment `// ── Module state ───…` (line 14)
9. blank — (line 15)
10. bind name to value — `_sheetCleanup` (line 16)
11. blank — (line 17)
12. comment — section comment `// ── Entry points ───…` (line 18)
13. blank — (line 19)
14. bind name to function definition — `loadInviteScreen`, exported (lines 20–41)
15. blank — (line 42)
16. bind name to function definition — `cleanupInviteScreen`, exported (lines 43–45)

## Agent 03
1. comment — block comment spanning lines 1–7 (JSDoc header)
2. blank — line 8
3. import — `safeRpc` from `'../auth.ts'`
4. import — type-only import of `InviteStats` from `'./home.invite-types.ts'`
5. import — `renderInvite` from `'./home.invite-render.ts'`
6. import — `openClaimSheet` from `'./home.invite-sheet.ts'`
7. blank — line 13
8. comment — line 14 (`// ── Module state …`)
9. blank — line 15
10. bind name to value — `_sheetCleanup` (let, initialized to `null`)
11. blank — line 17
12. comment — line 18 (`// ── Entry points …`)
13. blank — line 19
14. bind name to function definition — `loadInviteScreen` (exported async function)
15. blank — line 41
16. bind name to function definition — `cleanupInviteScreen` (exported function)

## Agent 04
1. comment — block comment (lines 1–7)
2. blank (line 8)
3. import — `safeRpc` from `'../auth.ts'` (line 9)
4. import — type-only import of `InviteStats` from `'./home.invite-types.ts'` (line 10)
5. import — `renderInvite` from `'./home.invite-render.ts'` (line 11)
6. import — `openClaimSheet` from `'./home.invite-sheet.ts'` (line 12)
7. blank (line 13)
8. comment — section comment `// ── Module state ───…` (line 14)
9. blank (line 15)
10. bind name to value — `_sheetCleanup` (line 16)
11. blank (line 17)
12. comment — section comment `// ── Entry points ───…` (line 18)
13. blank (line 19)
14. bind name to function definition — `loadInviteScreen`, exported (lines 20–41)
15. blank (line 42)
16. bind name to function definition — `cleanupInviteScreen`, exported (lines 43–45)

## Agent 05
1. comment — block comment spanning lines 1–7 (JSDoc header)
2. blank — line 8
3. import — `safeRpc` from `'../auth.ts'`
4. import — type-only import of `InviteStats` from `'./home.invite-types.ts'`
5. import — `renderInvite` from `'./home.invite-render.ts'`
6. import — `openClaimSheet` from `'./home.invite-sheet.ts'`
7. blank — line 13
8. comment — line 14 (`// ── Module state …`)
9. blank — line 15
10. bind name to value — `_sheetCleanup` (let binding, initialized to `null`)
11. blank — line 17
12. comment — line 18 (`// ── Entry points …`)
13. blank — line 19
14. bind name to function definition — `loadInviteScreen` (exported async function)
15. blank — line 42
16. bind name to function definition — `cleanupInviteScreen` (exported function)
