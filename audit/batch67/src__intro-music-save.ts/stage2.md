# Stage 2 Outputs — intro-music-save.ts

## Agent 01

### saveIntroMusic

`saveIntroMusic` is an async function that accepts three parameters: `trackId` (a string identifying which track was chosen), `file` (a `File` object or `null`), and `existingUrl` (a string, `null`, or `undefined` representing a previously uploaded URL). It returns `Promise<void>` and throws on any error rather than returning an error value.

The function begins with a branch on whether `trackId === 'custom'`. If it is, there are three sub-branches. If `file` is truthy, it calls `getSupabaseClient()` (an import from `auth.ts`) and throws `'Not connected'` if the client is absent. It then calls `getCurrentProfile()` and throws `'Not signed in'` if no profile is returned. It derives a storage path by splitting `file.name` on `'.'`, taking the last segment as the extension (defaulting to `'mp3'`), and composing the path as `${profile.id}/intro.${ext}`. It then awaits `client.storage.from('intro-music').upload(path, file, { upsert: true, contentType: file.type })`. If `upErr` is set, it throws with `upErr.message`. On success, it immediately awaits `client.storage.from('intro-music').createSignedUrl(path, 31536000)` (one year in seconds). If `signErr` is set or `signedData.signedUrl` is falsy, it throws `'Could not get URL'`; otherwise it assigns the signed URL to the local variable `uploadedUrl`. If `file` is falsy but `existingUrl` is truthy, the second sub-branch assigns `existingUrl` directly to `uploadedUrl` without any network call. If both `file` and `existingUrl` are falsy, the third sub-branch throws `'No file selected'`. When `trackId` is anything other than `'custom'`, the entire storage block is skipped and `uploadedUrl` remains `undefined`.

After the conditional storage block, the function always proceeds to await `safeRpc('save_intro_music', { p_track_id: trackId, p_custom_url: uploadedUrl ?? null })`. If the returned `error` field is set, the function throws using `error.message` or the fallback string `'Save failed'`. It then casts `data` to `{ error?: string } | null` and, if `result?.error` is set, throws with that application-level error string. Finally, the function calls `getCurrentProfile()` a second time. If a profile object is returned, it mutates two properties on it in place — `intro_music_id` is set to `trackId` and `custom_intro_url` is set to `uploadedUrl ?? null` — updating whatever in-memory profile object `getCurrentProfile()` returns by reference. There is no try/catch block anywhere in the function; all error paths propagate by throwing.

---

## Agent 02

### saveIntroMusic

`saveIntroMusic` is an `async` function that accepts three parameters: `trackId` (a string), `file` (a `File` object or `null`), and `existingUrl` (a string, `null`, or `undefined`). It does not read any module-level state directly, but it calls `getCurrentProfile()` and `getSupabaseClient()` from the auth module, which return values from their own internal state. It writes to the in-memory profile object in place and returns `void` (or throws on error).

The function begins by declaring a local variable `uploadedUrl` (initially `undefined`) and entering a branch conditioned on `trackId === 'custom'`. Inside that branch it checks whether `file` is truthy. If so, it calls `getSupabaseClient()` synchronously; if that returns falsy it immediately throws `'Not connected'`. It then calls `getCurrentProfile()` synchronously; if that returns falsy it throws `'Not signed in'`. With both available, it derives the storage path by splitting `file.name` on `.` to extract the extension (defaulting to `'mp3'` if no extension exists) and composing `<profile.id>/intro.<ext>`. It then `await`s a Storage upload via `client.storage.from('intro-music').upload(path, file, { upsert: true, contentType: file.type })`; if `upErr` is truthy it throws the error message. On success it `await`s `client.storage.from('intro-music').createSignedUrl(path, 31536000)` (one year TTL); if `signErr` is truthy or `signedData.signedUrl` is absent it throws `'Could not get URL'`, otherwise it assigns `signedData.signedUrl` to `uploadedUrl`. If `file` is falsy but `existingUrl` is truthy, `uploadedUrl` is set to `existingUrl`. If both are falsy, it throws `'No file selected'`. If `trackId` is not `'custom'`, the entire inner block is skipped and `uploadedUrl` remains `undefined`.

After the `trackId` branch, the function unconditionally `await`s `safeRpc('save_intro_music', { p_track_id: trackId, p_custom_url: uploadedUrl ?? null })`. If the returned `error` is truthy it throws using the error's `.message` property (falling back to `'Save failed'`). It then casts `data` to `{ error?: string } | null` and throws `result.error` if that field is present. On the success path it calls `getCurrentProfile()` a second time; if the returned profile object is truthy it mutates it in place, setting `intro_music_id` to `trackId` and `custom_intro_url` to `uploadedUrl ?? null`. No value is returned. There is no `try/catch` or `finally` block anywhere in the function, so all thrown errors propagate to the caller.

---

## Agent 03

### saveIntroMusic

`saveIntroMusic` is an `async` function that accepts three parameters: `trackId` (a string), `file` (a `File` object or `null`), and `existingUrl` (a string, `null`, or `undefined`). It returns `Promise<void>`. It declares one local variable, `uploadedUrl`, which starts as `undefined`.

The function enters a conditional block only when `trackId === 'custom'`. Inside that branch, it first checks whether `file` is truthy. If so, it calls `getSupabaseClient()` — a synchronous import from `./auth.ts` — and throws `'Not connected'` immediately if the result is falsy. It then calls `getCurrentProfile()` (also synchronous, from `./auth.ts`) and throws `'Not signed in'` if the result is falsy. With both the client and profile in hand, it derives a storage path by splitting `file.name` on `.` and taking the last segment (defaulting to `'mp3'`), then formatting it as `<profile.id>/intro.<ext>`. It awaits `client.storage.from('intro-music').upload(path, file, { upsert: true, contentType: file.type })`. If the upload returns a non-null `error`, it throws a new `Error` wrapping the error's message. It then awaits `client.storage.from('intro-music').createSignedUrl(path, 31536000)` (a one-year TTL). If that call returns an error or if `signedData.signedUrl` is absent, it throws `'Could not get URL'`. Otherwise it assigns the signed URL to `uploadedUrl`. If `file` was falsy but `existingUrl` is truthy, the `else if` branch simply assigns `existingUrl` to `uploadedUrl` without any awaits. If both `file` and `existingUrl` are falsy, the final `else` branch throws `'No file selected'`. None of these sub-branches is reached when `trackId` is anything other than `'custom'`, leaving `uploadedUrl` as `undefined` in that case.

After the `trackId` conditional, the function always calls `safeRpc('save_intro_music', { p_track_id: trackId, p_custom_url: uploadedUrl ?? null })` and awaits the result. `safeRpc` is imported from `./auth.ts` and handles 401 retries internally. If the returned `error` is truthy, the function throws using `error.message` with a fallback of `'Save failed'`. It then casts `data` to `{ error?: string } | null` and, if `result.error` is present, throws that string as an `Error`. If both checks pass, it calls `getCurrentProfile()` a second time. If the returned profile object is truthy, it mutates two properties on it directly via a `Record<string, unknown>` cast: `intro_music_id` is set to `trackId`, and `custom_intro_url` is set to `uploadedUrl ?? null`. This in-place mutation updates module-level state held inside the auth module without any further persistence call. The function has no explicit `try/catch` and no `finally` block; any thrown error propagates to the caller unwrapped.

---

## Agent 04

### saveIntroMusic

`saveIntroMusic` is an `async` function that accepts three parameters: `trackId` (a string), `file` (a `File` object or `null`), and `existingUrl` (a string, `null`, or `undefined`). It returns `Promise<void>`. It declares one local variable, `uploadedUrl`, initially `undefined`.

The function enters a branch only when `trackId === 'custom'`. Inside that branch, it checks whether `file` is truthy. If `file` is provided, it calls `getSupabaseClient()` — if that returns a falsy value it throws `'Not connected'` immediately. It then calls `getCurrentProfile()`; if that returns a falsy value it throws `'Not signed in'`. With both resolved, it derives a storage path by splitting `file.name` on `'.'` and taking the last segment as the extension (defaulting to `'mp3'` if none exists), then composing `${profile.id}/intro.${ext}`. It awaits `client.storage.from('intro-music').upload(path, file, { upsert: true, contentType: file.type })`. If that call returns an error object, it throws using the error's message. It then awaits `client.storage.from('intro-music').createSignedUrl(path, 60 * 60 * 24 * 365)` to generate a one-year signed URL. If that returns an error or no `signedUrl`, it throws `'Could not get URL'`. Otherwise `uploadedUrl` is set to the signed URL string. If `file` is falsy but `existingUrl` is truthy, `uploadedUrl` is set to `existingUrl` directly. If both are falsy, the function throws `'No file selected'`. When `trackId` is anything other than `'custom'`, the entire storage block is skipped and `uploadedUrl` remains `undefined`.

After the `trackId` branch, the function unconditionally awaits `safeRpc('save_intro_music', { p_track_id: trackId, p_custom_url: uploadedUrl ?? null })`. If the returned `error` is truthy, it throws using `error.message` if present, otherwise `'Save failed'`. It then casts `data` to `{ error?: string } | null` and throws with `result.error` if that field is present. On success, it calls `getCurrentProfile()` a second time (there is no guarantee the profile object returned here is the same reference as the one retrieved earlier, though in practice module-level singletons make it likely). If the profile is truthy, it directly mutates the profile object by setting `intro_music_id` to `trackId` and `custom_intro_url` to `uploadedUrl ?? null`. There is no try/catch in this function; any thrown error propagates to the caller.

---

## Agent 05

### saveIntroMusic

`saveIntroMusic` is an async function that accepts three parameters: `trackId` (a string identifying the selected music track), `file` (a `File` object or `null` representing a newly chosen audio file), and `existingUrl` (a string, `null`, or `undefined` representing a previously uploaded URL). It returns `Promise<void>` and propagates all errors as thrown `Error` instances rather than returning them.

The function opens with a `let uploadedUrl: string | undefined` that starts undefined. It enters a branch only when `trackId === 'custom'`. Inside that branch, if `file` is truthy, it calls `getSupabaseClient()` and throws `'Not connected'` if the client is absent. It then calls `getCurrentProfile()` and throws `'Not signed in'` if no profile is present. It derives a storage path by extracting the file extension from `file.name` (defaulting to `'mp3'` if no extension is found) and building the path `<profile.id>/intro.<ext>`. It then awaits `client.storage.from('intro-music').upload(path, file, { upsert: true, contentType: file.type })`, throwing with the storage error message if `upErr` is set. On success it immediately awaits `client.storage.from('intro-music').createSignedUrl(path, 31536000)` (a one-year TTL), throwing `'Could not get URL'` if `signErr` is set or if `signedData.signedUrl` is falsy. If both storage calls succeed, `uploadedUrl` is assigned the signed URL string. If `file` is falsy but `existingUrl` is truthy, `uploadedUrl` is assigned `existingUrl` directly. If both are falsy, the function throws `'No file selected'` immediately without reaching the RPC call.

After the `trackId === 'custom'` block (or if `trackId` is any other value), the function awaits `safeRpc('save_intro_music', { p_track_id: trackId, p_custom_url: uploadedUrl ?? null })`. If `safeRpc` returns a truthy `error`, the function throws using the error's `message` property or the fallback string `'Save failed'`. It then casts `data` to `{ error?: string } | null` and, if `data.error` is present, throws with that string. On a fully successful path, the function calls `getCurrentProfile()` a second time (independent of the earlier call inside the `custom` branch) and, if a profile object is returned, mutates it in place by setting `intro_music_id` to `trackId` and `custom_intro_url` to `uploadedUrl` or `null`. The function has no explicit `try/catch` block; all errors propagate to the caller. There is no return value.
