# Anchor List — profile-debate-archive.render.ts

1. renderTable  (line 13)
2. wireTable  (line 92)

## Resolution notes

- `renderTable` (line 13): Exported function declaration. Builds archive table HTML, sets container.innerHTML, calls wireTable.
- `wireTable` (line 92): Non-exported function declaration. Wires all event listeners.
- All inline callbacks inside wireTable: Excluded — anonymous.
- All import bindings: Excluded — none defined here.
- No module-level const/let non-function bindings, type aliases, interfaces, or side-effect statements in this file.
- Both arbiter runs independently produced the same 2-entry anchor list.
