# Stage 2 Outputs — arena-room-end.ts

## Agent 01

### endCurrentDebate (line 23)
Early-return if `view === 'postDebate'` (line 24). Sets `view` to `'postDebate'` via `set_view`, calls `pushArenaState('postDebate')` (lines 25-26). If `roundTimer` truthy, clears interval (line 27). Calls `stopReferencePoll()`, `stopModStatusPoll()`, `stopOpponentPoll()` (lines 28-30). Removes `'mod-request-modal'` DOM element (line 31). Reads `currentDebate!` into local `debate` (line 33). Snapshots `citedRefs` from `loadedRefs.filter(cited)` if `debate.mode === 'live'`, else empty array (line 36). If `debate.mode === 'live'`: calls `cleanupFeedRoom()` and `leaveDebate()` (lines 40-42). If `debate._nulled`: calls `renderNulledDebate(debate)` and returns early (lines 46-49). Awaits `generateScores(debate)` for `scoreA`, `scoreB`, `aiScores`, `winner` (line 51). Initializes `eloChangeMe = 0`, `endOfDebateBreakdown = null` (lines 53-54). If `!modView && !isPlaceholder() && !id.startsWith('ai-local-') && !id.startsWith('placeholder-') && mode !== 'ai'`: awaits `applyEndOfDebateModifiers(debate, scoreA, scoreB)`, updates `scoreA`, `scoreB`, `endOfDebateBreakdown` (lines 59-64). If `!modView && !isPlaceholder() && !id.startsWith('ai-local-') && !id.startsWith('placeholder-')`: awaits `finalizeDebate(...)`, updates `winner`, `scoreA`, `scoreB`, `eloChangeMe` (lines 67-73). Normalizes nulls: `scoreA ?? 0`, `scoreB ?? 0`, `winner || 'draw'` (lines 76-78). Computes `isDraw`, `didWin`; calls `nudge()` with outcome message (lines 80-84). If `silenceTimer`: clears interval, `set_silenceTimer(null)` (line 87). Calls `removeShieldIndicator()`, `set_shieldActive(false)`, `activatedPowerUps.clear()` (lines 88-90). Removes DOM elements `'powerup-silence-overlay'`, `'powerup-reveal-popup'` (lines 91-92). Calls `getCurrentProfile()` for `myName` (line 94). Calls `renderPostDebate(debate, {...})` with aggregated results (lines 97-99). Async: 3 await points. Note: line 59 guard includes `mode !== 'ai'`; line 67 guard does not — overlapping predicate (LM-END-002, already documented).

## Agent 02

### endCurrentDebate (line 23)
Guard: `if (view === 'postDebate') return` (line 24). Writes `set_view('postDebate')`, `pushArenaState('postDebate')` (lines 25-26). Reads `roundTimer`, clears if truthy (line 27). Calls `stopReferencePoll()`, `stopModStatusPoll()`, `stopOpponentPoll()` (lines 28-30). Removes `'mod-request-modal'` (line 31). Reads `currentDebate!` → `debate` (line 33). Snapshots `citedRefs` (filtered if live mode, else `[]`) (line 36). Branches `debate.mode === 'live'`: calls `cleanupFeedRoom()`, `leaveDebate()` (lines 40-42). Branches `debate._nulled`: calls `renderNulledDebate(debate)`, returns (lines 46-49). Awaits `generateScores(debate)` → `{scoreA, scoreB, aiScores, winner}` (line 51). Initializes `eloChangeMe = 0`, `endOfDebateBreakdown = null` (lines 53-54). 5-part guard (line 59): awaits `applyEndOfDebateModifiers()`, reassigns `scoreA`, `scoreB`, `endOfDebateBreakdown` (lines 60-63). 4-part guard (line 67, missing `mode !== 'ai'`): awaits `finalizeDebate()`, reassigns `winner`, `scoreA`, `scoreB`, `eloChangeMe` (lines 68-72). Normalizes `scoreA/B` to 0, `winner` to 'draw' if falsy (lines 76-78). Computes `isDraw`, `didWin`; calls `nudge()` (line 84). Clears `silenceTimer` (line 87). Calls `removeShieldIndicator()`, `set_shieldActive(false)`, `activatedPowerUps.clear()` (lines 88-90). DOM: removes silence overlay and reveal popup (lines 91-92). Reads profile `display_name` → `myName` (lines 94-95). Calls `renderPostDebate()` (lines 97-99).

## Agent 03

### endCurrentDebate (line 23)
Returns early if `view === 'postDebate'` (line 24). Sets `view` → `'postDebate'`, pushes arena state (lines 25-26). Clears `roundTimer` if truthy (line 27). Calls stop poll functions (lines 28-30). Removes mod-request-modal (line 31). Reads `currentDebate!` → `debate` (line 33). Computes `citedRefs` conditional on live mode (line 36). Live mode branch: `cleanupFeedRoom()`, `leaveDebate()` (lines 38-42). Nulled branch: `renderNulledDebate()`, return (lines 46-49). Awaits `generateScores(debate)` (line 51). Initializes locals (lines 53-54). Modifier branch (5-part guard, line 59): awaits `applyEndOfDebateModifiers()`, updates `scoreA`, `scoreB`, `endOfDebateBreakdown` (lines 60-63). Finalize branch (4-part guard, line 67): awaits `finalizeDebate()`, updates `winner`, `scoreA`, `scoreB`, `eloChangeMe` (lines 68-72). Score normalization (lines 76-78). Nudge dispatch with `isDraw`/`didWin` logic (lines 80-84). Power-up cleanup: clear silenceTimer, shield, activatedPowerUps, DOM overlays (lines 87-92). Profile read → `myName` (lines 94-95). `renderPostDebate()` (lines 97-99). Async: 3 await points.

## Agent 04

### endCurrentDebate (line 23)
Guard on `view === 'postDebate'`, returns (line 24). `set_view('postDebate')`, `pushArenaState('postDebate')` (lines 25-26). Conditional `clearInterval(roundTimer)` (line 27). `stopReferencePoll()`, `stopModStatusPoll()`, `stopOpponentPoll()` (lines 28-30). DOM: `getElementById('mod-request-modal')?.remove()` (line 31). `currentDebate!` → `debate` (line 33). `citedRefs` snapshot (line 36). `debate.mode === 'live'` branch: `cleanupFeedRoom()`, `leaveDebate()` (lines 38-43). `debate._nulled` branch: `renderNulledDebate()`, return (lines 46-49). `await generateScores(debate)` → `{scoreA, scoreB, aiScores, winner}` (line 51). Init `eloChangeMe = 0`, `endOfDebateBreakdown = null` (lines 53-54). 5-part modifier guard (line 59), `await applyEndOfDebateModifiers()` → updates scores (lines 60-63). 4-part finalize guard (line 67), `await finalizeDebate()` → updates winner/scores/elo (lines 68-72). Null normalization (lines 76-78). `isDraw`, `didWin` → `nudge()` (lines 80-84). `silenceTimer` clear → `set_silenceTimer(null)` (line 87). `removeShieldIndicator()`, `set_shieldActive(false)`, `activatedPowerUps.clear()` (lines 88-90). DOM remove silence overlay, reveal popup (lines 91-92). `getCurrentProfile()` → `myName` (lines 94-95). `renderPostDebate()` (lines 97-99).

## Agent 05

### endCurrentDebate (line 23)
Early-return: `view === 'postDebate'` (line 24). State writes: `set_view('postDebate')`, `pushArenaState('postDebate')` (lines 25-26). Cleanup: conditional `clearInterval(roundTimer)`, stop three poll functions, remove mod-request-modal (lines 27-31). Reads `currentDebate!` → `debate`, snapshots `citedRefs` (lines 33-36). Live mode branch: `cleanupFeedRoom()`, `leaveDebate()` (lines 38-43). Nulled early return: `renderNulledDebate()`, return (lines 46-49). `await generateScores(debate)` (line 51). Initialize `eloChangeMe`, `endOfDebateBreakdown` (lines 53-54). 5-part guard → `await applyEndOfDebateModifiers()` → reassign `scoreA`, `scoreB`, `endOfDebateBreakdown` (lines 59-63). 4-part guard (missing `mode !== 'ai'`) → `await finalizeDebate()` → reassign `winner`, `scoreA`, `scoreB`, `eloChangeMe` (lines 67-72). Null normalization (lines 76-78). Compute outcome, call `nudge()` (lines 80-84). Power-up cleanup: `silenceTimer`, shield, overlays (lines 87-92). Profile lookup → `myName` (lines 94-95). `renderPostDebate()` (lines 97-99).
