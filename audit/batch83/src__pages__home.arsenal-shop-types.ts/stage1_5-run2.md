# Anchor List — home.arsenal-shop-types.ts

(none)

## Resolution notes

All five agents agree unanimously: the file contains exactly two named exports — the type alias `ProductType` (line 8) and the interface `ShopState` (lines 10–18) — plus one `import type` statement and a block comment. There are zero top-level function definitions. The anchor list is empty.
