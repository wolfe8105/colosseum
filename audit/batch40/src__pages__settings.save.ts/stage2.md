# Stage 2 Outputs — settings.save.ts

## Agent 01

### saveSettings

The function reads the `isPlaceholder` module-level state variable, reads from the DOM using element IDs ('save-btn', 'set-display-name', 'set-username', 'set-bio', 'set-email-display', 'set-language', and several checkbox elements prefixed with 'set-notif-' and 'set-audio-' and 'set-privacy-'), reads the disabled state and textContent of the save button, and reads the value properties of input and textarea elements. The function writes to the DOM by setting the save button's disabled state and textContent properties, and writes to localStorage by calling `localStorage.setItem('colosseum_settings', JSON.stringify(settings))` with the constructed settings object.

The function starts by retrieving the save button and returning early if it is already disabled or does not exist. It then disables the button and changes its text to '⏳ Saving...'. It reads input values from three form fields and trims them. It validates the username with early returns: if a username is provided, it must be 3-20 characters and match `/^[a-zA-Z0-9_]+$/`, otherwise the function shows a toast, re-enables the button, and returns. Similarly, it validates displayName (max 30 characters) and bio (max 160 characters) with identical early-return paths that show a toast and restore the button. After validation passes, it constructs a `settings` object by reading 14 properties from the DOM (input values, checkbox states, select value, and text content). It writes this object to localStorage. Then, if `isPlaceholder` is falsy, it calls `updateProfile` as fire-and-forget with a `.catch()` that logs warnings, and calls `safeRpc('save_user_settings', {...})` also as fire-and-forget with `.then()` and `.catch()` handlers that log warnings. Finally, it shows a success toast, re-enables the button, and restores its text to '💾 SAVE CHANGES'. A known issue documented by comment LM-SET-002 is that if `updateProfile` throws an unhandled exception, the save button remains disabled since there is no try/finally block.

## Agent 02

### saveSettings

The function reads from the DOM by calling `getEl()` to fetch input and select elements for display name, username, bio, email display, language preference, and checkbox states for notifications, audio, and privacy settings. It also reads the module-level `isPlaceholder` boolean.

The function validates the username, display name, and bio fields against length and character constraints. If username is non-empty, it must be 3–20 characters and match `/^[a-zA-Z0-9_]+$/`. If display name is non-empty, it must be at most 30 characters. Bio must be at most 160 characters. On any validation failure, it calls `toast()` with an error message, re-enables the save button, restores its text to '💾 SAVE CHANGES', and returns early. The function also gates itself: if the save button is already disabled when the function is called, it returns immediately without processing.

After validation passes, the function constructs a `SettingsData` object with all settings and writes it to `localStorage` under the key 'colosseum_settings' as a JSON string. It then checks `isPlaceholder`: if false, it calls `updateProfile()` and `safeRpc('save_user_settings', ...)` as fire-and-forget async operations. The `updateProfile()` call is not awaited and attaches a `.catch()` that logs a warning if it fails. The `safeRpc()` call is not awaited; its `.then()` examines the result for an error field and logs a warning if present, and its `.catch()` logs a warning if the RPC itself throws. Whether or not these calls are made, the function calls `toast()` to display a success message, re-enables the save button, and restores its text. The function returns void in all paths. There is a documented landmine comment noting that the save button remains disabled if `updateProfile()` throws an unhandled exception, since there is no try/finally block.

## Agent 03

### saveSettings

The function is synchronous and void. It reads the module-level state variable `isPlaceholder` and reads six DOM elements by ID using `getEl`. It also reads the checked state of eight checkbox elements via `getChecked`. The function exits early with a return if the save button is already disabled (line 17). On early exit validation failures (lines 24–38), it displays a toast error message via `toast()` and resets the button state before returning. The function builds a `SettingsData` object by trimming input field values and collecting checkbox states. It writes this object to `localStorage` under the key `'colosseum_settings'` as a JSON string (line 59). If `isPlaceholder` is false, it calls `updateProfile()` asynchronously as a fire-and-forget operation with a `.catch()` that logs warnings on failure. In the same branch, it calls `safeRpc('save_user_settings', {...})` chained with `.then()` to check the response for an error field and log a warning if present, and a `.catch()` to log any thrown errors. In all paths, the save button is disabled at the start and re-enabled at the end of execution. A comment at line 13–14 flags a landmine: if `updateProfile` throws an unhandled exception before the button state is restored, the button remains disabled since there is no try/finally.

## Agent 04

### saveSettings

The function is synchronous and returns void. It reads the DOM through `getEl` to retrieve the save button element, display name input, username input, bio textarea, email display element, language select, and checkbox elements for various notification and privacy settings. It also reads the module-level constant `isPlaceholder`. The function performs three validation branches: it checks if the save button is already disabled and returns early if so; it validates the username against length (3-20 characters) and character constraints; it validates display name length (max 30 characters); and it validates bio length (max 160 characters). On any validation failure, it toggles the save button back to enabled, calls `toast` with an error message, and returns early.

If all validations pass, the function constructs a `SettingsData` object from the DOM values and writes it to localStorage as a stringified JSON value under the key 'colosseum_settings'. When `isPlaceholder` is false, the function calls `updateProfile` with a subset of the settings without awaiting it, attaching a `.catch()` handler that logs warnings. It also calls `safeRpc('save_user_settings', {...})` without awaiting, but chaining `.then()` to check for error responses and `.catch()` for network/RPC errors. On the success path, the function displays a success toast and re-enables the save button. If `updateProfile` or `safeRpc` throw exceptions, those errors are caught by their attached handlers and logged; the save button will not be re-enabled in these error cases, creating a known footgun documented in the landmine comment.

## Agent 05

### saveSettings

Reads the save button element from the DOM by ID `save-btn`, the module-level boolean `isPlaceholder`, and the current values of input/textarea/select elements from the DOM. Returns void. Early returns if the save button is already disabled, or if validation fails. First validates username (if provided) as 3–20 characters matching `/^[a-zA-Z0-9_]+$/`; display name (if provided) as max 30 characters; and bio as max 160 characters. If any validation fails, the function shows a toast error message, re-enables the save button with text `💾 SAVE CHANGES`, and returns. At the start, the function disables the save button and changes its text to `⏳ Saving...`.

On the success path, constructs a `settings` object of type `SettingsData` from the DOM values and writes it to localStorage under key `colosseum_settings` as JSON. If `isPlaceholder` is false, calls `updateProfile()` with four fields plus a computed `is_private` field (inverted `privacy_public` checkbox), but does not await — attaches `.catch()` that logs warnings. Also calls `safeRpc('save_user_settings', {...})` without awaiting, with `.then()` to check for error object and log warning, and `.catch()` for errors. Finally, shows success toast `✅ Settings saved`, re-enables the save button, and sets its text back to `💾 SAVE CHANGES`. Both async calls execute in the background; the function does not wait for either.
