# Anchor List — reference-arsenal.constants.ts

(Empty — no function definitions found)

## Resolution notes

The source file contains only type imports and exported const bindings to data structures (objects and arrays). No top-level named callable bindings exist. All five stage 1 agents correctly identified these as value bindings, not function definitions.
