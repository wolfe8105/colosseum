# Anchor List — src/powerups.ts

Source: src/powerups.ts
Produced by: stage 1.5 (arbiter + optional reconciliation)
Unresolved items: 0

(No function definitions found — pure barrel/re-export module)

## Resolution notes

Both arbiter runs agreed unanimously. No function definitions exist in this file. The `powerups` object literal (lines 24–28) is not a function. All implementations live in the sub-modules this barrel re-exports.
