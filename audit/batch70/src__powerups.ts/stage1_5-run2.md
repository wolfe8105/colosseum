# Anchor List — src/powerups.ts (Arbiter Run 2)

(No function definitions found)

## Resolution notes

The source file contains only re-exports and imports from submodules. The `powerups` object literal (lines 24–28) is not a function definition — it is an object literal aggregating imported bindings. Anchor list is empty.
