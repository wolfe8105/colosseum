# Stage 2 Outputs — src/powerups.ts

The anchor list for this file is empty. `src/powerups.ts` is a pure barrel module containing only re-exports, imports, and an object literal binding. There are no top-level function definitions to walk.

No Stage 2 agent output produced (no anchor entries to walk).
