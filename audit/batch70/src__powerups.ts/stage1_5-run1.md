# Anchor List — src/powerups.ts (Arbiter Run 1)

(No function definitions found)

## Resolution notes

The file is a pure re-export and aggregation module. No top-level function definitions exist. The `powerups` const on line 24 is an object literal, not a function. All function implementations reside in sub-modules.
