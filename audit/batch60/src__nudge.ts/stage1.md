# Stage 1 Outputs — nudge.ts

## Agent 01

Functions found:
1. `getSessionFired` (line 16) — private
2. `markSessionFired` (line 25) — private
3. `getHistory` (line 33) — private
4. `markHistory` (line 42) — private
5. `isOnCooldown` (line 50) — private
6. `nudge` (line 67) — exported

## Agent 02

Functions found:
1. `getSessionFired` (line 16) — private
2. `markSessionFired` (line 25) — private
3. `getHistory` (line 33) — private
4. `markHistory` (line 42) — private
5. `isOnCooldown` (line 50) — private
6. `nudge` (line 67) — exported

## Agent 03

Functions found:
1. `getSessionFired` (line 16) — private
2. `markSessionFired` (line 25) — private
3. `getHistory` (line 33) — private
4. `markHistory` (line 42) — private
5. `isOnCooldown` (line 50) — private
6. `nudge` (line 67) — exported

## Agent 04

Functions found:
1. `getSessionFired` (line 16) — private
2. `markSessionFired` (line 25) — private
3. `getHistory` (line 33) — private
4. `markHistory` (line 42) — private
5. `isOnCooldown` (line 50) — private
6. `nudge` (line 67) — exported

## Agent 05

Functions found:
1. `getSessionFired` (line 16) — private
2. `markSessionFired` (line 25) — private
3. `getHistory` (line 33) — private
4. `markHistory` (line 42) — private
5. `isOnCooldown` (line 50) — private
6. `nudge` (line 67) — exported
