# Stage 2 Outputs — auth.rivals.ts

## Agent 01

### declareRival
`declareRival` is an async function that takes a `targetId` string and an optional `message` string. It first calls `isUUID(targetId)` synchronously; if that returns false, it immediately returns `{ success: false, error: 'Invalid user ID' }` without making any network calls. Next it calls `getIsPlaceholderMode()` synchronously; if that returns true, it immediately returns `{ success: true }` without touching the network. If both guards pass, it enters a try block and awaits `safeRpc<AuthResult>('declare_rival', { p_target_id: targetId, p_message: message ?? null })`. The `message` parameter is coalesced to `null` if omitted or undefined. If `safeRpc` resolves with a truthy `error` field, that error is thrown. If it resolves without error, the function returns `data` if present, or falls back to `{ success: true }`. If anything in the try block throws — including the manual `throw error` — the catch block returns `{ success: false, error: (e as Error).message }`, extracting the message from the caught value with a type assertion.

### respondRival
`respondRival` is an async function that takes a `rivalId` string and an `accept` boolean. It has no UUID validation on `rivalId`. It calls `getIsPlaceholderMode()` synchronously first; if true, it returns `{ success: true }` immediately. Otherwise it enters a try block and awaits `safeRpc<AuthResult>('respond_rival', { p_rival_id: rivalId, p_accept: accept })`. If the resolved result has a truthy `error` field, that error is thrown. On success, it returns `data ?? { success: true }`. If the try block throws for any reason, the catch block returns `{ success: false, error: (e as Error).message }`. The return type is `Promise<AuthResult>` — notably without the `& { error?: string }` extension seen on `declareRival`, though the catch path still writes an `error` field onto the returned object.

### getMyRivals
`getMyRivals` is an async function that takes no parameters. It calls `getIsPlaceholderMode()` synchronously; if true, it returns an empty array `[]` immediately. Otherwise it enters a try block and awaits `safeRpc<RivalData[]>('get_my_rivals')` with no additional parameters. If the resolved result has a truthy `error` field, that error is thrown. On success, it returns `data ?? []`, falling back to an empty array if `data` is null or undefined. If the try block throws, the catch block calls `console.error('getMyRivals error:', e)` and then returns `[]` — so all error paths produce an empty array as the return value. Unlike `declareRival` and `respondRival`, the error path does not surface an error field to the caller; failures are silently swallowed after logging.

## Agent 02

### declareRival
`declareRival` is an async function that takes a `targetId` string and an optional `message` string. It first calls `isUUID(targetId)` synchronously; if the result is false, it returns immediately with `{ success: false, error: 'Invalid user ID' }` without touching the network. Next it calls `getIsPlaceholderMode()` synchronously; if true, it returns `{ success: true }` immediately. If both guards pass, it enters a try block and awaits `safeRpc<AuthResult>('declare_rival', { p_target_id: targetId, p_message: message ?? null })`. If `safeRpc` resolves with a truthy `error` field, that error is thrown, which falls into the catch block and returns `{ success: false, error: (e as Error).message }`. If `safeRpc` resolves cleanly, the function returns `data` if it is non-null, or falls back to `{ success: true }`. The catch block wraps any thrown value — whether from the explicit `throw error` or from a rejection of the `safeRpc` call itself — and returns the same shape. No state is written outside the return value; no fire-and-forget calls exist.

### respondRival
`respondRival` is an async function that takes a `rivalId` string and an `accept` boolean. It does not validate the format of `rivalId`. It calls `getIsPlaceholderMode()` synchronously; if true, it returns `{ success: true }` immediately without a network call. Otherwise it enters a try block and awaits `safeRpc<AuthResult>('respond_rival', { p_rival_id: rivalId, p_accept: accept })`. If the resolved value has a truthy `error` field, that error is thrown and caught, and the catch block returns `{ success: false, error: (e as Error).message }`. If the call resolves cleanly, the function returns `data` if non-null, otherwise `{ success: true }`. No module-level state is written; no calls are fire-and-forget. Compared to `declareRival`, this function skips the UUID guard entirely, so any string — including an empty string or a malformed ID — reaches the RPC call.

### getMyRivals
`getMyRivals` is an async function that takes no parameters and returns a `RivalData[]` array. It calls `getIsPlaceholderMode()` synchronously; if true, it returns an empty array immediately. Otherwise it enters a try block and awaits `safeRpc<RivalData[]>('get_my_rivals')` with no additional parameters. If the resolved value has a truthy `error` field, that error is thrown. The catch block logs the error to the console via `console.error('getMyRivals error:', e)` and then returns an empty array — it does not re-throw or surface the error to the caller in any other form. If `safeRpc` resolves cleanly, the function returns `data` if non-null, otherwise an empty array. This is the only one of the three functions that logs on the error path; the other two silently pack the error message into the return value without logging.

## Agent 03

### declareRival
`declareRival` is an async function that takes a `targetId` string and an optional `message` string. It first calls `isUUID(targetId)` synchronously; if the result is false, it immediately returns `{ success: false, error: 'Invalid user ID' }` with no further work. It then calls `getIsPlaceholderMode()` synchronously; if that returns true, it immediately returns `{ success: true }`. Inside a try block, it awaits `safeRpc<AuthResult>('declare_rival', { p_target_id: targetId, p_message: message ?? null })`, passing `null` for the message when the argument is absent or undefined. If the resolved `error` field is truthy, it throws that error, which falls to the catch block. If `error` is falsy, it returns `data` if present, or `{ success: true }` as a fallback. The catch block receives the thrown value, casts it to `Error`, and returns `{ success: false, error: e.message }`. The return type is declared as `AuthResult & { error?: string }`, so both the happy path and the error path satisfy the signature. No state is written beyond the return value; no fire-and-forget calls exist.

### respondRival
`respondRival` is an async function that takes a `rivalId` string and an `accept` boolean. It calls `getIsPlaceholderMode()` synchronously first; if true, it returns `{ success: true }` immediately without validating `rivalId` (notably, there is no UUID check here, unlike `declareRival`). Inside a try block, it awaits `safeRpc<AuthResult>('respond_rival', { p_rival_id: rivalId, p_accept: accept })`. If the resolved `error` field is truthy, it throws that error. If falsy, it returns `data ?? { success: true }`. The catch block casts the caught value to `Error` and returns `{ success: false, error: e.message }`. No module-level state is written. All calls are awaited; no fire-and-forget calls exist.

### getMyRivals
`getMyRivals` is an async function that takes no parameters. It calls `getIsPlaceholderMode()` synchronously; if true, it returns an empty array immediately. Inside a try block, it awaits `safeRpc<RivalData[]>('get_my_rivals')` with no additional parameters. If the resolved `error` field is truthy, it throws that error. If falsy, it returns `data ?? []`, falling back to an empty array when `data` is null or undefined. The catch block logs the caught error to the console via `console.error('getMyRivals error:', e)` and then returns an empty array — the error is swallowed and the caller receives `[]` rather than a thrown exception. This differs from `declareRival` and `respondRival`, which surface errors in their return values; `getMyRivals` discards the error detail from the caller's perspective entirely. No state is written beyond the return value.

## Agent 04

### declareRival
`declareRival` is an async function that takes a required `targetId` string and an optional `message` string. It immediately calls `isUUID(targetId)` and, if that returns false, returns synchronously with `{ success: false, error: 'Invalid user ID' }` — no async work is done. Next it calls `getIsPlaceholderMode()` and, if true, returns `{ success: true }` immediately. If both guards pass, it enters a try block and awaits `safeRpc<AuthResult>('declare_rival', { p_target_id: targetId, p_message: message ?? null })`. If `message` is undefined, `null` is passed to the RPC. If the resolved `error` field is truthy, that error is thrown, jumping to the catch block which returns `{ success: false, error: (e as Error).message }`. If `error` is falsy, the function returns `data` if present, otherwise the fallback `{ success: true }`. The return type is `Promise<AuthResult & { error?: string }>`, meaning the happy path may or may not include an `error` property depending on what the RPC returns in `data`.

### respondRival
`respondRival` is an async function that takes a `rivalId` string and a boolean `accept`. It has no UUID validation on `rivalId` — the value is passed to the RPC unchecked. It calls `getIsPlaceholderMode()` first; if true, it returns `{ success: true }` immediately. Otherwise it enters a try block and awaits `safeRpc<AuthResult>('respond_rival', { p_rival_id: rivalId, p_accept: accept })`. If the resolved `error` field is truthy, that error is thrown and the catch block returns `{ success: false, error: (e as Error).message }`. If `error` is falsy, it returns `data` if present, otherwise `{ success: true }`. Unlike `declareRival`, there is no early UUID guard, so an invalid `rivalId` format is not caught client-side and will be forwarded to the server.

### getMyRivals
`getMyRivals` is an async function that takes no parameters. It calls `getIsPlaceholderMode()` and, if true, returns an empty array `[]` immediately with no network call. Otherwise it enters a try block and awaits `safeRpc<RivalData[]>('get_my_rivals')` with no additional parameters. If the resolved `error` field is truthy, that error is thrown; the catch block logs the error to the console via `console.error('getMyRivals error:', e)` and returns `[]`. If `error` is falsy, it returns `data` if present, otherwise `[]`. The function never throws — all error paths resolve to an empty array, and the only observable side effect on failure is the console log.

## Agent 05

### declareRival
`declareRival` is an async function that accepts a `targetId` string and an optional `message` string. It first calls `isUUID(targetId)` synchronously; if the result is false, it returns immediately with `{ success: false, error: 'Invalid user ID' }` without awaiting anything. It then calls `getIsPlaceholderMode()` synchronously; if that returns true, it returns immediately with `{ success: true }`. If both checks pass, it enters a try block and awaits `safeRpc<AuthResult>('declare_rival', { p_target_id: targetId, p_message: message ?? null })` — `message` is coerced to `null` if not provided. If `safeRpc` resolves with a non-null `error` field, that error is thrown, falling into the catch block. If `error` is null, the function returns `data` if present, or the fallback `{ success: true }` if `data` is nullish. In the catch block, any thrown value is cast to `Error` and its `.message` is returned inside `{ success: false, error: ... }`. The return type is `Promise<AuthResult & { error?: string }>`.

### respondRival
`respondRival` is an async function that accepts a `rivalId` string and an `accept` boolean. It first calls `getIsPlaceholderMode()` synchronously; if true, it returns immediately with `{ success: true }`. Otherwise it enters a try block and awaits `safeRpc<AuthResult>('respond_rival', { p_rival_id: rivalId, p_accept: accept })`. There is no UUID validation on `rivalId` before passing it to the RPC — unlike `declareRival`, this function skips that guard. If `safeRpc` resolves with a non-null `error` field, that error is thrown. If `error` is null, the function returns `data` if present, or the fallback `{ success: true }` if `data` is nullish. In the catch block, the thrown value is cast to `Error` and returned as `{ success: false, error: (e as Error).message }`. The return type is `Promise<AuthResult>`, though the catch path produces an object with an `error` property that is compatible with `AuthResult` only if that type allows it.

### getMyRivals
`getMyRivals` is an async function that accepts no parameters. It first calls `getIsPlaceholderMode()` synchronously; if true, it returns an empty array immediately without awaiting anything. Otherwise it enters a try block and awaits `safeRpc<RivalData[]>('get_my_rivals')` with no extra parameters. If `safeRpc` resolves with a non-null `error` field, that error is thrown. If `error` is null, the function returns `data` if present, or an empty array if `data` is nullish. In the catch block — unlike the other two functions — the error is logged to `console.error` as `'getMyRivals error:'` followed by the caught value, and then an empty array is returned. The catch block does not cast the error before logging, and it does not propagate or re-throw. The return type is `Promise<RivalData[]>`.
