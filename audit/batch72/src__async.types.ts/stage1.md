# Stage 1 Outputs — async.types.ts

## Agent 01
1. comment (no operation) — file header block
2. blank (no operation)
3. bind name to type — `HotTake` (exported-in-place)
4. blank (no operation)
5. bind name to type — `Prediction` (exported-in-place)
6. blank (no operation)
7. bind name to type — `StandaloneQuestion` (exported-in-place)
8. blank (no operation)
9. bind name to type — `RivalEntry` (exported-in-place)
10. blank (no operation)
11. bind name to type — `ReactResult` (exported-in-place)
12. blank (no operation)
13. bind name to type — `CreateHotTakeResult` (exported-in-place)
14. blank (no operation)
15. bind name to type — `CategoryFilter` (exported-in-place)

## Agent 02
1. comment (no operation) — block comment, lines 1-6
2. blank (no operation)
3. bind name to type **HotTake** (interface, exported-in-place)
4. blank (no operation)
5. bind name to type **Prediction** (interface, exported-in-place)
6. blank (no operation)
7. bind name to type **StandaloneQuestion** (interface, exported-in-place)
8. blank (no operation)
9. bind name to type **RivalEntry** (interface, exported-in-place)
10. blank (no operation)
11. bind name to type **ReactResult** (interface, exported-in-place)
12. blank (no operation)
13. bind name to type **CreateHotTakeResult** (interface, exported-in-place)
14. blank (no operation)
15. bind name to type **CategoryFilter** (type alias, exported-in-place)

## Agent 03
1. comment — file header block (lines 1–7)
2. bind `HotTake` to interface, exported-in-place
3. bind `Prediction` to interface, exported-in-place
4. bind `StandaloneQuestion` to interface, exported-in-place
5. bind `RivalEntry` to interface, exported-in-place
6. bind `ReactResult` to interface, exported-in-place
7. bind `CreateHotTakeResult` to interface, exported-in-place
8. bind `CategoryFilter` to type alias, exported-in-place

## Agent 04
1. comment — file header block
2. blank
3. bind name to type — `HotTake` (interface, exported)
4. blank
5. bind name to type — `Prediction` (interface, exported)
6. blank
7. bind name to type — `StandaloneQuestion` (interface, exported)
8. blank
9. bind name to type — `RivalEntry` (interface, exported)
10. blank
11. bind name to type — `ReactResult` (interface, exported)
12. blank
13. bind name to type — `CreateHotTakeResult` (interface, exported)
14. blank
15. bind name to type — `CategoryFilter` (type alias, exported)

## Agent 05
1. comment — file header block
2. blank
3. bind name to type — `HotTake` (interface, exported-in-place)
4. blank
5. bind name to type — `Prediction` (interface, exported-in-place)
6. blank
7. bind name to type — `StandaloneQuestion` (interface, exported-in-place)
8. blank
9. bind name to type — `RivalEntry` (interface, exported-in-place)
10. blank
11. bind name to type — `ReactResult` (interface, exported-in-place)
12. blank
13. bind name to type — `CreateHotTakeResult` (interface, exported-in-place)
14. blank
15. bind name to type — `CategoryFilter` (type alias, exported-in-place)
