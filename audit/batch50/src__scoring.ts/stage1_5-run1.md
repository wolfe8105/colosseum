# Anchor List — scoring.ts (Run 1)

1. validateUUID (line 48)
2. castVote (line 65)
3. placePrediction (line 88)

## Resolution notes
- `isPlaceholder` at line 57 is a non-exported helper function — excluded.
- Interfaces (`CastVoteParams`, `CastVoteResult`, `PlacePredictionParams`, `PlacePredictionResult`) and the `scoring` default-export const are excluded per scope rules.
