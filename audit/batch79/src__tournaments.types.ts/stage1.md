# Stage 1 Outputs — tournaments.types.ts

## Agent 01
1. Comment (block comment, lines 1-5)
2. Bind name to type (interface `Tournament` exported, lines 7-18)
3. Bind name to type (interface `TournamentMatch` exported, lines 20-29)
4. Bind name to type (interface `BracketMatch` exported, lines 31-43)
5. Blank (line 44)

## Agent 02
1. comment (lines 1-5)
2. blank (line 6)
3. bind name to type (interface Tournament, exported) (lines 7-18)
4. blank (line 19)
5. bind name to type (interface TournamentMatch, exported) (lines 20-29)
6. blank (line 30)
7. bind name to type (interface BracketMatch, exported) (lines 31-43)

## Agent 03
1. comment
2. blank
3. bind name to type (interface Tournament) — exported
4. blank
5. bind name to type (interface TournamentMatch) — exported
6. blank
7. bind name to type (interface BracketMatch) — exported
8. blank

## Agent 04
1. comment (lines 1-5: multi-line comment block)
2. blank (line 6)
3. bind name to type (exported interface Tournament, line 7)
4. bind name to type (exported interface TournamentMatch, line 20)
5. bind name to type (exported interface BracketMatch, line 31)

## Agent 05
1. comment (lines 1–5)
2. bind name to type (interface Tournament, exported) (lines 7–18)
3. bind name to type (interface TournamentMatch, exported) (lines 20–29)
4. bind name to type (interface BracketMatch, exported) (lines 31–43)
5. blank (line 44)
