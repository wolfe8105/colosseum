# Stage 2 Outputs — bounties.types.ts

## Agent 01
No functions on the anchor list. This file contains only a type import, a type re-export (AuthResult), and five exported interface declarations (BountyRow, OpponentBounty, MyBountiesResult, PostBountyResult, SelectClaimResult). There is no runtime behavior to describe.

## Agent 02
No functions on the anchor list. This file contains only a type import, a type re-export (AuthResult), and five exported interface declarations (BountyRow, OpponentBounty, MyBountiesResult, PostBountyResult, SelectClaimResult). There is no runtime behavior to describe.

## Agent 03
No functions on the anchor list. This file contains only a type import, a type re-export (AuthResult), and five exported interface declarations (BountyRow, OpponentBounty, MyBountiesResult, PostBountyResult, SelectClaimResult). There is no runtime behavior to describe.

## Agent 04
No functions on the anchor list. This file contains only a type import, a type re-export (AuthResult), and five exported interface declarations (BountyRow, OpponentBounty, MyBountiesResult, PostBountyResult, SelectClaimResult). There is no runtime behavior to describe.

## Agent 05
No functions on the anchor list. This file contains only a type import, a type re-export (AuthResult), and five exported interface declarations (BountyRow, OpponentBounty, MyBountiesResult, PostBountyResult, SelectClaimResult). There is no runtime behavior to describe.
