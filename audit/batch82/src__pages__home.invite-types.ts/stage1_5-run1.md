# Anchor List — home.invite-types.ts

*(empty — no function definitions exist in this file)*

## Resolution notes

The file is a pure type-declaration module. All seven entries are either comments, blank lines, or `bind name to type` (exported interfaces: `InviteReward`, `ActivityEntry`, `InviteStats`). No agent classified any entry as a function definition. Direct source scan confirms: no function declarations, no arrow function bindings, no class definitions. The three interfaces are excluded by the rule "type signatures and interface method declarations."
