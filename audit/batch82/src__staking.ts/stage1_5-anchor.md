# Anchor List — staking.ts

Source: C:/Users/wolfe/colosseum/src/staking.ts
Produced by: stage 1.5 (arbiter + optional reconciliation)
Unresolved items: 0

*(empty — no function definitions exist in this file)*

## Resolution notes

Both arbiter runs agreed: the file is a pure barrel/re-export orchestrator. All named items in `export { }` statements are defined in sub-files (`staking.rpc.ts`, `staking.render.ts`, `staking.wire.ts`), not in this file. Type re-exports (`export type { }`) are excluded by definition. No function definitions to anchor.
