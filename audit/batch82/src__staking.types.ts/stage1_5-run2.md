# Anchor List — staking.types.ts

*(empty — no function definitions exist in this file)*

## Resolution notes

No candidates. File contains only a comment block and five exported interfaces (`StakeResult`, `PoolData`, `UserStake`, `Odds`, `SettleResult`). Interfaces are type constructs excluded from the function anchor list. No function definitions found on direct source scan.
