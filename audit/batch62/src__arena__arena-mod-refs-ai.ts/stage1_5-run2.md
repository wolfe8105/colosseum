# Anchor List — arena-mod-refs-ai.ts

1. requestAIModRuling  (line 8)

## Resolution notes
Single top-level exported async function declaration. The `.map()` arrow function on the `recentMessages` line is an inline callback within a method chain and is excluded per the rules. No other top-level function declarations or module-level function expressions exist in the file.
