# Anchor List — arena-css-after-effects.ts

1. injectAfterEffectsCSS  (line 5)

## Resolution notes

All five agents agreed unanimously. The file contains exactly one top-level callable binding: `injectAfterEffectsCSS`, exported function declaration on line 5, spanning to line 27. The only inner binding in the body is `const style` assigned to a DOM element — not a function.
