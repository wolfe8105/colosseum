# Anchor List — webrtc.turn.ts

1. buildTurnSequence  (line 42)

---

## Resolution notes

- `TURN_DURATION` (line 22): excluded — `const` bound to a numeric literal, not a function.
- `PAUSE_DURATION` (line 25): excluded — `const` bound to a numeric literal, not a function.
- `AD_BREAK_DURATION` (line 28): excluded — `const` bound to a numeric literal, not a function.
- `FINAL_AD_DURATION` (line 31): excluded — `const` bound to a numeric literal, not a function.
- `ROUND_DURATION` (line 34): excluded — `const` bound to a numeric expression (`DEBATE.roundDurationSec`), not a function.
- `BREAK_DURATION` (line 35): excluded — `const` bound to a numeric expression (`DEBATE.breakDurationSec`), not a function.
- `MAX_ROUNDS` (line 36): excluded — `const` bound to a numeric expression (`DEBATE.defaultRounds`), not a function.
