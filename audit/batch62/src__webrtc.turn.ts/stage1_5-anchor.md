# Anchor List — webrtc.turn.ts

Source: src/webrtc.turn.ts
Produced by: stage 1.5 (arbiter + optional reconciliation)
Unresolved items: 0

1. buildTurnSequence  (line 42)

## Resolution notes
Both arbiter runs agreed. All other exported bindings are numeric value constants, not function definitions.

- `TURN_DURATION` (line 22): excluded — const bound to numeric literal
- `PAUSE_DURATION` (line 25): excluded — const bound to numeric literal
- `AD_BREAK_DURATION` (line 28): excluded — const bound to numeric literal
- `FINAL_AD_DURATION` (line 31): excluded — const bound to numeric literal
- `ROUND_DURATION` (line 34): excluded — const bound to DEBATE.roundDurationSec (numeric expression)
- `BREAK_DURATION` (line 35): excluded — const bound to DEBATE.breakDurationSec (numeric expression)
- `MAX_ROUNDS` (line 36): excluded — const bound to DEBATE.defaultRounds (numeric expression)
