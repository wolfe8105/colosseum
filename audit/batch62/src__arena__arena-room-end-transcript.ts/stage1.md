# Stage 1 Outputs — arena-room-end-transcript.ts

## Agent 01
1. comment — `// arena-room-end-transcript.ts — Session 113: transcript bottom-sheet overlay`
2. comment — `// Attached after renderPostDebate paints the DOM.`
3. blank
4. import — `getCurrentProfile` from `'../auth.ts'`
5. import — `escapeHTML` from `'../config.ts'`
6. import — type `CurrentDebate` from `'./arena-types.ts'`
7. blank
8. bind name to function definition — `attachTranscriptHandler`, exported

## Agent 02
1. comment — `// arena-room-end-transcript.ts — Session 113: transcript bottom-sheet overlay`
2. comment — `// Attached after renderPostDebate paints the DOM.`
3. blank
4. import — `getCurrentProfile` from `../auth.ts`
5. import — `escapeHTML` from `../config.ts`
6. import — type-only import of `CurrentDebate` from `./arena-types.ts`
7. blank
8. bind name to function definition — `attachTranscriptHandler` (exported)

## Agent 03
1. comment — `// arena-room-end-transcript.ts — Session 113: transcript bottom-sheet overlay`
2. comment — `// Attached after renderPostDebate paints the DOM.`
3. blank
4. import — `getCurrentProfile` from `'../auth.ts'`
5. import — `escapeHTML` from `'../config.ts'`
6. import — type `CurrentDebate` from `'./arena-types.ts'`
7. blank
8. bind name to function definition — `attachTranscriptHandler` (exported)

## Agent 04
1. comment — `// arena-room-end-transcript.ts — Session 113: transcript bottom-sheet overlay`
2. comment — `// Attached after renderPostDebate paints the DOM.`
3. blank
4. import — `getCurrentProfile` from `'../auth.ts'`
5. import — `escapeHTML` from `'../config.ts'`
6. import — type-only import of `CurrentDebate` from `'./arena-types.ts'`
7. blank
8. bind name to function definition — `attachTranscriptHandler` (exported)

## Agent 05
1. comment — `// arena-room-end-transcript.ts — Session 113: transcript bottom-sheet overlay`
2. comment — `// Attached after renderPostDebate paints the DOM.`
3. blank
4. import — `getCurrentProfile` from `../auth.ts`
5. import — `escapeHTML` from `../config.ts`
6. import (type-only) — `CurrentDebate` from `./arena-types.ts`
7. blank
8. bind name to function definition — `attachTranscriptHandler` (exported)
