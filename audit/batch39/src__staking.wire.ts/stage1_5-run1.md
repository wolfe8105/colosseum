# Anchor List — staking.wire.ts

1. _updateConfirmButton (line 11)
2. wireStakingPanel (line 34)

## Resolution notes

Both top-level function declarations confirmed. All inline callbacks in forEach/addEventListener are excluded. No missed functions found on direct scan.
