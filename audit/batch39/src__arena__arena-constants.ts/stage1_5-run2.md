# Anchor List — arena-constants.ts

(No function definitions found)

## Resolution notes

No candidates to exclude. The source file contains no function definitions. All items identified by agents were either type imports, interface declarations, or const bindings to data values (objects, arrays, numbers). None of these are function definitions as specified in the criteria.
