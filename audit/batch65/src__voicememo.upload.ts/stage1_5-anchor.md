# Anchor List — voicememo.upload.ts

Source: src/voicememo.upload.ts
Produced by: stage 1.5 (arbiter + optional reconciliation)
Unresolved items: 0

1. revokeAllFallbackURLs  (line 13)
2. uploadVoiceMemo  (line 25)

## Resolution notes
Both arbiter runs agreed. Two top-level named function definitions in the file. `UploadResult` (line 20) excluded — exported interface, not a function. `MAX_FILE_BYTES` and `_fallbackObjectURLs` excluded — const bindings.
