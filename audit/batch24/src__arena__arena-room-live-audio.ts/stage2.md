# Stage 2 Outputs — arena-room-live-audio.ts

## Agent 01

Note: the file ends at line 117, but the final line is blank. The anchor lists `toggleLiveMute` at line 115, which matches the source. I also note that `advanceRound` and `addSystemMessage` are imported but not referenced in the body of this file.

### destroyLiveAudio
This function is synchronous and takes no parameters. It inspects each of the eight module-level handler reference variables (`_micReadyHandler`, `_connectedHandler`, `_disconnectedHandler`, `_reconnectingHandler`, `_connectionFailedHandler`, `_muteChangedHandler`, `_tickHandler`, `_debateEndHandler`) in sequence. For each one that is truthy, it calls `offWebRTC` (the `off` export from `../webrtc.ts`) with the matching event name string (`'micReady'`, `'connected'`, `'disconnected'`, `'reconnecting'`, `'connectionFailed'`, `'muteChanged'`, `'tick'`, `'debateEnd'`) and the stored handler reference, then assigns `null` back to the module-level variable. Handler references that are already `null` are skipped. The function returns `void` and performs no DOM reads, network calls, or writes beyond mutating these eight module-level slots and invoking `offWebRTC`.

### initLiveAudio
This is an `async` function with no parameters that returns `Promise<void>`. It begins by calling `destroyLiveAudio()` synchronously to clear any handler references that may be left over from a prior invocation, then reads the module-level `currentDebate` (imported from `./arena-state.ts`) and asserts it non-null via the `!` operator, binding it to a local `debate` constant. It then constructs eight arrow-function handlers and assigns each to its matching module-level slot, registering each one with `onWebRTC` (the `on` export from `../webrtc.ts`) under a corresponding event name. The `_micReadyHandler` reads `document.getElementById('arena-audio-status')` and, if present, sets its `textContent` to `'Microphone ready'`; it then reads `document.getElementById('arena-waveform')` cast to `HTMLCanvasElement | null` and calls `getLocalStream()`, and if both the canvas and the stream are truthy it calls `createWaveform(localStream, canvas)`. The `_connectedHandler` sets the status element's `textContent` to a green-circle string followed by `'Connected — debate is live!'`. The `_disconnectedHandler` destructures `recovering` from its `data` argument (typed via a cast to `{ state: string; recovering?: boolean }`) and writes either a yellow-circle "Connection interrupted — reconnecting..." or a red-circle "Connection lost" into the status element depending on the truthiness of `recovering`. The `_reconnectingHandler` destructures `attempt` and `max` from its `data` argument and writes a template-literal yellow-circle string `"Reconnecting (attempt/max)..."` into the status element. The `_connectionFailedHandler` writes a red-circle "Connection failed — audio unavailable" string.

The `_muteChangedHandler` destructures `muted` from its `data` argument, looks up `document.getElementById('arena-mic-btn')`, and if present toggles the `'muted'` class on that element according to `muted` and sets its `textContent` to a muted-speaker glyph or a studio-microphone glyph; it then looks up the status element and writes either `'Muted'` or `'Unmuted — speaking'`. The `_tickHandler` destructures `timeLeft`, reads `document.getElementById('arena-room-timer')`, and if present sets `textContent` to `formatTimer(timeLeft)` and toggles the `'warning'` class based on whether `timeLeft <= 15`. The `_debateEndHandler` invokes `endCurrentDebate()` (imported from `./arena-room-end.ts`) and discards its returned promise via `void`. After all handlers are registered, a `try` block awaits `joinDebate(debate.id, debate.role, debate.totalRounds)` — this is the sole `await` in the function. If `joinDebate` rejects, the `catch` clause (which ignores the error value) reads `document.getElementById('arena-audio-status')` and, if present, sets its `textContent` to `'Mic access blocked. Check your browser settings.'`. The function then returns. The module-level writes this function performs are the eight handler-slot assignments; external writes are the eight `onWebRTC` registrations, the DOM mutations performed by the handlers once invoked (not at registration time), and whatever side effects `joinDebate` produces.

### toggleLiveMute
This function is synchronous, takes no parameters, and returns `void`. Its entire body is a single call to `toggleMute()` (the export from `../webrtc.ts`). It reads no module-level state, performs no DOM access directly, and does not inspect or return the result of `toggleMute()`.

## Agent 02

### destroyLiveAudio
This synchronous, parameterless function reads the eight module-level handler references (`_micReadyHandler`, `_connectedHandler`, `_disconnectedHandler`, `_reconnectingHandler`, `_connectionFailedHandler`, `_muteChangedHandler`, `_tickHandler`, `_debateEndHandler`). For each reference, it checks truthiness; when the reference is non-null, it calls `offWebRTC` (imported as `off` from `../webrtc.ts`) with the corresponding event name string (`'micReady'`, `'connected'`, `'disconnected'`, `'reconnecting'`, `'connectionFailed'`, `'muteChanged'`, `'tick'`, `'debateEnd'`) and the stored handler, then writes `null` back to that module-level variable. The calls occur in the listed order, each as an independent `if` block with no early return. The function returns `void` and performs no DOM, network, or storage access.

### initLiveAudio
This async function takes no parameters and begins by invoking `destroyLiveAudio()`, which deregisters any previously stored handlers and nulls their module-level slots. It then reads `currentDebate` from `./arena-state.ts` and asserts it is non-null via the `!` operator, binding it to a local `debate` constant; if `currentDebate` is actually null at runtime, the later access to `debate.id`, `debate.role`, and `debate.totalRounds` will throw. The function then constructs eight callback closures, assigns each to its module-level handler slot, and registers each with `onWebRTC` in this order: `micReady`, `connected`, `disconnected`, `reconnecting`, `connectionFailed`, `muteChanged`, `tick`, `debateEnd`. The `micReady` handler looks up `#arena-audio-status` in the document, writes `'Microphone ready'` into its `textContent` if found, looks up `#arena-waveform` as an `HTMLCanvasElement`, calls `getLocalStream()` from `../webrtc.ts`, and if both the canvas and the stream are truthy calls `createWaveform(localStream, canvas)`. The `connected` handler sets `#arena-audio-status` textContent to a green-circle "Connected — debate is live!" string. The `disconnected` handler casts its `data` argument to `{ state: string; recovering?: boolean }`, reads only `recovering`, and sets the status element's textContent to a yellow-circle "reconnecting..." string when `recovering` is truthy or a red-circle "Connection lost" string otherwise. The `reconnecting` handler reads `attempt` and `max` from the cast data and writes `"🟡 Reconnecting (${attempt}/${max})..."` into the status element. The `connectionFailed` handler writes a red-circle "Connection failed — audio unavailable" string into the status element. The `muteChanged` handler reads `muted` from the cast data, looks up `#arena-mic-btn`, toggles its `muted` class based on the boolean, sets its textContent to either a muted-speaker or microphone emoji, then looks up `#arena-audio-status` and sets its textContent to `'Muted'` or `'Unmuted — speaking'`. The `tick` handler reads `timeLeft` from the cast data, looks up `#arena-room-timer`, sets its textContent to `formatTimer(timeLeft)`, and toggles a `warning` class when `timeLeft <= 15`. The `debateEnd` handler invokes `endCurrentDebate()` from `./arena-room-end.ts` and discards the returned promise via `void`. Each handler's DOM lookup uses `document.getElementById` and silently no-ops when the element is not found. After all handlers are wired, the function enters a `try` block and awaits `joinDebate(debate.id, debate.role, debate.totalRounds)` from `../webrtc.ts`; if that promise rejects, the `catch` block (with no error binding) looks up `#arena-audio-status` and writes `'Mic access blocked. Check your browser settings.'` into its textContent. The function then resolves with `void`. Note that the imports include `addSystemMessage` and `advanceRound` but neither is referenced inside the code shown; they are imported but unused in this file.

### toggleLiveMute
This synchronous, parameterless function calls `toggleMute()` from `../webrtc.ts` and returns `void`. It reads no module-level state, writes no module-level state, touches no DOM, and has no branches, loops, or error paths. Its entire body is a single delegated call.

## Agent 03

### destroyLiveAudio
This synchronous, non-async function takes no parameters and returns `void`. It reads the eight module-level handler reference variables (`_micReadyHandler`, `_connectedHandler`, `_disconnectedHandler`, `_reconnectingHandler`, `_connectionFailedHandler`, `_muteChangedHandler`, `_tickHandler`, `_debateEndHandler`). For each one, it runs a truthiness check; when the ref is non-null, it calls `offWebRTC` (imported as `off` from `../webrtc.ts`) with the event name string (`'micReady'`, `'connected'`, `'disconnected'`, `'reconnecting'`, `'connectionFailed'`, `'muteChanged'`, `'tick'`, `'debateEnd'`) and the stored handler reference, then assigns `null` back to the module-level variable. The checks run in sequence in the order listed above; there are no loops, no try/catch, and no early returns. If a given ref is already `null`, the corresponding `offWebRTC` call and reassignment are skipped. The function produces no return value.

### initLiveAudio
This `async` function takes no parameters and returns `Promise<void>`. Its first statement calls `destroyLiveAudio()` to clear any previously registered handlers. It then reads the module-level `currentDebate` import and assigns it to a local `debate` via non-null assertion (`currentDebate!`); if `currentDebate` is null at runtime this assertion is a TypeScript-only guarantee and the DOM object access below would throw. The function then defines eight callback functions and assigns each to its corresponding module-level handler ref (`_micReadyHandler`, `_connectedHandler`, `_disconnectedHandler`, `_reconnectingHandler`, `_connectionFailedHandler`, `_muteChangedHandler`, `_tickHandler`, `_debateEndHandler`), registering each via `onWebRTC(eventName, handler)` immediately after assignment. The registration order is: `micReady`, `connected`, `disconnected`, `reconnecting`, `connectionFailed`, `muteChanged`, `tick`, `debateEnd`.

The handler bodies write to the DOM by calling `document.getElementById` for IDs `'arena-audio-status'`, `'arena-waveform'`, `'arena-mic-btn'`, and `'arena-room-timer'`, each guarded by a truthiness check before mutation. The `micReady` handler sets status text to `'Microphone ready'`, reads the canvas element, calls `getLocalStream()`, and if both canvas and stream are truthy calls `createWaveform(localStream, canvas)`. The `connected` handler sets status text to a green-circle-emoji "Connected — debate is live!" string. The `disconnected` handler casts its `data` argument to `{ state: string; recovering?: boolean }`, destructures `recovering`, and sets status text to either a yellow-circle "Connection interrupted — reconnecting..." string or a red-circle "Connection lost" string based on the boolean. The `reconnecting` handler destructures `attempt` and `max` from `data` and writes a template-literal status string including those numbers. The `connectionFailed` handler writes a red-circle "Connection failed — audio unavailable" string. The `muteChanged` handler destructures `muted` from `data`, toggles the `'muted'` CSS class on `arena-mic-btn`, sets the button's `textContent` to either a speaker-with-cancellation-stroke emoji or a studio-microphone emoji, and sets the status text to `'Muted'` or `'Unmuted — speaking'`. The `tick` handler destructures `timeLeft` from `data`, calls `formatTimer(timeLeft)` to set `textContent`, and toggles the `'warning'` CSS class based on `timeLeft <= 15`. The `debateEnd` handler fires `endCurrentDebate()` as a fire-and-forget (its promise is discarded via `void`). After all registrations, the function enters a `try` block that awaits `joinDebate(debate.id, debate.role, debate.totalRounds)`; on any caught error the `catch` block looks up `arena-audio-status` and, if present, sets its text to `'Mic access blocked. Check your browser settings.'`. The caught error is not rethrown or logged, and `advanceRound`/`addSystemMessage` imports are not referenced inside this function despite being imported at the top of the file.

### toggleLiveMute
This synchronous, non-async function takes no parameters and returns `void`. Its single statement calls `toggleMute()` from `../webrtc.ts`. It reads no module-level state, writes no module-level state, has no branches, no loops, no try/catch, and passes no arguments. The return value of `toggleMute()` (if any) is discarded.

## Agent 04

Note: The file I read has 117 lines, and the anchor list references `toggleLiveMute` at line 115, which matches. I also note that `advanceRound` and `addSystemMessage` are imported but never referenced within the three listed functions — they are imports not used by the anchored functions.

### destroyLiveAudio
This synchronous, no-argument, no-return function inspects each of the eight module-level handler reference variables (`_micReadyHandler`, `_connectedHandler`, `_disconnectedHandler`, `_reconnectingHandler`, `_connectionFailedHandler`, `_muteChangedHandler`, `_tickHandler`, `_debateEndHandler`) in sequence. For each variable whose current value is truthy, it calls the imported `offWebRTC` function (aliased from `off` in `../webrtc.ts`) passing the corresponding event name string (`'micReady'`, `'connected'`, `'disconnected'`, `'reconnecting'`, `'connectionFailed'`, `'muteChanged'`, `'tick'`, `'debateEnd'`) and the stored handler reference, then reassigns that module-level variable to `null`. Variables that are already `null` are skipped without any call. The function does not read parameters, DOM, storage, or network state, and it performs no awaits, loops, or try/catch.

### initLiveAudio
This `async` function takes no arguments and returns a `Promise<void>`. It first calls `destroyLiveAudio()` synchronously, which deregisters any currently-stored handlers and nulls the module-level refs. It then reads `currentDebate` from `./arena-state.ts` via a non-null assertion (`currentDebate!`) and binds the result to the local `debate`. The function then constructs eight callback functions and assigns each, in turn, to the matching module-level handler variable, registering it immediately afterward via `onWebRTC(eventName, handlerRef)`: `_micReadyHandler` on `'micReady'`, `_connectedHandler` on `'connected'`, `_disconnectedHandler` on `'disconnected'`, `_reconnectingHandler` on `'reconnecting'`, `_connectionFailedHandler` on `'connectionFailed'`, `_muteChangedHandler` on `'muteChanged'`, `_tickHandler` on `'tick'`, and `_debateEndHandler` on `'debateEnd'`. The `micReady` callback calls `document.getElementById('arena-audio-status')` and, if non-null, writes `'Microphone ready'` to its `textContent`; it then calls `document.getElementById('arena-waveform')` (cast to `HTMLCanvasElement | null`) and invokes `getLocalStream()` from `../webrtc.ts`, and if both the canvas and the returned stream are truthy, calls `createWaveform(localStream, canvas)`. The `connected` callback writes the string `'🟢 Connected — debate is live!'` (encoded via the `\uD83D\uDFE2` surrogate pair and `\u2014`) into `arena-audio-status` if that element exists. The `disconnected` callback casts its `data` argument to `{ state: string; recovering?: boolean }`, destructures `recovering`, and sets `arena-audio-status` text to either `'🟡 Connection interrupted — reconnecting...'` when `recovering` is truthy or `'🔴 Connection lost'` otherwise. The `reconnecting` callback destructures `attempt` and `max` from `data` and writes `` `🟡 Reconnecting (${attempt}/${max})...` `` into `arena-audio-status`. The `connectionFailed` callback writes `'🔴 Connection failed — audio unavailable'` into `arena-audio-status`.

The `muteChanged` callback destructures `muted` from its `data` argument, looks up `arena-mic-btn`, and if present toggles the `muted` CSS class based on the boolean and sets `textContent` to `'🔇'` (`\uD83D\uDD07`) when muted or `'🎙️'` (`\uD83C\uDF99\uFE0F`) when unmuted; it also writes `'Muted'` or `'Unmuted — speaking'` to `arena-audio-status` if present. The `tick` callback destructures `timeLeft` from `data`, looks up `arena-room-timer`, and if present writes `formatTimer(timeLeft)` to its `textContent` and toggles the `warning` class on when `timeLeft <= 15`. The `debateEnd` callback calls `endCurrentDebate()` with `void` applied to the returned promise, making the call fire-and-forget. After all eight registrations, the function enters a `try` block that awaits `joinDebate(debate.id, debate.role, debate.totalRounds)` from `../webrtc.ts`; if that promise rejects, the `catch` block (which ignores the error value) looks up `arena-audio-status` and, if present, sets its `textContent` to `'Mic access blocked. Check your browser settings.'`. The `joinDebate` await is the only awaited call; the catch block is synchronous and the function then resolves.

### toggleLiveMute
This synchronous, no-argument, no-return function calls the imported `toggleMute` from `../webrtc.ts` and discards the result. It reads no parameters, module-level state, DOM, storage, or network; it writes no module-level state; it contains no branches, loops, or try/catch. It is a one-line pass-through to `toggleMute`.

## Agent 05

I note the anchor list references `advanceRound` as imported but the file does not appear to invoke it directly in the visible function bodies — that import is present in the module but not referenced by any of the three anchor functions.

### destroyLiveAudio
This synchronous function reads the eight module-level handler references (`_micReadyHandler`, `_connectedHandler`, `_disconnectedHandler`, `_reconnectingHandler`, `_connectionFailedHandler`, `_muteChangedHandler`, `_tickHandler`, `_debateEndHandler`) in sequence. For each one that is truthy, it calls `offWebRTC` (imported as `off` from `../webrtc.ts`) with the event name string (`'micReady'`, `'connected'`, `'disconnected'`, `'reconnecting'`, `'connectionFailed'`, `'muteChanged'`, `'tick'`, `'debateEnd'`) and the stored handler reference, then assigns `null` back to that same module-level variable. Handlers that are already `null` are skipped — their `offWebRTC` call is not made and no reassignment occurs. The function takes no parameters, returns `void`, is not async, and has no try/catch. After it completes, all eight handler slots hold `null`.

### initLiveAudio
This async function takes no parameters and returns `Promise<void>`. It first calls `destroyLiveAudio()` to clear any previously registered handlers, then reads the module-level `currentDebate` import from `./arena-state.ts` with a non-null assertion and binds it to the local `debate`. It then constructs eight handler functions and assigns each to its corresponding module-level slot, registering each one via `onWebRTC` (imported as `on`) in this order: `micReady`, `connected`, `disconnected`, `reconnecting`, `connectionFailed`, `muteChanged`, `tick`, `debateEnd`. The `micReady` handler looks up `arena-audio-status` via `document.getElementById`, writes the string `'Microphone ready'` to its `textContent` when present, then looks up `arena-waveform` as an `HTMLCanvasElement`, calls `getLocalStream()` for the current stream, and if both canvas and stream are truthy invokes `createWaveform(localStream, canvas)`. The `connected` handler sets `arena-audio-status` text to a green-circle-prefixed "Connected — debate is live!" message. The `disconnected` handler destructures `recovering` from the event data (cast to `{ state: string; recovering?: boolean }`) and writes either a yellow-circle "Connection interrupted — reconnecting..." string or a red-circle "Connection lost" string to the status element depending on that boolean. The `reconnecting` handler destructures `attempt` and `max` from the data and writes a yellow-circle `Reconnecting (${attempt}/${max})...` template into the status element. The `connectionFailed` handler writes a red-circle "Connection failed — audio unavailable" string.

The `muteChanged` handler destructures `muted: boolean` from the data, looks up `arena-mic-btn`, calls `classList.toggle('muted', muted)` on it, sets its `textContent` to a speaker-with-cancel-stroke glyph when muted or a studio-microphone glyph otherwise, then also writes `'Muted'` or `'Unmuted — speaking'` to `arena-audio-status`. The `tick` handler destructures `timeLeft: number`, looks up `arena-room-timer`, writes `formatTimer(timeLeft)` to its `textContent`, and toggles the `warning` class based on `timeLeft <= 15`. The `debateEnd` handler calls `endCurrentDebate()` and discards the returned promise via `void`. After all eight registrations, the function enters a `try` block and awaits `joinDebate(debate.id, debate.role, debate.totalRounds)`. If `joinDebate` throws or rejects, the `catch` block (which ignores the error value) looks up `arena-audio-status` and writes `'Mic access blocked. Check your browser settings.'` to it; the function then resolves normally without re-throwing. Note that if `currentDebate` is `null`, the non-null assertion will not prevent a runtime error when `debate.id` / `debate.role` / `debate.totalRounds` is read inside the `try`, and that error would be swallowed by the `catch`.

### toggleLiveMute
This synchronous function takes no parameters, reads no module-level state, and returns `void`. It calls `toggleMute()` (imported from `../webrtc.ts`) once and does nothing with the return value. There is no branching, no error handling, and no DOM interaction in the function body itself — any DOM update tied to a resulting mute state change is driven through the `_muteChangedHandler` registered by `initLiveAudio` when WebRTC subsequently emits a `muteChanged` event.
