# Anchor List — settings.ts (Arbiter Run 2)

(No function definitions found)

## Resolution notes

- `window.addEventListener('DOMContentLoaded', async () => {...})` (lines 20-66): Top-level statement with inline async callback function, not a named function definition; callbacks passed inline to addEventListener are excluded.
