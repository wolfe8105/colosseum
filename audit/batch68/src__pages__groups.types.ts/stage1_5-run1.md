# Anchor List — groups.types.ts

*(none)*

This file contains no function definitions. Every top-level binding is a TypeScript `interface` declaration (`GroupListItem`, `GroupDetail`, `GroupMember`). Interfaces are type-level constructs, not callable bindings, and are explicitly excluded from the anchor list.

## Resolution notes

- GroupListItem (line 10): `export interface` — type declaration, not a callable binding. Excluded.
- GroupDetail (line 25): `export interface` — type declaration, not a callable binding. Excluded.
- GroupMember (line 54): `export interface` — type declaration, not a callable binding. Excluded.
