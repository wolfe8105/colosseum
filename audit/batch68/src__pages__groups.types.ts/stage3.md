# Stage 3 Outputs — groups.types.ts

No Stage 2 claims to verify. Anchor list is empty — pure type-declaration module.

No findings. File is clean by construction: three TypeScript interface declarations with no runtime behavior.
