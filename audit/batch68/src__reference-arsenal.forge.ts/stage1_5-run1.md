# Anchor List — reference-arsenal.forge.ts

1. showForgeForm  (line 18)

## Resolution notes

All five agents in unanimous agreement. One exported function declaration at the top level.

- render (line 42): inner helper function defined inside `showForgeForm`'s body — excluded.
- Anonymous arrow function returned at line 50: anonymous, not a named top-level binding — excluded.
