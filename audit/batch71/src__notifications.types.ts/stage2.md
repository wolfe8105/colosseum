# Stage 2 Outputs — notifications.types.ts

No functions on the anchor list. Nothing to walk.

This file contains only type definitions (NotificationType, NotificationTypeInfo, Notification, NotificationFilter) and two exported const value bindings (TYPES object, ECONOMY_TYPES Set). Stage 2 runtime walk is vacuous.
