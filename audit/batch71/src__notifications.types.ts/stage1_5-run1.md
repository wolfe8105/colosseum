# Anchor List — notifications.types.ts

No function definitions found.

## Resolution notes
The source file contains only type aliases (NotificationType, NotificationFilter), interfaces (NotificationTypeInfo, Notification), and value bindings to non-function objects (TYPES object literal, ECONOMY_TYPES Set). No callable bindings present.
