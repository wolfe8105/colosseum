# Anchor List — home.invite-sheet.ts

1. openClaimSheet  (line 14)

## Resolution notes

`openClaimSheet` — top-level exported async function declaration. Excluded: all inner/closure-scoped arrow functions (`close`, event handlers, forEach/filter/find callbacks). No classes, interfaces, type aliases, enums, or non-function constants at module scope.
