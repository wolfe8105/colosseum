# Anchor List — groups.auditions.render.ts

1. renderAuditionsList  (line 33)

## Resolution notes

`PendingAudition` (interface) and `RULE_LABELS` (object value) excluded — not callable function definitions.
