# Anchor List — home.types.ts

No function definitions exist.

## Resolution notes

The source file contains only type definitions:
- `Category` interface (exported, lines 2-9)
- `LiveDebate` interface (exported, lines 11-24)

All five stage 1 agents correctly identified these as type bindings rather than function definitions. The file contains no function declarations, arrow functions, function expressions, or any other callable bindings. Therefore, the anchor list for stage 2 is empty.
