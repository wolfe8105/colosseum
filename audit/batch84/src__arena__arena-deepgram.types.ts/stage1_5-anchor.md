# Anchor List — arena-deepgram.types.ts

Source: src/arena/arena-deepgram.types.ts
Produced by: stage 1.5 (arbiter + optional reconciliation)
Unresolved items: 0

(No function definitions — pure type declaration module)

## Resolution notes

- `TranscriptCallback` (line 6): Type alias, not a callable function definition
- `StatusCallback` (line 7): Type alias, not a callable function definition
- `DeepgramStatus` (line 8): Type alias (string union), not a callable function definition
- `DeepgramResult` (lines 10-20): Interface definition, not a callable function definition

Both arbiter runs agreed. No reconciliation needed.
