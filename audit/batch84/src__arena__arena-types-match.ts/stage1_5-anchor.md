# Anchor List — arena-types-match.ts

Source: src/arena/arena-types-match.ts
Produced by: stage 1.5 (arbiter + optional reconciliation)
Unresolved items: 0

(No function definitions — pure type declaration module)

## Resolution notes

- `import type { DebateRole }` (line 5): Type-only import, no runtime effect, not a callable binding
- `MatchData` (lines 7-18): Interface definition, not a callable function definition
- `MatchAcceptResponse` (lines 20-24): Interface definition, not a callable function definition

Both arbiter runs agreed. No reconciliation needed.
