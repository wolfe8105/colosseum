# Stage 2 Outputs — profile.js

## Agent 01

### handler

The function is async. It reads the `username` query parameter from `req.query` and validates it against the regex pattern `^[a-zA-Z0-9_]{3,20}$` to ensure it contains only alphanumeric characters and underscores, is between 3 and 20 characters long. If validation fails, it sets response headers (`Content-Type: text/html; charset=utf-8`, `Cache-Control: no-cache`), sends a 404 status, and returns a 404 HTML page built by `build404Html(username || 'unknown')` without error handling. If validation passes, the function enters a try block that first checks the module-level `profileCache` Map for a cached entry with the key `username`, comparing its `expiresAt` timestamp to the current time. If a valid cache hit exists, it sets response headers with CDN cache directives (`public, s-maxage=300, stale-while-revalidate=600`), sends a 200 status, and returns the cached HTML without further processing. On cache miss, the function constructs a Supabase REST API URL using the SUPABASE_URL constant and encodes the username as a query parameter, then awaits a fetch call with an Authorization header containing the SUPABASE_ANON_KEY. If the fetch response is not ok, it throws an error with the Supabase status code. The function then awaits parsing the response as JSON. If the resulting data array is empty, it sets response headers with `no-cache`, sends a 404 status, and returns a 404 HTML page built by `build404Html(username)`. On successful data retrieval, the function extracts the first profile object from the array and awaits `buildProfileHtml(profile)` to generate HTML. It then writes to the module-level `profileCache` Map, storing the HTML and an expiration time calculated as `Date.now() + CACHE_TTL_MS` (60 seconds). Finally, it sets the same CDN cache headers as the cache hit path, sends a 200 status, and returns the generated HTML. If any error occurs in the try block, the catch clause logs the error message to the console and then sends a 500 status with a 404 HTML page built by `build404Html(username)`, again with `no-cache` headers.

## Agent 02

### handler

The function reads the `username` query parameter from the request and reads module-level state including `profileCache` (an in-memory Map), `CACHE_TTL_MS` (60 seconds), `SUPABASE_URL`, `SUPABASE_ANON_KEY`, and `BASE_URL`. It reads the current time via `Date.now()` for cache expiration checks. It writes response headers and status codes to the response object, and conditionally populates `profileCache` with username entries.

First, the function validates the username parameter against a regex `/^[a-zA-Z0-9_]{3,20}$/`. If the username is missing or invalid, it sets response headers for text/html, sets Cache-Control to no-cache, returns a 404 status, and sends the output of `build404Html(username || 'unknown')` without further processing.

Next, the function enters a try block that first checks `profileCache.get(username)` to see if a cached entry exists and whether `cached.expiresAt > Date.now()`. If the cache is valid, the function sets Content-Type and Cache-Control headers with a 5-minute s-maxage and 10-minute stale-while-revalidate, returns status 200, sends the cached HTML, and exits. If the cache is missing or expired, the function constructs a Supabase REST API URL by interpolating the `SUPABASE_URL`, encoding the username in a query parameter, and awaits a `fetch()` call with apikey and Bearer token authorization headers. It then awaits `response.json()`. If the response status is not ok, it throws an error. If the returned data array is empty (user not found), it sets response headers to no-cache and returns a 404 with `build404Html(username)`. Otherwise, it extracts the first profile object from the data array, awaits `buildProfileHtml(profile)` to generate HTML, caches the result in `profileCache` with an expiration timestamp of `Date.now() + CACHE_TTL_MS`, sets response headers with a 5-minute CDN cache, returns status 200, and sends the HTML.

The catch block at the end captures any errors thrown during the fetch or JSON parsing phases, logs the error message to console.error, sets response headers to no-cache, returns status 500, and sends the output of `build404Html(username)`. The function is async and awaits both the fetch and response.json() calls; no calls are fire-and-forget.

## Agent 03

### handler

The function is async and accepts `req` and `res` Vercel serverless function parameters. It reads the `username` query parameter from `req.query` and validates it against the regex `/^[a-zA-Z0-9_]{3,20}$/`. If the username is missing or invalid, it immediately sets `Content-Type` to `text/html; charset=utf-8` and `Cache-Control` to `no-cache`, then returns a 404 response containing the result of `build404Html(username || 'unknown')`.

If validation passes, the function enters a try block and checks the module-level `profileCache` Map for an entry matching the username. If a cached entry exists and its `expiresAt` timestamp is greater than the current `Date.now()`, the function sets `Content-Type` and `Cache-Control` headers (with `s-maxage=300, stale-while-revalidate=600`), returns a 200 response with the cached HTML, and exits early.

If no valid cache entry exists, the function constructs a Supabase REST API URL by encoding the username as a query parameter and awaits a fetch call to that URL, passing the `SUPABASE_ANON_KEY` in both the `apikey` and `Authorization` headers. If the response status is not ok, it throws an error. Otherwise, it awaits `response.json()` to parse the returned data. If the data array is empty or falsy, the function sets headers and returns a 404 response with `build404Html(username)`.

If a profile object exists in the data array, the function calls `buildProfileHtml(profile)` to generate HTML, stores the result in `profileCache` with a key of `username` and an `expiresAt` value of `Date.now() + CACHE_TTL_MS` (60 seconds), sets the same cache headers as the cached path, and returns a 200 response with the generated HTML.

If any error is caught, the function logs the error message to the console, sets headers to no-cache, and returns a 500 response containing `build404Html(username)`. All responses set the `Content-Type` header to `text/html; charset=utf-8`.

## Agent 04

### handler

The handler function is an async serverless function that receives HTTP requests with a username query parameter and returns HTML representing a user's profile page. It reads the username from `req.query`, the module-level `profileCache` Map and `CACHE_TTL_MS` constant, the `SUPABASE_URL`, `SUPABASE_ANON_KEY`, and `BASE_URL` environment variables, and the current time via `Date.now()`. It reads from Supabase via a REST API call and from the imported `buildProfileHtml` and `build404Html` functions. It writes to the HTTP response object `res` by setting headers and sending HTML responses with various status codes, and modifies `profileCache` by inserting entries keyed by username.

The function starts by validating the username parameter against the regex `/^[a-zA-Z0-9_]{3,20}$/`. If validation fails, it sets response headers (`Content-Type` and `Cache-Control`), calls `build404Html(username || 'unknown')`, and sends a 404 response immediately. If validation passes, it enters a try block that first checks `profileCache.get(username)` and compares `cached.expiresAt` to `Date.now()`; if the cached entry exists and is not expired, it sets response headers and sends the cached HTML as a 200 response. If no valid cache entry exists, it constructs a Supabase REST API URL using `SUPABASE_URL` and the username (URI-encoded), then awaits a `fetch()` call with headers containing the `SUPABASE_ANON_KEY` for both `apikey` and `Authorization` bearer token. If the response status is not ok, it throws an error synchronously. If the response is ok, it awaits `response.json()` to parse the returned data array. If the data array is empty or falsy, it sets response headers and sends a 404 response. If the data array is non-empty, it extracts `data[0]` as the profile object, calls `buildProfileHtml(profile)` to generate HTML, writes the result to `profileCache` with an expiration time of `Date.now() + CACHE_TTL_MS`, sets response headers with longer cache directives, and sends a 200 response. If any error is thrown in the try block (network failure, JSON parsing error, or the explicit Supabase error), the catch block logs the error message to the console, sets response headers, and sends a 500 response with 404-styled HTML by calling `build404Html(username)`.

## Agent 05

### handler

The `handler` function is invoked as a Vercel serverless function with `req` and `res` objects. It reads the `username` query parameter from `req.query`, validates it against the regex pattern `^[a-zA-Z0-9_]{3,20}$`, and returns early with a 404 response if validation fails or the username is missing. The function reads three module-level constants: `SUPABASE_URL`, `SUPABASE_ANON_KEY`, and `BASE_URL`, and reads from the module-level `profileCache` Map which stores cached HTML by username with expiration timestamps.

After validation, the function checks the in-memory cache by calling `profileCache.get(username)` and comparing the stored `expiresAt` timestamp to `Date.now()`; if a valid cache entry exists, it writes response headers (`Content-Type` and `Cache-Control`), sends a 200 status with the cached HTML via `res.send()`, and returns immediately. If no valid cache entry exists, the function constructs a Supabase REST API URL and calls `fetch()` with the `apikey` and `Authorization` headers containing `SUPABASE_ANON_KEY`, awaiting the response. It checks the response status and throws an error if not ok. It awaits `response.json()` to parse the response body; if the resulting array is empty or falsy, it writes response headers, sends a 404 status with the result of `build404Html(username)`, and returns. If data exists, the function calls `buildProfileHtml(profile)` with the first profile object and stores the returned HTML in the cache by calling `profileCache.set(username, { html, expiresAt: Date.now() + CACHE_TTL_MS })`. Finally, it writes response headers and sends a 200 status with the generated HTML. A try-catch block wraps the async operations; in the catch path, the function logs the error message via `console.error()`, writes response headers, sends a 500 status with the result of `build404Html(username)`, and returns. All response writing operations are via `res.setHeader()` and `res.status().send()`, and all HTTP calls are properly awaited.
