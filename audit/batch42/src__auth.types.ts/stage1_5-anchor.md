# Anchor List — auth.types.ts

Source: src/auth.types.ts
Produced by: stage 1.5 (arbiter + optional reconciliation)
Unresolved items: 0

(empty — no function definitions found)

## Resolution notes
Both arbiters agreed. No candidates to include or exclude. `src/auth.types.ts` is a pure type-declaration module: 11 exported interfaces, 1 exported type alias, 1 import type statement, zero runtime code. Stage 2 and Stage 3 are trivially complete with no functions to walk.
