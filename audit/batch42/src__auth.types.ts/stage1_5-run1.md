# Anchor List — auth.types.ts (Arbiter Run 1)

(empty — no function definitions found)

## Resolution notes
`src/auth.types.ts` is a pure type declaration file. It contains only TypeScript `interface` declarations (`SafeRpcResult`, `AuthResult`, `Profile`, `PublicProfile`, `FollowRow`, `ModeratorInfo`, `DebateReference`, `RivalData`, `ProfileUpdate`, `SignUpParams`, `LogInParams`), one `type` alias (`AuthListener`), and a single `import type` statement. There are no callable function definitions — no `function` declarations, no arrow function assignments, no method bodies of any kind.
