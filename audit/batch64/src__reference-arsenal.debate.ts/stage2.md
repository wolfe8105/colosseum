# Stage 2 Outputs — reference-arsenal.debate.ts

## Agent 01

### saveDebateLoadout
Called with a `debateId` string and a `referenceIds` string array. Immediately calls `safeRpc<{ success: boolean }>('save_debate_loadout', { p_debate_id: debateId, p_reference_ids: referenceIds })` and awaits the result. `safeRpc` wraps the underlying `supabase.rpc()` call with a 401-retry path; it resolves to `{ data, error }`. Only the `error` field is destructured — `data` is discarded. If `error` is truthy, throws a `new Error` using `error.message` if present, otherwise the literal string `'Failed to save loadout'`. If `error` is falsy, the function returns `undefined` (void). No return value on the success path.

### getMyDebateLoadout
Called with a `debateId` string. Calls `safeRpc<LoadoutRef[]>('get_my_debate_loadout', { p_debate_id: debateId })` and awaits the result. Both `data` and `error` are destructured. If `error` is truthy, throws `new Error` using `error.message` if present, otherwise `'Failed to get loadout'`. On the success path, evaluates `(data || []) as LoadoutRef[]`: if `data` is a non-null/non-undefined array it is returned directly cast as `LoadoutRef[]`; if `data` is null or undefined, an empty array is returned. The cast is purely a TypeScript assertion with no runtime effect.

### citeDebateReference
Called with `debateId`, `referenceId`, `round` (number), and `side` (string). Calls `safeRpc<CiteResult2>('cite_debate_reference', { p_debate_id: debateId, p_reference_id: referenceId, p_round: round, p_side: side })` and awaits the result. Both `data` and `error` are destructured. If `error` is truthy, throws `new Error` using `error.message` if present, otherwise `'Failed to cite reference'`. On the success path, returns `data as CiteResult2`. The `as` cast is a TypeScript-only assertion; at runtime the raw value returned by `safeRpc` is returned directly. If the RPC returns null and no error is set, the function returns `null` cast as `CiteResult2` — callers receive null without a throw.

### fileReferenceChallenge
Called with `debateId`, `referenceId`, `round` (number), and `side` (string). Calls `safeRpc<ChallengeResult2>('file_reference_challenge', { p_debate_id: debateId, p_reference_id: referenceId, p_round: round, p_side: side })` and awaits the result. Both `data` and `error` are destructured. If `error` is truthy, throws `new Error` using `error.message` if present, otherwise `'Failed to file challenge'`. On the success path, returns `data as ChallengeResult2`. Identical null-passthrough risk as `citeDebateReference`: if the RPC resolves with null data and no error, null is returned to the caller without a throw, despite the return type annotation promising a `ChallengeResult2` object.

## Agent 02

### saveDebateLoadout
Called with a `debateId` string and a `referenceIds` string array. Immediately awaits `safeRpc<{ success: boolean }>('save_debate_loadout', { p_debate_id: debateId, p_reference_ids: referenceIds })`. `safeRpc` wraps the Supabase `.rpc()` call with a 401-retry mechanism; it returns an object with `data` and `error` fields. The return value destructures only `error` — `data` is discarded entirely. If `error` is truthy, throws a `new Error` using `error.message` if present, otherwise the fallback string `'Failed to save loadout'`. If `error` is falsy, the function returns `undefined` (implicit void). No local state is read or written. No return value on the success path.

### getMyDebateLoadout
Called with a `debateId` string. Awaits `safeRpc<LoadoutRef[]>('get_my_debate_loadout', { p_debate_id: debateId })`. Destructures both `data` and `error` from the result. If `error` is truthy, throws a `new Error` using `error.message` if present, otherwise `'Failed to get loadout'`. On the success path, evaluates `(data || []) as LoadoutRef[]`: if `data` is null or undefined the expression resolves to an empty array, otherwise it returns `data` cast to `LoadoutRef[]`. The cast is a compile-time assertion only — no runtime shape validation occurs. Returns the array to the caller.

### citeDebateReference
Called with `debateId`, `referenceId`, `round` (number), and `side` (string). Awaits `safeRpc<CiteResult2>('cite_debate_reference', { p_debate_id: debateId, p_reference_id: referenceId, p_round: round, p_side: side })`. Destructures `data` and `error`. If `error` is truthy, throws a `new Error` using `error.message` if present, otherwise `'Failed to cite reference'`. On the success path, returns `data as CiteResult2` — a compile-time cast with no runtime shape check. If the RPC returns null for `data` and no error, this returns `null` cast to `CiteResult2`, which will be a runtime lie about the type. The server-side RPC is documented as atomic (mark + stats + feed event), but all of that logic executes inside the database; this function only transmits parameters and surfaces the result or error.

### fileReferenceChallenge
Called with `debateId`, `referenceId`, `round` (number), and `side` (string). Awaits `safeRpc<ChallengeResult2>('file_reference_challenge', { p_debate_id: debateId, p_reference_id: referenceId, p_round: round, p_side: side })`. Destructures `data` and `error`. If `error` is truthy, throws a `new Error` using `error.message` if present, otherwise `'Failed to file challenge'`. On the success path, returns `data as ChallengeResult2` — again a compile-time cast with no runtime shape validation. As with `citeDebateReference`, a null `data` with no error would silently return `null` typed as `ChallengeResult2`. The server-side atomicity (challenge-count limit enforcement, Shield power-up check, feed event emission) is fully delegated to the database RPC; the client function does not inspect any fields of the result beyond surfacing it to the caller.

## Agent 03

### saveDebateLoadout
Called with a `debateId` string and a `referenceIds` string array. Immediately calls `safeRpc<{ success: boolean }>` with RPC name `'save_debate_loadout'` and a params object containing `p_debate_id: debateId` and `p_reference_ids: referenceIds`. `safeRpc` wraps the underlying `supabase.rpc()` call with 401-retry logic. The return value is destructured to extract only `error`; `data` is discarded. If `error` is truthy, throws a `new Error` using `error.message` if present, otherwise the literal string `'Failed to save loadout'`. If `error` is falsy, the function returns `undefined` (void) with no further action. No local state is read or written. No return value on the success path.

### getMyDebateLoadout
Called with a `debateId` string. Calls `safeRpc<LoadoutRef[]>` with RPC name `'get_my_debate_loadout'` and params `{ p_debate_id: debateId }`. Destructures both `data` and `error` from the result. If `error` is truthy, throws a `new Error` using `error.message` if present, otherwise `'Failed to get loadout'`. If `error` is falsy, evaluates `(data || [])` — if `data` is null or undefined the expression resolves to an empty array — then casts the result `as LoadoutRef[]` and returns it. The caller receives either an array of `LoadoutRef` objects or an empty array. No local state is read or written beyond the RPC response.

### citeDebateReference
Called with `debateId`, `referenceId`, `round` (number), and `side` (string). Calls `safeRpc<CiteResult2>` with RPC name `'cite_debate_reference'` and params `{ p_debate_id: debateId, p_reference_id: referenceId, p_round: round, p_side: side }`. The server-side RPC is documented as atomic — it marks the citation, updates stats, and fires a feed event in a single transaction, but that is opaque to this function. Destructures `data` and `error`. If `error` is truthy, throws a `new Error` using `error.message` if present, otherwise `'Failed to cite reference'`. If `error` is falsy, returns `data as CiteResult2`. No null-guard is applied to `data` before the cast — if the RPC returns no rows and `data` is null, the caller receives `null` typed as `CiteResult2`. No local state is read or written.

### fileReferenceChallenge
Called with `debateId`, `referenceId`, `round` (number), and `side` (string). Calls `safeRpc<ChallengeResult2>` with RPC name `'file_reference_challenge'` and params `{ p_debate_id: debateId, p_reference_id: referenceId, p_round: round, p_side: side }`. The server-side RPC is documented as atomic — it enforces challenge limits, checks for a Shield power-up, and fires a feed event. Destructures `data` and `error`. If `error` is truthy, throws a `new Error` using `error.message` if present, otherwise `'Failed to file challenge'`. If `error` is falsy, returns `data as ChallengeResult2`. Same null-gap as `citeDebateReference`: no null-guard before the cast, so a no-row response yields `null` typed as `ChallengeResult2`. No local state is read or written.

## Agent 04

### saveDebateLoadout
Called with `debateId: string` and `referenceIds: string[]`. Immediately awaits `safeRpc<{ success: boolean }>('save_debate_loadout', { p_debate_id: debateId, p_reference_ids: referenceIds })`. `safeRpc` wraps the Supabase RPC call with 401-retry logic and returns `{ data, error }`. The generic type parameter `{ success: boolean }` is used only for TypeScript inference; the runtime result shape is whatever the server returns. Only `error` is destructured from the result — `data` is discarded entirely. If `error` is truthy, throws `new Error(error.message || 'Failed to save loadout')`, propagating to the caller. If `error` is falsy, the function returns `undefined` (implicit void). No local state is read or written. No return value on the success path.

### getMyDebateLoadout
Called with `debateId: string`. Awaits `safeRpc<LoadoutRef[]>('get_my_debate_loadout', { p_debate_id: debateId })`. Both `data` and `error` are destructured from the result. If `error` is truthy, throws `new Error(error.message || 'Failed to get loadout')`. On success, evaluates `(data || []) as LoadoutRef[]`: if `data` is null or undefined the expression short-circuits to an empty array `[]`; otherwise returns `data` cast to `LoadoutRef[]`. The cast is purely a TypeScript assertion with no runtime effect — no validation, filtering, or transformation of the server payload occurs. Returns the resulting array to the caller.

### citeDebateReference
Called with `debateId: string`, `referenceId: string`, `round: number`, and `side: string`. Awaits `safeRpc<CiteResult2>('cite_debate_reference', { p_debate_id: debateId, p_reference_id: referenceId, p_round: round, p_side: side })`. All four parameters are forwarded as named RPC parameters. Both `data` and `error` are destructured. If `error` is truthy, throws `new Error(error.message || 'Failed to cite reference')`. On success, returns `data as CiteResult2` — a bare TypeScript cast with no runtime shape validation. If the server returns null, `data` will be null at runtime despite the cast asserting `CiteResult2`; the caller receives null with no error thrown. The comment indicates the underlying RPC is atomic server-side (mark + stats + feed event), but the client function has no visibility into or control over that atomicity.

### fileReferenceChallenge
Called with `debateId: string`, `referenceId: string`, `round: number`, and `side: string`. Awaits `safeRpc<ChallengeResult2>('file_reference_challenge', { p_debate_id: debateId, p_reference_id: referenceId, p_round: round, p_side: side })`. All four parameters are forwarded as named RPC parameters, identical in structure to `citeDebateReference`. Both `data` and `error` are destructured. If `error` is truthy, throws `new Error(error.message || 'Failed to file challenge')`. On success, returns `data as ChallengeResult2` — same bare cast pattern as `citeDebateReference`, with the same null-passthrough risk if the server returns null. The comment notes the RPC is atomic server-side (limit enforcement + Shield check + feed event), but the client function only observes success or error from the RPC result.

## Agent 05

### saveDebateLoadout
Called with `debateId: string` and `referenceIds: string[]`. Immediately awaits `safeRpc<{ success: boolean }>('save_debate_loadout', { p_debate_id: debateId, p_reference_ids: referenceIds })`. `safeRpc` wraps the underlying `supabase.rpc()` call with 401-retry logic. The RPC passes both parameters to the `save_debate_loadout` Postgres function (SECURITY DEFINER), which enforces the max-5 loadout limit server-side. The generic type `{ success: boolean }` is used only for typing; the function destructures only `error` from the result and discards `data` entirely. If `error` is truthy, throws a `new Error` using `error.message` if present, otherwise the literal string `'Failed to save loadout'`. If `error` is falsy, returns `undefined` (implicit `void`) with no further action. No local state is written. No return value on success path.

### getMyDebateLoadout
Called with `debateId: string`. Awaits `safeRpc<LoadoutRef[]>('get_my_debate_loadout', { p_debate_id: debateId })`. The RPC fetches the calling user's pre-loaded references for the given debate from the server. Destructures both `data` and `error` from the result. If `error` is truthy, throws `new Error` using `error.message` if present, otherwise `'Failed to get loadout'`. On success, evaluates `(data || [])` — if `data` is null or undefined it substitutes an empty array — then casts to `LoadoutRef[]` and returns it. The caller receives either an array of `LoadoutRef` objects or an empty array. No local state written. The `||` fallback means a null response from the RPC is treated as an empty loadout rather than an error.

### citeDebateReference
Called with `debateId: string`, `referenceId: string`, `round: number`, `side: string`. Awaits `safeRpc<CiteResult2>('cite_debate_reference', { p_debate_id: debateId, p_reference_id: referenceId, p_round: round, p_side: side })`. The server-side RPC is documented as atomic: it marks the reference as cited, updates citation stats, and emits a feed event in a single transaction. Destructures `data` and `error`. If `error` is truthy, throws `new Error` using `error.message` if present, otherwise `'Failed to cite reference'`. On success, returns `data as CiteResult2` — a direct cast with no null-guard. If the RPC returns null data with no error, this returns `null` cast to `CiteResult2`, which could cause a runtime null-dereference downstream if the caller does not check. No local state written.

### fileReferenceChallenge
Called with `debateId: string`, `referenceId: string`, `round: number`, `side: string`. Awaits `safeRpc<ChallengeResult2>('file_reference_challenge', { p_debate_id: debateId, p_reference_id: referenceId, p_round: round, p_side: side })`. The server-side RPC is documented as atomic: it enforces challenge limits, checks whether the target reference has a Shield power-up active, and emits a feed event, all in one transaction. Destructures `data` and `error`. If `error` is truthy, throws `new Error` using `error.message` if present, otherwise `'Failed to file challenge'`. On success, returns `data as ChallengeResult2` — a bare cast with no null-guard, identical behavior to `citeDebateReference`: null data with no error silently propagates a null value typed as `ChallengeResult2`. No local state written.
