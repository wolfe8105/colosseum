# Anchor List — arena-entrance.ts

1. _getTier  (line 18)
2. playEntranceSequence  (line 28)

## Resolution notes

Both functions confirmed as top-level named callable bindings. `_getTier` is a non-exported function declaration. `playEntranceSequence` is an exported function declaration. No candidates excluded. No additional functions found in direct source scan.
