# Stage 2 Outputs — auth.follows.ts

## Agent 01

### followUser

The function takes a `targetUserId` string parameter. It first validates that the parameter is a UUID by calling `isUUID(targetUserId)`; if not, it returns immediately with `{ success: false, error: 'Invalid user ID' }`. Next, it calls `getIsPlaceholderMode()` to read module-level state and if true, returns `{ success: true }` without making any external calls. In the main path, the function awaits a call to `safeRpc('follow_user', { p_target_user_id: targetUserId })` with the target user ID. If the RPC call returns an error, the error is thrown. If the call succeeds, the function returns `{ success: true }`. Any exception caught by the try/catch block—whether from the RPC call or from throwing the error—is caught and the error message is extracted from the exception and returned in an AuthResult with `success: false`.

### unfollowUser

The function takes a `targetUserId` string parameter. It first validates that the parameter is a UUID by calling `isUUID(targetUserId)`; if not, it returns immediately with `{ success: false, error: 'Invalid user ID' }`. Next, it calls `getIsPlaceholderMode()` to read module-level state and if true, returns `{ success: true }` without making any external calls. In the main path, the function awaits a call to `safeRpc('unfollow_user', { p_target_user_id: targetUserId })` with the target user ID. If the RPC call returns an error, the error is thrown. If the call succeeds, the function returns `{ success: true }`. Any exception caught by the try/catch block—whether from the RPC call or from throwing the error—is caught and the error message is extracted from the exception and returned in an AuthResult with `success: false`.

### getFollowers

The function takes a `userId` string parameter. It first calls `getIsPlaceholderMode()` to read module-level state and if true, returns immediately with `{ success: true, data: [], count: 0 }`. In the main path, it calls `getSupabaseClient()!` to obtain a Supabase client, then chains a query on the `follows` table. The query selects `follower_id` and nested profile data (`username, display_name, elo_rating`) via the `follows_follower_id_fkey` foreign key, with `{ count: 'exact' }` to retrieve the total count. It filters for rows where `following_id` equals the `userId` parameter and limits results to 100. The function awaits this query and destructures the returned `data`, `count`, and `error`. If an error exists, it is thrown. If the query succeeds, the function returns the data cast to `FollowRow[]` with the exact count. Any exception is caught and the error message is extracted and returned in an AuthResult with `success: false`, `data: []`, and `count: 0`.

### getFollowing

The function takes a `userId` string parameter. It first calls `getIsPlaceholderMode()` to read module-level state and if true, returns immediately with `{ success: true, data: [], count: 0 }`. In the main path, it calls `getSupabaseClient()!` to obtain a Supabase client, then chains a query on the `follows` table. The query selects `following_id` and nested profile data (`username, display_name, elo_rating`) via the `follows_following_id_fkey` foreign key, with `{ count: 'exact' }` to retrieve the total count. It filters for rows where `follower_id` equals the `userId` parameter and limits results to 100. The function awaits this query and destructures the returned `data`, `count`, and `error`. If an error exists, it is thrown. If the query succeeds, the function returns the data cast to `FollowRow[]` with the exact count. Any exception is caught and the error message is extracted and returned in an AuthResult with `success: false`, `data: []`, and `count: 0`.

### getFollowCounts

The function takes a `userId` string parameter. It first calls `getIsPlaceholderMode()` to read module-level state and if true, returns immediately with `{ followers: 0, following: 0 }`. In the main path, it awaits a call to `safeRpc<{ followers: number; following: number }>('get_follow_counts', { p_user_id: userId })` with a generic type specifying the expected RPC return shape. The function destructures the returned `data` and `error`. If an error exists, it is thrown. If the call succeeds, the function returns the data (or defaults to `{ followers: 0, following: 0 }` if data is null). Any exception caught by the try/catch block is logged to the console with the prefix `'getFollowCounts error:'` and the function returns `{ followers: 0, following: 0 }`.

## Agent 02

### followUser

The function accepts a `targetUserId` string parameter and returns a Promise resolving to an `AuthResult` object. It reads the placeholder mode state by calling `getIsPlaceholderMode()` and calls `isUUID()` to validate the target user ID format. If the ID is not a valid UUID, it synchronously returns `{ success: false, error: 'Invalid user ID' }`. If placeholder mode is active, it returns `{ success: true }` early without making the RPC call. Otherwise, it awaits a call to `safeRpc()` with the RPC procedure name 'follow_user' and an object containing the parameter `p_target_user_id` set to the validated user ID. It checks the returned error property; if an error exists, it throws the error. If no error occurs, it returns `{ success: true }`. The entire async operation is wrapped in a try/catch block that catches any thrown error, extracts the error message, and returns `{ success: false, error: (e as Error).message }`.

### unfollowUser

The function accepts a `targetUserId` string parameter and returns a Promise resolving to an `AuthResult` object. It reads the placeholder mode state by calling `getIsPlaceholderMode()` and calls `isUUID()` to validate the target user ID format. If the ID is not a valid UUID, it synchronously returns `{ success: false, error: 'Invalid user ID' }`. If placeholder mode is active, it returns `{ success: true }` early without making the RPC call. Otherwise, it awaits a call to `safeRpc()` with the RPC procedure name 'unfollow_user' and an object containing the parameter `p_target_user_id` set to the validated user ID. It checks the returned error property; if an error exists, it throws the error. If no error occurs, it returns `{ success: true }`. The entire async operation is wrapped in a try/catch block that catches any thrown error, extracts the error message, and returns `{ success: false, error: (e as Error).message }`.

### getFollowers

The function accepts a `userId` string parameter and returns a Promise resolving to an `AuthResult<FollowRow[]>` object. It reads the placeholder mode state by calling `getIsPlaceholderMode()`; if active, it returns `{ success: true, data: [], count: 0 }` early. Otherwise, it enters a try/catch block and awaits a Supabase query chain: it calls `getSupabaseClient()!.from('follows').select()` with a selection string specifying 'follower_id' and a nested 'profiles' relation filtered to return 'username', 'display_name', and 'elo_rating', passing `{ count: 'exact' }` as an option. It then filters the result with `.eq('following_id', userId)` to match rows where the `following_id` column equals the input `userId`, and chains `.limit(100)` to cap results at 100 rows. After awaiting the query, it destructures the returned `data`, `count`, and `error` properties. If error is truthy, it throws the error. If no error occurs, it type-casts data to `FollowRow[]` and returns `{ success: true, data: data as FollowRow[], count: count ?? 0 }`. If the catch block catches an error, it returns `{ success: false, error: (e as Error).message, data: [], count: 0 }`.

### getFollowing

The function accepts a `userId` string parameter and returns a Promise resolving to an `AuthResult<FollowRow[]>` object. It reads the placeholder mode state by calling `getIsPlaceholderMode()`; if active, it returns `{ success: true, data: [], count: 0 }` early. Otherwise, it enters a try/catch block and awaits a Supabase query chain: it calls `getSupabaseClient()!.from('follows').select()` with a selection string specifying 'following_id' and a nested 'profiles' relation filtered to return 'username', 'display_name', and 'elo_rating', passing `{ count: 'exact' }` as an option. It then filters the result with `.eq('follower_id', userId)` to match rows where the `follower_id` column equals the input `userId`, and chains `.limit(100)` to cap results at 100 rows. After awaiting the query, it destructures the returned `data`, `count`, and `error` properties. If error is truthy, it throws the error. If no error occurs, it type-casts data to `FollowRow[]` and returns `{ success: true, data: data as FollowRow[], count: count ?? 0 }`. If the catch block catches an error, it returns `{ success: false, error: (e as Error).message, data: [], count: 0 }`.

### getFollowCounts

The function accepts a `userId` string parameter and returns a Promise resolving to an object with `followers` and `following` number properties. It reads the placeholder mode state by calling `getIsPlaceholderMode()`; if active, it returns `{ followers: 0, following: 0 }` early. Otherwise, it enters a try/catch block and awaits a call to `safeRpc()` with the RPC procedure name 'get_follow_counts', an object containing the parameter `p_user_id` set to the input `userId`, and a generic type parameter specifying the return data shape as `{ followers: number; following: number }`. It destructures the returned `data` and `error` properties. If error is truthy, it throws the error. If no error occurs, it returns the data object, falling back to `{ followers: 0, following: 0 }` if data is null or undefined. If the catch block catches an error, it logs the error to the console with the label 'getFollowCounts error:' and returns `{ followers: 0, following: 0 }`.

## Agent 03

### followUser

This async function accepts a target user ID and returns a promise of an AuthResult. It first reads the target user ID parameter and reads the placeholder mode state via `getIsPlaceholderMode()`. If the target user ID is not a valid UUID as determined by `isUUID()`, it returns immediately with a failure result containing the error message "Invalid user ID". If placeholder mode is active, it returns immediately with a success result, indicating the write is mocked. In the normal path, it calls `safeRpc()` with the RPC name 'follow_user' and passes the target user ID as the parameter `p_target_user_id`. The function awaits this RPC call. If the RPC response contains an error, the error is thrown into the catch block. If the RPC succeeds with no error, the function returns a success result. Any exception caught in the catch block (including thrown RPC errors) is converted to a failure result containing the exception message.

### unfollowUser

This async function accepts a target user ID and returns a promise of an AuthResult. It first reads the target user ID parameter and reads the placeholder mode state via `getIsPlaceholderMode()`. If the target user ID is not a valid UUID as determined by `isUUID()`, it returns immediately with a failure result containing the error message "Invalid user ID". If placeholder mode is active, it returns immediately with a success result, indicating the write is mocked. In the normal path, it calls `safeRpc()` with the RPC name 'unfollow_user' and passes the target user ID as the parameter `p_target_user_id`. The function awaits this RPC call. If the RPC response contains an error, the error is thrown into the catch block. If the RPC succeeds with no error, the function returns a success result. Any exception caught in the catch block (including thrown RPC errors) is converted to a failure result containing the exception message.

### getFollowers

This async function accepts a user ID and returns a promise of an AuthResult typed to contain an array of FollowRow objects. It first reads the placeholder mode state via `getIsPlaceholderMode()`. If placeholder mode is active, it returns immediately with a success result containing an empty data array and a count of zero. In the normal path, it obtains the Supabase client via `getSupabaseClient()` and constructs a query against the 'follows' table with an exact count. The query selects the 'follower_id' column along with nested 'profiles' data (username, display_name, elo_rating) via the 'follows_follower_id_fkey' foreign key, filters rows where 'following_id' matches the provided user ID, and limits results to 100 rows. The function awaits this query. If the query response contains an error, the error is thrown into the catch block. If the query succeeds, the function returns a success result with the data cast as FollowRow[] and the count from the query response, or 0 if count is null. Any exception caught in the catch block is converted to a failure result containing the exception message, with data as an empty array and count as zero.

### getFollowing

This async function accepts a user ID and returns a promise of an AuthResult typed to contain an array of FollowRow objects. It first reads the placeholder mode state via `getIsPlaceholderMode()`. If placeholder mode is active, it returns immediately with a success result containing an empty data array and a count of zero. In the normal path, it obtains the Supabase client via `getSupabaseClient()` and constructs a query against the 'follows' table with an exact count. The query selects the 'following_id' column along with nested 'profiles' data (username, display_name, elo_rating) via the 'follows_following_id_fkey' foreign key, filters rows where 'follower_id' matches the provided user ID, and limits results to 100 rows. The function awaits this query. If the query response contains an error, the error is thrown into the catch block. If the query succeeds, the function returns a success result with the data cast as FollowRow[] and the count from the query response, or 0 if count is null. Any exception caught in the catch block is converted to a failure result containing the exception message, with data as an empty array and count as zero.

### getFollowCounts

This async function accepts a user ID and returns a promise of an object with 'followers' and 'following' numeric properties. It first reads the placeholder mode state via `getIsPlaceholderMode()`. If placeholder mode is active, it returns immediately with an object containing both counts as zero. In the normal path, it calls `safeRpc()` with the RPC name 'get_follow_counts', passes the user ID as the parameter `p_user_id`, and specifies the expected return type as an object with 'followers' and 'following' numbers. The function awaits this RPC call. If the RPC response contains an error, the error is thrown into the catch block. If the RPC succeeds with no error, the function returns the data from the response, or an object with both counts as zero if data is null. Any exception caught in the catch block is logged to the console (with the key 'getFollowCounts error:') and the function returns an object with both counts as zero.

## Agent 04

### followUser

This function reads the `targetUserId` parameter and calls `isUUID(targetUserId)` to validate the format. If validation fails, it immediately returns a failure result with the error message 'Invalid user ID'. If placeholder mode is enabled via `getIsPlaceholderMode()`, it returns a success result without any side effects. In the normal flow, it awaits a call to `safeRpc('follow_user', { p_target_user_id: targetUserId })`, which sends a remote procedure call to the backend with the target user ID as a parameter. If the RPC response contains an error, that error is thrown. If no error is thrown, the function returns `{ success: true }`. Any exception caught in the try-catch block is returned as a failure result containing the error message.

### unfollowUser

This function reads the `targetUserId` parameter and calls `isUUID(targetUserId)` to validate the format. If validation fails, it returns a failure result with the error message 'Invalid user ID'. If placeholder mode is enabled via `getIsPlaceholderMode()`, it returns a success result without side effects. In the normal flow, it awaits a call to `safeRpc('unfollow_user', { p_target_user_id: targetUserId })`, which sends a remote procedure call with the target user ID. If the RPC response contains an error, that error is thrown. If no error is thrown, the function returns `{ success: true }`. Any exception caught in the try-catch block is returned as a failure result containing the error message.

### getFollowers

This function reads the `userId` parameter. If placeholder mode is enabled via `getIsPlaceholderMode()`, it immediately returns a success result with an empty data array and count of 0. In the normal flow, it awaits a Supabase client query on the 'follows' table, selecting the 'follower_id' field and the associated 'profiles' record with username, display_name, and elo_rating. The query filters by `following_id` equal to the provided `userId` and limits results to 100 rows, with an exact row count. If the query response contains an error, that error is thrown. If successful, the function returns `{ success: true, data: data as FollowRow[], count: count ?? 0 }`. Any exception caught in the try-catch block is returned as a failure result with the error message, an empty data array, and a count of 0.

### getFollowing

This function reads the `userId` parameter. If placeholder mode is enabled via `getIsPlaceholderMode()`, it immediately returns a success result with an empty data array and count of 0. In the normal flow, it awaits a Supabase client query on the 'follows' table, selecting the 'following_id' field and the associated 'profiles' record with username, display_name, and elo_rating. The query filters by `follower_id` equal to the provided `userId` and limits results to 100 rows, with an exact row count. If the query response contains an error, that error is thrown. If successful, the function returns `{ success: true, data: data as FollowRow[], count: count ?? 0 }`. Any exception caught in the try-catch block is returned as a failure result with the error message, an empty data array, and a count of 0.

### getFollowCounts

This function reads the `userId` parameter. If placeholder mode is enabled via `getIsPlaceholderMode()`, it returns `{ followers: 0, following: 0 }` without making any queries. In the normal flow, it awaits a call to `safeRpc('get_follow_counts', { p_user_id: userId })`, which is typed to return an object with `followers` and `following` number properties. If the RPC response contains an error, that error is thrown. If no error is thrown, the function returns `data ?? { followers: 0, following: 0 }`, using the RPC response data or falling back to zero counts if data is null or undefined. Any exception caught in the try-catch block logs the error to the console via `console.error('getFollowCounts error:', e)` and returns `{ followers: 0, following: 0 }`.

## Agent 05

### followUser

The function is async and accepts a targetUserId string parameter. It first reads the targetUserId and validates it by calling isUUID(targetUserId) to check if it's a valid UUID format; if not, it returns immediately with `{ success: false, error: 'Invalid user ID' }`. Next, it reads module-level state by calling getIsPlaceholderMode(); if placeholder mode is active, it returns `{ success: true }` without performing any database operations. If validation passes and not in placeholder mode, it awaits a call to safeRpc('follow_user', { p_target_user_id: targetUserId }), which sends an RPC request to the backend. The response destructures to extract an error property; if error exists, the catch block catches the thrown error. In the catch block, it reads the error message and returns `{ success: false, error: (e as Error).message }`. If the RPC call succeeds with no error, the function returns `{ success: true }`.

### unfollowUser

The function is async and accepts a targetUserId string parameter. It reads and validates the targetUserId by calling isUUID(targetUserId); if validation fails, it returns `{ success: false, error: 'Invalid user ID' }` immediately. It then reads module-level state by calling getIsPlaceholderMode(); if true, it returns `{ success: true }` without proceeding. When not in placeholder mode, it awaits a call to safeRpc('unfollow_user', { p_target_user_id: targetUserId }). The response destructures to check for an error; if present, the catch block is triggered and the function returns `{ success: false, error: (e as Error).message }`. If the RPC call succeeds without error, it returns `{ success: true }`.

### getFollowers

The function is async and accepts a userId string parameter. It first reads module-level state by calling getIsPlaceholderMode(); if true, it returns immediately with `{ success: true, data: [], count: 0 }` without querying the database. When not in placeholder mode, it reads from external storage by calling getSupabaseClient() to obtain a database client, then chains a query on the 'follows' table with a select statement that retrieves follower_id and a related profiles object containing username, display_name, and elo_rating. The query filters rows by equality (eq) on the following_id column matching the userId parameter and limits results to 100 rows. The response includes data, count, and error properties. If error exists, the catch block throws it and returns `{ success: false, error: (e as Error).message, data: [], count: 0 }`. On success, it casts data to FollowRow[] and returns `{ success: true, data: data as FollowRow[], count: count ?? 0 }`.

### getFollowing

The function is async and accepts a userId string parameter. It reads module-level state by calling getIsPlaceholderMode(); if true, it returns `{ success: true, data: [], count: 0 }` immediately without database access. When not in placeholder mode, it reads from external storage by calling getSupabaseClient() to get the database client, then queries the 'follows' table. The select statement retrieves following_id and a related profiles object with username, display_name, and elo_rating. The query filters by equality on the follower_id column matching the userId parameter and limits results to 100 rows. The response destructures data, count, and error. If error is present, the catch block throws it and returns `{ success: false, error: (e as Error).message, data: [], count: 0 }`. On success, it casts data to FollowRow[] and returns `{ success: true, data: data as FollowRow[], count: count ?? 0 }`.

### getFollowCounts

The function is async and accepts a userId string parameter. It reads module-level state by calling getIsPlaceholderMode(); if true, it returns `{ followers: 0, following: 0 }` immediately. When not in placeholder mode, it awaits a call to safeRpc typed as `<{ followers: number; following: number }>` with the RPC name 'get_follow_counts' and parameter `{ p_user_id: userId }`. The response destructures data and error properties. If error exists, the catch block is executed, which logs the error to the console via console.error('getFollowCounts error:', e) and returns `{ followers: 0, following: 0 }`. If the RPC call succeeds, the function returns data with the nullish coalescing operator: `data ?? { followers: 0, following: 0 }`, defaulting to zeros if data is null or undefined.
