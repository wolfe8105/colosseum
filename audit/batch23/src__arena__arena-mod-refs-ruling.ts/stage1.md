# Stage 1 Outputs — src/arena/arena-mod-refs-ruling.ts

## Agent 01

Read 121 lines of 121 total.

Flat numbered inventory in source order:

1. import — `ruleOnReference` from `'../auth.ts'`
2. import — `escapeHTML`, `friendlyError` from `'../config.ts'`
3. import — `_rulingCountdownTimer`, `set__rulingCountdownTimer`, `referencePollTimer`, `set_referencePollTimer`, `set_pendingReferences` from `'./arena-state.ts'` (spans lines 3–6)
4. import (type-only) — `ReferenceItem` from `'./arena-types-results.ts'`
5. import — `addSystemMessage` from `'./arena-room-live-messages.ts'`
6. blank (line 9)
7. bind name to function definition — `showRulingPanel` (exported in place; spans lines 10–105)
8. blank (line 106)
9. bind name to function definition — `startReferencePoll` (exported in place; spans lines 107–116)
10. blank (line 117)
11. bind name to function definition — `stopReferencePoll` (exported in place; spans lines 118–121)

## Agent 02

Read 121 lines of 121 total.

1. Line 1 — import: `ruleOnReference` from `'../auth.ts'`
2. Line 2 — import: `escapeHTML`, `friendlyError` from `'../config.ts'`
3. Lines 3–6 — import: `_rulingCountdownTimer`, `set__rulingCountdownTimer`, `referencePollTimer`, `set_referencePollTimer`, `set_pendingReferences` from `'./arena-state.ts'`
4. Line 7 — import (type-only): `ReferenceItem` from `'./arena-types-results.ts'`
5. Line 8 — import: `addSystemMessage` from `'./arena-room-live-messages.ts'`
6. Line 9 — blank
7. Lines 10–105 — bind name to function definition: `showRulingPanel` (exported)
8. Line 106 — blank
9. Lines 107–116 — bind name to function definition: `startReferencePoll` (exported)
10. Line 117 — blank
11. Lines 118–121 — bind name to function definition: `stopReferencePoll` (exported)

## Agent 03

Read 121 lines of 121 total.

1. import — `ruleOnReference` from `'../auth.ts'`
2. import — `escapeHTML`, `friendlyError` from `'../config.ts'`
3. import — `_rulingCountdownTimer`, `set__rulingCountdownTimer`, `referencePollTimer`, `set_referencePollTimer`, `set_pendingReferences` from `'./arena-state.ts'`
4. import — type `ReferenceItem` from `'./arena-types-results.ts'`
5. import — `addSystemMessage` from `'./arena-room-live-messages.ts'`
6. blank
7. bind name to function definition — `showRulingPanel` (exported)
8. blank
9. bind name to function definition — `startReferencePoll` (exported)
10. blank
11. bind name to function definition — `stopReferencePoll` (exported)

## Agent 04

Read 121 lines of 121 total.

1. import — `ruleOnReference` from `../auth.ts` (line 1)
2. import — `escapeHTML`, `friendlyError` from `../config.ts` (line 2)
3. import — `_rulingCountdownTimer`, `set__rulingCountdownTimer`, `referencePollTimer`, `set_referencePollTimer`, `set_pendingReferences` from `./arena-state.ts` (lines 3–6)
4. import — type-only `ReferenceItem` from `./arena-types-results.ts` (line 7)
5. import — `addSystemMessage` from `./arena-room-live-messages.ts` (line 8)
6. blank (line 9)
7. bind name to function definition — `showRulingPanel` (exported) (lines 10–105)
8. blank (line 106)
9. bind name to function definition — `startReferencePoll` (exported) (lines 107–116)
10. blank (line 117)
11. bind name to function definition — `stopReferencePoll` (exported) (lines 118–121)

## Agent 05

Read 121 lines of 121 total.

Flat numbered inventory in source order:

1. import — `ruleOnReference` from `'../auth.ts'`
2. import — `escapeHTML`, `friendlyError` from `'../config.ts'`
3. import — `_rulingCountdownTimer`, `set__rulingCountdownTimer`, `referencePollTimer`, `set_referencePollTimer`, `set_pendingReferences` from `'./arena-state.ts'` (spans lines 3-6)
4. import — type-only `ReferenceItem` from `'./arena-types-results.ts'`
5. import — `addSystemMessage` from `'./arena-room-live-messages.ts'`
6. blank (line 9)
7. bind name to function definition — `showRulingPanel` (exported in place, spans lines 10-105)
8. blank (line 106)
9. bind name to function definition — `startReferencePoll` (exported in place, spans lines 107-116)
10. blank (line 117)
11. bind name to function definition — `stopReferencePoll` (exported in place, spans lines 118-121)
