# Stage 1 Outputs — arena-mod-queue-browse.ts

## Agent 01

1. import — `safeRpc`, `getCurrentProfile` from `'../auth.ts'`
2. import — `showToast` from `'../config.ts'`
3. import — `view`, `screenEl`, `modQueuePollTimer`, `set_view`, `set_modQueuePollTimer` from `'./arena-state.ts'` (lines 3-6)
4. import — type-only `ModQueueItem` from `'./arena-types-moderator.ts'`
5. import — `showModDebatePicker` from `'./arena-mod-debate-picker.ts'`
6. blank (line 9)
7. comment (lines 10-11)
8. blank (line 12)
9. bind name to function definition — `showModQueue` (exported) (lines 13-49)
10. blank (line 50)
11. bind name to function definition — `loadModQueue` (exported) (lines 51-97)
12. blank (line 98)
13. bind name to function definition — `claimModRequest` (exported) (lines 99-119)
14. blank (line 120)
15. bind name to function definition — `startModQueuePoll` (exported) (lines 121-131)
16. blank (line 132)
17. bind name to function definition — `stopModQueuePoll` (exported) (lines 133-138)

## Agent 02

1. import — `safeRpc`, `getCurrentProfile` from `../auth.ts`
2. import — `showToast` from `../config.ts`
3. import — `view`, `screenEl`, `modQueuePollTimer`, `set_view`, `set_modQueuePollTimer` from `./arena-state.ts` (lines 3–6)
4. import — type-only `ModQueueItem` from `./arena-types-moderator.ts`
5. import — `showModDebatePicker` from `./arena-mod-debate-picker.ts`
6. blank
7. comment (lines 10–11)
8. blank
9. bind name to function definition — `showModQueue` (exported)
10. blank
11. bind name to function definition — `loadModQueue` (exported)
12. blank
13. bind name to function definition — `claimModRequest` (exported)
14. blank
15. bind name to function definition — `startModQueuePoll` (exported)
16. blank
17. bind name to function definition — `stopModQueuePoll` (exported)

## Agent 03

(Same as Agent 02 with line numbers; identical anchor list.)

## Agent 04

(Same as Agent 03; identical.)

## Agent 05

(Same as Agent 02 with line numbers; identical.)
