# Stage 2 Outputs — reference-arsenal.forge-render.ts

## Agent 01

Read 141 lines of 141 total.

### _buildForgeContent
This synchronous function reads two parameters: `state` (a `ForgeFormState` object) and `isEdit` (a boolean). It declares a local `html` string initialized to the return value of calling `_renderStepIndicator(state.step)`, then appends a literal opening `<div class="forge-body">` tag, then appends the return value of calling `_renderStep(state, isEdit)`, then appends the literal closing `</div>` tag, then appends the return value of calling `_renderNav(state.step, isEdit)`. The function returns the concatenated `html` string. It writes nothing to module-level state, the DOM, storage, or the network. Control flow is linear with no branches, loops, or try/catch. The calls on the anchor list are invoked in order: `_renderStepIndicator`, then `_renderStep`, then `_renderNav`.

### _renderStepIndicator
This synchronous function reads one parameter: `currentStep` (a number). It declares a local `stepNames` array containing five literal strings. It initializes `html` to the literal `<div class="forge-steps">`. It then runs a `for` loop with `i` from 1 to 5 inclusive. On each iteration it computes `cls` using a nested ternary: if `i === currentStep` the value is `'forge-step active'`, otherwise if `i < currentStep` the value is `'forge-step done'`, otherwise it is `'forge-step'`. It appends a `<div>` with that class containing a numbered `<span>` and a label `<span>` whose text is `stepNames[i - 1]`. After the loop it appends the closing `</div>` and returns `html`. The integer `i`, the computed class string, and the step name are all interpolated into the template without HTML escaping — the values are trusted module constants and a numeric argument. No module state, DOM, storage, or network is touched. There are no error paths.

### _renderStep
This synchronous function reads two parameters: `state` and `isEdit`. It executes a `switch` on `state.step` with five case labels (1 through 5) and a default. Each case performs an early `return` that delegates to a different step renderer on the anchor list: case 1 calls `_renderStep1(state)`; case 2 calls `_renderStep2(state)`; case 3 calls `_renderStep3(state, isEdit)`; case 4 calls `_renderStep4(state)`; case 5 calls `_renderStep5(state, isEdit)`. The returned value from the matching branch is passed through as the function's return value. The default branch returns the empty string. No module-level state, DOM, storage, or network is touched. There are no loops or try/catch blocks; the only branching is the `switch`.

### _renderStep1
This synchronous function reads one parameter: `state`. It aliases the imported `escapeHTML` function from `./config.ts` as `esc` for brevity. It returns a single template literal containing an `<h3>` heading "Source Details", a hint paragraph, and five labeled input elements with ids `forge-title`, `forge-author`, `forge-date`, `forge-locator`, and `forge-url`. The `value` attribute of each input is populated by calling `esc(...)` on the corresponding state fields `state.source_title`, `state.source_author`, `state.source_date`, `state.locator`, and `state.source_url` in that order. The function writes nothing, has no branches, loops, or try/catch, and makes no external calls other than the five synchronous `escapeHTML` invocations.

### _renderStep2
This synchronous function reads one parameter: `state`. It aliases `escapeHTML` as `esc`. It returns a template literal containing an `<h3>` heading "Your Claim", a hint paragraph stating the 120-character limit, a `<textarea>` with id `forge-claim`, `maxlength="120"`, and three rows whose inner text is `esc(state.claim_text)`, followed by a character-count `<div>` that reports `state.claim_text.length` inside a `<span id="forge-claim-count">`. The `.length` value is interpolated as a raw number without escaping. The function writes nothing, has no branches or loops, and calls `escapeHTML` once.

### _renderStep3
This synchronous function reads two parameters: `state` and `isEdit`. It initializes `html` to a template literal containing an `<h3>` heading "Source Type", a hint paragraph whose wording varies by a ternary on `isEdit` (either a bolded "Locked at creation — cannot be changed." phrase or the string "This determines the power ceiling."), and an opening `<div class="forge-source-types">`. It then iterates `Object.entries(SOURCE_TYPES)` — `SOURCE_TYPES` is the constant imported from `./reference-arsenal.constants.ts`. On each iteration it computes `selected` as the string `' selected'` when `state.source_type === key` else the empty string, and `disabled` as `' disabled'` when `isEdit` is truthy else the empty string. It appends a `<button>` element whose class is `forge-source-btn` concatenated with `selected` and `disabled`, whose `data-source` attribute is the untrusted-but-trusted constant `key`, and which is additionally marked `disabled` via an inline attribute when `isEdit` is true (producing a redundant second `disabled` attribute in the edit branch). The button contains two `<span>` children reading `info.label`, `info.tier`, and `info.ceiling` from the `SOURCE_TYPES` entry. None of these interpolated values are escaped. After the loop it appends the closing `</div>` and returns `html`. No module state, DOM, storage, or network is written, and there are no try/catch blocks.

### _renderStep4
This synchronous function reads one parameter: `state`. It initializes `html` to a template literal containing an `<h3>` heading "Choose Your Arena", a hint paragraph, and an opening `<div class="forge-categories">`. It iterates the imported `CATEGORIES` constant with a `for...of` loop. On each iteration it computes `selected` as `' selected'` when `state.category === cat` else the empty string, and appends a `<button>` whose class is `forge-cat-btn` concatenated with `selected`, whose `data-cat` attribute is `cat`, and whose text content is `CATEGORY_LABELS[cat]` (both imported constants). Neither `cat` nor the label is HTML-escaped. After the loop it appends the closing `</div>` and returns `html`. No module state, DOM, storage, or network is touched, and no error paths exist.

### _renderStep5
This synchronous function reads two parameters: `state` and `isEdit`. It aliases `escapeHTML` as `esc`. It computes `srcInfo` by checking whether `state.source_type` is truthy; if so it indexes `SOURCE_TYPES` with `state.source_type` cast to `SourceType`, otherwise it assigns `null`. It then returns a single template literal composing a review card. The card interpolates `esc(state.claim_text)` inside quote marks, `esc(state.source_title)` in a `<strong>`, `esc(state.source_author)` and `esc(state.source_date)` separated by a middle-dot, and `esc(state.locator)`. If `state.source_url` is truthy, a nested ternary inserts a `<br/>` followed by an `<a>` element whose `href` is the return value of `sanitizeUrl(state.source_url ?? '')` (imported from `./config.ts`) and whose visible text is `esc(state.source_url)`; otherwise that fragment is the empty string. If `srcInfo` is truthy it interpolates a line composed of `srcInfo.label`, `srcInfo.tier`, and `srcInfo.ceiling` (unescaped, from the constant table); otherwise that line is empty. The category line interpolates `CATEGORY_LABELS[state.category as ReferenceCategory]` when `state.category` is truthy else the empty string. A final hint paragraph varies by `isEdit`: "Editing costs 10 tokens." when true, "Forging costs 50 tokens." when false. The function writes nothing external, runs no loops, and has no try/catch.

### _renderNav
This synchronous function reads two parameters: `step` (a number) and `isEdit` (a boolean). It initializes `html` to the literal `<div class="forge-nav">`. The first branch checks `step > 1`: the true branch appends a `<button id="forge-back" class="forge-btn-secondary">Back</button>`; the else branch appends a `<button id="forge-cancel" class="forge-btn-secondary">Cancel</button>`. The second branch checks `step < 5`: the true branch appends a `<button id="forge-next" class="forge-btn-primary">Next</button>`; the else branch appends a `<button id="forge-submit" class="forge-btn-primary">` whose inner text is selected by a ternary on `isEdit` — either the pencil-emoji "Save Changes (10t)" label or the crossed-swords "Forge Weapon (50t)" label. It appends the closing `</div>` and returns `html`. No module state, DOM, storage, or network is written, and there are no loops or try/catch blocks.

## Agent 02

Read 141 lines of 141 total.

### _buildForgeContent
This synchronous function reads two parameters: `state` (a `ForgeFormState` object) and `isEdit` (a boolean). It initializes a local `html` string by calling `_renderStepIndicator` with `state.step`, then appends the literal string `<div class="forge-body">`, then appends the result of calling `_renderStep` with `state` and `isEdit`, then appends the closing `</div>`, then appends the result of calling `_renderNav` with `state.step` and `isEdit`. It returns the concatenated `html` string. It writes nothing to module-level state, the DOM, storage, or any external API; all output is contained in the returned string. It calls, in order, `_renderStepIndicator`, `_renderStep`, and `_renderNav`. It has no branches, loops, try/catch blocks, or early returns, and it is not async.

### _renderStepIndicator
This synchronous function reads one parameter, `currentStep` (a number). It declares a local `stepNames` array of five string labels (`'Source Details'`, `'Your Claim'`, `'Source Type'`, `'Choose Arena'`, `'Review & Forge'`) and initializes a local `html` string with `<div class="forge-steps">`. It then enters a `for` loop with `i` running from 1 through 5 inclusive. In each iteration it computes a class string by a nested ternary: `'forge-step active'` when `i === currentStep`, `'forge-step done'` when `i < currentStep`, and `'forge-step'` otherwise. It appends a `<div>` with that class containing two `<span>` elements holding the step number `i` and the label `stepNames[i - 1]`. After the loop it appends `</div>` and returns the `html` string. It writes nothing outside the returned string, calls no other anchor-list functions, and is not async. It has no try/catch or early returns; the only control flow is the fixed-count loop and the inline ternary.

### _renderStep
This synchronous function reads `state` (a `ForgeFormState`) and `isEdit` (a boolean), dispatching on `state.step` via a `switch` statement. Case `1` returns the result of `_renderStep1(state)`. Case `2` returns the result of `_renderStep2(state)`. Case `3` returns the result of `_renderStep3(state, isEdit)` — note that `isEdit` is forwarded only for steps 3 and 5. Case `4` returns the result of `_renderStep4(state)`. Case `5` returns the result of `_renderStep5(state, isEdit)`. The `default` branch returns the empty string, which is the fallback path for any `state.step` value outside 1 through 5. Each case is an early return; no concatenation or post-processing occurs. It writes nothing to module-level or external state, has no try/catch, and is not async. It calls exactly one of `_renderStep1` through `_renderStep5` per invocation, chosen by the `state.step` value.

### _renderStep1
This synchronous function reads the `state` parameter and aliases the imported `escapeHTML` function as local `esc`. It returns a single template literal containing an `<h3>` header, a hint paragraph, and five pairs of `<label>`/`<input>` elements for source title, author/organization, source date (typed `date`), locator, and source URL (typed `url`). Each input's `value` attribute is populated by calling `esc` on the corresponding state field: `state.source_title`, `state.source_author`, `state.source_date`, `state.locator`, and `state.source_url`. The `id` attributes on the inputs are `forge-title`, `forge-author`, `forge-date`, `forge-locator`, and `forge-url`. It calls `escapeHTML` five times and nothing else. It writes nothing outside the returned string, has no branches, loops, try/catch, or early returns, and is not async. It does not touch the DOM — it only produces markup text for a caller to insert later.

### _renderStep2
This synchronous function reads the `state` parameter and aliases `escapeHTML` as local `esc`. It returns a template literal with an `<h3>` header, a hint paragraph specifying a 120-character limit, a `<textarea>` with `id="forge-claim"`, `maxlength="120"`, `rows="3"`, a placeholder, and inner text set to `esc(state.claim_text)`, and a character counter `<div>` that embeds `state.claim_text.length` inside a `<span id="forge-claim-count">`. It calls `escapeHTML` once on `state.claim_text` and reads `state.claim_text.length` directly without escaping (the numeric length is interpolated as-is). It writes nothing outside the returned string, has no branches, loops, try/catch, or early returns, and is not async.

### _renderStep3
This synchronous function reads `state` and `isEdit`. It initializes a local `html` string with an `<h3>` header and a hint paragraph whose text depends on `isEdit`: when `isEdit` is true the paragraph ends with a bolded lock notice (`Locked at creation — cannot be changed.`), otherwise it ends with `This determines the power ceiling.`. It appends an opening `<div class="forge-source-types">`. It then iterates over `Object.entries(SOURCE_TYPES)` with a `for...of` loop; iteration order follows the insertion order of the `SOURCE_TYPES` object imported from `./reference-arsenal.constants.ts`. For each `[key, info]` pair, it computes a `selected` suffix (`' selected'` when `state.source_type === key`, else the empty string) and a `disabled` suffix (`' disabled'` when `isEdit` is true, else empty) — the `disabled` value is appended twice per button: once as part of the CSS class string and once as a bare attribute placed via a second inline ternary `${isEdit ? 'disabled' : ''}`. Each appended `<button>` has class `forge-source-btn` plus the two suffixes, a `data-source` attribute holding `key`, and two inner `<span>` elements showing `info.label` and a tier/ceiling line formed from `info.tier` and `info.ceiling`. After the loop it appends `</div>` and returns `html`. It writes nothing outside the returned string, calls no anchor-list functions, and is not async. It has no try/catch or early returns.

### _renderStep4
This synchronous function reads the `state` parameter. It initializes a local `html` string with an `<h3>` header, a hint paragraph, and an opening `<div class="forge-categories">`. It then iterates over the imported `CATEGORIES` array with a `for...of` loop. For each `cat`, it computes a `selected` suffix (`' selected'` if `state.category === cat`, else empty) and appends a `<button>` with class `forge-cat-btn` plus that suffix, a `data-cat` attribute holding `cat`, and inner text `CATEGORY_LABELS[cat]` — the label is read from the imported map keyed by the category identifier. After the loop it appends `</div>` and returns `html`. No escaping is applied to `cat` or to `CATEGORY_LABELS[cat]`; safety relies on those values being trusted constants from `./reference-arsenal.constants.ts`. It writes nothing outside the returned string, calls no anchor-list functions, has no try/catch or early returns, and is not async.

### _renderStep5
This synchronous function reads `state` and `isEdit` and aliases `escapeHTML` as local `esc`. It computes `srcInfo` by a ternary: when `state.source_type` is truthy it looks up `SOURCE_TYPES[state.source_type as SourceType]`, otherwise it sets `srcInfo` to `null`. It returns a template literal containing an `<h3>` header and a `<div class="forge-review-card">` with four nested sections. The first shows the escaped `state.claim_text` wrapped in quotes. The second shows the escaped `state.source_title` in `<strong>`, followed by a line break and the escaped author and date joined by a middle dot. The third shows the escaped locator, and when `state.source_url` is truthy it appends a `<br/>` and an `<a>` tag whose `href` is the result of `sanitizeUrl(state.source_url ?? '')` (imported from `./config.ts`) and whose visible text is the escaped URL; the `target="_blank"` and `rel="noopener"` attributes are hardcoded. The fourth section shows the source type description when `srcInfo` is non-null using `srcInfo.label`, `srcInfo.tier`, and `srcInfo.ceiling`, or an empty string when `srcInfo` is null. A fifth div shows the category label: when `state.category` is truthy it renders `CATEGORY_LABELS[state.category as ReferenceCategory]`, otherwise an empty string. After the card it appends a hint paragraph whose text depends on `isEdit`: `Editing costs 10 tokens.` when true, `Forging costs 50 tokens.` when false. It calls `escapeHTML` six times and `sanitizeUrl` at most once (only when `state.source_url` is truthy). It writes nothing outside the returned string, has no try/catch or early returns, and is not async.

### _renderNav
This synchronous function reads `step` (a number) and `isEdit` (a boolean). It initializes a local `html` string with `<div class="forge-nav">`. The first branch keys on `step`: when `step > 1` it appends a `<button id="forge-back" class="forge-btn-secondary">Back</button>`, otherwise it appends a `<button id="forge-cancel" class="forge-btn-secondary">Cancel</button>`. The second branch keys on `step` again: when `step < 5` it appends a `<button id="forge-next" class="forge-btn-primary">Next</button>`, otherwise it appends a `<button id="forge-submit" class="forge-btn-primary">` whose inner text depends on `isEdit` — `✏️ Save Changes (10t)` when true, `⚔ Forge Weapon (50t)` when false. It appends the closing `</div>` and returns `html`. The branches are independent, so for `step === 1` the nav shows Cancel + Next; for `2 <= step <= 4` it shows Back + Next; for `step === 5` it shows Back + Submit. It writes nothing outside the returned string, calls no anchor-list functions, has no loops or try/catch, and is not async.

## Agent 03

Read 141 lines of 141 total.

### _buildForgeContent
Synchronous function. Reads its two parameters `state` (a `ForgeFormState`) and `isEdit` (boolean). Declares a local `html` string initialized by calling `_renderStepIndicator(state.step)` which returns the step-indicator markup. Appends the literal `<div class="forge-body">`, then appends the result of calling `_renderStep(state, isEdit)` which dispatches to one of the five per-step renderers based on `state.step`. Appends the closing `</div>`, then appends the return value of `_renderNav(state.step, isEdit)` which returns the bottom navigation markup. Returns the concatenated `html` string. No external state is read or written, no DOM or network access, no try/catch, no branches, no loops, no awaits.

### _renderStepIndicator
Synchronous function. Reads its single parameter `currentStep`. Declares a local array `stepNames` with the five fixed labels. Initializes `html` with `<div class="forge-steps">`. Enters a `for` loop with `i` from 1 through 5 inclusive. On each iteration it computes a class string via a nested ternary: `'forge-step active'` when `i === currentStep`, `'forge-step done'` when `i < currentStep`, `'forge-step'` otherwise. It appends a `<div>` with that class containing two spans — the numeric step `${i}` and the label `stepNames[i - 1]`. After the loop, appends `</div>` and returns `html`. Labels are interpolated without escaping — they come from the hardcoded literal array, not user input.

### _renderStep
Synchronous function. Reads `state` and `isEdit`, dispatching on `state.step`. Case 1 returns `_renderStep1(state)`; case 2 returns `_renderStep2(state)`; case 3 returns `_renderStep3(state, isEdit)`; case 4 returns `_renderStep4(state)`; case 5 returns `_renderStep5(state, isEdit)`; default returns the empty string. Each case is an early return.

### _renderStep1
Synchronous. Reads `state.source_title`, `state.source_author`, `state.source_date`, `state.locator`, `state.source_url`. Aliases `escapeHTML` as `esc`. Returns a single template literal containing a heading, a hint paragraph, and five labeled inputs: `#forge-title`, `#forge-author`, `#forge-date`, `#forge-locator`, `#forge-url`. Each input's `value` attribute is populated by calling `esc(...)` on the corresponding state field. Calls `escapeHTML` five times.

### _renderStep2
Synchronous. Reads `state.claim_text`. Aliases `escapeHTML` as `esc`. Returns a template literal with an `<h3>` header, a hint paragraph, a `<textarea id="forge-claim" maxlength="120" rows="3">` whose inner text is `esc(state.claim_text)`, and a character counter `<div>` that embeds `state.claim_text.length` inside a `<span id="forge-claim-count">`. The length is interpolated as a raw number.

### _renderStep3
Synchronous. Reads `state.source_type`, `isEdit`, and the imported `SOURCE_TYPES`. Initializes `html` with a heading and a hint paragraph whose content branches on `isEdit`. Appends `<div class="forge-source-types">`. Iterates `Object.entries(SOURCE_TYPES)` with a `for...of` loop. For each pair: computes `selected` and `disabled` suffixes; appends a `<button>` with class `forge-source-btn${selected}${disabled}`, `data-source="${key}"`, and a trailing inline `${isEdit ? 'disabled' : ''}` that emits a real disabled attribute when editing. The button body contains `info.label` and a tier/ceiling line. None of the values are escaped.

### _renderStep4
Synchronous. Reads `state.category` and the imported `CATEGORIES` array and `CATEGORY_LABELS` map. Iterates `CATEGORIES` with a `for...of` loop. For each `cat`: computes `selected`; appends `<button class="forge-cat-btn${selected}" data-cat="${cat}">${CATEGORY_LABELS[cat]}</button>`. Neither `cat` nor the label is escaped.

### _renderStep5
Synchronous. Reads `state.claim_text`, `state.source_title`, `state.source_author`, `state.source_date`, `state.locator`, `state.source_url`, `state.source_type`, `state.category`, `isEdit`, and the imported `SOURCE_TYPES` and `CATEGORY_LABELS`. Calls `escapeHTML` (aliased as `esc`) and `sanitizeUrl` from `./config.ts`. Computes `srcInfo` by ternary: if `state.source_type` truthy, `srcInfo = SOURCE_TYPES[state.source_type as SourceType]`, else `null`. Returns a template literal rendering a review card. The card interpolates `esc(claim_text)` in quotes, `esc(title)` in `<strong>`, `esc(author)` and `esc(date)` joined by middle dot, `esc(locator)`, and — only when `state.source_url` is truthy — an `<a>` whose `href` is `sanitizeUrl(state.source_url ?? '')` and whose visible text is `esc(state.source_url)`. The source-type line interpolates `srcInfo.label`, `srcInfo.tier`, `srcInfo.ceiling` when non-null, else empty. The category line interpolates `CATEGORY_LABELS[state.category as ReferenceCategory]` when truthy, else empty. A trailing hint paragraph branches on `isEdit`.

### _renderNav
Synchronous. Reads `step` and `isEdit`. Opens `<div class="forge-nav">`. First branch: if `step > 1` appends `<button id="forge-back">Back</button>`, else `<button id="forge-cancel">Cancel</button>`. Second branch: if `step < 5` appends `<button id="forge-next">Next</button>`, else appends `<button id="forge-submit">` whose label ternary emits `'\u270F\uFE0F Save Changes (10t)'` when `isEdit` else `'\u2694 Forge Weapon (50t)'`. Closes `</div>` and returns `html`.

## Agent 04

Read 141 lines of 141 total.

### _buildForgeContent
Synchronous. Reads `state.step` and the `isEdit` parameter. Writes nothing outside its local `html` string variable. Calls, in order: `_renderStepIndicator(state.step)` to seed `html`; appends the literal opening `<div class="forge-body">`; calls `_renderStep(state, isEdit)` and appends its return value; appends the literal closing `</div>`; calls `_renderNav(state.step, isEdit)` and appends its return value. No branches, no loops, no try/catch. Returns the concatenated string.

### _renderStepIndicator
Synchronous. Reads its `currentStep` parameter and the local `stepNames` array of five label strings. Writes nothing outside the local `html` string. No other anchor-list functions or external APIs are called. Control flow: a single `for` loop iterating `i` from 1 through 5. Inside the loop, a nested ternary selects `cls`: `'forge-step active'` when `i === currentStep`, `'forge-step done'` when `i < currentStep`, otherwise `'forge-step'`. The loop appends one `<div>` per iteration interpolating `cls`, `i`, and `stepNames[i - 1]`. No early return, no error path. After the loop, appends a closing `</div>` and returns `html`.

### _renderStep
Synchronous. Reads `state.step` and the `isEdit` parameter; forwards `state` (and for cases 3 and 5, `isEdit`) to the selected step renderer. Writes nothing. Control flow is a `switch` on `state.step`: case 1 returns `_renderStep1(state)`; case 2 returns `_renderStep2(state)`; case 3 returns `_renderStep3(state, isEdit)`; case 4 returns `_renderStep4(state)`; case 5 returns `_renderStep5(state, isEdit)`; `default` returns the empty string. Each case is an early return; no fallthrough, no loop, no try/catch.

### _renderStep1
Synchronous. Reads `state.source_title`, `state.source_author`, `state.source_date`, `state.locator`, and `state.source_url`. Writes nothing outside its returned template literal. Aliases the imported `escapeHTML` as `esc` and invokes it five times, once per field value interpolated into the template: title, author, date, locator, URL — in that textual order. No branches, no loops, no error path. Returns a template literal containing an `<h3>`, a hint paragraph, and five `<label>`/`<input>` pairs for `forge-title`, `forge-author`, `forge-date` (type `date`), `forge-locator`, and `forge-url` (type `url`).

### _renderStep2
Synchronous. Reads `state.claim_text` twice: once passed through `escapeHTML` (aliased as `esc`) as the textarea's initial content, and once as `state.claim_text.length` for the numeric character count. Writes nothing outside the returned template literal. Calls `escapeHTML` once. No branches, no loops, no error path. Returns a template literal with an `<h3>`, a hint, a `<textarea id="forge-claim" maxlength="120" rows="3">`, and a `<div class="forge-charcount">` whose `<span id="forge-claim-count">` shows the current length against `/120`.

### _renderStep3
Synchronous. Reads the imported `SOURCE_TYPES` object, `state.source_type`, and the `isEdit` parameter. Writes nothing outside its local `html` string. Control flow: first appends a header block whose hint text branches on `isEdit` — if true, emits a bolded "Locked at creation" notice; otherwise emits "This determines the power ceiling." Then iterates `Object.entries(SOURCE_TYPES)` with a `for...of` loop. Each iteration computes `selected` (the string ` selected` when `state.source_type === key`, else empty) and `disabled` (the string ` disabled` when `isEdit`, else empty — this variable is declared but in practice only the inline `${isEdit ? 'disabled' : ''}` is used as the actual attribute; the `disabled` local concatenates into the class list, so it injects the literal text ` disabled` into the `class` attribute when editing — uncertainty: this appears to be an incidental class-name artifact rather than an intended CSS hook). Appends one `<button class="forge-source-btn...">` per entry, interpolating `key` as `data-source`, `info.label`, `info.tier`, and `info.ceiling`. After the loop, appends a closing `</div>` and returns `html`. No other anchor-list functions called. No try/catch.

### _renderStep4
Synchronous. Reads the imported `CATEGORIES` array, the imported `CATEGORY_LABELS` map, and `state.category`. Writes nothing outside its local `html` string. No other anchor-list functions called; no `escapeHTML` invocation (category keys come from a fixed constants table). Control flow: appends a static header block with `<h3>` and hint, then iterates `CATEGORIES` with a `for...of` loop. Each iteration computes `selected` as ` selected` when `state.category === cat`, else empty, and appends a `<button class="forge-cat-btn${selected}" data-cat="${cat}">${CATEGORY_LABELS[cat]}</button>`. After the loop, appends a closing `</div>` and returns `html`. No branches beyond the per-iteration ternary, no error path.

### _renderStep5
Synchronous. Reads many state fields and constants (see above agents). Writes nothing outside the returned template literal. Aliases `escapeHTML` as `esc`. Computes `srcInfo` via ternary. Calls, in textual order in the template: `esc(state.claim_text)`, `esc(state.source_title)`, `esc(state.source_author)`, `esc(state.source_date)`, `esc(state.locator)`, then (conditionally) `sanitizeUrl(state.source_url ?? '')` and `esc(state.source_url)`. Control flow uses ternaries inside the template literal to conditionally emit URL anchor, source-type line, category line, and cost hint.

### _renderNav
Synchronous. Reads the `step` and `isEdit` parameters. Writes nothing outside its local `html` string. Calls no other functions. Control flow: opens a `<div class="forge-nav">`; first `if` branches on `step > 1` — true appends a Back button, else a Cancel button. Second `if` branches on `step < 5` — true appends a Next button, else a Submit button whose label ternary emits the pencil-emoji save label when `isEdit` is true else the crossed-swords forge label. Closes `</div>` and returns `html`. No loops, no error path, no try/catch.

## Agent 05

Read 141 lines of 141 total.

### _buildForgeContent
Synchronous. Reads `state.step` (to pass to `_renderStepIndicator` and `_renderNav`) and the full `state` plus `isEdit` (to pass to `_renderStep`). Writes nothing outside its local `html` string. Calls, in order: `_renderStepIndicator(state.step)` — the returned string is assigned to `html`; appends the literal `<div class="forge-body">` opening tag; calls `_renderStep(state, isEdit)` and concatenates its return onto `html`; appends the literal `</div>` closing tag; calls `_renderNav(state.step, isEdit)` and concatenates its return onto `html`; returns `html`. No branches, no loops, no try/catch, no early returns.

### _renderStepIndicator
Synchronous. Reads only the `currentStep` parameter. Writes nothing outside its local `html` string and local `stepNames` array. Builds a fixed-length 5-element `stepNames` array of labels, opens a `<div class="forge-steps">` wrapper, then enters a `for` loop running `i` from 1 to 5 inclusive. Inside the loop, a ternary chain picks the class. Each iteration appends a `<div>` containing a numbered span and the label at `stepNames[i - 1]`. The label is interpolated raw — not escaped — but the source of `stepNames` is a local literal, so no user-controlled input reaches the interpolation. After the loop, closes the wrapper `</div>` and returns `html`. No other function calls, no error path.

### _renderStep
Synchronous dispatcher. Reads `state.step` and switches on it. Branches: `case 1` returns `_renderStep1(state)`; `case 2` returns `_renderStep2(state)`; `case 3` returns `_renderStep3(state, isEdit)`; `case 4` returns `_renderStep4(state)`; `case 5` returns `_renderStep5(state, isEdit)`; `default` returns the empty string `''`. Each case is an early return. Cases 1, 2, and 4 receive only `state` while cases 3 and 5 also receive `isEdit`.

### _renderStep1
Synchronous. Reads five state fields. Aliases `escapeHTML` as `esc`. Returns a template literal producing a heading, a hint paragraph, and five labeled inputs. Each input's `value` attribute is the corresponding state field passed through `esc(...)`. `esc` is invoked exactly five times in the order title, author, date, locator, URL. No branches, no loops, no try/catch.

### _renderStep2
Synchronous. Reads `state.claim_text` twice — once passed through `esc` for the textarea content, and once as `state.claim_text.length` for the counter. Writes nothing. The textarea has `maxlength="120"` and `rows="3"`. Returns the assembled template literal. `escapeHTML` is called once via `esc`.

### _renderStep3
Synchronous. Reads `isEdit`, `state.source_type`, and `SOURCE_TYPES`. Builds `html` starting with the heading; the hint-paragraph text branches on `isEdit`. Iterates `Object.entries(SOURCE_TYPES)`. Per entry, computes `selected` and `disabled`, appends a `<button>` with the class carrying both flags, a `data-source="${key}"` attribute, and a second `disabled` attribute emitted only when `isEdit` is true. The button's inner spans interpolate `info.label`, `info.tier`, and `info.ceiling` without escaping; uncertainty: this is safe only if `SOURCE_TYPES` values are trusted static constants. After the loop, closes `</div>` and returns `html`.

### _renderStep4
Synchronous. Reads `state.category`, `CATEGORIES`, `CATEGORY_LABELS`. Emits a heading, a hint, and a `<div class="forge-categories">`. Enters a `for...of` loop. Each iteration computes `selected` and appends a `<button class="forge-cat-btn${selected}" data-cat="${cat}">` with `CATEGORY_LABELS[cat]` as inner text. Neither `cat` nor the label is escaped; uncertainty: this is safe only if both are static constants. After the loop, closes `</div>` and returns `html`.

### _renderStep5
Synchronous. Reads many state fields. Aliases `escapeHTML` as `esc`. Computes `srcInfo` with a ternary. Returns a template literal with ternaries for URL anchor, source-type line, category line, and final cost hint. `escapeHTML` via `esc` is called eight times; `sanitizeUrl` is called once and only inside the URL branch. No try/catch.

### _renderNav
Synchronous. Reads `step` and `isEdit`. Opens `<div class="forge-nav">`. First branch: when `step > 1`, appends a Back button with id `forge-back`; else appends a Cancel button with id `forge-cancel`. Second branch: when `step < 5`, appends a Next button with id `forge-next`; else appends a submit button with id `forge-submit` whose label branches again on `isEdit` — when true, the pencil-emoji "Save Changes (10t)" string (`\u270F\uFE0F`); when false, the crossed-swords "Forge Weapon (50t)" string (`\u2694`). Closes `</div>` and returns `html`.
