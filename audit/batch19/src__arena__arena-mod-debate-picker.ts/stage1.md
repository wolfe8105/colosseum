# Stage 1 Outputs — arena-mod-debate-picker.ts

## Agent 01

Read 116 lines of 116 total.

1. import — `safeRpc` from `../auth.ts`
2. import — `showToast`, `friendlyError` from `../config.ts`
3. import — `screenEl`, `selectedRounds`, `set_view`, `set_modDebateId` from `./arena-state.ts`
4. import (type-only) — `DebateMode` from `./arena-types.ts`
5. import — `roundPickerCSS`, `roundPickerHTML`, `wireRoundPicker` from `./arena-config-round-picker.ts`
6. import — `showModDebateWaitingMod` from `./arena-mod-debate-waiting.ts`
7. comment — LANDMINE LM-MODDEBATE-001
8. blank
9. bind name to function definition `showModDebatePicker` (exported) — lines 12-87
10. blank
11. bind name to function definition `createModDebate` (exported, async) — lines 89-116

## Agent 02

Read 116 lines of 116 total.

1. import — `safeRpc` from `../auth.ts`
2. import — `showToast`, `friendlyError` from `../config.ts`
3. import — `screenEl`, `selectedRounds`, `set_view`, `set_modDebateId` from `./arena-state.ts`
4. import — type-only `DebateMode` from `./arena-types.ts`
5. import — `roundPickerCSS`, `roundPickerHTML`, `wireRoundPicker` from `./arena-config-round-picker.ts`
6. import — `showModDebateWaitingMod` from `./arena-mod-debate-waiting.ts`
7. comment — LANDMINE [LM-MODDEBATE-001]
8. blank
9. bind name to function definition — `showModDebatePicker` (exported)
10-19. (inline enumerations of function body statements)
20. blank
21. bind name to function definition — `createModDebate` (exported, async)
22-37. (inline enumerations of function body)

## Agent 03

Read 116 lines of 116 total.

1. import — `safeRpc` from `'../auth.ts'`
2. import — `showToast`, `friendlyError` from `'../config.ts'`
3. import — `screenEl`, `selectedRounds`, `set_view`, `set_modDebateId` from `'./arena-state.ts'`
4. import type — `DebateMode` from `'./arena-types.ts'`
5. import — `roundPickerCSS`, `roundPickerHTML`, `wireRoundPicker` from `'./arena-config-round-picker.ts'`
6. import — `showModDebateWaitingMod` from `'./arena-mod-debate-waiting.ts'`
7. comment — LANDMINE [LM-MODDEBATE-001]
8. blank
9. bind name to function definition — `showModDebatePicker` (exported)
10. bind name to function definition — `createModDebate` (exported, async)

## Agent 04

Read 116 lines of 116 total.

1. import — `safeRpc` from `../auth.ts`
2. import — `showToast`, `friendlyError` from `../config.ts`
3. import — `screenEl`, `selectedRounds`, `set_view`, `set_modDebateId` from `./arena-state.ts`
4. import type — `DebateMode` from `./arena-types.ts`
5. import — `roundPickerCSS`, `roundPickerHTML`, `wireRoundPicker` from `./arena-config-round-picker.ts`
6. import — `showModDebateWaitingMod` from `./arena-mod-debate-waiting.ts`
7. comment — LANDMINE LM-MODDEBATE-001
8. blank
9. bind name to function definition — `showModDebatePicker` (exported), returns void
10-19. (inline body statements)
20. blank
21. bind name to function definition — `createModDebate` (exported, async), returns Promise<void>
22-36. (inline body statements)

## Agent 05

Read 116 lines of 116 total.

1. L1 — import `safeRpc` from `../auth.ts`
2. L2 — import `showToast`, `friendlyError` from `../config.ts`
3. L3-5 — import `screenEl`, `selectedRounds`, `set_view`, `set_modDebateId` from `./arena-state.ts`
4. L6 — import type `DebateMode` from `./arena-types.ts`
5. L7 — import `roundPickerCSS`, `roundPickerHTML`, `wireRoundPicker` from `./arena-config-round-picker.ts`
6. L8 — import `showModDebateWaitingMod` from `./arena-mod-debate-waiting.ts`
7. L9-10 — comment (LANDMINE LM-MODDEBATE-001)
8. L11 — blank
9. L12-87 — bind name to function definition `showModDebatePicker` (exported)
10. L88 — blank
11. L89-116 — bind name to function definition `createModDebate` (exported, async)
