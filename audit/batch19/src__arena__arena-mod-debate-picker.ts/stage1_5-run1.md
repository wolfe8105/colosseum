# Anchor List — arena-mod-debate-picker.ts

1. showModDebatePicker  (line 12)
2. createModDebate  (line 89)

## Resolution notes
- Excluded inline async IIFE at line 78 (anonymous arrow inside addEventListener callback).
- Excluded arrow callbacks passed to `addEventListener` at lines 77 and 84 (inline callbacks, not named bindings).
- No class methods, overload signatures, or object-literal methods present.
- Both anchors verified against source.
