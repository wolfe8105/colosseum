# Anchor List — debate-landing.data.ts (Arbiter Run 2)

(empty — no function definitions)

## Resolution notes
- All top-level bindings are value bindings to objects, strings, or expressions, not callable functions.
- `getFingerprint` re-export excluded per rule.
- No function declarations, arrow functions, or function expressions at top level.
