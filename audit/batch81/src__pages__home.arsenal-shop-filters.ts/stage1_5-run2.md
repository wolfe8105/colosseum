# Anchor List — home.arsenal-shop-filters.ts

1. applyFilters (line 9)

## Resolution notes

None — all five agents correctly identified the only function definition in the file. The inline arrow function in the `.filter()` call (line 10) was correctly excluded as a callback, not a top-level function definition.
