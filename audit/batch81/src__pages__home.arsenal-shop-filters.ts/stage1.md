# Stage 1 Outputs — home.arsenal-shop-filters.ts

## Agent 01
Based on the file content, here are the primitive language operations in source order:

1. Comment (lines 1-4)
2. Import type: `ModifierEffect` from `../modifiers.ts`
3. Import type: `ShopState` from `./home.arsenal-shop-types.ts`
4. Bind name to function definition (exported): `applyFilters` — takes `catalog` and `state` parameters, returns filtered array

## Agent 02
Here are the primitive language operations for the file:

1. import (type-only import of `ModifierEffect` from '../modifiers.ts')
2. import (type-only import of `ShopState` from './home.arsenal-shop-types.ts')
3. bind name to function definition (exported: `applyFilters`)

## Agent 03
Based on the file content, here are the primitive language operations in source order:

1. Comment (lines 1-4)
2. Blank (line 5)
3. Import type `ModifierEffect` from `'../modifiers.ts'`
4. Import type `ShopState` from `'./home.arsenal-shop-types.ts'`
5. Blank (line 8)
6. Bind name to function definition: `applyFilters` (exported)
7. Blank (line 21)

## Agent 04
Here are the primitive language operations in source order:

1. **comment** — Lines 1-4 (JSDoc comment block)
2. **import** — Line 6 (type-only import of `ModifierEffect` from `../modifiers.ts`)
3. **import** — Line 7 (type-only import of `ShopState` from `./home.arsenal-shop-types.ts`)
4. **bind name to function definition** (exported) — Line 9 (`applyFilters` function, exported)
5. **blank** — Line 21

## Agent 05
Based on my analysis of the file, here are the primitive language operations in source order:

At the module level:
1. Comment (line 1-3)
2. Blank (line 4)
3. Import (line 6) — type-only import of `ModifierEffect`
4. Import (line 7) — type-only import of `ShopState`
5. Blank (line 8)
6. Bind name to function definition and export (line 9): `applyFilters`
