# Anchor List — arena-types-private-lobby.ts

(empty — no function definitions in this file)

## Resolution notes

All five agents identified the same four bindings: PrivateLobbyResult, PendingChallenge, CheckPrivateLobbyResult, JoinPrivateLobbyResult. All are interface (type) bindings, not function definitions. No function candidates were identified by any agent. Direct source scan confirms: file contains only interface declarations and no callable bindings.
