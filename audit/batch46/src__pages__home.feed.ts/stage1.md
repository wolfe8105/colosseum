# Stage 1 Outputs — home.feed.ts

## Agent 01
Read 80 lines of 80 total.

1. **comment** — `/** Home — Live debate card feed */` (lines 1–3)
2. **import** — `getSupabaseClient`, `getIsPlaceholderMode` from `'../auth.ts'` (line 4)
3. **import** — `escapeHTML` from `'../config.ts'` (line 5)
4. **import** — `bountyDot` from `'../bounties.ts'` (line 6)
5. **import** — `ModeratorAsync` from `'../async.ts'` (line 7)
6. **import** — type-only import of `LiveDebate` from `'./home.types.ts'` (line 8)
7. **blank** (line 9)
8. **bind name to function definition** — `fetchLiveDebates` (lines 10–39)
9. **blank** (line 40)
10. **bind name to function definition** — `renderFeed`, exported (lines 41–80)

## Agent 02
Read 80 lines of 80 total.

1. comment — block comment (lines 1–3)
2. import — `getSupabaseClient`, `getIsPlaceholderMode` from `'../auth.ts'`
3. import — `escapeHTML` from `'../config.ts'`
4. import — `bountyDot` from `'../bounties.ts'`
5. import — `ModeratorAsync` from `'../async.ts'`
6. import — type-only import of `LiveDebate` from `'./home.types.ts'`
7. blank
8. bind `fetchLiveDebates` to function definition (async function, lines 10–39)
9. blank
10. bind `renderFeed` to function definition (async function, exported, lines 41–80)

## Agent 03
Read 80 lines of 80 total.

1. **comment** — `/** Home — Live debate card feed */` (lines 1–3)
2. **import** — `getSupabaseClient`, `getIsPlaceholderMode` from `'../auth.ts'` (line 4)
3. **import** — `escapeHTML` from `'../config.ts'` (line 5)
4. **import** — `bountyDot` from `'../bounties.ts'` (line 6)
5. **import** — `ModeratorAsync` from `'../async.ts'` (line 7)
6. **import** — type-only import of `LiveDebate` from `'./home.types.ts'` (line 8)
7. **blank** (line 9)
8. **bind name to function definition** — `fetchLiveDebates` (lines 10–39)
9. **blank** (line 40)
10. **bind name to function definition** — `renderFeed`, exported (lines 41–80)

## Agent 04
Read 80 lines of 80 total.

1. comment — block comment: "Home — Live debate card feed"
2. import — `getSupabaseClient`, `getIsPlaceholderMode` from `'../auth.ts'`
3. import — `escapeHTML` from `'../config.ts'`
4. import — `bountyDot` from `'../bounties.ts'`
5. import — `ModeratorAsync` from `'../async.ts'`
6. import — type-only import of `LiveDebate` from `'./home.types.ts'`
7. blank
8. bind name to function definition — `fetchLiveDebates`
9. blank
10. bind name to function definition — `renderFeed` (exported)

## Agent 05
Read 80 lines of 80 total.

1. **comment** — block comment `/** Home — Live debate card feed */`
2. **blank**
3. **import** — `getSupabaseClient`, `getIsPlaceholderMode` from `'../auth.ts'`
4. **import** — `escapeHTML` from `'../config.ts'`
5. **import** — `bountyDot` from `'../bounties.ts'`
6. **import** — `ModeratorAsync` from `'../async.ts'`
7. **import** — type-only import of `LiveDebate` from `'./home.types.ts'`
8. **blank**
9. **bind name to function definition** — `fetchLiveDebates` (async, not exported)
10. **blank**
11. **bind name to function definition** — `renderFeed` (async, exported)
