# Anchor List — home.arsenal-shop-wiring.ts

Source: src/pages/home.arsenal-shop-wiring.ts
Produced by: stage 1.5 (arbiter + optional reconciliation)
Unresolved items: 0

1. wireShopEvents  (line 17)

## Resolution notes
Both arbiter runs agreed. `ProductType` type alias excluded. All `import type` entries excluded. `openBottomSheet` excluded (imported value, not defined here). All forEach callbacks and addEventListener handlers excluded as inner closures.
