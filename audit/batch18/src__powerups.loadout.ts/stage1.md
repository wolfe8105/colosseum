# Stage 1 Outputs — powerups.loadout.ts

## Agent 01
1. comment (JSDoc header, lines 1-8)
2. blank
3. import `escapeHTML` from `./config.ts`
4. import `getTier, getPowerUpSlots, getNextTier` from `./tiers.ts`
5. import `CATALOG` from `./powerups.types.ts`
6. import `equip` from `./powerups.rpc.ts`
7. import type `PowerUpId, InventoryItem, EquippedItem, PowerUpResult` from `./powerups.types.ts`
8. blank
9. exported function definition `renderLoadout` (lines 16-80)
10. blank
11. exported function definition `wireLoadout` (lines 82-114)

## Agent 02
1. comment — JSDoc block header (lines 1-8)
2. blank
3. import — `escapeHTML` from './config.ts'
4. import — `getTier, getPowerUpSlots, getNextTier` from './tiers.ts'
5. import — `CATALOG` from './powerups.types.ts'
6. import — `equip` from './powerups.rpc.ts'
7. import — type `PowerUpId, InventoryItem, EquippedItem, PowerUpResult` from './powerups.types.ts'
8. blank
9. exported function definition — `renderLoadout`
10. blank
11. exported function definition — `wireLoadout`

## Agent 03
1. comment (JSDoc block header describing module, renderLoadout, wireLoadout, LM-PU-002, LM-PU-003)
2. blank
3. import (escapeHTML from './config.ts')
4. import (getTier, getPowerUpSlots, getNextTier from './tiers.ts')
5. import (CATALOG from './powerups.types.ts')
6. import (equip from './powerups.rpc.ts')
7. import type (PowerUpId, InventoryItem, EquippedItem, PowerUpResult from './powerups.types.ts')
8. blank
9. exported function definition: renderLoadout (lines 16-80)
10. blank
11. exported function definition: wireLoadout (lines 82-114)

## Agent 04
1. comment (JSDoc header describing file purpose and landmines, lines 1-8)
2. blank
3. import — `escapeHTML` from `./config.ts`
4. import — `getTier, getPowerUpSlots, getNextTier` from `./tiers.ts`
5. import — `CATALOG` from `./powerups.types.ts`
6. import — `equip` from `./powerups.rpc.ts`
7. import type — `PowerUpId, InventoryItem, EquippedItem, PowerUpResult` from `./powerups.types.ts`
8. blank
9. exported function definition — `renderLoadout` (lines 16-80)
10. blank
11. exported function definition — `wireLoadout` (lines 82-114)

## Agent 05
1. comment (JSDoc header block, lines 1-8)
2. blank
3. import — `escapeHTML` from `./config.ts`
4. import — `getTier`, `getPowerUpSlots`, `getNextTier` from `./tiers.ts`
5. import — `CATALOG` from `./powerups.types.ts`
6. import — `equip` from `./powerups.rpc.ts`
7. import type — `PowerUpId`, `InventoryItem`, `EquippedItem`, `PowerUpResult` from `./powerups.types.ts`
8. blank
9. bind name to function definition — `renderLoadout` (exported)
[function body lines 10-22, described as internal statements but within renderLoadout]
23. blank
24. bind name to function definition — `wireLoadout` (exported)
[function body lines 25-29, described as internal statements but within wireLoadout]
