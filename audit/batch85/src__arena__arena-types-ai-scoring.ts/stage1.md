# Stage 1 Outputs — arena-types-ai-scoring.ts

## Agent 01
1. **comment** (lines 1-3: block comment)
2. **blank** (line 4)
3. **bind name to type** (lines 5-8: `CriterionScore` interface, exported)
4. **blank** (line 9)
5. **bind name to type** (lines 10-15: `SideScores` interface, exported)
6. **blank** (line 16)
7. **bind name to type** (lines 17-22: `AIScoreResult` interface, exported)
8. **blank** (line 23)

## Agent 02
1. comment
2. blank
3. bind name to type (exported) — `CriterionScore` interface
4. blank
5. bind name to type (exported) — `SideScores` interface
6. blank
7. bind name to type (exported) — `AIScoreResult` interface

## Agent 03
1. **comment** (lines 1-3: block comment)
2. **blank** (line 4)
3. **bind name to type** — `CriterionScore` interface, exported (lines 5-8)
4. **blank** (line 9)
5. **bind name to type** — `SideScores` interface, exported (lines 10-15)
6. **blank** (line 16)
7. **bind name to type** — `AIScoreResult` interface, exported (lines 17-22)
8. **blank** (line 23)

## Agent 04
1. comment (lines 1-3)
2. blank (line 4)
3. bind name to type (exported): `CriterionScore` interface (lines 5-8)
4. blank (line 9)
5. bind name to type (exported): `SideScores` interface (lines 10-15)
6. blank (line 16)
7. bind name to type (exported): `AIScoreResult` interface (lines 17-22)
8. blank (line 23)

## Agent 05
1. comment (lines 1–3: JSDoc block)
2. blank (line 4)
3. bind name to type — `CriterionScore` interface, exported
4. blank (line 9)
5. bind name to type — `SideScores` interface, exported
6. blank (line 16)
7. bind name to type — `AIScoreResult` interface, exported
8. blank (line 23)
