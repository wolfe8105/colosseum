# Anchor List — login.types.ts

(No function definitions found)

## Resolution notes

This file is a pure type-declaration module. All five Stage 1 agents consistently classified every entry as either comment, blank, or "bind name to type" (interface). Two interfaces are defined: RateLimitState (lines 6–10) and RateLimitCheck (lines 12–15). Neither qualifies as a function definition per the specified criteria — interfaces are TypeScript type-only constructs with no runtime effect.
