# Anchor List — home.state.ts

Source: src/pages/home.state.ts
Produced by: stage 1.5 arbiter run 2

No function definitions found. The file contains only type-only imports and two const value bindings (`CATEGORIES` array and `state` object).

## Resolution notes

- `CATEGORIES` (line 8): exported const array — not a callable function; excluded.
- `state` (line 17): exported const object — not a callable function; excluded.
