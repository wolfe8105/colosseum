# Stage 1 Outputs — profile-debate-archive.filter.ts

## Agent 01
Read 29 lines of 29 total.

1. comment — block comment (lines 1–4)
2. blank — (line 5)
3. import — named import of `entries`, `filterCat`, `filterResult`, `filterSearch` from `./profile-debate-archive.state.ts` (line 6)
4. import — type-only import of `ArchiveEntry` from `./profile-debate-archive.types.ts` (line 7)
5. blank — (line 8)
6. bind name to function definition — `archiveUrl`, exported (lines 9–13)
7. blank — (line 14)
8. bind name to function definition — `filtered`, exported (lines 15–29)

## Agent 02
Read 29 lines of 29 total.

1. comment — JSDoc block (lines 1–4)
2. blank — (line 5)
3. import — named import of `entries`, `filterCat`, `filterResult`, `filterSearch` from `./profile-debate-archive.state.ts` (line 6)
4. import — type-only import of `ArchiveEntry` from `./profile-debate-archive.types.ts` (line 7)
5. blank — (line 8)
6. bind name to function definition — `archiveUrl`, exported (lines 9–13)
7. blank — (line 14)
8. bind name to function definition — `filtered`, exported (lines 15–29)

## Agent 03
Read 29 lines of 29 total.

1. comment — block comment (lines 1–4)
2. blank — line 5
3. import — named import of `entries`, `filterCat`, `filterResult`, `filterSearch` from `./profile-debate-archive.state.ts` (line 6)
4. import — type-only import of `ArchiveEntry` from `./profile-debate-archive.types.ts` (line 7)
5. blank — line 8
6. bind name to function definition — `archiveUrl`, exported (lines 9–13)
7. blank — line 14
8. bind name to function definition — `filtered`, exported (lines 15–29)

## Agent 04
Read 29 lines of 29 total.

1. comment — JSDoc block (lines 1–4)
2. blank — (line 5)
3. import — `entries`, `filterCat`, `filterResult`, `filterSearch` from `./profile-debate-archive.state.ts` (line 6)
4. import — type-only import of `ArchiveEntry` from `./profile-debate-archive.types.ts` (line 7)
5. blank — (line 8)
6. bind name to function definition — `archiveUrl`, exported (lines 9–13)
7. blank — (line 14)
8. bind name to function definition — `filtered`, exported (lines 15–29)

## Agent 05
Read 29 lines of 29 total.

1. comment — JSDoc block (lines 1–4)
2. blank — (line 5)
3. import — named import of `entries`, `filterCat`, `filterResult`, `filterSearch` from `./profile-debate-archive.state.ts` (line 6)
4. import — type-only import of `ArchiveEntry` from `./profile-debate-archive.types.ts` (line 7)
5. blank — (line 8)
6. bind name to function definition — `archiveUrl`, exported (lines 9–13)
7. blank — (line 14)
8. bind name to function definition — `filtered`, exported (lines 15–29)
