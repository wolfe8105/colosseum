# Anchor List — reference-arsenal.ts

Source: src/reference-arsenal.ts
Produced by: stage 1.5 (arbiter + optional reconciliation)
Unresolved items: 0

*(empty — no function definitions in this file)*

## Resolution notes

`src/reference-arsenal.ts` is a barrel orchestrator. It contains only re-exports (`export { ... } from` and `export type { ... } from`), imports for the window bridge, and one top-level assignment expression (`window.ModeratorArsenal = { ... }`). No function declarations, arrow function bindings, or function expression bindings exist at top level. Both arbiter runs agreed unanimously with all five Stage 1 agents.
