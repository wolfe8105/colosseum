# Anchor List — reference-arsenal.ts

*(empty — no function definitions)*

## Resolution notes

`src/reference-arsenal.ts` is a pure orchestrator barrel. Every top-level binding is a re-export, import, or a bare assignment expression to `window.ModeratorArsenal`. There are zero function definitions. All five stage 1 agents agree unanimously.
