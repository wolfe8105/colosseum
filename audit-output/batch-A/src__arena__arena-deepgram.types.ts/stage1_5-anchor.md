# Anchor List — arena-deepgram.types.ts

Source: src/arena/arena-deepgram.types.ts
Produced by: stage 1.5 (arbiter + optional reconciliation)
Unresolved items: 0

(none — file contains only type aliases and interfaces; no callable function definitions)

## Resolution notes
- `TranscriptCallback` (line 6): excluded — `export type` alias, not a callable function definition.
- `StatusCallback` (line 7): excluded — `export type` alias, not a callable function definition.
- `DeepgramStatus` (line 8): excluded — `export type` union alias, not a callable function definition.
- `DeepgramResult` (lines 10–20): excluded — `export interface` declaration, not a callable function definition.
Both arbiter runs agreed. No reconciliation required.
