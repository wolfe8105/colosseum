# Stage 1 Outputs — arena-deepgram.types.ts

## Agent 01
1. comment — block comment (lines 1–4)
2. blank (line 5)
3. bind name to type — `TranscriptCallback`, exported (line 6)
4. bind name to type — `StatusCallback`, exported (line 7)
5. bind name to type — `DeepgramStatus`, exported (line 8)
6. blank (line 9)
7. bind name to type — `DeepgramResult`, exported (lines 10–20)

## Agent 02
Here is the flat inventory for `/src/arena/arena-deepgram.types.ts`:

1. comment — block comment (file header)
2. blank
3. bind name to type — `TranscriptCallback` (exported)
4. bind name to type — `StatusCallback` (exported)
5. bind name to type — `DeepgramStatus` (exported)
6. blank
7. bind name to type — `DeepgramResult` (exported)

## Agent 03
1. comment — block comment (lines 1–4)
2. blank (line 5)
3. bind name to type — `TranscriptCallback`, exported (line 6)
4. bind name to type — `StatusCallback`, exported (line 7)
5. bind name to type — `DeepgramStatus`, exported (line 8)
6. blank (line 9)
7. bind name to type — `DeepgramResult`, exported (lines 10–20)
8. blank (line 21)

## Agent 04
1. comment — block comment (lines 1–4)
2. blank — line 5
3. bind name to type — `TranscriptCallback` (exported)
4. bind name to type — `StatusCallback` (exported)
5. bind name to type — `DeepgramStatus` (exported)
6. blank — line 9
7. bind name to type — `DeepgramResult` (exported)

## Agent 05
Here is the flat inventory for `/src/arena/arena-deepgram.types.ts`:

1. comment — block comment: file header (lines 1–4)
2. blank (line 5)
3. bind `TranscriptCallback` to type — exported (line 6)
4. bind `StatusCallback` to type — exported (line 7)
5. bind `DeepgramStatus` to type — exported (line 8)
6. blank (line 9)
7. bind `DeepgramResult` to type (interface) — exported (lines 10–20)
