# Stage 1 Outputs — src/arena/arena-mod-scoring.ts

## Agent 01

1. `import` — named imports `getCurrentProfile`, `scoreModerator` from `../auth.ts` (line 1)
2. `import` — named imports `escapeHTML`, `friendlyError` from `../config.ts` (line 2)
3. `import` — type import `CurrentDebate` from `./arena-types.ts` (line 3)
4. `import` — named import `injectAdSlot` from `./arena-ads.ts` (line 4)
5. `blank` — blank line (line 5)
6. `bind name to function definition` — exported function `renderModScoring(debate, container)` (lines 6–85)

## Agent 02

1. `import` — imports `getCurrentProfile` and `scoreModerator` from `../auth.ts` (line 1)
2. `import` — imports `escapeHTML` and `friendlyError` from `../config.ts` (line 2)
3. `import` — type-only import of `CurrentDebate` from `./arena-types.ts` (line 3)
4. `import` — imports `injectAdSlot` from `./arena-ads.ts` (line 4)
5. `blank` — blank line (line 5)
6. `bind name to function definition` — exported function `renderModScoring(debate, container)` renders moderator scoring UI and wires event listeners (lines 6–85)

## Agent 03

1. `import` — named imports `getCurrentProfile`, `scoreModerator` from `'../auth.ts'` (line 1)
2. `import` — named imports `escapeHTML`, `friendlyError` from `'../config.ts'` (line 2)
3. `import` — type import `CurrentDebate` from `'./arena-types.ts'` (line 3)
4. `import` — named import `injectAdSlot` from `'./arena-ads.ts'` (line 4)
5. `blank` — (line 5)
6. `bind name to function definition` — exported `renderModScoring(debate, container): void` (lines 6–85)

## Agent 04

1. `import` — named imports `getCurrentProfile`, `scoreModerator` from `../auth.ts` (line 1)
2. `import` — named imports `escapeHTML`, `friendlyError` from `../config.ts` (line 2)
3. `import` — type import `CurrentDebate` from `./arena-types.ts` (line 3)
4. `import` — named import `injectAdSlot` from `./arena-ads.ts` (line 4)
5. `blank` — blank line (line 5)
6. `bind name to function definition` — exported function `renderModScoring(debate, container): void` (lines 6–85)

## Agent 05

1. `import` — named imports `getCurrentProfile`, `scoreModerator` from `../auth.ts` (line 1)
2. `import` — named imports `escapeHTML`, `friendlyError` from `../config.ts` (line 2)
3. `import` — type import `CurrentDebate` from `./arena-types.ts` (line 3)
4. `import` — named import `injectAdSlot` from `./arena-ads.ts` (line 4)
5. `blank` — blank line (line 5)
6. `bind name to function definition` — exported function `renderModScoring(debate, container): void` (lines 6–85)
