# Stage 1 Outputs — src/pages/home.types.ts

## Agent 01
1. **comment** — JSDoc block on line 1 (`/** Shape of entries in the CATEGORIES array */`)
2. **bind name to type** — `Category` (exported interface, lines 2–9)
3. **blank** — line 10
4. **bind name to type** — `LiveDebate` (exported interface, lines 11–24)
5. **blank** — line 25 (trailing)

## Agent 02
1. comment — JSDoc comment: `/** Shape of entries in the CATEGORIES array */`
2. bind name to type — `Category` interface, exported
3. blank
4. bind name to type — `LiveDebate` interface, exported

## Agent 03
1. **comment** — JSDoc block on line 1 (`/** Shape of entries in the CATEGORIES array */`)
2. **bind name to type** — `Category` (interface), exported in place (lines 2–9)
3. **blank** — line 10
4. **bind name to type** — `LiveDebate` (interface), exported in place (lines 11–24)

## Agent 04
1. **comment** — `/** Shape of entries in the CATEGORIES array */` (line 1)
2. **bind name to type** — `interface Category`, exported (lines 2–9)
3. **blank** — (line 10)
4. **bind name to type** — `interface LiveDebate`, exported (lines 11–24)
5. **blank** — (line 25, trailing newline)

## Agent 05
1. **comment** — JSDoc block describing the shape of entries in the CATEGORIES array (line 1)
2. **bind name to type** — `Category` interface, exported (lines 2–9)
3. **blank** — (line 10)
4. **bind name to type** — `LiveDebate` interface, exported (lines 11–24)
5. **blank** — (line 25, trailing newline)
