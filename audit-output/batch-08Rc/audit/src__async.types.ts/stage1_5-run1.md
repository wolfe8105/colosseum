# Anchor List — src/async.types.ts (Arbiter Run 1)

*(none)*

## Resolution notes

All five agents agree unanimously: `src/async.types.ts` is a pure type-declaration file. Direct inspection of the source confirms this. The file contains only:

- A block-comment file header (lines 1–6)
- Six exported `interface` declarations (`HotTake`, `Prediction`, `StandaloneQuestion`, `RivalEntry`, `ReactResult`, `CreateHotTakeResult`)
- One exported `type` alias (`CategoryFilter`)

There are no `function` declarations, no `const`/`let`/`var` bindings to arrow functions or function expressions, no class bodies, no runtime statements, and no imports. The candidate list produced from the five stage 1 inventories contained zero function-definition candidates. The direct source scan confirms zero function definitions are present. The anchor list is therefore empty.
