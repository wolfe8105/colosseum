# Stage 1 Outputs — src/async.types.ts

## Agent 01
1. comment — block comment (file header: "THE MODERATOR — Async Module: Type Definitions")
2. blank
3. bind name to type — `HotTake` (exported interface)
4. blank
5. bind name to type — `Prediction` (exported interface)
6. blank
7. bind name to type — `StandaloneQuestion` (exported interface)
8. blank
9. bind name to type — `RivalEntry` (exported interface)
10. blank
11. bind name to type — `ReactResult` (exported interface)
12. blank
13. bind name to type — `CreateHotTakeResult` (exported interface)
14. blank
15. bind name to type — `CategoryFilter` (exported type alias)

## Agent 02
1. comment — block comment header (lines 1–6)
2. blank
3. bind name to type — `HotTake` (interface), exported
4. blank
5. bind name to type — `Prediction` (interface), exported
6. blank
7. bind name to type — `StandaloneQuestion` (interface), exported
8. blank
9. bind name to type — `RivalEntry` (interface), exported
10. blank
11. bind name to type — `ReactResult` (interface), exported
12. blank
13. bind name to type — `CreateHotTakeResult` (interface), exported
14. blank
15. bind name to type — `CategoryFilter` (type alias), exported

## Agent 03
1. **comment** — block comment (lines 1–6): file-level JSDoc header
2. **blank** — line 7
3. **bind name to type** — `HotTake` (exported interface, lines 8–22)
4. **blank** — line 23
5. **bind name to type** — `Prediction` (exported interface, lines 24–36)
6. **blank** — line 37
7. **bind name to type** — `StandaloneQuestion` (exported interface, lines 38–50)
8. **blank** — line 51
9. **bind name to type** — `RivalEntry` (exported interface, lines 52–62)
10. **blank** — line 63
11. **bind name to type** — `ReactResult` (exported interface, lines 64–67)
12. **blank** — line 68
13. **bind name to type** — `CreateHotTakeResult` (exported interface, lines 69–71)
14. **blank** — line 72
15. **bind name to type** — `CategoryFilter` (exported type alias, lines 73–80)

All 15 entries are exported in place. The file contains no imports, no runtime statements, no function or class bindings, and no re-exports.

## Agent 04
1. comment — block comment (file header: "THE MODERATOR — Async Module: Type Definitions")
2. blank
3. bind name to type — `HotTake` (exported interface)
4. blank
5. bind name to type — `Prediction` (exported interface)
6. blank
7. bind name to type — `StandaloneQuestion` (exported interface)
8. blank
9. bind name to type — `RivalEntry` (exported interface)
10. blank
11. bind name to type — `ReactResult` (exported interface)
12. blank
13. bind name to type — `CreateHotTakeResult` (exported interface)
14. blank
15. bind name to type — `CategoryFilter` (exported type alias)

## Agent 05
1. comment — block comment: "THE MODERATOR — Async Module: Type Definitions / All shared interfaces and types for the async subsystem. / No imports needed — pure type declarations."
2. blank
3. bind name to type — `HotTake` (exported interface)
4. blank
5. bind name to type — `Prediction` (exported interface)
6. blank
7. bind name to type — `StandaloneQuestion` (exported interface)
8. blank
9. bind name to type — `RivalEntry` (exported interface)
10. blank
11. bind name to type — `ReactResult` (exported interface)
12. blank
13. bind name to type — `CreateHotTakeResult` (exported interface)
14. blank
15. bind name to type — `CategoryFilter` (exported type alias)
