# Stage 1 Outputs — home.arsenal.ts

## Agent 01
1. comment — JSDoc block on lines 1–3
2. comment — inline comment on line 4
3. blank
4. import — `showForgeForm`, `renderArsenal`, `renderArmory` from `../reference-arsenal.ts`
5. import — type-only import of `ArsenalReference` from `../reference-arsenal.ts`
6. import — `state` from `./home.state.ts`
7. import — `loadShopScreen`, `cleanupShopScreen` from `./home.arsenal-shop.ts`
8. blank
9. bind name to function definition — `loadArsenalScreen` (exported)
10. blank
11. bind name to function definition — `loadMyArsenal`
12. blank
13. bind name to function definition — `loadArmory`
14. blank
15. bind name to function definition — `loadForge`
16. blank
17. bind name to function definition — `wireArsenalButtons`
18. blank
19. comment — inline comment on line 81
20. top-level statement — `document.querySelectorAll('[data-arsenal-tab]').forEach(...)` call on lines 82–99

## Agent 02
1. comment — block comment: `Home — Arsenal screen tab management and forge/edit wiring`
2. comment — line comment: `LM-HOME-003: #arsenal-back-btn is wired in home.ts (orchestrator), not here.`
3. blank
4. import — named imports `showForgeForm`, `renderArsenal`, `renderArmory` from `'../reference-arsenal.ts'`
5. import — type-only named import `ArsenalReference` from `'../reference-arsenal.ts'`
6. import — named import `state` from `'./home.state.ts'`
7. import — named imports `loadShopScreen`, `cleanupShopScreen` from `'./home.arsenal-shop.ts'`
8. blank
9. bind name to function definition — `loadArsenalScreen` (exported)
10. blank
11. bind name to function definition — `loadMyArsenal`
12. blank
13. bind name to function definition — `loadArmory`
14. blank
15. bind name to function definition — `loadForge`
16. blank
17. bind name to function definition — `wireArsenalButtons`
18. blank
19. comment — line comment: `Tab switching`
20. top-level statement — `document.querySelectorAll('[data-arsenal-tab]').forEach(...)` call executed at module load

## Agent 03
1. comment — block comment: "Home — Arsenal screen tab management and forge/edit wiring"
2. comment — line comment: LM-HOME-003 note about `#arsenal-back-btn` wiring
3. blank
4. import — `showForgeForm`, `renderArsenal`, `renderArmory` from `'../reference-arsenal.ts'`
5. import — type-only: `ArsenalReference` from `'../reference-arsenal.ts'`
6. import — `state` from `'./home.state.ts'`
7. import — `loadShopScreen`, `cleanupShopScreen` from `'./home.arsenal-shop.ts'`
8. blank
9. bind name to function definition — `loadArsenalScreen` (exported)
10. blank
11. bind name to function definition — `loadMyArsenal`
12. blank
13. bind name to function definition — `loadArmory`
14. blank
15. bind name to function definition — `loadForge`
16. blank
17. bind name to function definition — `wireArsenalButtons`
18. blank
19. comment — line comment: "Tab switching"
20. top-level statement — `document.querySelectorAll('[data-arsenal-tab]').forEach(...)` call executed at module load

## Agent 04
1. comment — block comment: "Home — Arsenal screen tab management and forge/edit wiring"
2. comment — line comment referencing LM-HOME-003 land mine
3. blank
4. import — `showForgeForm`, `renderArsenal`, `renderArmory` from `'../reference-arsenal.ts'`
5. import — type-only import of `ArsenalReference` from `'../reference-arsenal.ts'`
6. import — `state` from `'./home.state.ts'`
7. import — `loadShopScreen`, `cleanupShopScreen` from `'./home.arsenal-shop.ts'`
8. blank
9. bind name to function definition — `loadArsenalScreen` (exported)
10. blank
11. bind name to function definition — `loadMyArsenal`
12. blank
13. bind name to function definition — `loadArmory`
14. blank
15. bind name to function definition — `loadForge`
16. blank
17. bind name to function definition — `wireArsenalButtons`
18. blank
19. comment — line comment: "Tab switching"
20. top-level statement — `document.querySelectorAll('[data-arsenal-tab]').forEach(...)` call executed at module load

## Agent 05
1. comment — block comment: "Home — Arsenal screen tab management and forge/edit wiring"
2. comment — line comment: LM-HOME-003 note about `#arsenal-back-btn`
3. blank
4. import — named imports `showForgeForm`, `renderArsenal`, `renderArmory` from `'../reference-arsenal.ts'`
5. import — type-only import of `ArsenalReference` from `'../reference-arsenal.ts'`
6. import — named import `state` from `'./home.state.ts'`
7. import — named imports `loadShopScreen`, `cleanupShopScreen` from `'./home.arsenal-shop.ts'`
8. blank
9. bind name to function definition — `loadArsenalScreen` (exported)
10. blank
11. bind name to function definition — `loadMyArsenal`
12. blank
13. bind name to function definition — `loadArmory`
14. blank
15. bind name to function definition — `loadForge`
16. blank
17. bind name to function definition — `wireArsenalButtons`
18. blank
19. comment — line comment: "Tab switching"
20. top-level statement — `document.querySelectorAll('[data-arsenal-tab]').forEach(...)` call executed at module load
