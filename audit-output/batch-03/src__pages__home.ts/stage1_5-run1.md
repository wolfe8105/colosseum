# Anchor List — home.ts (Arbiter Run 1)

1. appInit  (line 69)

## Notes
Only one named function declaration in the file. Imported functions are defined elsewhere and excluded. Anonymous callbacks (addEventListener, onChange) are not named bindings. urlParams and catParam are data values, not functions.
