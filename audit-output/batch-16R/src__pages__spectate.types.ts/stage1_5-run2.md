# Anchor List — spectate.types.ts

The file contains exclusively `export interface` declarations. Every item all five agents inventoried is a type-level construct, not a callable function definition.

**Final anchor list: (none)**

## Resolution notes

All five agents agreed unanimously. Direct source inspection confirms 14 exported interfaces (`SpectateDebate`, `AICriterion`, `AISideScores`, `AIScorecard`, `ReplayPowerUp`, `ReplayReference`, `ReplayModScore`, `PointAwardMeta`, `ReplayPointAward`, `ReplaySpeechEvent`, `ReplayData`, `TimelineEntry`, `DebateMessage`, `SpectatorChatMessage`) and nothing else. No `function` keyword, no arrow function assignment, no callable binding appears anywhere. Anchor list is empty.
