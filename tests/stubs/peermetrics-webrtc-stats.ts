// Stub for @peermetrics/webrtc-stats — package not installed.
// Used by vitest resolve.alias to prevent Vite transform failure.
export class WebRTCStats {
  addConnection() {}
  destroy() {}
  on() {}
  off() {}
}
