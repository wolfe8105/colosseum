{
  "name": "the-moderator",
  "version": "1.0.0",
  "description": "The Moderator — Live audio debate platform / emergence engine",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build && cp og-card-default.png dist/",
    "typecheck": "tsc --noEmit",
    "test": "vitest run",
    "test:watch": "vitest",
    "preview": "vite preview",
    "gen:types": "npx supabase gen types typescript --project-id faomczmipsccwbhpivmp > src/types/database.ts"
  },
  "devDependencies": {
    "@supabase/supabase-js": "^2.98.0",
    "dependency-cruiser": "^17.3.10",
    "dotenv": "^16.4.0",
    "jsdom": "^29.0.1",
    "typescript": "^5.7.0",
    "vite": "^6.2.0",
    "vitest": "^3.0.0"
  },
  "engines": {
    "node": ">=18.0.0"
  },
  "dependencies": {
    "@upstash/ratelimit": "^2.0.8",
    "@upstash/redis": "^1.37.0"
  }
}
