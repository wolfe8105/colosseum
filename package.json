{
  "name": "the-colosseum",
  "version": "1.0.0",
  "description": "The Colosseum — Live audio debate platform / emergence engine",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc --noEmit && vite build",
    "typecheck": "tsc --noEmit",
    "preview": "vite preview",
    "gen:types": "npx supabase gen types typescript --project-id faomczmipsccwbhpivmp > src/types/database.ts"
  },
  "devDependencies": {
    "typescript": "^5.7.0",
    "vite": "^6.2.0",
    "@supabase/supabase-js": "^2.98.0"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
