# THE MODERATOR — PUNCH LIST
### Created: Session 162 (March 23, 2026)
### Last Updated: April 16, 2026 (post-S279 — design phase close)

> **What this is:** The single source of truth for everything that needs doing.
> Loads every session via project knowledge. Read this first, pick what's next, go.
> Four sections: Housekeeping, Bugs, Features, Dependency Tiers. Each item has a status.
>
> **Status key:** ⏳ = open, 🔶 = in progress, ✅ = done, ❌ = won't do, 🅿️ = parked (blocked)

---

# SECTION 1: HOUSEKEEPING (clear before adding features)

These are tech debt, cleanup, and infrastructure items. None are features — they're the foundation work that prevents future sessions from hitting landmines.

| # | Item | Status | Notes |
|---|------|--------|-------|
| H-01 | 3 page modules have `any` annotations (`spectate.ts`, `groups.ts`, `home.ts`) | ✅ | Session 165–166. `home.ts`: 4 `any` → `Category`, `Profile`, `User`. `groups.ts`: 4 `any` → `SupabaseClient`, `User`, `GroupListItem`. `spectate.ts`: 14 `any` → `SpectateDebate`, `DebateMessage`, `SpectatorChatMessage`. All eslint-disables removed. Vite build clean. |
| H-02 | `(window as any).navigateTo` in `home.ts` | ✅ | Session 163. Created `src/navigation.ts` with register/call pattern. 4 consumers updated. Zero `window.navigateTo` refs remain. |
| H-03 | Bible docs stale after Colosseum→Moderator rename | 🔶 | Session 173: NT fully rewritten (build logs moved to OT, all refs updated, modules/pages tables current). OT updated (sessions 108-173 appended). CLAUDE.md fully rewritten. Wiring Manifest and Land Mine Map internal content still have "Colosseum" references — not yet updated. Session 190: api/profile.js BASE_URL fixed (env var + fallback updated to themoderator.app). Env var set in Vercel. Deployed. Session 205: Full visual token migration — all hardcoded hex colors (#d4a843, #cc2936, #a0a8b8, etc.) and old fonts (Cinzel, Barlow) replaced with var(--mod-*) tokens across all .ts files. moderator-groups.html reskinned with bottom tab bar, footer removed. Only cards.ts Canvas API retains hardcoded colors (correct — CSS vars don't work in Canvas). Session 240: NT updated (arena module split documented — 23 sub-files under src/arena/, barrel re-export, page modules list updated, language pipeline + spectator entry in critical notes + infrastructure summary). Land Mine Map updated (LM-013 allow-list, LM-200 stamp_debate_language trigger, LM-201 spectator entry wiring). Punch List changelog updated. |
| H-04 | `colosseum-arena.html` in Wiring Manifest but NOT in `vite.config.ts` | ✅ | Session 163. Non-issue — stale Wiring Manifest reference. Arena is a screen inside index.html via `arena.init()`, not a separate HTML file. |
| H-05 | Bot army quarantined from rename | ❌ | SCRATCHED S248. Bot army system retired in full (see F-06/Bot Army scratch note in pending spec file). Internal "Colosseum" strings in `bot-config.ts`, `bot-engine.ts`, `lib/*`, `tests/*` are irrelevant — code is inert and files are "floating in the ether" with no teardown plan. |
| H-06 | Stripe Edge Function templates use old imports | ❌ | SCRATCHED S277. No monetization at this time. |
| H-07 | Edge Function CORS allowlist missing mirror domain | ✅ | Mirror deprecated S245 (F-40 scratched). All 5 edge functions have correct CORS for current domains (themoderator.app, the-moderator.vercel.app, localhost:3000). Closed S293. |
| H-08 | 3 older RLS policies still have `{public}` scope | ✅ | Session 270. mod_dropout_log SELECT narrowed to authenticated + participants. debate_effect_state SELECT and debate_powerup_loadout SELECT converted to TO authenticated explicitly. |
| H-09 | `bot-engine.js` straggler in repo root | ✅ | Session 163. Already deleted. |
| H-10 | TS Migration Plan — remove from project knowledge | ✅ | Session 163. Removed. |
| H-11 | Navigation Architecture — remove from project knowledge | ✅ | Session 163. Removed. |
| H-12 | Land Mine Map internal filenames are pre-TS-migration | ✅ | Session 192. All `colosseum-auth.js`, `colosseum-arena.js` etc. updated to `src/auth.ts`, `src/arena.ts` etc. throughout. HTML filenames updated from `colosseum-*.html` to `moderator-*.html`. |
| H-13 | Wiring Manifest internal filenames are pre-TS-migration | ✅ | Session 192. All JS/HTML filenames updated. Section 3.7 Moderator System rewritten for F-47. Section 6B Page Load Map rewritten for post-TS reality. Section 8 Source Map overhauled. |
| H-14 | THE-MODERATOR-TEST-WALKTHROUGH.md needs full update | ✅ | Complete. Bot army section deleted, auto-debate scenarios (lines 874, 884) deleted, URLs and filenames updated for post-TS reality. 105-item `WALKTHROUGH-TEST-PLAN.md` also created and pushed (S279) — single-user QA walk covering sections 0–15, 2-user items parked at bottom. |
| H-15 | PRODUCT-WALKTHROUGH.md needs continuation | ✅ | Complete. All screens walked and documented. |
| H-16 | Groups page is a separate HTML page, not an inline SPA screen | ✅ | Session 205. Bottom tab bar added to moderator-groups.html with Groups active, other tabs link to index.html?screen=X. Back arrow removed from lobby view. Legal footer removed for consistency with main app. Detail view back arrow kept (navigates within page). |
| H-17 | Replace placeholder PWA icons with real art | ✅ | Session 277. Generated from brand tokens (S201 CSS palette): black bg, magenta outer ring, cyan inner ring, bold M lettermark, magenta accent dot. All 4 variants: icon-192.png, icon-512.png, icon-192-maskable.png, icon-512-maskable.png. Replaces 1276-byte placeholder blobs. Unblocks F-52. |

---

# SECTION 2: BUGS (all closed as of Session 227)

> **Full security recon (Sessions 212-214) identified 29 medium, 14 low, and multiple critical/high bugs across all phases. All were closed in Sessions 215-227. Only dormant payment stack bugs remain (ADV-4, PAY-BUG-1/2/3) — fix before Stripe goes live. SESSION-218-BUG-TRACKER.md removed from project knowledge Session 228.**

| # | Item | Status | Notes |
|---|------|--------|-------|
| B-01 | Arena view renders blank | ✅ | Session 163. Not a bug — arena is a screen inside index.html via `arena.init()`, not a separate HTML file. |
| B-02 | Auth redirect loop on cold visit | ✅ | Session 163. Root cause: 4000ms page timeout vs 5000ms auth timeout = 1s gap. Fix: 4000→6000ms in home.ts, profile-depth.ts, settings.ts. |
| B-03 | `get_my_milestones` — column 'action' does not exist | ✅ | Fixed Session 124. |
| B-04 | `claim_milestone` — column 'action' does not exist | ✅ | Fixed Session 124. |
| B-05 | Tier threshold gap — Tiers 3-5 unreachable | ✅ | Session 164. Expanded questionnaire from 39→100 questions (12→20 sections). 8 new B2B-driven sections added. All tier thresholds (10/25/50/75/100) now reachable. |
| B-06 | AI Sparring pre-debate navigation bug | ✅ | Session 163. Not reproducible — killed by TS migration Session 142. |
| B-07 | No responsive breakpoints | ✅ | Session 165. `@media (min-width: 768px)` content constraint: `.screen` capped at `max-width: 640px` + centered. Home screen (ring nav) exempted. Profile-depth grid 4-col on desktop. Groups/settings get body constraint. CSS only. |
| B-08 | AI sparring badge mobile overlap | ✅ | Session 169. `ai-generated-badge` div moved out of `.arena-room-header` flex row, placed between header and `.arena-vs-bar` as its own centered element with `align-self:center`. |
| B-09 | Spectator feed not filtered by category | ✅ | Session 206. Root cause: `get_arena_feed` RPC had no `p_category` parameter — client passed it, Supabase silently ignored it. Fix: added `p_category TEXT DEFAULT NULL` to RPC with WHERE clause on both sub-queries. Client fallback: if category returns empty, re-fetches with NULL (general feed). |
| B-10 | Middleware CORS missing themoderator.app | ✅ | Session 206. `middleware.js` ALLOWED_ORIGINS only had `colosseum-six.vercel.app` and `thecolosseum.app`. Added `themoderator.app`. Pre-existing bug from rename — undetected because all app API calls go to Supabase directly, not through `/api/*`. Discovered when `/go` page serverless function returned 403. |

---

# SECTION 3: FEATURES (things that could be added)

Organized by area. Priority column is empty — Pat decides priority, not the doc.

## 3A. Arena / Debates

| # | Feature | Priority | Spec Exists? | Notes |
|---|---------|----------|-------------|-------|
| F-01 | Waiting room | | ✅ Full research doc | Layer 1 ✅ Session 167: dual search ring, 4-phase status, 60s AI fallback prompt, 180s hard timeout, cancel button. Layer 2 ✅ Session 169: queue population count, spectator feed. Layer 3 ✅ Session 170: category-scoped queues with backoff. |
| F-02 | Match Found accept/decline screen | | ✅ Research doc §1.6 | ✅ Session 168. 12s countdown, accept/decline buttons, respond_to_match + check_match_acceptance RPCs, player_a_ready/player_b_ready columns. |
| F-03 | Entrance sequence / battle animations | | 🔶 Partial | Base 3-tier entrance shipped S274: `src/arena/arena-entrance.ts` + `arena-entrance-render.ts` + `arena-entrance-css.ts`. Tier 1 = fade, Tier 2 = slide-in clash, Tier 3 = dramatic arena reveal. Wired into `arena-room-enter.ts`. **Tournament entrance variant (gold/trophy overlay when `debate.tournament_match_id` set) and GvG entrance variant (group name + banner tier) were never built — planned for S279 but lost. Not in repo.** Core feature works; the two variants are the only gap. |
| F-04 | Instant rematch from post-debate | | ❌ No spec | Button exists in nav map, no design. |
| F-05 | Debate recording + replay | | ✅ | Session 233: Replay enrichment live. Session 246: fully walked. **(1) AI scorecard persistence — DONE.** Client write in `src/arena/arena-room-end.ts`, `save_ai_scorecard` RPC called after `requestAIScoring()` completes. **(2) Inline point awards — ✅ SHIPPED S277.** Live feed half (arena-feed-events.ts point_award case) was silently shipped S267 as part of F-57 Phase 2. Replay viewer shipped S277: `get_debate_replay_data` extended with `point_awards` + `speech_events` buckets; `spectate.render.ts` `renderTimeline` handles `speech` entries with inline `+N × M = T pts` badges via `scored_event_id` map, and `score` standalone entries for older debates. F-51 empty-replay bug fixed. SQL: `session-277-f05-replay-point-awards.sql`. |
| F-06 | Debate analytics overlay | | ✅ Attack Plan 3.1 | Speaking time, argument count, interruption count, score timeline. **Dep: needs F-05 (recording).** |
| F-07 | Spectator features (pulse, chat, live share) | | ✅ SHIPPED S277 | **(1) In-debate spectator chat** — `arena-feed-spec-chat.ts` (new): collapsed panel in spectator view of live feed room, 5s poll via `get_spectator_chat` RPC, send via `send_spectator_chat` RPC, report button → mailto, `cleanupSpecChat()` on room exit. Hidden from debaters/mods entirely. **(2) Pre-debate share link** — "SHARE TO WATCH LIVE" copy button on pre-debate screen, copies `?spectate=<debateId>` URL to clipboard with 2s "Copied!" confirm. Only on creation screen — gone once battle starts. Reuses S240 `?spectate=` URL pattern. **(3) Ephemeral cleanup** — `_trg_erase_spectator_chat` trigger deletes `spectator_chat` rows when debate reaches terminal status. SQL: `session-277-f07-spectator-chat-cleanup.sql`. |
| F-08 | Tournament system | | ✅ SHIPPED S278 | Singles only. Single-elimination, ELO-seeded, min 8/max 64, highest ELO gets bye. Standard ranked format. Single-sitting, creator sets start time, auto-locks. 70/20/10 of 90% winner pool, platform 10%, 5% mod pool only when mod present. F-33 gate. No tournament power-ups v1. Spectator betting deferred. Gold fast-blink dot top-left of bell on match ready. Cancel before lock = full refund. SQL confirmed run in Supabase. Client: `src/tournaments.ts`, arena-types, arena-room-end, home.ts, index.html. |
| F-45 | Desktop-optimized arena layout | | ✅ SHIPPED S196 | Left-hand sidebar at ≥1024px via `#desktop-panel` in `index.html` + `lcars-shell.css`. Shows avatar, ELO, wins, losses, streak, token balance, profile depth bar, nav links. Verified intact after S201-203 cyberpunk shell redesign. Renders on Feed/Arena/Ranks/Profile tabs. Not on Groups tab (reconciled as part of Groups work). Hidden on mobile. No further work planned. |
| F-46 | Private lobby / invite-only debate | | ✅ Session 173 | ✅ DONE. 3 visibility modes: username challenge, group members only, shareable join code. Client: showPrivateLobbyPicker, showUserSearchPicker, showGroupLobbyPicker, createAndWaitPrivateLobby, startPrivateLobbyPoll, joinWithCode. SQL: create_private_lobby, check_private_lobby, join_private_lobby, cancel_private_lobby, search_users_by_username. Columns on arena_debates: visibility, join_code, invited_user_id, lobby_group_id. No debate_invites table needed. |
| F-47 | Moderator Marketplace | | ✅ Session 179 | ✅ DONE. SQL Phases 1-3 ✅. Client Steps 1-6 ✅. Step 7 ✅ (renderModScoring: debaters get 👍/👎, spectators get slider 1–50, wired to score_moderator RPC). Step 8 ✅ (8 test cases in tests/f47-moderator-scoring.test.ts, all passing). |
| F-48 | Mod-initiated debate | | ✅ Session 210 | ✅ DONE. SQL: arena_debates.debater_a DROP NOT NULL. 4 RPCs: create_mod_debate, join_mod_debate (FOR NO KEY UPDATE SKIP LOCKED), check_mod_debate, cancel_mod_debate. Client: CREATE DEBATE button in Mod Queue, showModDebatePicker (mode/category/topic/ranked/ruleset), createModDebate, showModDebateWaitingMod (join code + slot names), showModDebateWaitingDebater, startModDebatePoll (4s), onModDebateReady (mod→observer, debater→matchFound). joinWithCode falls back to join_mod_debate. F48-MOD-INITIATED-DEBATE.sql committed to repo. |
| F-51 | Live Moderated Debate Feed | | ✅ LIVE TESTED | **THE core product feature.** Turn-based moderated debate — 4 rounds × 2min turns, one speaker at a time, live inline scoring (two-tap), references (max 5, one-and-done), 60s ad breaks between rounds, spectator sentiment gauge, Deepgram STT, concede after R1, moderator eject/null. Post-debate: vote gate → winner → archive. Phase 1 + Phase 2 code complete (S234/S235). 20/20 tests passing (S261). All deps wired S271 (WebRTC bridge, deepgram-token Edge Function, AdSense). **Live E2E test passed — 2 debaters + 1 moderator, audio, Deepgram transcription in feed, ad breaks confirmed.** |
| F-61 | Debate card expiration + creator cancel | | 📋 Specced S279 | 30-min auto-expire on unmatched ranked cards via pg_cron (every 5 min). Creator cancel any time up to 10 sec before match. Full token refund both paths. New RPCs: `cancel_pending_debate`, `expire_stale_debates`. New column: `arena_debates.matched_at TIMESTAMPTZ`. Client: countdown timer + cancel button on creator's own pending cards. |
| F-62 | Link card debates (Reddit-style OG preview) | | ✅ SHIPPED S293 | Optional URL at forge time. Server scrapes OG preview (image, domain, og:title) at creation. Stored as snapshot in `arena_debates.link_preview JSONB`. New column: `arena_debates.link_url TEXT` (nullable). Preview block renders between title and metadata on card. Tap = open URL in new tab. URL locked at forge. Broken link = Reddit behavior (snapshot renders, dead URL on tap). No allowlist/blocklist at launch. |

## 3B. Token Economy / Staking

| # | Feature | Priority | Spec Exists? | Notes |
|---|---------|----------|-------------|-------|
| F-09 | Token prediction staking | | ✅ Pending spec (S249 audit) | ✅ SHIPPED. Parimutuel pool, pre-debate only. `src/staking.ts` (343 lines), 3 live RPCs (`place_stake`, `get_stake_pool`, `settle_stakes`), 2 tables (`stakes`, `stake_pools`). S249 audit captured as-shipped state in pending spec file (original `TOKEN-STAKING-POWERUP-PLAN.docx` deleted S191). Three known issues logged against LM-210: stale deployed-functions export, silent try/catch in `arena-room-end.ts:184`, `cast_sentiment_vote` missing from export. 2x Multiplier "doubles staking payout on win" preserved as F-09 standalone rule (NOT F-57 in-debate effect, NOT applied to F-58 tip refunds), wired via `settle_stakes.p_multiplier` param (currently hardcoded 1.0 pending F-57 build). Tier ladder: Unranked (0-9Q locked) / Spectator+ (10-24Q max 5) / Contender (25-49Q max 25) / Gladiator (50-74Q max 50) / Champion (75-99Q max 100) / Legend (100+Q max 999999). LM-172 dual-update rule governs tier sync between `src/tiers.ts` and `place_stake` RPC. |
| F-58 | Sentiment tipping (paid gauge movement) | | ✅ SHIPPED S266 | Replaces scratched F-51 Phase 5 free-vote model. No free vote button, every gauge point costs tokens. 2/3/5/10 tip-button strip, min 2 tokens, unlimited cumulative taps. Tier gate based on `debate_watches` table (Observer 1 / Fan 5 / Analyst 15 / Insider 50). Binary unlock — no per-tier amount caps. 50% refund on winning side, burn on losing side, burn on draws. Live + human-vs-human only. Both-side tipping allowed. Cap 1 billion per side per debate. 2 new tables (`debate_watches`, `sentiment_tips`), 2 new columns on `arena_debates`, 4 new RPCs (`log_debate_watch`, `get_user_watch_tier`, `cast_sentiment_tip`, `settle_sentiment_tips`), drops `cast_sentiment_vote` RPC. Client wiring: `arena-feed-wiring.ts`, `arena-feed-ui.ts`, `arena-feed-events.ts`, `arena-types.ts`, `spectate.ts`, `arena-css.ts`. dm_eligibility triggers on both new tables (closes F-23 gap from S262). SQL: `session-266-f58-sentiment-tipping.sql`. **Live tested — tip as Observer+, gauge moves by amount, Unranked locked, 50% refund on winner confirmed.** |
| F-59 | Invite Rewards (growth loop) | | ✅ Session 268 | **SHIPPED S268.** **NEW feature walked S250.** Replaces broken invite infrastructure (existing `generateRefCode()` in `src/share.ts` returns random suffix — not attributable; `handleDeepLink()` captures `?ref=` into localStorage but nothing reads it back). Item-reward ladder (not tokens): **1 invite = 1 Legendary power-up, 5 invites = 1 Mythic power-up, 25 invites = 1 Mythic modifier (permanent, one-time prestige)**. Repeating: +1 Mythic power-up every additional 25 invites after 25 (no further permanents). User picks specific effect at claim time from F-57 catalog (filterable grid). **Successful invite** = invitee completes first ranked debate (not AI sparring). **Invitee bonus**: 500 tokens on first ranked debate completion. **Anti-fraud**: device fingerprint block, IP rate-limit (3 per 30d), email domain rate-limit (5 per 30d), manual review at 25-invite tier before Mythic modifier drops. **Link format** `themoderator.app/i/<5-char-base36>` via new `/i/:code` Vercel route. **Stable `profiles.ref_code TEXT UNIQUE`** generated at Plinko completion (gated). **Attribution**: hybrid localStorage happy-path + server-side click log fallback (IP+device match within 30 days). Last-click-wins on competing referrers. Existing users get "you're already a member" toast, no attribution. **4 proactive nudges**: Plinko completion screen, post-debate modal (existing button rewired), first-win celebration, home feed nudge card when 1-2 invites from milestone. **Dedicated Invite & Rewards profile sub-page**: progress band, invite link, recent activity feed (usernames only), unclaimed rewards with pulsing CLAIM buttons. Celebration modal on claim with rarity-tier animation. Rewards never expire. **Group rewards explicitly CUT from v1** (groups have no inventory — deferred). **2 new tables** (`referrals`, `invite_rewards_log`) + 1 column (`profiles.ref_code`). **7 new RPCs**: `get_my_invite_link`, `record_invite_click`, `attribute_signup`, `convert_referral`, `claim_invite_reward`, `review_invite_reward`, `get_my_invite_stats`. **Build dependency**: F-57 inventory tables (`user_powerups`, `user_modifiers`) must exist at claim time — cleanest path is F-57 → F-59. Full spec in pending spec file. |
| F-60 | Saved Loadouts | | ✅ SHIPPED S272 | Up to 6 named presets per user. `user_loadout_presets` table + RLS. RPCs: `get_my_loadout_presets`, `save_loadout_preset`, `delete_loadout_preset`. SQL confirmed live in Supabase S276. |
| F-10 | Power-up shop | | ✅ Session 268 | **SHIPPED S268.** Shop tab added to Reference Arsenal. Modifier/Power-Up toggle, category + rarity + timing + affordability chip-row filters, card grid via `renderEffectCard()`, bottom-sheet confirm-purchase flow. Calls `buy_modifier`/`buy_powerup` RPCs from F-57. CSS for all modifier card classes added to `index.html`. `src/pages/home.arsenal-shop.ts` (new). |
| F-11 | Marketplace (cosmetics + references) | | ❌ DELETED S182/S183 | No spec. Not building. Reference economy handled via forging + royalties (F-55). Cosmetics handled via F-31 (shipped). |
| F-12 | Seasonal token boosts | | 🅿️ Session 182 | Another mechanism for people to earn tokens. Parked until token economy needs tuning. No design work needed until then. **Phase 6 open decisions (from TOKEN-STAKING-POWERUP-PLAN.docx, doc deleted Session 191):** (1) Platform rake: 0% at launch — revisit if token inflation becomes a problem, obvious fix is 5–10% rake on staking pools as a token sink. (2) Silence power-up in text debate mode: unresolved — block opponent typing for 10s, or audio-only? Needs testing. (3) Power-up pricing (15/25/20/30 tokens): starting points only, requires real earn-rate data before finalizing. |
| F-13 | Fantasy league-style picks | | ❌ Session 182 | **Scratched.** |
| F-57 | Modifier & Power-Up System | | ✅ COMPLETE S270 | All 59 effects live. Phases 1-4 S267. S269 deferred cluster (token/tip/shield). S270: inventory cluster (mirror/burn_notice/parasite/chain_reaction/insurance), round-end cluster (pressure/momentum + debate_round_scores + close_debate_round), final 3 (streak_saver token path, token_drain library path, insurance). See NT F-57 critical notes. |

## 3C. Groups

| # | Feature | Priority | Spec Exists? | Notes |
|---|---------|----------|-------------|-------|
| F-14 | Role hierarchy (Leader, Co-Leader, Elder, Member) | | ✅ Research doc §3 | ✅ DONE. Session 181. 4 roles with permission matrix. SQL + client complete. |
| F-15 | Kick/ban/promote | | ✅ Research doc §3 | ✅ DONE. Session 181. Tied to role hierarchy. SQL + client complete. |
| F-16 | Group settings (post-creation edit) | | ✅ SHIPPED S265 | Walked S250 as a cluster with F-17/F-18. Full-screen mobile-forward Settings modal from Group Detail (gear icon, leader-only). Editable: description, category, is_public, avatar_emoji, join_mode, entry_requirements, audition_config. **Name permanently locked** (audit trail preservation). Leader-only — no co-leader role added. Delete group requires typing group name to confirm; cascades members/bans/auditions/pending_challenges but PRESERVES GvG debate history by nulling group_id on arena_debates. New RPCs: `update_group_settings`, `delete_group`. New column: `groups.join_mode` (open/requirements/audition/invite_only) + `entry_requirements` JSONB + `audition_config` JSONB. Bundled with F-17/F-18 in one migration. |
| F-17 | Entry requirements (min Elo, tier, profile completion) | | ✅ SHIPPED S265 | Walked S250. Active only when `join_mode = requirements`. Three gates: min global Elo (single Elo system — no per-category Elo exists, out of scope), min token staking tier (F-09 ladder), profile_complete boolean. **AND logic** across all set gates. Non-members on a gated group see member list but the Join button is **hidden entirely** — no explanation of why, prevents gate-farming. Grandfathered: new gates apply only to new joiners. Enforced inside `join_group` RPC (single entry point). Generic client error message; specific failure stays server-side. Stored as JSONB on `groups.entry_requirements`. |
| F-18 | Audition system (debate-based entry) | | ✅ SHIPPED S265 | Walked S250. **Sibling to F-17, not dependent** (Model A — mutually exclusive join modes). Exhibition-only (no Elo/XP/stats/Group Elo impact). 5 rules via leader dropdown: allowed_by_leader / debate_leader_any / debate_member_any / debate_leader_win / debate_member_win. Win determined by normal debate scoring. Candidate initiation flow: tap Join → modal → pick debate params (leader can lock any/all of topic/category/ruleset/rounds) → Request Audition → row inserted. For member-facing rules: pending auditions visible to all members, first-come-first-served Accept Audition button launches debate. New table `group_auditions` with serialized concurrency (one pending audition per candidate per group, DEFERRABLE UNIQUE). Unlimited retries. Never auto-expires — leader/candidate can delete/withdraw. New RPCs: `request_audition`, `accept_audition`, `approve_audition`, `deny_audition`, `withdraw_audition`, `resolve_audition_from_debate`, `get_pending_auditions`. Join flow: `join_group` returns `{audition_required: true}` instead of inserting member row. |
| F-19 | Three-tier banner progression | | ✅ Research doc §6 | Avg group win %: 0–25% standard icons, 26–50% custom static, 51%+ custom animated (10s max, 1080p small-space). Auto-unlock, permanent. Same rules for battle animations. Session 166. |
| F-20 | Shared fate mechanic | | ✅ Session 166 | Token multiplier: `floor(avg_questions / 100 × win_pct × 80)`. 25Q × 50% = 10%. Max 80%. Permanent once reached. Lookup table in Session 166 chat. |
| F-21 | Intro music (personal + group, 2 tiers) | | ✅ Session 166 | Renamed from "battle cries." Tier 1: 10 standard intros for everyone. Tier 2: custom 10-sec upload unlocked at 35%+ profile questions answered. |
| F-22 | GvG battle animations | | ❌ Collapsed into F-03 | Same animation system as F-03, scaled to group-vs-group. Not a separate build. See F-03. |

## 3D. Social

| # | Feature | Priority | Spec Exists? | Notes |
|---|---------|----------|-------------|-------|
| F-23 | DM/Chat system | | ✅ SHIPPED S262 | Walked S253, built S262. 1:1 text only, shared-debate eligibility gate (`dm_eligibility`), silent DM-scoped block (LM-219 Option 1 — RLS on realtime publication), soft-delete no-edit, single user-scoped realtime channel. 5 tables + 9 RPCs. Rate limits 30 msg/min + 5 new threads/24h. Client: `src/dm-inbox.ts`, `src/dm-realtime.ts`. Entry points: MESSAGE button (arena-room-end.ts), DM icon + badge (header + home.nav.ts). Migration: `session-260-f23-dm-system.sql`. Note: `arena_votes.user_id` used (not `voter_id`). F-58 eligibility triggers deferred to F-58 migration. All 3 gates passed. |
| F-24 | Search (users, debates, groups) | | ✅ SHIPPED S264 | Walked S253, built S264. 4 SQL migrations on `faomczmipsccwbhpivmp`: `pg_trgm` extension, `search_index` table with trigram GIN index + RLS, backfill (profiles/debates/groups), 3 triggers, `refresh_search_engagement()` RPC, pg_cron nightly 3AM UTC, `search_all()` + `get_trending()` SECURITY DEFINER RPCs (block-hide, anon filter, searchable), `p_searchable` param added to `update_profile()`. Client: `src/search.ts` (new — full-screen overlay, 300ms debounce, 60s cache), search button in `index.html`, inline debate search in `arena-lobby.ts`, inline group search in `groups.ts`, DM picker wired to `search_all` with `get_dm_eligibility_list` fallback, "Hide Me From Search" toggle in settings (3 files). All 3 gates passed (build 5.12KB chunk, madge 38 pre-existing, knip OOM pre-existing). |
| F-25 | Rival online alerts | | ✅ SHIPPED S266 | `rivals-presence.ts` was already feature-complete. Shipped by adding 2 RLS policies on `realtime.messages` for `extension='presence'`, `topic='global-online'`. Global-online Realtime presence channel, accepted rivals only, slide-in popup on rival join, CHALLENGE opens profile, LATER/auto-dismiss at 8s, re-alertable after leave+rejoin. **Live tested — popup fires correctly.** Email delivery (Tier 1) needs F-35 (deferred). |
| F-26 | Follow notifications | | ✅ Session 182 | Renamed from "follow recommendations." NOT a suggestion engine — just deliver notifications when followed users are online or have a new archived debate. Follows table and follow/unfollow UI already exist. Work is wiring follow events to whatever notification channel exists (email for now via F-35, push later). **Dep: needs F-35 for email delivery.** |

## 3E. Reference Arsenal

| # | Feature | Priority | Spec Exists? | Notes |
|---|---------|----------|-------------|-------|
| F-27 | Reference Library (browse by category) — The Armory | | ✅ SHIPPED S270 | Full build. Three-tab screen (The Armory / My Arsenal / Forge / Shop). The Armory: search bar, Sharpen filter drawer, chip rows (category/rarity/source type/status/sort), trending shelf (top 5 most-cited refs last 7 days), card stack. Bottom sheet on tap: full details table, italic claim callout, Second/Challenge/Close. My Arsenal: socket dots per card (filled/empty per rarity slot count). Forge tab: forge form loads immediately. SQL: get_reference_library extended (search/source_type/graduated/challenge_status/sort), get_my_arsenal extended (sockets[]), get_trending_references new RPC. Client: renderArmory, RefSocket + TrendingReference types, getLibrary with filters. Desktop grid reflow. |
| F-28 | Bounty board (rivals-only, graduated gate) | | ✅ SHIPPED S275 | `bounties` + `bounty_attempts` tables. `_bounty_slot_limit` (25%→1…75%→6), `_expire_stale_bounties`. RPCs: `post_bounty`, `cancel_bounty`, `get_my_bounties`, `get_opponent_bounties`, `select_bounty_claim`, `resolve_bounty_attempt`, `get_bounty_dot_user_ids`, `get_user_bounty_dot`. Trigger: auto-cancel on rival remove (zero refund). Client: `src/bounties.ts` (all RPCs + `bountyDot()` + `loadBountyDotSet()` + profile/own-profile sections), `src/arena/arena-bounty-claim.ts` (pre-debate dropdown), `arena-room-setup.ts` (bounty zone), `auth.profile.ts` (bounty section in showUserProfile), `arena-room-end.ts` (resolve wired). Gold dot callsites (leaderboards/feeds/lobby/spectate) deferred — exported, grep-and-add pass next session. |
| F-29 | Source Meta Report | | ✅ SHIPPED S275 | `get_source_meta_report()` RPC — public (granted to anon + authenticated). 5 sections: top source per category (strikes), most persuasive (win rate from `reference_royalty_log`, min 3 cites), most contested (upheld challenges from `reference_challenges`), biggest Elo movers this week (min 3 ranked debates), trending last 7 days. `moderator-source-report.html` — standalone public page, Supabase CDN, OG tags, skeleton loaders, category/rarity badges, footer CTA. Registered in `vite.config.ts` as `sourceReport` entry. |
| F-30 | Reference marketplace | | ❌ DELETED S182/S183 | No spec. Not building. Reference economy handled via forging, seconding, and escalating per-round cite cost — see F-55. |
| F-55 | Reference System Overhaul | | ✅ SHIPPED S269 | Walked S244–S247, fully specced S247. **SQL deployed S269** — `session-269-f55-overhaul.sql`: `profiles.is_bot` (LM-209 ✅), new `arsenal_references` schema (canonical_fingerprint, graduated, deleted_at, challenge_status), `reference_seconds`, `reference_challenges`, `reference_royalty_log`, `reference_sockets` (F-57-compatible), all 5 indexes. Full RPC suite: forge/edit/delete/second/cite (bot-block)/challenge/rule-on/loadout/library/royalties. `pay_reference_royalties` called from `arena-room-end.ts` post-debate. LM-209 resolved. |

## 3F. Profile / Identity

| # | Feature | Priority | Spec Exists? | Notes |
|---|---------|----------|-------------|-------|
| F-31 | Cosmetics/achievements store | | ✅ SHIPPED | Backend: `cosmetic_items`, `user_cosmetics` tables; `get_cosmetic_catalog()`, `purchase_cosmetic()`, `equip_cosmetic()` RPCs. UI: `moderator-cosmetics.html`, `src/pages/cosmetics.ts`. Auto-unlock trigger system referenced by F-08 (tournament-exclusive cosmetics) and F-55 (graduated-forger badges). No further spec or build work needed. |
| F-32 | AI Judge (4-criteria scorecard) | | ✅ SHIPPED S220, documented S253 | **SHIPPED.** Claude Sonnet 4 (`claude-sonnet-4-20250514`) in `supabase/functions/ai-sparring/index.ts`. Post-debate 4-criteria scorecard (Logic / Evidence / Delivery / Rebuttal) 1-10 per criterion. Scoring prompt at `buildScoringPrompt()` line 67 with calibration guidance ("tough judge, most scores 4-7, differentiate"). Temperature 0.3. Persists via `save_ai_scorecard` RPC (S234). Renders in replay via `renderAIScorecard()`. **Intentionally mid per product strategy** — AI mod is the onboarding hook, not the destination; weirdness is a feature. Punch list row historically misnamed ("AI Coach / post-debate feedback / Argument strength analysis") — there was never a user-facing coach, and the scorecard was already shipping. Discovered and documented S253 walk-close. No build work required, no planned iteration. Tuning = 5-minute edit-and-deploy of the prompt string, not a feature build. Full doc in pending spec file SHIPPED section. |
| F-33 | Verified Gladiator badge | | ✅ SHIPPED S273 | Earn path: 60% profile depth, retroactive at launch, permanent. `profiles.verified_gladiator BOOLEAN`, `_trg_grant_verified_gladiator` trigger, `get_leaderboard` + `get_public_profile` rebuilt. Tournament-creation gate for F-08. SQL confirmed live in Supabase S276. |
| F-34 | Trust scores | | ❌ Session 182 | **Scratched.** |
| F-53 | Profile Debate Archive | | ✅ SHIPPED S273 | Spreadsheet-style table on profile page. `profile_debate_archive_entries` table + RLS. RPCs: `get_my_debate_archive`, `get_public_debate_archive`, `get_my_recent_debates_for_archive`, `add_debate_to_archive`, `update_archive_entry`, `remove_from_archive`. Manual curation, public by default, archive links permanent. SQL confirmed live in Supabase S276. |
| F-54 | Private Profile Toggle | | ✅ SHIPPED S272 | `profiles.is_private BOOLEAN DEFAULT false`. `update_profile`, `get_public_profile`, `get_leaderboard`, `get_arena_feed` all rebuilt to respect privacy flag. Individual archive links still work if shared directly. SQL confirmed live in Supabase S276. |

## 3G. Notifications / Engagement

| # | Feature | Priority | Spec Exists? | Notes |
|---|---------|----------|-------------|-------|
| F-35 | Weekly newsletter + in-app toasts | | ✅ Session 190 | **(A) Newsletter via Resend** ✅ Session 187: newsletter.ts on VPS, cron live (Thu 8PM EST). **(B) In-app nudge toasts** ✅ Session 190: nudge.ts module (suppression: once/session, 24h cooldown, 3/session cap). 8 trigger points wired: enter_debate (arena.ts), round_end (arena.ts), final_score win/loss (arena.ts), return_visit (tokens.ts), first_signup (plinko.ts), replay_entry (spectate.ts), first_vote spectate (spectate.ts), first_vote hot-take feed (async.ts — added Session 190). |
| F-36 | 7-day onboarding drip | | ✅ SHIPPED S274 | `onboarding_drip_log` table, 7 cosmetic rewards, drip card UI, all trigger points wired. Day 1–7 progression with escalating rewards and titles ("Rookie"→"Regular"→"Gladiator"). SQL run in Supabase. Client: `src/onboarding-drip.ts`. |
| F-37 | Granular notification preferences | | ✅ SHIPPED S274 | `notif_rivals` + `notif_economy` toggle columns on profiles. Settings UI wired: `settings.helpers.ts`, `settings.load.ts`, `settings.save.ts`. Passed through `update_profile` RPC via `p_notif_rivals`/`p_notif_economy` params. |

## 3H. External / Growth

| # | Feature | Priority | Spec Exists? | Notes |
|---|---------|----------|-------------|-------|
| F-38 | Browser extension ("Take it to The Moderator") | | ❌ SCRATCHED S293 | Replaced by F-49 `/go` — guest AI sparring landing page serves the same cold-traffic funnel without requiring an install. Original concept was companion to bot army (scratched S248). |
| F-39 | Embeddable challenge links | | ✅ Session 182 | URL you paste into Reddit, Twitter, Discord, group chats. Format: `moderator.app/challenge?topic=X&user=Y`. Other person clicks, lands on challenge page (works without auth — guest sees OG tags with topic and challenger), signs up/logs in, auto-routed into private lobby. Extends F-46 infrastructure (create_private_lobby, join_private_lobby already exist). New work: public URL format, guest landing page with OG tags, signup flow that preserves challenge context, auto-routing after auth. Every link posted anywhere = user acquisition funnel. |
| F-40 | Mirror pages with live counts | | ❌ Session 245 | **Scratched.** Mirror deprecated. SEO cannibalization risk (second indexable property competing with main app + /u/ profiles + /go for same keywords). /go owns cold-traffic funnel. Bot army quarantined S195, no traffic pointed at mirror recently. Disaster-recovery role not worth carrying a whole separate deployment. Mirror generator (`/opt/colosseum/colosseum-mirror-generator.js`) and Cloudflare Pages deployment (`colosseum-f30.pages.dev`) to be sunset. See Land Mine Map LM-202 for deprecation entry. NT/CLAUDE.md Ring 6 framing needs cleanup (not urgent). |
| F-41 | Celebrity/influencer challenge events | | ❌ Session 182 | **Way back burner.** Conceptual. |
| F-49 | `/go` — Guest AI Sparring landing page | | ✅ Session 206 | `themoderator.app/go` — standalone page, no auth, no DB. User types topic, picks For/Against, debates AI via voice or text. 3 rounds, running score after each round, final verdict with 4-criteria scorecard (Logic/Evidence/Delivery/Rebuttal). Signup CTAs throughout. Vercel serverless `api/go-respond.js` calls Groq directly. `GROQ_API_KEY` in Vercel env vars. Permanent Reddit/Discord comment: "You think you're right? Prove it: themoderator.app/go" |
| F-52 | TWA wrapper + Google Play submission | | ✅ SHIPPED | Session 233 (PWA). Session 277: real icons (H-17), `assetlinks.json` with real SHA-256 fingerprint, `vercel.json` CORP header, `TWA-SETUP-GUIDE.md`. Signing keystore: `themoderator.keystore`, alias `themoderator`, pass `moderator2024`. **Bubblewrap run on Windows, AAB built and uploaded to Play Console. Submission complete.** |

## 3I. B2B / Revenue

| # | Feature | Priority | Spec Exists? | Notes |
|---|---------|----------|-------------|-------|
| F-42 | B2B data dashboard / API | | ✅ Session 182 | Industry standard for alt data vendors is API access. War Chest §14.2 lists requirements: topic-level access controls, real-time vs delayed feeds, custom dashboards per client, API gating by tier. Delivery: buyer gets API key → hits endpoints scoped to paid topics/categories → gets JSON. Tier 1 = delayed (24-48h lag), Tier 2 = near-real-time, Tier 3 = real-time streaming + raw exports. Dashboard is a nice-to-have web portal on top. Auto-generated Data Integrity Report ships with every delivery (War Chest describes this). **Parked: blocked on having a buyer. War Chest §14.3: "not day-one architecture, build when first buyer is close."** |
| F-43 | Google Ads in structural slots | | ✅ SHIPPED S276 | Publisher `ca-pub-1800696416995461`, unit `8647716209`. New `src/arena/arena-ads.ts`: `injectAdSlot()` (inline responsive banner) + `showAdInterstitial()` (8s countdown overlay, skip after 3s, no-op safe if AdSense not loaded). 5 of 6 structural slots wired: Slot 1 (final score reveal) + Slot 2 (debater scorecard) in `arena-room-end.ts`; Slot 3 (moderator verdict) in `arena-mod-scoring.ts`; Slot 4 (pre-debate lobby) in `arena-room-setup.ts`; Slot 5 (replay entry interstitial) in `profile-debate-archive.ts`. Slot 6 (highlight clip pre-roll) deferred — no clip feature built yet. AdSense script tag already in `index.html` from S271. |
| F-44 | Stripe subscription tiers ($9.99/$19.99/$29.99) | | ❌ SCRATCHED S277 | No monetization at this time. Schema exists if needed later. |

## 3J. UX / Onboarding

| # | Feature | Priority | Spec Exists? | Notes |
|---|---------|----------|-------------|-------|
| F-35.3 | Orange Dot indicator | | ✅ Session 182 | Persistent indicator on nav for unclaimed token-earning opportunities (daily login not claimed, milestone ready, streak freeze available, unread notification). Current notification bell polls every 30s for unread count — Orange Dot is a second, always-visible indicator specifically for token actions. Simple highlight, not guided walkthrough. |
| F-50 | Moderator Discovery — 5 touchpoints | | ✅ Session 206 | The app is called The Moderator — the role should be front and center. 5 fixes: (1) Post-debate nudge in arena.ts via nudge.ts. (2) Arena lobby inline banner with one-tap toggleModerator(). (3) Home feed card at position 2 in hot takes. (4) Newsletter "Moderator Spotlight" section with top mod stats. (5) Plinko signup step 4 (opt-in, skippable) before Done step. All gated: only shows for logged-in non-moderators. Card/banner disappear once is_moderator=true. |
| F-67 | First-time user tutorial (post-signup onboarding walkthrough) | | ⏳ | Guided walkthrough for new users after Plinko signup completes. Introduces core loop: hot takes → reactions → challenges → debates. Should highlight key UI elements (feed, arena, profile). Dismissible, never re-shows after completion. Track via `profiles` column or `localStorage` flag. Lightweight — tooltips/highlights or step-through overlay, not a video. |
| F-68 | Unified Feed (Kill Hot Takes) | | ✅ SHIPPED S294 | Hot takes and debate cards unified into one entity. Everything is an `arena_debates` row. `status='open'` = posted opinion (was a hot take). Challenge transitions `open → pending → live`. New RPCs: `create_debate_card`, `react_debate_card`, `accept_challenge`, `get_unified_feed`. New table: `debate_reactions`. Category filter pills on home feed. Composer: "Let your opinion be heard..." 5 async hot-take files deleted, 6 trimmed (-904 lines). `hot_takes`, `hot_take_reactions`, `async_debates` tables dropped. `create_hot_take`, `react_hot_take`, `create_challenge` RPCs retired. |

## 3K. Security / Anti-Manipulation

| # | Feature | Priority | Spec Exists? | Notes |
|---|---------|----------|-------------|-------|
| F-63 | Spectator participation gate (25% depth) | | ✅ SHIPPED S295 | SQL-level 25% depth gate on `cast_sentiment_tip`, `place_stake`, `send_spectator_chat`. Client pre-flight via `isDepthBlocked()` in `src/depth-gate.ts`. Sub-25% users see confirm dialog redirecting to profile-depth page. Server returns `profile_incomplete` error as defense in depth. LM-227 documents the 7-place threshold sync. |
| F-64 | SQL-level ranked eligibility hardening | | ✅ SHIPPED S295 | Server-side 25% depth gate in `join_debate_queue` RPC. RAISE EXCEPTION if p_ranked=true and profile_depth_pct < 25. Previously client-side advisory only. Single SQL migration, no new tables, no client changes. LM-226 documents the three-place threshold sync. |
| F-65 | Vote velocity detection (invisible fraud) | | 📋 Specced S279 | Server-side invisible velocity analysis on spectator votes. Flag debates where >X votes land within Y seconds for same side. Zero user friction. Holds suspicious votes pending review or discards silently. |
| F-66 | Friction-right security strategy | | 📋 Specced S279 | Philosophy doc, not code. 4-layer defense: L1 Cloudflare Turnstile on signup, L2 SQL depth gates (F-63/F-64), L3 invisible velocity (F-65), L4 existing `rate_limits` table coverage audit. No CAPTCHA. |

---

# SECTION 4: DEPENDENCY TIERS (build order)

Features ordered by what can go first. Check this before picking work.

**Tier 0 — no dependencies, can build now:**
- F-04 Instant rematch
- F-05 Debate recording + replay
- F-07 Spectator features
- F-08 Tournament system
- F-09 Token prediction staking
- F-10 Power-up shop ✅ F-57 tables live — buildable now (no longer depends on F-57 being unbuilt)
- F-59 Invite Rewards ✅ F-57 inventory tables live — buildable now
- F-16 Group settings
- F-17 Entry requirements
- F-19 Three-tier banner progression
- F-20 Shared fate mechanic
- F-21 Intro music
- F-23 DM/Chat system
- F-24 Search
- F-25 Rival online alerts (popup only — email needs F-35) ✅ SHIPPED S266
- F-27 Reference Library
- F-29 Source meta report
- F-33 Verified Gladiator badge
- F-35 Newsletter + in-app toasts ✅ DONE
- F-39 Challenge links (extends F-46, already done)
- F-40 Mirror pages with live counts ❌ SCRATCHED S245
- F-43 Google Ads in structural slots
- F-45 Desktop-optimized arena layout
- F-48 Mod-initiated debate (extends F-46 + F-47, both done) ✅ DONE
- F-31 Cosmetics brainstorm (design session, no code)
- F-35.3 Orange Dot indicator
- F-12 Seasonal token boosts (parked — use when token economy needs tuning)
- F-67 First-time user tutorial
- F-61 Debate card expiration + creator cancel
- F-62 Link card debates (Reddit-style OG preview)
- F-63 Spectator participation gate
- F-64 SQL-level ranked eligibility hardening
- F-65 Vote velocity detection

**Tier 1 — needs one Tier 0 item first:**
- F-06 Debate analytics overlay → needs F-05 (recording)

**Tier 2 — needs two+ Tier 0/1 items:**
- F-03 Entrance sequence / battle animations → needs F-31 + F-21

**Tier 3 — blocked by external conditions:**
- F-42 B2B data dashboard / API → needs a buyer
- F-41 Celebrity/influencer events → way back burner

**Scratched:** F-13, F-34, F-38

---

# OPEN QUESTIONS FROM RESEARCH DOCS

These are unresolved design questions that block specific features. Captured here so they don't get lost.

1. ~~What milestones trigger each group banner tier unlock? (F-19)~~ ✅ Session 166. Avg group win %: 0–25% = standard icons we provide, 26–50% = custom static flag, 51%+ = custom animated flag (max 10s, 1080p small-space equivalent).
2. ~~Does the group leader spend tokens to unlock tiers, or automatic at threshold? (F-19)~~ ✅ Session 166. Automatic at threshold. Permanent unlock — once crossed, never revoked.
3. ~~Animated banner format constraints — GIF size limit, loop duration? (F-19)~~ ✅ Session 166. Answered by Q1: max 10 sec, sized for 1080p equivalent in a small space. Same rules for battle animations.
4. ~~Battle cry audio format and duration constraints? (F-21)~~ ✅ Session 166. Renamed to "intro music." Two tiers: (a) 10 standard intros we provide for everyone, (b) custom 10-sec intro music upload unlocked at 35%+ profile questions answered.
5. ~~Exact shared fate calculation formula — percentage, flat, scaled by group size? (F-20)~~ ✅ Session 166. Formula: `floor(avg_questions / 100 × win_pct × 80)`. Anchor: 25 avg Q's × 50% win rate = 10% token multiplier. Max: 80% at 100 Q's × 100% wins. Permanent once reached.
6. ~~Audition debate: affects Elo or exhibition only? (F-18)~~ ✅ Session 166. Exhibition only. Group leader sets entry rule via dropdown: allowed by leader, must debate leader, must debate member, must win vs leader, must win vs member.
7. Member contribution tracking: what metrics, how displayed? (F-14) — **Still open.**

---

# SOURCE DOCUMENTS

This punch list was compiled from:
- THE-MODERATOR-NEW-TESTAMENT.md (known bugs, tech debt, infrastructure status)
- TYPESCRIPT-MIGRATION-PLAN.md (Phase 6 status, remaining items)
- THE-MODERATOR-NAVIGATION-ARCHITECTURE.md (screen inventory, planned screens)
- THE-MODERATOR-WAR-PLAN.md (shelved features, open decisions)
- THE-MODERATOR-PRODUCT-VISION.md (psychology framework, ad model, gamification)
- MODERATOR-FEATURE-ROOM-MAP.md (Ideas Master Map placement, ~90 items, 7 new rooms)
- SESSION-RESEARCH-WAITING-ROOM-AND-GROUPS.md (waiting room + groups deep design)
- TOKEN-STAKING-POWERUP-PLAN.docx (staking + power-up implementation plan)
- THE-MODERATOR-ATTACK-PLAN.md (original roadmap — DMs, search, replay, AI coach)
- Session handoffs 143, 144, 160, 161, 163, 164

---

# CHANGE LOG

| Date | Session | What |
|------|---------|------|
| 2026-03-23 | 162 | Initial creation. 11 housekeeping, 6 bugs, 44 features, 7 open questions. |
| 2026-03-23 | 163 | B-01/B-02/B-03/B-04/B-06 closed. H-02/H-04/H-09/H-10/H-11 closed. B-07 added. |
| 2026-03-23 | 164 | B-05 closed. Profile depth expanded 39→100 questions (12→20 sections) for B2B data coverage. |
| 2026-03-23 | 165 | B-07 closed (responsive breakpoints, CSS only). H-01 partial: home.ts + groups.ts `any` removed. F-45 added. |
| 2026-03-23 | 166 | H-01 closed. spectate.ts: 14 `any` → 3 interfaces. 6 of 7 open questions answered: F-18 audition (exhibition, 5 entry rules), F-19 banners (win% tiers, auto/permanent), F-20 shared fate (token multiplier formula), F-21 renamed to intro music (2 tiers). Q7 still open. |
| 2026-03-23 | 167 | F-01 Layer 1 done. Queue screen upgrade: dual search ring, 4-phase status text, 60s AI fallback prompt, 180s hard timeout, cancel button. F-02 JS started (not uploaded). |
| 2026-03-23 | 168 | F-02 done. Match found accept/decline screen. 7 new functions, MatchAcceptResponse interface, matchFound view. SQL migration: player_a_ready/player_b_ready columns, respond_to_match + check_match_acceptance RPCs. |
| 2026-03-23 | 169 | B-08 done (AI sparring badge mobile overlap). F-01 Layer 2 done: queue population count + spectator feed. SQL: check_queue_status() replaced via moderator-queue-population-migration.sql. |
| 2026-03-25 | 173 | F-46 complete (private lobby). F-47 SQL Phases 1-3 done, Client Step 4 done (mod category chips). H-03 partial: NT rewritten, OT updated (108-173), CLAUDE.md rewritten. |
| 2026-03-25 | 174 | F-47 Steps 5-6 done. Step 5: Mod Queue tab (MOD QUEUE button lobby-gated by is_moderator, showModQueue screen, loadModQueue 5s poll, claimModRequest race-condition-safe). browse_mod_queue() RPC fixed (ambiguous id→debate_id, added 'live' to status filter). Step 6: Debater opt-in toggle in category picker, request_mod_for_debate RPC, get_debate_mod_status RPC, startModStatusPoll in debate room, showModRequestModal with 30s countdown, handleModResponse accept/decline. F-48 added (mod-initiated debate concept). Files: src/arena.ts (GitHub), browse_mod_queue + request_mod_for_debate + get_debate_mod_status (Supabase). |
| 2026-03-26 | 179 | F-46 closed (verified complete: 5 RPCs live in Supabase, all columns on arena_debates, client fully wired). F-47 closed (Step 7 renderModScoring confirmed implemented, Step 8 8 tests passing — jsdom added to devDependencies). Vitest: 113 tests passing. |
| 2026-03-27 | 182 | Major spec session. F-13 scratched. F-34 scratched. F-41 way back burner. Specs written for: F-12 (parked token mechanism), F-25 (rival online alerts with popup + red dot fallback), F-26 renamed to follow notifications (not recommendations), F-29 (public source meta report for marketing), F-35 renamed to weekly newsletter + in-app toasts (full content list defined, Resend delivery, 10 polite nudge touchpoints), F-35.3 (Orange Dot = unclaimed token indicator), F-36 parked (blocked on F-35 + F-31), F-37 parked (blocked on F-35), F-38 parked (blocked on 50+ users), F-39 (challenge links extending F-46 with public URLs), F-42 (API-first B2B delivery, tiered access, parked until buyer), F-43 renamed to Google Ads in structural slots. B-09 added (spectator feed category filter). Section 4 added: full dependency tier map across all open features. F-31 needs brainstorming session before code. |
| 2026-03-28 | 190 | F-35B closed: nudge.ts confirmed complete, first_vote nudge added to async.ts. H-03 partial: api/profile.js BASE_URL → process.env.BASE_URL with themoderator.app fallback. Env var set in Vercel, deployed. Domain confirmed: themoderator.app live on Vercel production. |
| 2026-03-28 | 192 | Full bible audit. H-12/H-13 closed. 5 stray files deleted from repo (session-85-88 chart traces, profile-depth-expansion.md). CLAUDE.md: War Plan ref removed, OT session count fixed, nudge.ts added, F-47 RPCs completed. Punch List: change log reordered chronologically, H-03 notes corrected. War Chest: title fixed, all Colosseum→Moderator, duplicate content removed. Product Vision: title and all Colosseum refs fixed. Land Mine Map: title, LM-172 corrected (100Q all tiers reachable), LM-084 corrected (6s timeout + correct filenames), LM-022 dead note removed, LM-051 rewritten, all HTML/JS filenames updated. Wiring Manifest: title, container counts corrected, tier thresholds fixed, Section 3.7 fully rewritten for F-47, Section 6B rewritten for post-TS reality, Section 8 Source Map overhauled, maintenance log updated. |
| 2026-03-29 | 195 | Adversarial audit L1 + L8 resolved. L1 ✅: `app_config` table created (17 rows: 13 milestone configs + 4 power-up costs). `src/app-config.ts` new module — fetches live economy constants, 60-min cache, hardcoded fallback. `src/tokens.ts` + `src/powerups.ts` updated to pull from app-config. `src/arena.ts`: `showPowerUpShop()` made async, `await renderShop()`. L8 ❌ won't do: bot army growth strategy quarantined — `category-classifier.ts` has no live consumer, wiring it was pointless. Bot army quarantine confirmed and reflected in NT (sections 5, 7, 10, 11). `classifier_keywords` table and updated `lib/category-classifier.ts` + `lib/leg2-news-scanner.ts` exist in repo but are inert. NT updated: bot army quarantined throughout, `src/app-config.ts` added to modules table. |
| 2026-03-30 | 205 | Full visual token migration. ~350 hardcoded rgba/hex/font values replaced with var(--mod-*) CSS tokens across 17 .ts files (3 rounds: Claude Code rgba sweep, Claude Code hex+font sweep, manual stragglers in leaderboard.ts/rivals-presence.ts/groups.ts). moderator-groups.html: bottom tab bar added, back arrow removed, legal footer removed. H-16 added and closed. Zero old colors (#d4a843, #cc2936, #a0a8b8) or fonts (Cinzel, Barlow) remain in .ts files outside cards.ts Canvas API. |
| 2026-03-30 | 206 | B-09 closed (spectator feed category filter — added p_category to get_arena_feed RPC). B-10 added and closed (middleware CORS missing themoderator.app). F-49 added and closed (/go guest AI Sparring page + api/go-respond.js serverless function + GROQ_API_KEY in Vercel env vars). F-50 added and closed (5 moderator discovery touchpoints: post-debate nudge, arena lobby banner, home feed card, newsletter spotlight, Plinko signup step). Plinko now 5 steps (mod opt-in before Done). |
| 2026-03-31 | 209 | Amplified vs Unplugged ruleset system complete. arena_debates.ruleset TEXT column + CHECK constraint. 4 RPCs updated (update_arena_debate, join_debate_queue, create_private_lobby, get_arena_feed). Client: 3-step picker flow, unplugged debate room (no ELO/tokens/power-ups/spectator bar), post-debate token skip, 🎸 UNPLUGGED feed section. AI Sparring forced amplified. CSS: .arena-rank-badge.unplugged. ai-moderator edge function redeployed (auth validation + no localhost CORS). 5 stale Claude Code branches deleted. |
| 2026-03-31 | 210 | F-48 closed (mod-initiated debate). SQL: debater_a DROP NOT NULL, 4 new RPCs (create_mod_debate, join_mod_debate, check_mod_debate, cancel_mod_debate). FOR NO KEY UPDATE SKIP LOCKED used (not FOR UPDATE — prevents unnecessary FK child table blocking per CYBERTEC research). Client: ruleset picker added to showModDebatePicker, createModDebate passes p_ruleset, onModDebateReady uses result.topic/result.ruleset instead of hardcodes, cancelModDebate calls cancel_mod_debate (was cancel_private_lobby). ModDebateCheckResult interface updated with topic + ruleset fields. F48-MOD-INITIATED-DEBATE.sql committed. 8 F-48 tests passing, 77 total. Bible docs updated. |
| 2026-03-31 | 212-214 | Full app security recon. Every screen, every RPC, every flow traced. Bug tracker created (SESSION-218-BUG-TRACKER.md). Identified critical/high/medium/low bugs across all phases. |
| 2026-03-31 | 215 | Foundation fixes. 91 deployed functions exported to repo (supabase-deployed-functions-export.sql). Guard trigger expanded to 7 moderator columns (ADV-1 closed). 4 dangerous RLS write policies dropped (ADV-3, ADV-8, ADV-9 closed). CORS wildcard fixed (ADV-5). Hardcoded anon keys removed from API routes (SHARE-BUG-1). api/profile.js branding fixed (PROF-BUG-6). save_profile_depth section count 12→20 (PROF-BUG-1). 8 bugs closed. |
| 2026-04-01 | 216-219 | PROF-BUG-2 closed (profile column mismatches). PROF-BUG-3 closed (settings column mismatches). RTC-BUG-2 closed (round count chaos — config.ts as single source of truth). update_arena_debate rewritten S219. |
| 2026-04-01 | 220 | AI-BUG-1 closed (AI scoring mode). ai-sparring Edge Function rewritten: two modes (debate response + 4-criteria scoring). ai-moderator swapped Groq → Claude/Anthropic. Both Edge Functions now use ANTHROPIC_API_KEY. AI-BUG-2 closed (AI moderator). |
| 2026-04-01 | 221 | RTC-BUG-1 closed. Cloudflare TURN server set up. turn-credentials Edge Function created. Client fetches TURN creds before PeerConnection, falls back to STUN. ICE restart with 3 attempts added to webrtc.ts. |
| 2026-04-01 | 222 | RTC-BUG-3 closed (30s setup timeout). FEED-BUG-1 closed (ModeratorTokens window global → ES import). ECON-BUG-5 closed (leaderboard Week/Month tabs removed). ARENA-BUG-1 verified already fixed (opponent polling). |
| 2026-04-01 | 223 | Group RPC fixes: resolve_group_challenge, join_group hardened, update_group_elo dropped. Private group info leaks discovered (get_group_details, get_group_members). |
| 2026-04-01 | 224 | 9 medium bugs closed: JSON.parse crash (profile-depth.ts, settings.ts), logout/leaveDebate wired, grant_cosmetic RPC dropped (dead code), private group info leaks fixed, 56 inline onclick handlers removed (7 files, via Claude Code), escapeHTML duplicate removed (spectate.ts), MutationObserver leak fixed (leaderboard.ts), objectURL leak fixed (voicememo.ts). opponentName XSS in powerups.ts discovered already fixed. |
| 2026-04-01 | 225 | Remaining medium bugs triaged and closed/deferred. All 4 Edge Functions redeployed with correct CORS origins. AUTH-BUG-4 closed (analytics hardcoded key → import from config.ts). |
| 2026-04-02 | 226 | ECON-BUG-1 closed (9 RPC security fixes: token_balance CHECK constraint, race condition FOR UPDATE locks, auth checks on debit_tokens/finalize_debate, duplicate claim prevention). GRP-BUG-1 closed (14 group RPCs audited, respond_to_group_challenge restricted to leader/co_leader). turn-credentials CORS fixed. All 4 Edge Functions redeployed. Prediction system type mismatch discovered (not fixed). |
| 2026-04-02 | 227 | Prediction system fixed end-to-end (place_prediction UUID→TEXT revert, wager picker UI 1-500 tokens, refund-on-update with net charge). supabase-deployed-functions-export.sql fully re-synced (174 RPCs). localStorage key renamed colosseum_settings → moderator_settings with migration code. |
| 2026-04-02 | 228 | Documentation update session. NT updated through S228. Punch List changelog updated through S227. OT session logs S211-227 appended. SESSION-218-BUG-TRACKER.md removed from project knowledge (all bugs closed or dormant). |
| 2026-04-04 | 229 | Major doc cleanup. Land Mine Map: 66 entries killed (173→107), 4 sections removed, LM-092 corrected, LM-199 added, LM-029/LM-110 updated. 3 docs deleted from repo (PRODUCT-WALKTHROUGH, TECHNICAL-AUDIT, LCARS-Reference-Guide). 4 docs removed from project knowledge (Old Testament, Wiring Manifest, Test Walkthrough, Product Vision — all kept in repo). Product Vision unique content merged into NT Section 8. F-51 added (Live Moderated Debate Feed, references LIVE-DEBATE-FEED-SPEC.md). |
| 2026-04-05 | 233 | PWA infrastructure live. F-05 🔶 partial (replay enrichment: 1 new RPC `get_debate_replay_data`, spectate.ts enriched timeline with power-ups/references/mod-scores). H-17 added (placeholder PWA icons). F-52 added (TWA wrapper + Google Play). NT open decision closed (PWA manifest + service worker). |
| 2026-04-05 | 235 | F-51 ❌→🔶 (Phase 1 code complete S234, Phase 2 code complete S235 — both untested). Decisions locked: scoring budget per round (20 actions, 50 max points, per-value limits via hardcoded CASE in `score_debate_comment`). `scoring_budget_per_round` column on `arena_debates` now unused. Moderator token payout designed (~7 tokens, split: debater half instant, spectator half delayed 48h, zero spectators = spectator half stays 0). |
| 2026-04-06 | 240 | Gap #3 closed (language preference → Deepgram pipeline): 2 columns (`profiles.preferred_language`, `arena_debates.language`), 1 BEFORE INSERT trigger (`stamp_debate_language`), `update_profile` RPC updated (BCP-47 validation, per LM-013), 6 read-path RPCs return `language`, 6 TS interfaces updated, 7 `CurrentDebate` construction sites wired, Deepgram language wire in arena-feed-room.ts, settings UI with 24-language dropdown, `session-240-language-preference.sql` deployed. Gap #2 closed (spectator entry wiring): lobby `.card-live` click → `enterFeedRoomAsSpectator()` direct, `?spectate=<debateId>` URL param handler in arena-core.ts, old spectate page redirects live debates to arena. Bible docs updated: LM-013 updated, LM-200 + LM-201 added, NT updated (arena module split, language pipeline, spectator entry, cosmetics.ts in page modules), Punch List updated. |
| 2026-04-08 | 244 | Walk session. F-27 Reference Library Browse locked ("The Armory", three-tab mobile-first). F-53 Profile Debate Archive locked. F-54 Private Profile Toggle locked. F-55 Reference System Overhaul locked (dedup via canonical fingerprint, escalating cite cost schedule inherited from debate_references.token_cost already live in prod, rarity by composite `(seconds×2)+strikes`, graduation at 25 strikes, loadout tiered by profile depth, challenge mechanic with graduated penalty). Parked: 17 ref-system questions for a dedicated future session. |
| 2026-04-08 | 245 | Walk session. F-28 Bounty Board locked (rivals-only, graduated posting gate, 1 token/day, 85% cancel refund, 5% attempt fee, gold indicator dot, ranked-only, mod blind, pre-debate dropdown selection). F-33 Verified Gladiator Badge locked (60% depth earn, permanent, tournament-creation gate for F-08, optional intro upload with 5s/5MB/MP3-M4A-OGG/MP4-1080p rules, play-it-dangerously copyright). F-40 Mirror Pages with Live Counts SCRATCHED (SEO cannibalization, /go owns cold traffic, bot army quarantined). F-08 sentence edit to point at F-33 gate. Punch list additions for F-53/F-54/F-55/F-40 scratch pending until S246. |
| 2026-04-08 | 246 | Massive session. Mechanical S245 writes landed: pending spec file regenerated with F-28, F-33, F-40 scratch, F-08 edit. **F-05 fully walked** (AI scorecard persistence client write + inline point awards). **F-10 fully walked** (shop storefront, tab in The Armory, 4 new RPCs needed). **F-57 Modifier & Power-Up System created** — the 59-effect system designed in S182 but never propagated to canonical spec is now locked in the pending spec file with full pricing tables for both end-of-debate (30) and in-debate (29) effect sets, rarity-to-socket mapping (1/2/3/4/5), compounding stacking math, archive storage columns, cap of 3 power-ups per debate. **F-55 edited** to rename rarity tier Epic → Legendary (resolving S182/F-55 conflict), add socket counts. **F-51 spec file edited** — §4.2 event type 3 rewritten for inline `+N × M = T pts` format, §6.6 Modifier Math added, §9 power-ups replaced with pointer to F-57. **Dependency chain walked and reconciled**: F-05 no longer "blocked on F-51" — the block was that F-51's original point-award event type was incompatible with the inline format Pat locked in F-05, plus F-51 had no modifier concept. Both resolved in this session. Land Mine Map: LM-202 added (mirror deprecation), LM-203 added (S182 design stranded in handoff docx — propagated now), LM-204 added (F-10 stale docx pointer), LM-205 added (F-57 rename tracker — any lingering "Epic" in older docs needs fixing). Punch list: F-05/F-10/F-28/F-33/F-40 rows updated, F-53/F-54/F-55/F-57 rows added. Files this session: 4 produced for Pat to upload (pending spec, punch list, Land Mine Map, F-51 spec) + NT edit + S246 handoff. 17 ref-system questions from S244 still parked. |
| 2026-04-08 | 247 | Dedicated ref-system session. **All 17 parked questions (#24-40 from S243) walked and locked.** F-55 pending spec paragraph fully rewritten to absorb the locks. New subsystems: (1) **Leasing/royalty model** — any user can cite any forged ref with no lease fee; tiered royalty to owner scales with live rarity (Common 0.1 → Mythic 2.0 per cite); win bonus doubles; batched at match-end; Disputed 75%/Heavily Disputed 25%/frozen 0%; no graduation bonus; rounds up to 0.1; silent in F-05 feed; B2B-grade distinct line items in token ledger. (2) **Soft-delete** — `deleted_at` column, immediate and final, max 7/24hr rate limit, grandfathers in-flight debates, burns socketed modifiers, zero post-delete royalties, historical pointers preserved for replay integrity. (3) **Source-type policy** — ceilings locked (primary 5 / academic 4 / book 3 / news 1 / other 1), forger picks from dropdown, `source_type` is the sole field locked at creation forever (not editable even with 10-token edit fee), mis-categorization is a valid challenge ground. (4) **10-token edit button** — forgers can edit all fields except sockets and source_type; recomputes canonical fingerprint; badges survive edits. (5) **Category taxonomy** — shared enum with debate categories (politics/sports/entertainment/music/couples_court), `general` blocked from Forge picker, admin-only to add, one per ref. (6) **Immortality** — refs never decay; stale-claim and dead-link enforcement flows through the existing challenge system; no auto-crawler at launch. (7) **Bots/AI blocked from citing refs** — closes farm-your-own-ref loophole structurally. (8) **Leaser modifier visibility** — leasers see full socket contents as a marketing signal, but modifiers only fire for the ref owner. (9) **Five required indexes** locked for launch migration. Zero refs currently in production — greenfield launch, no migration concerns. Land Mine Map: LM-206 added (`source_type` locked-at-creation as the one edit-button exception), LM-207 added (bot/AI cite block must be enforced at `cite_debate_reference` RPC level), LM-208 added (asymmetric `general` category enforcement — allowed in hot takes, blocked in ref forge). Punch list: F-55 row updated to "fully specced, no parked items remaining." **F-55 is complete.** The 13-feature S243 unspec'd list is now fully spec'd except for build-phase TBDs (F-57 drop rates + pricing re-balance, forging cost code verification, old `submit_reference` retirement plan). Next session can move to the role × capability map (the original S243 end goal). Files produced: pending spec (full replace), punch list (full replace), Land Mine Map (full replace), S247 handoff. |
| 2026-04-09 | 248 | Bot Army Growth System SCRATCHED in full. F-06 naming correction (punch list F-06 is Debate Analytics Overlay, untouched; bot army was never formally F-numbered). Files edited: pending spec (bot army scratch note added, F-55 cite-block paragraph rewritten), punch list (F-29 Source Meta Report distribution line, H-05 closed, H-14 phrasing updated), NT (line 332 stale Groq claim fixed), LM map (LM-005, LM-066, LM-207 rewritten; LM-209 added for missing `profiles.is_bot` F-55 build blocker; Session 43 Groq-model-kill lesson scratched; B2B secrecy LM cleaned). Bot army code/tables/VPS/Groq key all "float in the ether" — no teardown plan. Product Vision, CLAUDE.md, NT sections 5/7/10/11/127/136/237/308-317, Old Testament, War Chest, Wiring Manifest, Playbook, User Guide, analytics SQL, moderator-bot-army-schema.sql all have bot army references parked for future cleanup pass. New LM entry: LM-209 (`profiles.is_bot` column missing, F-55 build blocker). |
| 2026-04-09 | 250 | Multi-thread session. **Deploy actions:** (1) **LM-210 re-export completed** — Pat ran `pg_proc` export query against production `faomczmipsccwbhpivmp` in Supabase SQL Editor, Claude rebuilt `supabase-deployed-functions-export.sql` with standard header. 186 functions (up from S227's 174 — twelve RPCs had drifted in during 23-session window). File committed via GitHub web UI. (2) **LM-211 auto-debate DROPs run against production** — signatures verified against fresh export first: only 2 of the 6 originally-guessed RPCs actually existed (`cast_auto_debate_vote`, `view_auto_debate`); the 4 stake RPCs were never deployed. Verified SQL run successfully, no rows returned. Auto-debate rip now complete end-to-end (repo-side S249, DB-side S250). (3) **`cast_sentiment_vote` investigation closed** — RPC exists in production with signature `(p_debate_id uuid, p_side text, p_round integer)`, matches client call at `arena-feed-room.ts:544` exactly. Not dead, not broken. Was a staleness artifact of the old export. (4) **LM-210 doc cleanup sweep** — NT line 127 de-STALE-ed (174+ → 186, LM-210 resolved), NT line 186 database header de-STALE-ed, LM-210 entry in Land Mine Map marked ✅ RESOLVED with S250 resolution block prepended to original entry. (5) **F-09 silent catch block fixed** — `src/arena/arena-room-end.ts:184` `catch { /* warned */ }` replaced with `catch (err) { console.error('[Arena] settleStakes failed:', err); }`. **Spec walks:** (6) **F-16/F-17/F-18 cluster walked** — Group Settings, Entry Requirements, Auditions walked as a single cluster because they share a `join_mode` state machine. **Model A** locked (mutually exclusive join modes: open / requirements / audition / invite_only), which dissolved the apparent F-18→F-17 dependency from the original stub — they're siblings now. F-16: leader-only full-screen mobile-forward Settings modal, everything editable post-creation EXCEPT name (audit trail preservation), delete group cascades everything except GvG debate history (nulls group_id on arena_debates). No co-leader role added. F-17: three gates (min global Elo — no per-category Elo exists, flagged as out of scope; min F-09 staking tier; profile_complete boolean), AND logic, non-members see member list but Join button hidden entirely (no gate-farming). F-18: exhibition-only, 5 leader-picked rules, candidate fills debate params with leader per-field override support, member-facing rules use first-come-first-served claim by any member from a Pending Auditions section. New table `group_auditions` with DEFERRABLE UNIQUE for serialized concurrency. Unlimited retries. Never auto-expires. One combined migration for the cluster. (7) **F-59 Invite Rewards walked (NEW feature)** — replaces broken `generateRefCode()` infrastructure. Item-reward ladder (not tokens per Pat's S250 pivot): 1 invite = Legendary power-up, 5 = Mythic power-up, 25 = Mythic modifier (one-time prestige), repeating Mythic power-up every +25 after. User picks specific effect at claim time. Successful invite = invitee completes first ranked debate. Invitee bonus: 500 tokens. Anti-fraud: device fingerprint block, IP rate-limit (3/30d), email domain rate-limit (5/30d), manual review at 25-invite tier. Link format `themoderator.app/i/<5-char-base36>` via new Vercel route. Stable `profiles.ref_code` generated at Plinko completion. Hybrid localStorage + server-side click log fallback attribution. Last-click-wins. Existing users get "already a member" toast. 4 proactive nudge surfaces. Dedicated Invite & Rewards profile sub-page with recent activity feed. Celebration modal on claim. Never expires. **Group rewards explicitly CUT from v1** (groups have no inventory). 2 new tables + 1 column. 7 new RPCs. Build dependency: F-57 inventory tables must exist at claim time (F-57→F-59 ordering preferred). Files produced this session: `supabase-deployed-functions-export.sql` (S250 re-export), NT, Land Mine Map, Punch List, pending spec file (F-16/17/18 cluster + F-59 appended), `src/arena/arena-room-end.ts` (F-09 fix), S250 handoff. Next: F-55 is_bot migration (LM-209), F-58 build, F-57 build, walk more unwalked features (F-23, F-24, F-32), or begin build on any of the now-specced features in this session. |
| 2026-04-09 | 249 | Multi-thread session. (1) **F-09 Token Prediction Staking audited** — existing live system captured in pending spec (parimutuel math, tier ladder, 2x Multiplier carveover, known issues). Three issues logged against LM-210: stale deployed-functions export, silent try/catch in arena-room-end.ts:184, missing `cast_sentiment_vote` in export. (2) **F-58 Sentiment Tipping walked and locked** — new feature replacing scratched F-51 Phase 5 free-vote model. No free vote button; every sentiment gauge point costs tokens. 2/3/5/10 tip strip, min 2 tokens, unlimited cumulative taps, watch-based tier ladder (Observer 1 / Fan 5 / Analyst 15 / Insider 50), binary unlock, 50% refund on winning side, burn on losers/draws, live+human-vs-human only, 1B per-side cap, both-side tipping allowed. New tables `debate_watches` + `sentiment_tips`, 2 new columns on `arena_debates`, 4 new RPCs (`log_debate_watch`, `get_user_watch_tier`, `cast_sentiment_tip`, `settle_sentiment_tips`), drops `cast_sentiment_vote`. Dwell data explicitly out of scope (S249 research: Litmus $25-35K/year email dwell analytics + DOOH premium CPMs confirm the market, but The Moderator has no ad inventory yet — cheap ALTER TABLE later). (3) **Auto-debate consumer side FULL RIP** — S248 bot army scratch extension. Auto-debate generation was already dead (Leg 3 killed S248); S249 demolishes the consumer plumbing: `auto_debates` + `auto_debate_votes` + `auto_debate_stakes` tables dropped, 6 RPCs dropped (`cast_auto_debate_vote`, `view_auto_debate`, `place_auto_debate_stake` + 3 S99 staking RPCs), `moderator-auto-debate.html` + `src/auto-debate.ts` deleted, vite autoDebate entry removed, UNPLUGGED-QUEUE-FIX.sql UNION annotated as historical, NT line 180 entry removed, CLAUDE.md lines 89/92 cleaned. Wiring Manifest 528-553 block marked SCRATCHED. H-14 updated to include auto-debate scenario deletion. New LM entries: LM-210 (stale deployed-functions export — last synced S227, NT claim of sync is false), LM-211 (auto-debate system retired). Files produced: pending spec, punch list, LM map, NT, Wiring Manifest, CLAUDE.md, vite.config.ts, UNPLUGGED-QUEUE-FIX.sql, S249 handoff. Deletions: moderator-auto-debate.html, src/auto-debate.ts. Next: F-58 build work, or continue walking unwalked features (F-16, F-17, F-18, F-23, F-24, F-32). |
| 2026-04-12 | 269 | **F-59 SQL deployed** (`session-268-f59-invite-rewards.sql` — 2 tables, 6 RPCs, RLS). **F-55 Reference System Overhaul shipped** (`session-269-f55-overhaul.sql`): `profiles.is_bot` (LM-209 ✅), full `arsenal_references` overhaul, `reference_seconds`/`reference_challenges`/`reference_royalty_log`/`reference_sockets` (F-57-compatible), all 5 indexes, Phases B–E RPCs (forge/edit/delete/second/cite-with-bot-block/challenge/rule-on/loadout/library/royalties/finalize), legacy RPCs retired. Client: `arena-room-end.ts` wired to `pay_reference_royalties`. **F-57 deferred effects** (`session-269-f57-deferred-token-tip-shield.sql`): `_has_eod_effect` helper + 6 effects — `crowd_pleaser` (1.5× gauge), `tip_magnet` (+15% pool bonus), `token_boost` (+10% win+cite), `token_multiplier` (2× earnings), `token_drain` (−8% challenger on denied challenge), `point_shield` (absorbs first debuff). 50 of 59 F-57 effects live; 9 deferred. NT + Punch List updated. |
| 2026-04-12 | 268 | **F-10 Power-Up Shop shipped.** Arsenal Shop tab, modifier/powerup catalog, all filter chips, bottom-sheet purchase flow. `src/pages/home.arsenal-shop.ts` (new). `buy_modifier`/`buy_powerup` RPCs from F-57. **F-59 Invite Rewards shipped.** Referral attribution system end-to-end: `profiles.ref_code`, `referrals` + `invite_rewards_log` tables, 6 RPCs, `/i/:code` Vercel route + `api/invite.js`, `src/share.ts` fixed (stable ref code), `src/pages/home.invite.ts` (new — Invite & Rewards screen with progress band, claim sheet, activity feed), `index.html` invite screen + CSS, `home.nav.ts` wired, `home.ts` back button, `arena-room-end.ts` `convert_referral` hook on ranked debates, `plinko.ts` step-5 invite nudge. Build: clean 4.45s. |
| 2026-04-12 | 267 | F-57 Modifier & Power-Up System shipped (Phases 1-4). 6 new tables, 12 new RPCs/helpers, `src/modifiers.ts`. `score_debate_comment` rewritten with in-debate modifier math. `apply_end_of_debate_modifiers` new RPC + post-debate "after effects" display. `update_arena_debate` updated with elo_shield/elo_amplifier/xp_boost/trophy_hunter/streak_saver. `score_a`/`score_b` widened to NUMERIC. 44 of 59 effects live. 15 effects deferred (architectural blockers). NT + Punch List updated. F-57 🔶. F-10 + F-59 unblocked. | 2 new tables (`debate_watches`, `sentiment_tips`), 2 columns on `arena_debates`, 4 new RPCs, drops `cast_sentiment_vote`. dm_eligibility triggers on both tables (closes F-23 gap). Tip strip CSS. F-25 Rival Online Alerts shipped — `rivals-presence.ts` was feature-complete, missing RLS policies on `realtime.messages` for presence channel added. Duplicate import cleaned. Both features untested (live test needed). Supabase function count ~212. |
| 2026-04-12 | 267 | NT updated (F-58 + F-25 critical notes sections added, infrastructure summary updated, function count ~212, deployed-functions-export marked STALE). Punch List updated (F-58 + F-25 marked ✅ SHIPPED S266). F-57 build started. | 1 new table (`group_auditions`), 3 new columns on `groups`, 1 new column on `arena_debates`, 9 new RPCs, 2 replaced RPCs, 2 new client files (`groups.settings.ts`, `groups.auditions.ts`), 4 edited files (`groups.types.ts`, `groups.ts`, `moderator-groups.html`, `arena-room-end.ts`). Build clean 7.03s, zero TS errors, main.js 362KB unchanged, zero new circular deps. All 3 gates passed. Supabase function count ~208. |
| 2026-04-12 | 264 | F-24 Search shipped. 4 SQL migrations applied. Client: `src/search.ts` (new), search button in `index.html`, inline search in `arena-lobby.ts` + `groups.ts`, DM picker wired, settings toggle in `settings.ts`/`settings.types.ts`/`settings.load.ts`. pg_cron nightly engagement refresh live at 3AM UTC. All 3 gates passed. F-24 row updated to SHIPPED. |
| 2026-04-12 | 263 | Stale row corrections: F-31 ⏳→✅ SHIPPED (cosmetics store live since before S182 brainstorm note was written). F-45 ❌→✅ SHIPPED (desktop sidebar live S196). F-11 ❌ No spec→DELETED S182/S183. F-30 ❌ No spec→DELETED S182/S183. F-22 🔶→collapsed into F-03 (not a separate build). All corrections sourced from THE-MODERATOR-FEATURE-SPECS-PENDING.md canonical record. |
| 2026-04-12 | 262 | F-23 DM/Chat shipped. Migration `session-260-f23-dm-system.sql`: 5 tables, RLS (LM-219 Option 1 — RLS on realtime publication for silent-block), 2 eligibility triggers on `arena_debates`/`arena_votes` with ON CONFLICT DO NOTHING (LM-218), atomic backfill (LM-217), 9 SECURITY DEFINER RPCs. Client: `src/dm-inbox.ts`, `src/dm-realtime.ts`. Modified: `index.html`, `arena-room-end.ts`, `home.nav.ts`, `home.ts`. Key deviation: `arena_votes.user_id` used (not `voter_id`). F-58 triggers deferred. All 3 gates passed (build clean 1.05s, madge 38 pre-existing, knip OOM pre-existing). F-23 row updated to SHIPPED. |
| 2026-04-12 | 261 | F-51 Phase 1 + Phase 2 testing complete. 20/20 tests passing in `tests/f51-live-debate-feed.test.ts`. TC1-TC13, TC20 cover Phase 1 (turn alternation, round structure, state reset, dedup, event rendering, RPC params). TC10-TC11, TC14-TC19 cover Phase 2 (fireworks, scoreboard update, budget tracking, budget exhaustion, pin fallback, UUID rejection). All 3 gates passed: build clean, madge 38 cycles (37 pre-existing + 1 from prior unstaged src/ changes unrelated to test work), knip pre-existing oxc-parser crash. F-51 row updated. NT F-51 section updated. |
| 2026-04-12 | 254 | TypeScript refactor session — all 3 tracks complete. **Track A (source decomposition):** 14 large files split into domain modules (webrtc, spectate, reference-arsenal, home, arena-feed-machine, arena-feed-wiring, tokens, leaderboard, settings, powerups + async, arena-feed-room, groups, auth). Zero TS errors, zero new circular deps. Madge + Knip installed, `knip.config.ts` added. 3-gate verification process established (build → madge → knip). Stale `src/settings.ts` (duplicate of `src/pages/settings.ts`) deleted. LM-224 added (stale settings.ts duplicate). **Track B (bundle fix):** 8 static imports of `arena-lobby.ts` converted to `await import()` across arena.ts, arena-core.ts, arena-match.ts, arena-mod-debate.ts, arena-mod-queue.ts, arena-private-lobby.ts, arena-queue.ts, arena-room-end.ts. `arena-lobby` is now a standalone async chunk (11.73 kB). main.js: ~370 kB → 362 kB. Vite warning eliminated. Circular deps: 38 → 37. All 3 gates passed. **Track C (SQL reorganization):** `supabase-deployed-functions-export.sql` (191 function blocks, 151 unique names, 11,015 lines) split into 10 domain files under `supabase/functions/`: arena.sql (55), auth.sql (33), moderation.sql (22), references.sql (20), groups.sql (17), tokens.sql (11), predictions.sql (9), hot-takes.sql (10), admin.sql (10), notifications.sql (4). Zero unclassified functions. Done via Claude Code. Commits: 013a08f (housekeeping), b714460 (Track B). |
| 2026-04-09 | 253 | Mother-doc walk-close session. 3 unwalked features crossed off the blocker list. **F-23 DM/Chat walked** — 5 rounds, 25 questions, all defaults. 5 tables + 9 RPCs, materialized `dm_eligibility` table with triggers on `arena_debates`/`arena_votes`/`sentiment_tips`/`debate_watches`, silent DM-scoped block, single user-scoped realtime channel, 1:1 text only, 30 msg/min + 5 new threads/24h rate limits, no attachments/media/push/edit at launch, free forever, retention forever, post-debate CTA next to Rematch button, standalone build (no F-57/F-58/F-59 dependency). **F-24 Search walked** — 5 rounds, 25 questions, all defaults except #4. Materialized `search_index` with pg_trgm GIN, engagement-boosted nightly cron refresh, users/debates/groups at launch, global top-nav + contextual inline bars, 2-char min + 300ms debounce, `profiles.searchable` opt-out column (Pat picked Option A over full private profiles — F-54 is the future full-profile toggle). Bidirectional block hide via F-23 `dm_blocks`. **Build dependency: F-23 → F-24** (block-hide clause references dm_blocks). **F-32 discovered SHIPPED S220** — started walking as "AI Coach" per punch list but Pat stopped at Q3 ("cart before horse, this isn't doable"). Reframe: F-32 is the scoring rubric the AI uses to grade comments, not a coach. Grep revealed `buildScoringPrompt()` at `supabase/functions/ai-sparring/index.ts:67` already shipping full 4-criteria rubric (Logic/Evidence/Delivery/Rebuttal 1-10 each) on Claude Sonnet 4 (`claude-sonnet-4-20250514`) with calibration guidance ("tough judge, most scores 4-7, differentiate") at temp 0.3. Swapped in S220 during AI-BUG-2 fix. Pat locked intentionally-mid product posture: AI mod is the onboarding hook, users are pushed toward human mods for real scoring, weirdness is a feature. **Surfaced incoherence (not a bug, documented for clarity):** AI judge is 1-10 per criterion (max 40/side), F-51 human mod is 1-5 per comment — the two scoring artifacts are not numerically comparable by design. Scale harmonization = separate future feature if ever pursued. Also noted: `ai-moderator/index.ts` is a reference/evidence validator, NOT the debate scorer — misleading filename, worth renaming in a future doc sweep. **Net effect on mother doc path:** 3 unwalked → 0 unwalked. Every feature in the app is now either specced or shipped. Mother doc synthesis is unblocked, estimated 1-2 session mechanical job. **Deliverables produced:** `F-23-SPEC-ADDITION.md`, `F-23-LANDMINES.md`, `F-24-SPEC-ADDITION.md`, `F-24-LANDMINES.md`, `F-32-SPEC-ADDITION.md`, S253 handoff. Doc sweep applied S254: pending specs file got F-23/F-24 appended plus new SHIPPED section with F-32; punch list F-23/F-24/F-32 rows updated (F-32 removed from Tier 0 buildable list); LM Map gained LM-217/218/219 (F-23) and LM-220/221/222/223 (F-24); NT gained a short F-23/F-24/F-32 status section; new file `THE-MODERATOR-MOTHER-DOC-PREP.md` created with source doc list for synthesis. Function count unchanged at 186 (no migration ran). No CC sessions. No code changes. No deploys. |

| 2026-04-12 | 270 | F-57 COMPLETE (59/59 effects live): inventory cluster (mirror/burn_notice/parasite/chain_reaction/insurance), round-end cluster (pressure/momentum + debate_round_scores + close_debate_round), final 3 (streak_saver token path, token_drain library path, insurance). F-27 The Armory shipped: get_reference_library extended, get_my_arsenal with sockets, get_trending_references, renderArmory with full browse UI (search/filters/trending shelf/bottom sheet), socket dots on My Arsenal, Forge tab. H-08 closed (mod_dropout_log + debate_effect_state + debate_powerup_loadout RLS policies). F-60 Saved Loadouts added to punch list. |
| 2026-04-12 | 271 | F-51 Live Moderated Debate Feed — code complete. WebRTC bridge wired: `initFeedAudio()` in `arena-feed-room.ts` calls `joinDebate`/`leaveDebate`, registers micReady/connected/disconnected/connectionFailed handlers, fire-and-forget on `enterFeedRoom`, cleaned up in `cleanupFeedRoom`. `deepgram-token` Edge Function deployed to Supabase (DEEPGRAM_API_KEY was already live in secrets since Apr 6). AdSense fully wired: publisher `ca-pub-1800696416995461` + ad unit `8647716209` in `showAdOverlay()` with `adsbygoogle.push({})` call, script tag in `index.html`. F-51 status → 🔶 Code complete / needs live test. |
| 2026-04-12 | 274 | 9 features shipped — largest session to date. F-35.3 Orange Dot (daily login unclaimed OR unread > 0). F-39 Embeddable Challenge Links (public landing page + localStorage handoff + arena auto-join). F-21 Intro Music (10 synth tracks + Tier 2 custom upload + match-found playback). F-03 Entrance Sequences (`arena-entrance.ts`, 3-tier animated reveal). F-19 Three-Tier Group Banner Progression (`gvg_wins/losses` + banner columns + storage bucket + leader upload). F-20 Shared Fate Mechanic (`shared_fate_pct` wired into `claim_debate_tokens`). F-26 Follow Notifications (online + archive fan-out, 2 new types). F-37 Granular Notification Preferences (`notif_rivals` + `notif_economy` toggles). F-36 7-Day Onboarding Drip (`onboarding_drip_log`, 7 cosmetic rewards, drip card, all trigger points). All SQL run in Supabase. Build clean 4.51s. |
| 2026-04-12 | 275 | F-28 Bounty Board + F-29 Source Meta Report shipped. F-28: `bounties` + `bounty_attempts` tables, `_bounty_slot_limit`/`_expire_stale_bounties` helpers, 8 RPCs. Trigger auto-cancels on rival remove. Client: `src/bounties.ts`, `src/arena/arena-bounty-claim.ts` (pre-debate dropdown), `arena-room-setup.ts` + `auth.profile.ts` + `arena-room-end.ts` edited. Gold dot callsites deferred (exported, grep-and-add pass next session). F-29: `get_source_meta_report()` RPC (public, 5 sections), `moderator-source-report.html` (standalone public page, OG tags, Supabase CDN). NT + Punch List updated. |
| 2026-04-13 | 277 | F-05 replay point awards shipped (SQL + client). H-17 closed (real branded PWA icons). F-52 infrastructure shipped (assetlinks.json, vercel.json, keystore, setup guide). H-06 + F-44 scratched (no monetization). F-07 spectator chat + pre-debate share link shipped (SQL + client). F-08 next. |
| 2026-04-16 | post-S279 | **Design phase closed.** M-A1 bug fixed: `unpauseFeed()` moved inside `.then()` (fires after RPCs resolve), idempotency guard added (`feedPaused` check prevents double-call from Realtime subscription). `arena-feed-machine-pause.ts`. Build clean. Bibles updated (NT + Punch List). |
| 2026-04-13–16 | S279+ | **Full TypeScript refactor complete.** 372 modules, every file under 300 lines. Wave 0 cycle-breaking (4 tasks: `arena-core.utils.ts` extraction, `arena-config-round-picker.ts` extraction, mod-queue dynamic import, orphan deletion). All 60 refactor candidates processed. Final split: `config.ts` → `config.types.ts` + `config.toast.ts`. Build clean throughout: zero TS errors, 7 pre-existing Vite dynamic/static warnings. `BUG-FIX-PATTERNS.md` committed. `CC-BUG-FIX-PROMPTS.md` committed (9 prompts, all executed). |
| 2026-04-13–16 | S279+ | **Full codebase audit complete.** 63 files audited (52 original + 11 refactor sub-modules in Batches A–B). Method: 5-agent, 4-stage, Claude Sonnet, 4 files/batch. Batches 1–16R + A + B. Final totals: **0 High, 47 Medium, 86 Low. 3 findings FIXED in-session (H-A2 live DB bug, H-K1 stored XSS, L-C8 withdraw handler).** All output in `audit-output/`. `AUDIT-FINDINGS.md` is the canonical record. `THE-MODERATOR-AUDIT-METHOD-V3.md` is the reusable method. |
| 2026-04-15–16 | S279+ | **All bug fix sweeps complete.** 9 CC prompts from `CC-BUG-FIX-PROMPTS.md` executed: Sweep B (XSS — Number() + escapeHTML() violations), Sweep A (try/finally on disable-button handlers + missing .catch()), Sweep D (hardcoded hex colors), Sweep C (dead imports + dead code), Pattern 7 (timer/cleanup gaps), Pattern 6 (stale hex), Pattern 8 (stale module state), Prompt 7 (race conditions — simple: M-J1, M-B7, L-N1), Prompt 8 (race condition — M-K1 spec-chat dedup), Prompt 9 (auth race M-C4 slow-connection UX). M-A1 deferred until refactor complete — fixed separately April 16. |
| 2026-04-13 | 278 | F-08 Tournament System spec fully locked + code shipped. Token bar removed from header (balance on profile only, orange dot logic deleted). Agent Handoff Section 2 updated (S274-S277 file manifest). SQL pending Pat run: `session-278-f08-tournament-system.sql` (3 tables: tournaments/tournament_entries/tournament_matches; 8 RPCs; arena_debates.tournament_match_id FK). Client: `src/tournaments.ts` (new — gold dot, poll, create/join/cancel, render helpers, resolve), arena-types/arena-room-end/home.ts/index.html updated. Next: F-03 entrance sequences. |
| 2026-04-12 | 276 | Gold dot callsites wiring pass (F-28 follow-up): `bountyDot()` wired into leaderboard rows, hot take cards, live feed debater names, pre-debate + live room opponent names, profile modal. `loadBountyDotSet()` called on home init. `renderMyBountiesSection()` wired to own-profile view. `LiveDebate` type updated with debater IDs. 7 files, build clean. F-43 Google Ads structural slots shipped: `src/arena/arena-ads.ts` (new — `injectAdSlot` + `showAdInterstitial`), 5 slots wired across `arena-room-end.ts`, `arena-mod-scoring.ts`, `arena-room-setup.ts`, `profile-debate-archive.ts`. Build clean 7.63s. |
