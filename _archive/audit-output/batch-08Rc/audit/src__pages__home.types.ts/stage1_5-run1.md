# Anchor List — src/pages/home.types.ts (Arbiter Run 1)

(none)

## Resolution notes

The file contains zero function definitions. Its entire content is two exported TypeScript `interface` declarations (`Category`, lines 2–9, and `LiveDebate`, lines 11–24) plus a JSDoc comment. Interfaces are type-only constructs — they have no runtime representation and are not callable bindings. All five stage 1 agents correctly identified only type-level constructs; none proposed any function candidates. A direct scan of the source confirms no `function` keyword, no arrow-function assignments, and no `const`/`let`/`var` bindings of any kind. The anchor list is therefore empty.
