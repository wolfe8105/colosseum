# Anchor List — src/async.types.ts (Arbiter Run 2)

(none)

## Resolution notes

All five stage 1 agents agree unanimously: the file contains zero function definitions. A direct scan of the source confirms this. The file is 81 lines of pure TypeScript type declarations — six exported `interface` blocks (`HotTake`, `Prediction`, `StandaloneQuestion`, `RivalEntry`, `ReactResult`, `CreateHotTakeResult`) and one exported `type` alias (`CategoryFilter`). There are no `function` declarations, no arrow-function bindings, no function expressions, no class bodies, and no runtime statements of any kind. No stage 1 agent proposed any function candidate, and the direct scan found none either. The anchor list is empty.
