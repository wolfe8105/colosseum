# Anchor List — src/async.types.ts

Source: src/async.types.ts
Produced by: stage 1.5 (arbiter + optional reconciliation)
Unresolved items: 0

(empty — no top-level named function definitions found)

## Resolution notes

Both arbiter runs agreed. The file is pure TypeScript type declarations with no runtime code. No candidates emerged from Stage 1 or direct source scan.

- `HotTake` (line 8): excluded — exported interface, not a function definition.
- `Prediction` (line 24): excluded — exported interface, not a function definition.
- `StandaloneQuestion` (line 38): excluded — exported interface, not a function definition.
- `RivalEntry` (line 52): excluded — exported interface, not a function definition.
- `ReactResult` (line 64): excluded — exported interface, not a function definition.
- `CreateHotTakeResult` (line 69): excluded — exported interface, not a function definition.
- `CategoryFilter` (line 73): excluded — exported type alias, not a function definition.
