# Anchor List — arena-deepgram.types.ts

(none)

---

**Resolution notes**

- `TranscriptCallback` (line 6) — excluded: `export type` alias; a type-level binding, not a callable function definition.
- `StatusCallback` (line 7) — excluded: `export type` alias; a type-level binding, not a callable function definition.
- `DeepgramStatus` (line 8) — excluded: `export type` union alias; a type-level binding, not a callable function definition.
- `DeepgramResult` (lines 10–20) — excluded: `export interface` declaration; a type-level binding, not a callable function definition.

All five agents identified only type/interface bindings. Source file confirmed: the file contains zero function declarations, zero arrow-function bindings, and zero function-expression bindings. No candidates were missed by the agents and no additional candidates were found on direct source scan.
