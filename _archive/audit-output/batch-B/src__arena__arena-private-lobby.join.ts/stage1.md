# Stage 1 Outputs — arena-private-lobby.join.ts

## Agent 01

Top-level named callable function bindings:

1. `joinWithCode` — line 19, exported async function declaration

Other top-level items (not functions):
- 10 import statements (lines 8–17)
- `void modDebateId` expression statement (line 88) — top-level statement, not a callable

---

## Agent 02

Top-level named callable function bindings:

1. `joinWithCode` — line 19, exported async function declaration

Other top-level items (not functions):
- Import statements (lines 8–17): `safeRpc`, `showToast`, `friendlyError`, `DEBATE`, `set_selectedMode`, `modDebateId`, `set_modDebateId`, type imports, `AI_TOPICS`, `isPlaceholder`, `randomFrom`, `showMatchFound`, `showModDebateWaitingDebater`
- `void modDebateId` — line 88, top-level void expression (not a callable)

---

## Agent 03

Top-level named callable function bindings:

1. `joinWithCode` — line 19, exported async function declaration (async)

Other top-level items (not functions):
- 10 import statements (lines 8–17)
- `void modDebateId` statement (line 88) — not a callable binding

---

## Agent 04

Top-level named callable function bindings:

1. `joinWithCode` — line 19, exported async function declaration

Other top-level items (not functions):
- Import block (lines 8–17): runtime imports + type-only imports
- `void modDebateId` — line 88, top-level side-effect expression (not a callable)

---

## Agent 05

Top-level named callable function bindings:

1. `joinWithCode` — line 19, exported async function declaration

Other top-level items (not functions):
- Imports (lines 8–17)
- `void modDebateId` — line 88, void expression statement, not a callable binding
