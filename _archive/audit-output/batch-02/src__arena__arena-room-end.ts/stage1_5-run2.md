# Anchor List — arena-room-end.ts (Arbiter Run 2)

1. endCurrentDebate  (line 32)
2. renderAfterEffects  (line 462)

## Resolution notes

- All other top-level bindings in this file are imports from other modules.
- `renderChain` and `renderInventoryEvent` are inner helper functions within `renderAfterEffects`, not top-level callables.
- Both runs agree.
