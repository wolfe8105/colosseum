# Anchor List — arena-mod-refs-ai.ts

1. requestAIModRuling  (line 8)

## Resolution notes
Single top-level exported async function declaration. The `.map()` callback on line 24 is an inline anonymous arrow function inside a method chain — excluded per rules. No other top-level function definitions exist in the file.
