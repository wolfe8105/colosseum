# Stage 1 Outputs — bounties.ts

## Agent 01
Read 10 lines of 10 total.

1. comment — JSDoc block (lines 1–5): "THE MODERATOR — Bounty Board (F-28)", refactor note
2. blank — line 6
3. re-export — `BountyRow`, `OpponentBounty`, `MyBountiesResult`, `PostBountyResult`, `SelectClaimResult` (type re-exports from `./bounties.types.ts`)
4. re-export — `loadBountyDotSet`, `userHasBountyDot`, `bountyDot` (value re-exports from `./bounties.dot.ts`)
5. re-export — `postBounty`, `cancelBounty`, `getMyBounties`, `getOpponentBounties`, `selectBountyClaim`, `bountySlotLimit` (value re-exports from `./bounties.rpc.ts`)
6. re-export — `renderProfileBountySection`, `renderMyBountiesSection` (value re-exports from `./bounties.render.ts`)

## Agent 02
Read 10 lines of 10 total.

1. comment — block comment (lines 1–5): module header for Bounty Board (F-28)
2. blank — line 6
3. re-export — `BountyRow`, `OpponentBounty`, `MyBountiesResult`, `PostBountyResult`, `SelectClaimResult` (type re-exports from `./bounties.types.ts`)
4. re-export — `loadBountyDotSet`, `userHasBountyDot`, `bountyDot` from `./bounties.dot.ts`
5. re-export — `postBounty`, `cancelBounty`, `getMyBounties`, `getOpponentBounties`, `selectBountyClaim`, `bountySlotLimit` from `./bounties.rpc.ts`
6. re-export — `renderProfileBountySection`, `renderMyBountiesSection` from `./bounties.render.ts`

## Agent 03
Read 10 lines of 10 total.

1. comment — JSDoc block (lines 1–5): "THE MODERATOR — Bounty Board (F-28) / Refactored…"
2. blank — line 6
3. re-export — `BountyRow`, `OpponentBounty`, `MyBountiesResult`, `PostBountyResult`, `SelectClaimResult` (type re-exports from `./bounties.types.ts`)
4. re-export — `loadBountyDotSet`, `userHasBountyDot`, `bountyDot` (value re-exports from `./bounties.dot.ts`)
5. re-export — `postBounty`, `cancelBounty`, `getMyBounties`, `getOpponentBounties`, `selectBountyClaim`, `bountySlotLimit` (value re-exports from `./bounties.rpc.ts`)
6. re-export — `renderProfileBountySection`, `renderMyBountiesSection` (value re-exports from `./bounties.render.ts`)

## Agent 04
Read 10 lines of 10 total.

1. comment — JSDoc block (lines 1–5): module header "THE MODERATOR — Bounty Board (F-28)"
2. blank — line 6
3. re-export — `BountyRow`, `OpponentBounty`, `MyBountiesResult`, `PostBountyResult`, `SelectClaimResult` (type re-exports from `./bounties.types.ts`)
4. re-export — `loadBountyDotSet`, `userHasBountyDot`, `bountyDot` (value re-exports from `./bounties.dot.ts`)
5. re-export — `postBounty`, `cancelBounty`, `getMyBounties`, `getOpponentBounties`, `selectBountyClaim`, `bountySlotLimit` (value re-exports from `./bounties.rpc.ts`)
6. re-export — `renderProfileBountySection`, `renderMyBountiesSection` (value re-exports from `./bounties.render.ts`)

## Agent 05
Read 10 lines of 10 total.

1. comment — JSDoc block (lines 1–5): "THE MODERATOR — Bounty Board (F-28)"
2. blank — line 6
3. re-export — `BountyRow`, `OpponentBounty`, `MyBountiesResult`, `PostBountyResult`, `SelectClaimResult` (type re-exports from `./bounties.types.ts`)
4. re-export — `loadBountyDotSet`, `userHasBountyDot`, `bountyDot` from `./bounties.dot.ts`
5. re-export — `postBounty`, `cancelBounty`, `getMyBounties`, `getOpponentBounties`, `selectBountyClaim`, `bountySlotLimit` from `./bounties.rpc.ts`
6. re-export — `renderProfileBountySection`, `renderMyBountiesSection` from `./bounties.render.ts`
