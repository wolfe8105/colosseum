# Stage 1 Outputs — reference-arsenal.utils.ts

## Agent 01
Read 20 lines of 20 total.

1. **comment** — block comment (lines 1–5)
2. **blank** — line 6
3. **import** — type-only import of `ArsenalReference` from `./reference-arsenal.types.ts`
4. **import** — value import of `SOURCE_TYPES` from `./reference-arsenal.constants.ts`
5. **blank** — line 9 (between imports and first function)
6. **comment** — line 10 (`/** Composite score for display */`)
7. **bind name to function definition** — `compositeScore`, exported
8. **blank** — line 14
9. **comment** — line 15 (`/** Power display string */`)
10. **bind name to function definition** — `powerDisplay`, exported

## Agent 02
Read 20 lines of 20 total.

1. comment — block comment (lines 1–5)
2. blank — (line 6)
3. import — `import type { ArsenalReference }` from `./reference-arsenal.types.ts` (line 7)
4. import — `import { SOURCE_TYPES }` from `./reference-arsenal.constants.ts` (line 8)
5. blank — (line 9)
6. comment — line comment (line 10)
7. bind name to function definition — `compositeScore`, exported (lines 11–13)
8. blank — (line 14)
9. comment — line comment (line 15)
10. bind name to function definition — `powerDisplay`, exported (lines 16–20)

## Agent 03
Read 20 lines of 20 total.

1. comment — block comment (file header, lines 1–5)
2. blank — line 6
3. import — type-only import of `ArsenalReference` from `./reference-arsenal.types.ts`
4. import — value import of `SOURCE_TYPES` from `./reference-arsenal.constants.ts`
5. blank — line 9
6. comment — line 10
7. bind name to function definition — `compositeScore`, exported
8. blank — line 14
9. comment — line 15
10. bind name to function definition — `powerDisplay`, exported

## Agent 04
Read 20 lines of 20 total.

1. comment — block comment (lines 1–5)
2. blank — line 6
3. import — `import type { ArsenalReference }` from `./reference-arsenal.types.ts` (type-only import)
4. import — `import { SOURCE_TYPES }` from `./reference-arsenal.constants.ts`
5. blank — line 9
6. comment — line 10
7. bind name to function definition — `compositeScore`, exported
8. blank — line 14
9. comment — line 15
10. bind name to function definition — `powerDisplay`, exported

## Agent 05
Read 20 lines of 20 total.

1. comment — block comment (lines 1–5)
2. blank — line 6
3. import — type-only import of `ArsenalReference` from `./reference-arsenal.types.ts`
4. import — named import of `SOURCE_TYPES` from `./reference-arsenal.constants.ts`
5. blank — line 9
6. comment — line 10
7. bind name to function definition — `compositeScore`, exported
8. blank — line 14
9. comment — line 15
10. bind name to function definition — `powerDisplay`, exported
