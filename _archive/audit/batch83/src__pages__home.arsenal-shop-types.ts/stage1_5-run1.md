# Anchor List — home.arsenal-shop-types.ts

(none)

## Resolution notes

All five agents independently inventoried the file and found no function definitions. The file contains only a block comment, a type-only import, a `type` alias (`ProductType`, line 8), and an `interface` declaration (`ShopState`, lines 10–18). None are top-level callable bindings. The anchor list is empty.
