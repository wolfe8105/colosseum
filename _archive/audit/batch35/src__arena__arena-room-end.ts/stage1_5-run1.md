# Anchor List — arena-room-end.ts

1. endCurrentDebate (line 23)

## Resolution notes
No exclusions. The file exports only one top-level function: `endCurrentDebate`, declared as `export async function` on line 23. All other code is import statements or inner logic within the exported function body. All helper functions (generateScores, applyEndOfDebateModifiers, finalizeDebate, renderPostDebate) are imported, not defined here.
