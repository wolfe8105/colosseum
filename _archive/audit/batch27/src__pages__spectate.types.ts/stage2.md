# Stage 2 Outputs — spectate.types.ts

## Agent 01
The anchor list for this file is empty. There are no top-level function definitions to walk. This file is a pure type-declaration module exporting 9 TypeScript interfaces: SpectateDebate, AICriterion, AISideScores, AIScorecard, ReplayPowerUp, ReplayReference, ReplayModScore, PointAwardMeta, ReplayPointAward, ReplaySpeechEvent, ReplayData, TimelineEntry, DebateMessage, SpectatorChatMessage. No runtime behavior exists.

## Agent 02
Anchor list is empty. No functions to describe. This file contains only TypeScript interface declarations with no runtime content.

## Agent 03
Empty anchor list. Pure types module. No functions to walk. All 14 exported interfaces are ambient type declarations with zero runtime effect.

## Agent 04
No functions in anchor list. File is entirely composed of exported interface declarations. Nothing to describe in runtime terms.

## Agent 05
Anchor list is empty. No callable bindings. Confirming 14 interface exports and zero function definitions in this 179-line file.
