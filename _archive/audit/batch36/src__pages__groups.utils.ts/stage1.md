# Stage 1 Outputs — groups.utils.ts

## Agent 01
Read 106 lines of 106 total.

1. comment — block comment: module header (lines 1–7)
2. blank (line 8)
3. comment — block comment: LANDMINE [LM-GROUPS-001] note (lines 9–16)
4. blank (line 17)
5. import — `import type { GroupListItem } from './groups.types.ts'` (line 18)
6. import — `import { escapeHTML } from '../config.ts'` (line 19)
7. import — `import { CATEGORY_LABELS } from './groups.state.ts'` (line 20)
8. blank (line 21)
9. comment — line comment: `// ── ROLE HELPERS ──…` (line 22)
10. comment — line comment: `// Mirrors group_role_rank() SQL function…` (line 23)
11. bind name to function definition — `clientRoleRank`, exported (lines 24–32)
12. blank (line 33)
13. comment — line comment: `// Roles a caller can assign…` (line 34)
14. comment — line comment: `// Server enforces the full matrix…` (line 35)
15. bind name to function definition — `assignableRoles`, exported (lines 36–42)
16. blank (line 43)
17. bind name to function definition — `roleLabel`, exported (lines 44–52)
18. blank (line 53)
19. comment — line comment: `// ── RENDER HELPERS ──…` (line 54)
20. bind name to function definition — `renderEmpty`, exported (lines 55–61)
21. blank (line 62)
22. bind name to function definition — `renderGroupList`, exported (lines 63–106)

## Agent 02
Read 106 lines of 106 total.

1. comment — block comment: module-level file header (lines 1–7)
2. blank (line 8)
3. comment — block comment: LANDMINE [LM-GROUPS-001] explanation (lines 9–16)
4. blank (line 17)
5. import — `import type { GroupListItem } from './groups.types.ts'` (line 18)
6. import — `import { escapeHTML } from '../config.ts'` (line 19)
7. import — `import { CATEGORY_LABELS } from './groups.state.ts'` (line 20)
8. blank (line 21)
9. comment — line comment: `// ── ROLE HELPERS ──…` (line 22)
10. comment — line comment: `// Mirrors group_role_rank() SQL function…` (line 23)
11. bind name to function definition — `clientRoleRank`, exported (lines 24–32)
12. blank (line 33)
13. comment — line comment: `// Roles a caller can assign via promote_group_member…` (line 34)
14. comment — line comment: `// Server enforces the full matrix…` (line 35)
15. bind name to function definition — `assignableRoles`, exported (lines 36–42)
16. blank (line 43)
17. bind name to function definition — `roleLabel`, exported (lines 44–52)
18. blank (line 53)
19. comment — line comment: `// ── RENDER HELPERS ──…` (line 54)
20. bind name to function definition — `renderEmpty`, exported (lines 55–61)
21. blank (line 62)
22. bind name to function definition — `renderGroupList`, exported (lines 63–106)

## Agent 03
Read 106 lines of 106 total.

1. comment — JSDoc block header (lines 1–7)
2. blank (line 8)
3. comment — JSDoc landmine note LM-GROUPS-001 (lines 9–16)
4. blank (line 17)
5. import — `import type { GroupListItem } from './groups.types.ts'` (line 18)
6. import — `import { escapeHTML } from '../config.ts'` (line 19)
7. import — `import { CATEGORY_LABELS } from './groups.state.ts'` (line 20)
8. blank (line 21)
9. comment — section banner "ROLE HELPERS" (line 22)
10. comment — inline note about SQL mirror (line 23)
11. bind name to function definition — `clientRoleRank`, exported (lines 24–32)
12. blank (line 33)
13. comment — inline note about assignable roles and server enforcement (lines 34–35)
14. bind name to function definition — `assignableRoles`, exported (lines 36–42)
15. blank (line 43)
16. bind name to function definition — `roleLabel`, exported (lines 44–52)
17. blank (line 53)
18. comment — section banner "RENDER HELPERS" (line 54)
19. bind name to function definition — `renderEmpty`, exported (lines 55–61)
20. blank (line 62)
21. bind name to function definition — `renderGroupList`, exported (lines 63–106)

## Agent 04
Read 106 lines of 106 total.

1. comment — block comment: file header (lines 1–7)
2. comment — block comment: LANDMINE [LM-GROUPS-001] note (lines 9–16)
3. import — `import type { GroupListItem } from './groups.types.ts'` (line 18)
4. import — `import { escapeHTML } from '../config.ts'` (line 19)
5. import — `import { CATEGORY_LABELS } from './groups.state.ts'` (line 20)
6. comment — line comment: ROLE HELPERS banner (line 22)
7. comment — line comment: mirrors SQL (line 23)
8. bind name to function definition — `clientRoleRank` (exported) (lines 24–32)
9. comment — line comment: assignable roles (line 34)
10. comment — line comment: server enforces matrix (line 35)
11. bind name to function definition — `assignableRoles` (exported) (lines 36–42)
12. bind name to function definition — `roleLabel` (exported) (lines 44–52)
13. comment — line comment: RENDER HELPERS banner (line 54)
14. bind name to function definition — `renderEmpty` (exported) (lines 55–61)
15. bind name to function definition — `renderGroupList` (exported) (lines 63–106)

## Agent 05
Read 106 lines of 106 total.

1. comment — block comment (lines 1–7): file header / module doc
2. comment — block comment (lines 9–16): LANDMINE [LM-GROUPS-001] note
3. blank — line 17
4. import — `import type { GroupListItem } from './groups.types.ts'` (line 18)
5. import — `import { escapeHTML } from '../config.ts'` (line 19)
6. import — `import { CATEGORY_LABELS } from './groups.state.ts'` (line 20)
7. blank — line 21
8. comment — line comment: ROLE HELPERS (line 22)
9. comment — line comment: Mirrors group_role_rank() SQL (line 23)
10. bind name to function definition — `clientRoleRank`, exported (lines 24–32)
11. blank — line 33
12. comment — line comment: assignable roles (line 34)
13. comment — line comment: server enforces matrix (line 35)
14. bind name to function definition — `assignableRoles`, exported (lines 36–42)
15. blank — line 43
16. bind name to function definition — `roleLabel`, exported (lines 44–52)
17. blank — line 53
18. comment — line comment: RENDER HELPERS (line 54)
19. bind name to function definition — `renderEmpty`, exported (lines 55–61)
20. blank — line 62
21. bind name to function definition — `renderGroupList`, exported (lines 63–106)
