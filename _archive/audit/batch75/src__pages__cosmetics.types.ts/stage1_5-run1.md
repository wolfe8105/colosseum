# Anchor List — cosmetics.types.ts

Source: src/pages/cosmetics.types.ts
Produced by: stage 1.5 arbiter run 1

No function definitions found. The file contains only type aliases (`Category`), interfaces (`CosmeticItem`), and const value bindings (`TABS`, `DEPTH_LABEL`). None of these are top-level, named, callable bindings.

## Resolution notes

- `Category`: type alias — excluded.
- `CosmeticItem`: interface — excluded.
- `TABS`: const array value — not a function; excluded.
- `DEPTH_LABEL`: const Record value — not a function; excluded.
