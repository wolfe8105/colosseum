# Stage 2 Outputs — home.state.ts

No functions in anchor list. Stage 2 has nothing to walk.

This file is a pure state-container module with no callable functions. It exports a `CATEGORIES` array constant and a mutable `state` object. No runtime walk required.
