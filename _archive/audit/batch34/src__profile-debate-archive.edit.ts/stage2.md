# Stage 2 Outputs — profile-debate-archive.edit.ts

## Agent 01

### loadAndRender (line 12)
`loadAndRender` is `async`, takes one parameter `container` of type `HTMLElement`, and returns `Promise<void>`. It reads no module-level state variables. It calls `safeRpc<ArchiveEntry[]>('get_my_debate_archive', {})` with an empty params object and awaits the result, destructuring `{ data, error }`. If `error` is truthy, it sets `container.innerHTML` to `'<div class="dba-empty">Could not load archive.</div>'` and returns early. If `error` is falsy, it calls `setEntries` (imported from `profile-debate-archive.state.ts`) with `(data ?? []) as ArchiveEntry[]` — substituting an empty array if `data` is null/undefined. It then calls `renderTable(container)` (imported from `profile-debate-archive.render.ts`), passing the same `container` element. The return value is `undefined` (implicit `void`). Side effects are: a single RPC network call, a conditional DOM mutation on `container.innerHTML`, a state write via `setEntries`, and a DOM mutation triggered inside `renderTable`.

### showEditSheet (line 22)
`showEditSheet` is synchronous, takes two parameters — `entry` of type `ArchiveEntry` and `container` of type `HTMLElement` — and returns `void`. It reads `entry.custom_name`, `entry.custom_desc`, `entry.hide_from_public`, and `entry.entry_id` from the `entry` argument. It aliases `escapeHTML` (imported from `config.ts`) as local `esc`. It calls `document.createElement('div')` to create an overlay element, sets `overlay.className` to `'dba-picker-overlay'`, then sets `overlay.innerHTML` to a multi-line template literal. Inside the template literal it calls `esc(entry.custom_name ?? '')` and `esc(entry.custom_desc ?? '')` to XSS-sanitize user content for the two input `value` attributes, and uses a ternary on `entry.hide_from_public` to conditionally include `'checked'` in the checkbox. It attaches a click listener on `overlay` itself: if `e.target === overlay` (i.e., a click on the backdrop), it calls `overlay.remove()`. It then calls `document.body.appendChild(overlay)`, mounting the overlay into the live DOM. It queries `#dba-edit-cancel` and attaches a click listener that calls `overlay.remove()`. It queries `#dba-edit-save` and attaches an `async` click listener. That listener reads the trimmed `.value` of `#dba-edit-name` (coercing empty string to `null`), the trimmed `.value` of `#dba-edit-desc` (same coercion), and the `.checked` state of `#dba-edit-hide`. It then queries `#dba-edit-save` again as `saveBtn`, sets `saveBtn.disabled = true` and `saveBtn.textContent = '…'`. It awaits `safeRpc('update_archive_entry', { p_entry_id: entry.entry_id, p_custom_name: name, p_custom_desc: desc, p_hide_from_public: hide })`, destructuring `{ error }`. On error: calls `showToast('Could not save', 'error')`, re-enables `saveBtn.disabled = false`, resets `saveBtn.textContent = 'SAVE'`, and returns. On success: calls `overlay.remove()`, calls `showToast('Saved', 'success')`, then awaits `loadAndRender(container)`. No return value. Side effects: DOM creation and appending, DOM removal (conditionally), two event listener registrations, one RPC call (in async save handler), one or two toast notifications, and a full archive re-fetch + re-render via `loadAndRender`.

### toggleHide (line 79)
`toggleHide` is `async`, takes `entry: ArchiveEntry` and `container: HTMLElement`, returns `Promise<void>`. It reads `entry.hide_from_public` and `entry.entry_id` from the `entry` argument. It computes `newHide` as `!entry.hide_from_public` (a local `boolean`). It awaits `safeRpc('update_archive_entry', { p_entry_id: entry.entry_id, p_hide_from_public: newHide })`, destructuring `{ error }`. If `error` is truthy, it calls `showToast('Could not update', 'error')` and returns early. If `error` is falsy, it calls `showToast(newHide ? 'Hidden from public' : 'Now visible', 'info')` — the ternary on `newHide` selects the toast message — then awaits `loadAndRender(container)`. No return value. Side effects: one RPC call, one toast notification, and (on success) a full archive re-fetch + re-render.

### removeEntry (line 90)
`removeEntry` is `async`, takes `entry: ArchiveEntry` and `container: HTMLElement`, returns `Promise<void>`. It reads `entry.entry_id` from the `entry` argument. The first statement calls the synchronous browser-native `confirm('Remove this debate from your archive?')`; if the return value is falsy (user dismissed), the function returns early immediately with no further side effects. If the user confirmed, it awaits `safeRpc('remove_from_archive', { p_entry_id: entry.entry_id })`, destructuring `{ error }`. If `error` is truthy, it calls `showToast('Could not remove', 'error')` and returns early. If `error` is falsy, it calls `showToast('Removed from archive', 'info')`, then awaits `loadAndRender(container)`. No return value. Side effects: a blocking `confirm` dialog, one RPC call, one toast notification, and (on success) a full archive re-fetch + re-render.

---

## Agent 02

### loadAndRender (line 12)
`loadAndRender` is an `async` function that accepts one parameter, `container` of type `HTMLElement`, and returns `Promise<void>`. It calls `safeRpc<ArchiveEntry[]>('get_my_debate_archive', {})` (imported from `./auth.rpc.ts`), awaiting the result destructured as `{ data, error }`. If `error` is truthy, it writes the string `'<div class="dba-empty">Could not load archive.</div>'` to `container.innerHTML` and returns early. If no error, it calls `setEntries` (imported from `./profile-debate-archive.state.ts`) with `(data ?? []) as ArchiveEntry[]`, coercing the nullish result to an empty array. It then calls `renderTable(container)` (imported from `./profile-debate-archive.render.ts`), passing `container`. No value is returned. Side effects: one RPC call, one conditional DOM mutation on error, one state write (`setEntries`), and one call to `renderTable` which mutates `container`.

### showEditSheet (line 22)
`showEditSheet` is a synchronous function (not `async`) accepting `entry` of type `ArchiveEntry` and `container` of type `HTMLElement`, returning `void`. It aliases `escapeHTML` (imported from `./config.ts`) to the local constant `esc`. It calls `document.createElement('div')` to create `overlay`, sets `overlay.className` to `'dba-picker-overlay'`, and assigns a multi-line template-literal string to `overlay.innerHTML`. Within that template literal, `esc(entry.custom_name ?? '')` and `esc(entry.custom_desc ?? '')` are interpolated for the two input `value` attributes, and `entry.hide_from_public` is checked to conditionally add `'checked'` to the checkbox. It attaches a click listener on `overlay` that calls `overlay.remove()` only when `e.target === overlay` (i.e., click on the backdrop). It calls `document.body.appendChild(overlay)`. It then calls `overlay.querySelector('#dba-edit-cancel')?.addEventListener('click', () => overlay.remove())`. It calls `overlay.querySelector('#dba-edit-save')?.addEventListener('click', async () => { ... })` registering an async handler. Inside that handler: the `#dba-edit-name` input's `.value.trim()` is read and coerced to `null` if empty string, stored as `name`; same pattern for `#dba-edit-desc` stored as `desc`; `#dba-edit-hide` `.checked` boolean is stored as `hide`. A reference to the `#dba-edit-save` button is obtained as `saveBtn`. `saveBtn.disabled` is set to `true` and `saveBtn.textContent` to `'…'`. `safeRpc('update_archive_entry', { p_entry_id: entry.entry_id, p_custom_name: name, p_custom_desc: desc, p_hide_from_public: hide })` is awaited, destructuring `{ error }`. If `error` is truthy, `showToast('Could not save', 'error')` is called, `saveBtn.disabled` is set to `false`, `saveBtn.textContent` set back to `'SAVE'`, and the handler returns early. If no error: `overlay.remove()` is called (removing the button from DOM without re-enabling it — per the documented landmine LM-DBA-002), `showToast('Saved', 'success')` is called, and `loadAndRender(container)` is awaited. Reads `entry.entry_id`, `entry.custom_name`, `entry.custom_desc`, `entry.hide_from_public`. No return value.

### toggleHide (line 79)
`toggleHide` is an `async` function accepting `entry` of type `ArchiveEntry` and `container` of type `HTMLElement`, returning `Promise<void>`. It reads `entry.hide_from_public`, negates it, and stores the result as `newHide` (boolean). It awaits `safeRpc('update_archive_entry', { p_entry_id: entry.entry_id, p_hide_from_public: newHide })`, destructuring `{ error }`. No other fields (`p_custom_name`, `p_custom_desc`) are passed to the RPC — only `p_entry_id` and `p_hide_from_public`. If `error` is truthy, it calls `showToast('Could not update', 'error')` and returns early. If no error, it calls `showToast(newHide ? 'Hidden from public' : 'Now visible', 'info')` — ternary on `newHide` determines the message — then awaits `loadAndRender(container)`. Reads `entry.entry_id`. No return value.

### removeEntry (line 90)
`removeEntry` is an `async` function accepting `entry` of type `ArchiveEntry` and `container` of type `HTMLElement`, returning `Promise<void>`. It calls the synchronous `confirm('Remove this debate from your archive?')` (native browser dialog); if the result is falsy (user cancelled), it returns early with no further action. Otherwise it awaits `safeRpc('remove_from_archive', { p_entry_id: entry.entry_id })`, destructuring `{ error }`. Reads only `entry.entry_id`. If `error` is truthy, it calls `showToast('Could not remove', 'error')` and returns early. If no error, it calls `showToast('Removed from archive', 'info')` then awaits `loadAndRender(container)`. No return value.

---

## Agent 03

### loadAndRender (line 12)
`loadAndRender` is an `async` function that accepts one parameter, `container` of type `HTMLElement`, and returns `Promise<void>`. It reads no module-level state variables directly. It calls `safeRpc<ArchiveEntry[]>('get_my_debate_archive', {})` (imported from `./auth.rpc.ts`), destructuring `data` and `error` from the result, and awaits that call. If `error` is truthy, it mutates `container.innerHTML` to `'<div class="dba-empty">Could not load archive.</div>'` and returns early. If no error, it calls `setEntries` (imported from `./profile-debate-archive.state.ts`) with `(data ?? []) as ArchiveEntry[]`, writing the fetched entries into module-level state. It then calls `renderTable(container)` (imported from `./profile-debate-archive.render.ts`), passing `container` by reference, which performs DOM mutations inside that function. No return value.

### showEditSheet (line 22)
`showEditSheet` is a synchronous function accepting two parameters: `entry` of type `ArchiveEntry` and `container` of type `HTMLElement`. It returns `void`. It reads no module-level state variables directly. It assigns the `escapeHTML` import (from `./config.ts`) to the local alias `esc`. It calls `document.createElement('div')` to create a new `div`, sets its `className` to `'dba-picker-overlay'`, then assigns a template-literal string to `overlay.innerHTML` that builds a bottom-sheet form; the template interpolates `esc(entry.custom_name ?? '')` and `esc(entry.custom_desc ?? '')` into input `value` attributes, and conditionally inserts the string `'checked'` based on `entry.hide_from_public`. It calls `overlay.addEventListener('click', (e) => { if (e.target === overlay) overlay.remove(); })` to close the overlay on backdrop click. It calls `document.body.appendChild(overlay)`, appending the overlay to the live DOM. It calls `overlay.querySelector('#dba-edit-cancel')?.addEventListener('click', () => overlay.remove())` to wire cancel. It calls `overlay.querySelector('#dba-edit-save')?.addEventListener('click', async () => { ... })` with an async handler. Inside that async handler: it queries `#dba-edit-name` input's `.value.trim()` (empty string yields `null`), `#dba-edit-desc` input's `.value.trim()` (same), and `#dba-edit-hide` checkbox's `.checked`; stores results in `name`, `desc`, `hide`. It queries `#dba-edit-save` button as `saveBtn`, sets `saveBtn.disabled = true` and `saveBtn.textContent = '…'`. It calls `await safeRpc('update_archive_entry', { p_entry_id: entry.entry_id, p_custom_name: name, p_custom_desc: desc, p_hide_from_public: hide })` and destructures `error`. If `error` is truthy, it calls `showToast('Could not save', 'error')` (imported from `./config.ts`), re-enables `saveBtn.disabled = false`, restores `saveBtn.textContent = 'SAVE'`, and returns. If no error, it calls `overlay.remove()` (DOM side effect), calls `showToast('Saved', 'success')`, then awaits `loadAndRender(container)`. No return value from the outer function or the handler.

### toggleHide (line 79)
`toggleHide` is an `async` function accepting two parameters: `entry` of type `ArchiveEntry` and `container` of type `HTMLElement`. It returns `Promise<void>`. It reads no module-level state. It computes `newHide` as `!entry.hide_from_public` (boolean flip). It calls `await safeRpc('update_archive_entry', { p_entry_id: entry.entry_id, p_hide_from_public: newHide })`, destructuring `error`. If `error` is truthy, it calls `showToast('Could not update', 'error')` and returns early. If no error, it calls `showToast(newHide ? 'Hidden from public' : 'Now visible', 'info')` — the message is determined by the value of `newHide`. It then awaits `loadAndRender(container)`, which performs the `safeRpc` fetch, `setEntries` state write, and `renderTable` DOM mutation. No return value.

### removeEntry (line 90)
`removeEntry` is an `async` function accepting two parameters: `entry` of type `ArchiveEntry` and `container` of type `HTMLElement`. It returns `Promise<void>`. It reads no module-level state. It calls `confirm('Remove this debate from your archive?')` synchronously; if the user cancels (falsy return), it returns early immediately. If confirmed, it calls `await safeRpc('remove_from_archive', { p_entry_id: entry.entry_id })`, destructuring `error`. If `error` is truthy, it calls `showToast('Could not remove', 'error')` and returns early. If no error, it calls `showToast('Removed from archive', 'info')`, then awaits `loadAndRender(container)`, which performs the RPC fetch, `setEntries` state write, and `renderTable` DOM mutation. No return value.

---

## Agent 04

### loadAndRender (line 12)
`loadAndRender` is an `async` function that takes one parameter `container` of type `HTMLElement` and returns `Promise<void>`. It has no module-level state reads of its own beyond the imported functions. It calls `await safeRpc<ArchiveEntry[]>('get_my_debate_archive', {})`, destructuring `{ data, error }` from the result. If `error` is truthy, it mutates `container.innerHTML` to `'<div class="dba-empty">Could not load archive.</div>'` and returns early. If `error` is falsy, it calls `setEntries((data ?? []) as ArchiveEntry[])` — passing `data` coerced to `ArchiveEntry[]`, or an empty array if `data` is nullish — writing to the module-level state in `profile-debate-archive.state.ts`. It then calls `renderTable(container)`, passing the `container` element. No return value (implicitly `undefined`).

### showEditSheet (line 22)
`showEditSheet` is a synchronous function that takes `entry` of type `ArchiveEntry` and `container` of type `HTMLElement`, returning `void`. It reads no module-level state variables directly. It assigns `escapeHTML` to a local alias `esc`. It calls `document.createElement('div')` to create an overlay element, sets `overlay.className` to `'dba-picker-overlay'`, and assigns a large template-literal string to `overlay.innerHTML`. That template calls `esc(entry.custom_name ?? '')` and `esc(entry.custom_desc ?? '')` for safe insertion, and conditionally adds `'checked'` to the hide checkbox based on `entry.hide_from_public`. The injected HTML contains: a `.dba-picker-sheet` wrapper, `.dba-picker-handle`, `.dba-picker-title`, two `.dba-edit-field` inputs (`#dba-edit-name` maxlength 80, `#dba-edit-desc` maxlength 200), a `.dba-edit-toggle` checkbox (`#dba-edit-hide`), and two action buttons (`#dba-edit-cancel`, `#dba-edit-save`). An event listener on `overlay` for the `'click'` event calls `overlay.remove()` when `e.target === overlay` (backdrop dismiss). It calls `document.body.appendChild(overlay)` to inject the overlay into the DOM. It registers a `'click'` listener on `#dba-edit-cancel` via `overlay.querySelector` that calls `overlay.remove()`. It registers an async `'click'` listener on `#dba-edit-save` that: reads `.value.trim() || null` from `#dba-edit-name` into `name`, from `#dba-edit-desc` into `desc`, and `.checked` from `#dba-edit-hide` into `hide`. It retrieves `saveBtn` via `overlay.querySelector<HTMLButtonElement>('#dba-edit-save')`, sets `saveBtn.disabled = true` and `saveBtn.textContent = '…'`. It then `await`s `safeRpc('update_archive_entry', { p_entry_id: entry.entry_id, p_custom_name: name, p_custom_desc: desc, p_hide_from_public: hide })`, destructuring `{ error }`. If `error` is truthy: calls `showToast('Could not save', 'error')`, sets `saveBtn.disabled = false` and `saveBtn.textContent = 'SAVE'`, then returns. If `error` is falsy: calls `overlay.remove()`, calls `showToast('Saved', 'success')`, and `await`s `loadAndRender(container)` — which re-fetches and re-renders the archive table. No return value.

### toggleHide (line 79)
`toggleHide` is an `async` function taking `entry` of type `ArchiveEntry` and `container` of type `HTMLElement`, returning `Promise<void>`. It reads `entry.hide_from_public` and computes `newHide = !entry.hide_from_public`. It `await`s `safeRpc('update_archive_entry', { p_entry_id: entry.entry_id, p_hide_from_public: newHide })`, destructuring `{ error }`. Note: `p_custom_name` and `p_custom_desc` are not passed — only `p_entry_id` and `p_hide_from_public`. If `error` is truthy: calls `showToast('Could not update', 'error')` and returns early. If `error` is falsy: calls `showToast(newHide ? 'Hidden from public' : 'Now visible', 'info')` — message is conditional on `newHide`. Then `await`s `loadAndRender(container)` to re-fetch and re-render the archive. No return value.

### removeEntry (line 90)
`removeEntry` is an `async` function taking `entry` of type `ArchiveEntry` and `container` of type `HTMLElement`, returning `Promise<void>`. It calls the synchronous `confirm('Remove this debate from your archive?')` — a blocking browser dialog — and returns early without any side effects if the user cancels (falsy result). If the user confirms, it `await`s `safeRpc('remove_from_archive', { p_entry_id: entry.entry_id })`, destructuring `{ error }`. If `error` is truthy: calls `showToast('Could not remove', 'error')` and returns early. If `error` is falsy: calls `showToast('Removed from archive', 'info')`, then `await`s `loadAndRender(container)` to re-fetch and re-render the archive table. No return value.

---

## Agent 05

### loadAndRender (line 12)
`loadAndRender` is `async`, takes one parameter `container` of type `HTMLElement`, and returns `Promise<void>`. It calls `safeRpc<ArchiveEntry[]>('get_my_debate_archive', {})` with an empty params object and destructs the result into `{ data, error }`. If `error` is truthy, it sets `container.innerHTML` to `'<div class="dba-empty">Could not load archive.</div>'` and calls early return. If no error, it calls `setEntries((data ?? []) as ArchiveEntry[])` — passing `data` coerced to `ArchiveEntry[]`, falling back to an empty array if `data` is nullish — which writes to the module-level state in `profile-debate-archive.state.ts`. It then calls `renderTable(container)`, passing the same `HTMLElement`, which mutates the DOM inside `container`. No return value.

### showEditSheet (line 22)
`showEditSheet` is synchronous, takes `entry` of type `ArchiveEntry` and `container` of type `HTMLElement`, returns `void`. It aliases `escapeHTML` as `esc`. It creates a new `<div>` via `document.createElement('div')`, assigns it `className = 'dba-picker-overlay'`, and sets its `innerHTML` to a template-literal sheet containing: a handle bar, a title, two `<input>` elements (`#dba-edit-name` with `maxlength="80"` pre-filled with `esc(entry.custom_name ?? '')`, and `#dba-edit-desc` with `maxlength="200"` pre-filled with `esc(entry.custom_desc ?? '')`), a checkbox `#dba-edit-hide` conditionally given the `checked` attribute when `entry.hide_from_public` is truthy, plus `#dba-edit-cancel` and `#dba-edit-save` buttons. It attaches a `click` listener on the `overlay` itself that calls `overlay.remove()` if `e.target === overlay` (backdrop dismiss). It appends `overlay` to `document.body`. It adds a `click` listener on `#dba-edit-cancel` that calls `overlay.remove()`. It adds an `async` `click` listener on `#dba-edit-save`: that handler reads `.value.trim() || null` from `#dba-edit-name` into `name`, from `#dba-edit-desc` into `desc`, and `.checked` from `#dba-edit-hide` into `hide`. It queries `#dba-edit-save` as an `HTMLButtonElement`, sets `saveBtn.disabled = true` and `saveBtn.textContent = '…'`. It then awaits `safeRpc('update_archive_entry', { p_entry_id: entry.entry_id, p_custom_name: name, p_custom_desc: desc, p_hide_from_public: hide })`. On error: calls `showToast('Could not save', 'error')`, sets `saveBtn.disabled = false`, sets `saveBtn.textContent = 'SAVE'`, and returns. On success: calls `overlay.remove()`, calls `showToast('Saved', 'success')`, and awaits `loadAndRender(container)` — which re-fetches and re-renders the table. The save button is never explicitly re-enabled on the success path (it is removed from the DOM with the overlay). No return value from `showEditSheet` itself; the async save handler returns `Promise<void>` implicitly.

### toggleHide (line 79)
`toggleHide` is `async`, takes `entry: ArchiveEntry` and `container: HTMLElement`, returns `Promise<void>`. It computes `newHide = !entry.hide_from_public`. It awaits `safeRpc('update_archive_entry', { p_entry_id: entry.entry_id, p_hide_from_public: newHide })`, destructing only `{ error }`. If `error` is truthy, it calls `showToast('Could not update', 'error')` and returns early — no DOM mutation. If no error, it calls `showToast(newHide ? 'Hidden from public' : 'Now visible', 'info')`, then awaits `loadAndRender(container)`, which calls `safeRpc('get_my_debate_archive', {})`, writes state via `setEntries`, and re-renders `container` via `renderTable`. No return value.

### removeEntry (line 90)
`removeEntry` is `async`, takes `entry: ArchiveEntry` and `container: HTMLElement`, returns `Promise<void>`. It calls the synchronous `confirm('Remove this debate from your archive?')`; if the user cancels (falsy return), it returns immediately with no side effects. If confirmed, it awaits `safeRpc('remove_from_archive', { p_entry_id: entry.entry_id })`, destructing only `{ error }`. If `error` is truthy, it calls `showToast('Could not remove', 'error')` and returns early. If no error, it calls `showToast('Removed from archive', 'info')`, then awaits `loadAndRender(container)`, which re-fetches the archive via `safeRpc`, updates module-level state via `setEntries`, and re-renders via `renderTable(container)`. No return value.
