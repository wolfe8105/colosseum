# Anchor List — auth.types.ts (Arbiter Run 2)

(empty — no function definitions found)

## Resolution notes
`src/auth.types.ts` is a pure type declaration file. It contains only `import type` statements, `interface` declarations (`SafeRpcResult`, `AuthResult`, `Profile`, `PublicProfile`, `FollowRow`, `ModeratorInfo`, `DebateReference`, `RivalData`, `ProfileUpdate`, `SignUpParams`, `LogInParams`), and one `type` alias (`AuthListener`). There are no callable function definitions with a body anywhere in the file.
