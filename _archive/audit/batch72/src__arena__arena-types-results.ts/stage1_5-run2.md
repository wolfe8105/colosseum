# Anchor List — arena-types-results.ts

No function definitions found in this file.

## Resolution notes

The file contains exclusively type-level declarations: five `interface` declarations (`PowerUpEquipped`, `RankedCheckResult`, `UpdateDebateResult`, `EndOfDebateBreakdown`, `ReferenceItem`) and one `type` alias (`InventoryEffect`). None of these are callable bindings. There are no function declarations, no `const`/`let`/`var` bindings to arrow functions or function expressions, and no async or exported function variants. All five stage-1 agents correctly identified zero function definitions. No candidates required adjudication and no omissions were found on source scan.
