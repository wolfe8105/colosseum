# Anchor List — tournaments.render.ts

1. _timeUntil  (line 11)
2. renderTournamentBanner  (line 24)
3. renderTournamentCard  (line 47)

## Resolution notes

All three confirmed as top-level named callable bindings. `_timeUntil` is a non-exported synchronous function. `renderTournamentBanner` and `renderTournamentCard` are exported functions returning string. No candidates excluded. No additional functions found in direct source scan.
