# Stage 1 Outputs — tournaments.render.ts

## Agent 01
1. comment (lines 1-6)
2. import (line 8: `escapeHTML` from './config.ts')
3. import type (line 9: `Tournament`, `TournamentMatch` from './tournaments.types.ts')
4. blank (line 10)
5. bind name to function definition (_timeUntil, lines 11-18)
6. blank (line 19)
7. comment (lines 20-23)
8. bind name to function definition, exported (renderTournamentBanner, lines 24-42)
9. blank (line 43)
10. comment (lines 44-46)
11. bind name to function definition, exported (renderTournamentCard, lines 47-74)
12. blank (line 75)

## Agent 02
1. Comment (lines 1–6)
2. Import (line 8): `escapeHTML` from './config.ts'
3. Import, type-only (line 9): `Tournament`, `TournamentMatch` from './tournaments.types.ts'
4. Bind name to function definition (lines 11–18): `_timeUntil`
5. Comment (lines 20–23)
6. Bind name to function definition, exported (lines 24–42): `renderTournamentBanner`
7. Comment (lines 44–46)
8. Bind name to function definition, exported (lines 47–74): `renderTournamentCard`

## Agent 03
1. comment (lines 1–6)
2. import (line 8: escapeHTML from './config.ts')
3. import type (line 9: Tournament, TournamentMatch from './tournaments.types.ts')
4. blank (line 10)
5. bind name to function definition (_timeUntil, lines 11–18)
6. blank (line 19)
7. comment (lines 20–23)
8. bind name to function definition, exported (renderTournamentBanner, lines 24–42)
9. blank (line 43)
10. comment (lines 44–46)
11. bind name to function definition, exported (renderTournamentCard, lines 47–74)

## Agent 04
(Line-by-line, condensed summary)
1. Import `escapeHTML`
2. Import types `Tournament`, `TournamentMatch`
3. Bind name to function definition `_timeUntil`
4. Bind name to function definition `renderTournamentBanner` (exported)
5. Bind name to function definition `renderTournamentCard` (exported)

## Agent 05
1. comment
2. import `escapeHTML` from `./config.ts`
3. import type `Tournament`, `TournamentMatch` from `./tournaments.types.ts`
4. bind name to function definition `_timeUntil`
5. comment
6. bind name to function definition and export `renderTournamentBanner`
7. comment
8. bind name to function definition and export `renderTournamentCard`
