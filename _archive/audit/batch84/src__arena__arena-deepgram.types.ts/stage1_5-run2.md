# Anchor List — arena-deepgram.types.ts

No function definitions exist in this file.

## Resolution notes

All five agents correctly identified that the file contains only type definitions and an interface definition, not function definitions. The file exports three type aliases (`TranscriptCallback`, `StatusCallback`, `DeepgramStatus`) and one interface (`DeepgramResult`), none of which are callable function bindings. The source file is purely a type definitions module with no executable functions.
