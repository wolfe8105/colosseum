# Stage 1 Outputs — arena-deepgram.types.ts

## Agent 01

1. comment (lines 1-4)
2. bind name to type — `TranscriptCallback` (line 6, exported)
3. bind name to type — `StatusCallback` (line 7, exported)
4. bind name to type — `DeepgramStatus` (line 8, exported)
5. blank (line 9)
6. bind name to type — `DeepgramResult` (lines 10-20, exported as interface)

## Agent 02

1. **comment** — lines 1–4, block documentation comment
2. **bind name to type** (exported) — line 6, `TranscriptCallback` type alias
3. **bind name to type** (exported) — line 7, `StatusCallback` type alias
4. **bind name to type** (exported) — line 8, `DeepgramStatus` type alias
5. **bind name to type** (exported) — lines 10–20, `DeepgramResult` interface definition
6. **blank** — line 21

## Agent 03

1. comment (lines 1-4: documentation block)
2. bind name to type (line 6: `TranscriptCallback` type alias) — exported
3. bind name to type (line 7: `StatusCallback` type alias) — exported
4. bind name to type (line 8: `DeepgramStatus` type alias) — exported
5. bind name to type (line 10: `DeepgramResult` interface) — exported

## Agent 04

1. **comment** — block comment at lines 1-4
2. **blank** — line 5
3. **bind name to type** (exported) — `TranscriptCallback` type alias at line 6
4. **bind name to type** (exported) — `StatusCallback` type alias at line 7
5. **bind name to type** (exported) — `DeepgramStatus` type alias at line 8
6. **blank** — line 9
7. **bind name to type** (exported) — `DeepgramResult` interface at lines 10-20
8. **blank** — line 21 (file ends)

## Agent 05

1. comment (lines 1–4: block comment)
2. blank (line 5)
3. bind name to type — `TranscriptCallback` (exported, line 6)
4. bind name to type — `StatusCallback` (exported, line 7)
5. bind name to type — `DeepgramStatus` (exported, line 8)
6. blank (line 9)
7. bind name to type — `DeepgramResult` (exported interface, lines 10–20)
8. blank (line 21)
