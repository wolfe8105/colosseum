# Anchor List — debate-landing.types.ts

No function definitions found.

## Resolution notes

The file contains only type definitions (two exported interfaces: `HotTake` and `DebateEntry`). All Stage 1 agents correctly classified these as type bindings rather than function definitions. A direct scan of the source confirms no function declarations, arrow functions, or function expressions exist at the top level. The file is purely a type definition module.
