# Stage 1 Outputs — home.invite-types.ts

## Agent 01

Read 29 lines of 29 total.

1. comment — JSDoc block (lines 1–4)
2. blank — line 5
3. bind name to type — `InviteReward` (interface), exported (lines 6–12)
4. blank — line 13
5. bind name to type — `ActivityEntry` (interface), exported (lines 14–18)
6. blank — line 19
7. bind name to type — `InviteStats` (interface), exported (lines 20–29)

## Agent 02

Read 29 lines of 29 total.

1. **comment** — JSDoc block (lines 1–4)
2. **blank** — line 5
3. **bind name to type** (`InviteReward`), exported — `export interface InviteReward { … }` (lines 6–12)
4. **blank** — line 13
5. **bind name to type** (`ActivityEntry`), exported — `export interface ActivityEntry { … }` (lines 14–18)
6. **blank** — line 19
7. **bind name to type** (`InviteStats`), exported — `export interface InviteStats { … }` (lines 20–29)

## Agent 03

Read 29 lines of 29 total.

1. comment — block comment (lines 1–4)
2. blank — line 5
3. bind name to type — `InviteReward` (exported in place), interface (lines 6–12)
4. blank — line 13
5. bind name to type — `ActivityEntry` (exported in place), interface (lines 14–18)
6. blank — line 19
7. bind name to type — `InviteStats` (exported in place), interface (lines 20–29)

## Agent 04

Read 29 lines of 29 total.

1. **comment** — block comment (lines 1–4)
2. **blank** — line 5
3. **bind name to type** — `InviteReward` (exported interface, lines 6–12)
4. **blank** — line 13
5. **bind name to type** — `ActivityEntry` (exported interface, lines 14–18)
6. **blank** — line 19
7. **bind name to type** — `InviteStats` (exported interface, lines 20–29)

## Agent 05

Read 29 lines of 29 total.

1. comment — block comment (lines 1–4)
2. blank — line 5
3. bind name to type — `InviteReward` (exported interface, lines 6–12)
4. blank — line 13
5. bind name to type — `ActivityEntry` (exported interface, lines 14–18)
6. blank — line 19
7. bind name to type — `InviteStats` (exported interface, lines 20–29)
