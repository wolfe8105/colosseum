# Stage 1 Outputs — staking.ts

## Agent 01

Read 16 lines of 16 total.

1. comment — block comment (lines 1–11)
2. blank — line 12
3. re-export — `export type { StakeResult, PoolData, UserStake, Odds, SettleResult }` from `./staking.types.ts` (line 13)
4. re-export — `export { placeStake, getPool, settleStakes, getOdds }` from `./staking.rpc.ts` (line 14)
5. re-export — `export { renderStakingPanel }` from `./staking.render.ts` (line 15)
6. re-export — `export { wireStakingPanel }` from `./staking.wire.ts` (line 16)

## Agent 02

Read 16 lines of 16 total.

1. **comment** — block comment (lines 1–11)
2. **blank** — line 12
3. **re-export** — `export type { StakeResult, PoolData, UserStake, Odds, SettleResult }` from `./staking.types.ts` (line 13)
4. **re-export** — `export { placeStake, getPool, settleStakes, getOdds }` from `./staking.rpc.ts` (line 14)
5. **re-export** — `export { renderStakingPanel }` from `./staking.render.ts` (line 15)
6. **re-export** — `export { wireStakingPanel }` from `./staking.wire.ts` (line 16)

## Agent 03

Read 16 lines of 16 total.

1. comment — block comment (lines 1–11)
2. blank — line 12
3. re-export — `export type { StakeResult, PoolData, UserStake, Odds, SettleResult }` from `./staking.types.ts`
4. re-export — `export { placeStake, getPool, settleStakes, getOdds }` from `./staking.rpc.ts`
5. re-export — `export { renderStakingPanel }` from `./staking.render.ts`
6. re-export — `export { wireStakingPanel }` from `./staking.wire.ts`

## Agent 04

Read 16 lines of 16 total.

1. comment — block comment spanning lines 1–11
2. blank — line 12
3. re-export — `export type { StakeResult, PoolData, UserStake, Odds, SettleResult }` from `./staking.types.ts`
4. re-export — `export { placeStake, getPool, settleStakes, getOdds }` from `./staking.rpc.ts`
5. re-export — `export { renderStakingPanel }` from `./staking.render.ts`
6. re-export — `export { wireStakingPanel }` from `./staking.wire.ts`

## Agent 05

Read 16 lines of 16 total.

1. **comment** — block comment spanning lines 1–11 (JSDoc/header block)
2. **blank** — line 12
3. **re-export** — `export type { StakeResult, PoolData, UserStake, Odds, SettleResult }` from `./staking.types.ts`
4. **re-export** — `export { placeStake, getPool, settleStakes, getOdds }` from `./staking.rpc.ts`
5. **re-export** — `export { renderStakingPanel }` from `./staking.render.ts`
6. **re-export** — `export { wireStakingPanel }` from `./staking.wire.ts`
