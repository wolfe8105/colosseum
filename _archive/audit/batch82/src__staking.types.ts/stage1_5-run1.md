# Anchor List — staking.types.ts

*(empty — no function definitions exist in this file)*

## Resolution notes

The file is a pure type-declaration module. All entries are either comments, blank lines, or `bind name to type` (exported interfaces: `StakeResult`, `PoolData`, `UserStake`, `Odds`, `SettleResult`). No agent classified any entry as a function definition. Direct source scan confirms: no function declarations, no arrow function bindings, no class definitions. All five interfaces are excluded per the rule "type signatures and interface method declarations."
