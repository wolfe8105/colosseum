# Stage 2 Outputs — arena.ts

## Agent 01

### showPowerUpShop

`showPowerUpShop` is async and takes no parameters. The barrel version in `arena.ts` (lines 41–44) dynamically imports `./arena/arena-lobby.ts` on first call (lazy-loading the lobby chunk), destructures the real `showPowerUpShop` from it as `fn`, calls `fn()` synchronously, and returns. The inner `fn()` call is NOT awaited — so the outer wrapper's promise resolves as soon as the dynamic import completes, before any of the real implementation's async or DOM work runs. Any rejection from `fn()` is silently dropped by the wrapper. There are no branches, no error paths, and no try/catch in the wrapper itself.

The real implementation in `arena-lobby.ts` is also async. On entry it calls `getCurrentUser()` and `isPlaceholder()`. If there is no current user and `isPlaceholder()` returns false, it immediately assigns `window.location.href` to `'moderator-plinko.html'` and returns. Otherwise it calls `set_view('powerUpShop')` and `pushArenaState('powerUpShop')`. It reads `getCurrentProfile()?.token_balance`, casts it with `Number()` (defaulting to `0`), then `await`s `renderShop(tokenBalance)` from `src/powerups.ts`. Once the HTML resolves, it writes it into `screenEl.innerHTML` (full DOM replacement). It wires a click listener on `#powerup-shop-back` that calls `renderLobby()`. It then queries all `.powerup-buy-btn` elements and attaches an async click handler to each: on click the button is disabled and text set to `'...'`, then `buyPowerUp(id, 1, cost)` is awaited. On success it calls `showToast()` and recursively calls `showPowerUpShop()` to re-render. On failure it calls `showToast()` with the error and re-enables the button. There is no try/catch around the `await renderShop()` or `await buyPowerUp()` calls.

## Agent 02

### showPowerUpShop

`showPowerUpShop` is declared twice: once in `src/arena.ts` (the barrel, line 41) as a thin async dynamic-import shim, and once as the real implementation in `src/arena/arena-lobby.ts` (line 233). The barrel version dynamically imports `arena-lobby.ts`, then immediately calls the inner `fn()` and returns. The real implementation is also async. On entry it calls `getCurrentUser()` and `isPlaceholder()`; if the user is not logged in and `isPlaceholder()` is false, it sets `window.location.href` to `moderator-plinko.html` and returns immediately. If that check passes, it calls `set_view('powerUpShop')` on arena state, then `pushArenaState('powerUpShop')` to push a browser history entry. It reads `getCurrentProfile()?.token_balance`, casts it with `Number()`, defaulting to `0`, then `await`s `renderShop(tokenBalance)` from `src/powerups.ts`. If `screenEl` exists, it sets `screenEl.innerHTML` to a wrapper div containing a back button and the awaited shop HTML. It queries `#powerup-shop-back` and attaches a click listener calling `renderLobby()`. Next it calls `document.querySelectorAll('.powerup-buy-btn')` and attaches an async click listener to each. That listener reads `dataset.id` and `dataset.cost`, disables the button, sets its text to `'...'`, then `await`s `buyPowerUp(id, 1, cost)`. On success it calls `showToast(...)` and recursively calls `showPowerUpShop()` to re-render. On failure it calls `showToast(result.error || 'Purchase failed')`, re-enables the button, and restores its text. There is no try/catch around the buy listener or the outer function body.

## Agent 03

### showPowerUpShop

`showPowerUpShop` takes no parameters and returns `Promise<void>`. In `arena.ts` it is a thin async wrapper that dynamically imports `./arena/arena-lobby.ts` on first call (lazy-loading the lobby chunk), destructures the real `showPowerUpShop` from it, calls it, and resolves — so the first invocation pays the import cost; subsequent calls hit the module cache. The real implementation in `arena-lobby.ts` begins with an auth guard: it calls `getCurrentUser()` and `isPlaceholder()` (both synchronous reads from in-memory state); if the user is not logged in and not in placeholder mode it immediately hard-navigates to `moderator-plinko.html` and returns. Otherwise it calls `set_view('powerUpShop')` and `pushArenaState('powerUpShop')` to update arena state and browser history, then reads `getCurrentProfile()?.token_balance` synchronously, casts it to a number, and `await`s `renderShop(tokenBalance)` from `src/powerups.ts`. Once the HTML resolves, it writes it directly into `screenEl.innerHTML` (full DOM replacement), inserting a BACK button and the shop markup. It wires a click listener on `#powerup-shop-back` calling `renderLobby()`. Finally it queries all `.powerup-buy-btn` elements and attaches an async click handler to each: on click the button is disabled and text set to `'...'`, then `buyPowerUp(id, 1, cost)` is awaited; on success it calls `showToast()` and re-enters `showPowerUpShop()` recursively to re-render with updated balance; on failure it calls `showToast()` with the error and re-enables the button. There is no try/catch around the `await renderShop()` call or the `await buyPowerUp()` call.

## Agent 04

### showPowerUpShop

`showPowerUpShop` takes no parameters and returns `Promise<void>`. The barrel version in `src/arena.ts` (lines 41–44) `await`s a dynamic `import()` of `arena-lobby.ts` (lazy chunk), destructures the real function as `fn`, then calls it synchronously — `fn()` is NOT awaited. Any rejection from the inner async function is silently dropped by the wrapper. Callers who `await` the wrapper only wait for the module load, not for the inner async work.

The real implementation in `arena-lobby.ts` (line 233): Step 1 — auth gate: calls `getCurrentUser()` and `isPlaceholder()`; if no user and not placeholder, redirects `window.location.href = 'moderator-plinko.html'` and returns. Step 2 — state mutations: `set_view('powerUpShop')` writes the arena view state; `pushArenaState('powerUpShop')` pushes a browser history entry. Step 3 — reads `Number(getCurrentProfile()?.token_balance) || 0` from cached profile. Step 4 — `const shopHtml = await renderShop(tokenBalance)` — the only awaited call (async, likely RPC into `src/powerups.shop.ts`). Step 5 — DOM write: if `screenEl` non-null, fully replaces `screenEl.innerHTML` with a wrapper containing a BACK button and `shopHtml`. Step 6 — wires `#powerup-shop-back` click → `renderLobby()`. Step 7 — wires each `.powerup-buy-btn` with an async handler: reads `data-id` and `data-cost`, disables button, sets text to `'...'`, awaits `buyPowerUp(id!, 1, cost)`; on success calls `showToast` + recursive `showPowerUpShop()` for re-render; on failure calls `showToast(result.error || 'Purchase failed')`, re-enables button, restores label. No `try/catch` around `buyPowerUp` — thrown error leaves button permanently disabled.

## Agent 05

### showPowerUpShop

`showPowerUpShop` takes no parameters, returns `Promise<void>`. The barrel wrapper (arena.ts lines 41–44): `await`s dynamic import of `arena-lobby.ts` (lazy chunk — not bundled eagerly), destructures real function as `fn`, calls `fn()` synchronously without awaiting it. The wrapper's promise resolves as soon as the module load completes, before any inner async work finishes. Callers treating the wrapper as a completion signal get incorrect behavior.

Real implementation (arena-lobby.ts line 233): Auth gate — calls `getCurrentUser()` and `isPlaceholder()` (sync, in-memory reads); if no user and not placeholder, redirects to `moderator-plinko.html` and returns. State writes: `set_view('powerUpShop')` (arena-state), `pushArenaState('powerUpShop')` (history.pushState). Reads `Number(getCurrentProfile()?.token_balance) || 0` from cached profile. Awaits `renderShop(tokenBalance)` (likely RPC via `src/powerups.shop.ts`) — the sole async suspension. On resolution: if `screenEl` non-null, fully replaces `screenEl.innerHTML` with wrapper div + shopHtml. Wires `#powerup-shop-back` click → `renderLobby()`. Wires each `.powerup-buy-btn` with async click handler: reads `data-id`, `Number(data-cost)`, disables button, sets text to `'...'`, awaits `buyPowerUp(id!, 1, cost)` (safeRpc-based RPC in powerups.rpc.ts); on success: `showToast('Power-up purchased! 🎉')` + recursive `showPowerUpShop()` (re-triggers full render, fresh RPC); on failure: `showToast(result.error || 'Purchase failed')`, re-enables button, restores `${cost} 🪙` label. No try/catch around `buyPowerUp` — thrown error leaves button stuck disabled (disable-button-no-finally pattern from audit sweep).
