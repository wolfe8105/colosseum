# Anchor List — rivals-presence.ts

Source: src/rivals-presence.ts
Produced by: stage 1.5 (arbiter + optional reconciliation)
Unresolved items: 0

1. init  (line 45)
2. destroy  (line 65)

## Resolution notes
Both runs agreed. `rivalsPresence` excluded (object literal, not a function). `PresencePayload` excluded (interface). All module-level state bindings excluded (non-function values). Inner `onAlert` arrow (line 60) excluded (inner callback, not top-level named binding).
