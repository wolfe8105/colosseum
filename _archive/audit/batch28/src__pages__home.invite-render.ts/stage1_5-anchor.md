# Anchor List — home.invite-render.ts

1. renderInvite  (line 11)

## Arbiter consensus
- Run 1 and Run 2: identical. No disagreements.
- arbiter_runs: 2 | runs_agreed: true | unresolved_count: 0
- renderInvite: only exported function at module scope (lines 11-98) — included.
- Internal variable bindings (converts, nextMilestone, progressPct, headlineTo, unclaimedHtml, activityHtml): all inside renderInvite body, not module-level — excluded.
- Imports: dependencies, not callable exports — excluded.
