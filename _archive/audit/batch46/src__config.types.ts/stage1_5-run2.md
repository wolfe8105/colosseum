# Anchor List — config.types.ts

(empty — no top-level callable function definitions in this file)

## Resolution notes

All five agents unanimously identified only `bind name to type` operations (interfaces and type aliases) in this file. No candidate function definitions exist. Direct scan of source confirms: the file contains only type and interface declarations with no runtime callables. Anchor list is empty.
