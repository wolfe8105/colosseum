# Anchor List — reference-arsenal.constants.ts

Source: src/reference-arsenal.constants.ts
Produced by: stage 1.5 (arbiter + optional reconciliation)
Unresolved items: 0

(No function definitions — file contains only type imports and exported constant object/array literals)

## Resolution notes

Both arbiter runs agreed. The file contains five exported const value bindings (SOURCE_TYPES, CATEGORIES, CATEGORY_LABELS, RARITY_COLORS, CHALLENGE_STATUS_LABELS) and one type-only import. None are callable functions. There is also a TODO comment on line 29 noting that RARITY_COLORS contains hardcoded hex values rather than CSS var tokens.
