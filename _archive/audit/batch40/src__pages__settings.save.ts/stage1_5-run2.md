# Anchor List — settings.save.ts

1. saveSettings (line 15)

## Resolution notes

All five agents agreed. `saveSettings` is the only exported function definition. All other top-level bindings are imports and a const value binding.
