# Anchor List — powerups.shop.ts

1. renderShop  (line 12)

## Resolution notes
- All five agents agreed on `renderShop` as the sole function. Confirmed at line 12: `export function renderShop(tokenBalance: number): string`.
- The `.map()` callback at line 14 is an inner arrow-function callback — excluded.
- No other top-level function definitions exist.
