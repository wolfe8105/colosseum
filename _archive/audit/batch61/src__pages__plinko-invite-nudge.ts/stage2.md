# Stage 2 Outputs — plinko-invite-nudge.ts

## Agent 01

### injectInviteNudge

`injectInviteNudge` is an `async` function that takes no parameters and returns `Promise<void>`. It reads the DOM immediately on entry, querying for an element with id `step-5` and an element with id `plinko-invite-nudge`. If `step-5` is absent or if the nudge element already exists in the DOM, the function returns early with no further action.

If both guard conditions pass, the function dynamically imports `safeRpc` from `../auth.ts` (awaited, deferred until this point). It then awaits a `safeRpc` call to the RPC `get_my_invite_link` with an empty parameter object, and reads `result.data.url` into the local variable `inviteUrl`. The entire import-and-RPC block is wrapped in a `try/catch` with an empty catch body, so any thrown error (network failure, auth error, import failure) is silently swallowed and `inviteUrl` remains `null`. After the try/catch, if `inviteUrl` is still `null` the function returns early with no DOM mutation.

If a non-null `inviteUrl` was obtained, the function creates a `div` element, assigns it the id `plinko-invite-nudge`, applies inline `cssText` for layout and visual styling, and sets its `innerHTML` to a static template containing a heading, a subtitle, and a button with id `plinko-invite-copy`. The static template text is not derived from user-supplied data and is not passed through `escapeHTML`. The completed element is appended to `step-5` via `appendChild`.

The function then queries the newly appended element for `#plinko-invite-copy` and attaches an async click listener via `addEventListener`. The listener captures `inviteUrl` from the enclosing scope as a non-null assertion. On click, it re-queries `#plinko-invite-copy` from `nudgeEl` and casts it to `HTMLButtonElement`. Inside a `try` block, it awaits `navigator.clipboard.writeText(inviteUrl)`. On success, it sets `btn.textContent` to `'✓ COPIED!'` and registers a `setTimeout` (fire-and-forget, 2500 ms) to restore the original label. If `navigator.clipboard.writeText` throws (e.g., permission denied), the `catch` block creates a `textarea` element off-screen at `left: -9999px`, sets its value to `inviteUrl`, appends it to `document.body`, calls `.select()` and then `document.execCommand('copy')` (the legacy clipboard API), removes the element, and then applies the same `'✓ COPIED!'` label change and 2500 ms timeout to restore the label. Both the success path and the fallback path produce identical visible feedback to the user.

## Agent 02

### injectInviteNudge

`injectInviteNudge` is an `async` function that takes no parameters and returns `Promise<void>`. It reads the DOM on entry, querying for an element with id `step-5` and for a pre-existing element with id `plinko-invite-nudge`. If either `step-5` is absent or `plinko-invite-nudge` already exists, the function returns immediately with no further action — this is the deduplication guard.

If neither early-return condition applies, the function dynamically imports `../auth.ts` (a deferred import, awaited inline) and calls `safeRpc('get_my_invite_link', {})`, also awaited. The `data` field of the result is cast to `{ url?: string } | null`, and `inviteUrl` is set to `data?.url` or `null`. The entire network sequence is wrapped in a `try/catch` block with an empty catch body, meaning any thrown error — including import failure, network error, or RPC error — is silently swallowed and execution continues with `inviteUrl` remaining `null`. After the try/catch, if `inviteUrl` is still `null` (either because the catch fired or the RPC returned no URL), the function returns early with no DOM mutations.

When `inviteUrl` is a non-null string, the function creates a `<div>` element, assigns it the id `plinko-invite-nudge`, sets its inline style directly via `cssText`, and assigns a static `innerHTML` block containing two display `<div>` elements and a `<button>` with id `plinko-invite-copy`. The content in `innerHTML` is entirely static strings — no user-supplied data is interpolated, so there is no XSS surface here. The nudge element is then appended as a child of `step-5`.

Finally, the function queries the newly created element for `#plinko-invite-copy` and attaches a `click` event listener as a fire-and-forget async callback (not awaited by `injectInviteNudge` itself). When the button is clicked, the listener first tries `navigator.clipboard.writeText(inviteUrl!)`, which is awaited. On success, it sets the button's `textContent` to `'✓ COPIED!'` and schedules a `setTimeout` to restore it to `'📋 COPY INVITE LINK'` after 2500 ms. If `navigator.clipboard.writeText` throws (e.g., the Clipboard API is unavailable or permission is denied), the catch block creates a `<textarea>`, sets its `value` to `inviteUrl!`, appends it to `document.body` with off-screen positioning, calls `ta.select()` followed by the deprecated `document.execCommand('copy')`, removes the textarea from the DOM, and then applies the same `'✓ COPIED!'` / 2500 ms restore sequence. There is no error path if `execCommand('copy')` itself fails — its return value is not checked and no catch wraps it. The `inviteUrl` variable captured in the click closure is the non-null value confirmed before the DOM was built; it is asserted non-null with `!` at each clipboard call site.

## Agent 03

### injectInviteNudge

`injectInviteNudge` is an async function that takes no parameters and returns `Promise<void>`. It reads from the DOM on entry, querying for an element with id `step-5` and an element with id `plinko-invite-nudge`. If `step-5` does not exist, or if the nudge element already exists (indicating the function has already run), it returns immediately without doing anything further.

If neither early-return condition is met, the function enters a `try/catch` block where it dynamically imports `safeRpc` from `../auth.ts` and then awaits `safeRpc('get_my_invite_link', {})`. It casts `result.data` to `{ url?: string } | null` and assigns `data?.url ?? null` to the local `inviteUrl` variable. If the dynamic import or the RPC call throws for any reason, the catch block silently discards the error and `inviteUrl` remains `null`. After the try/catch, if `inviteUrl` is still `null`, the function returns early a second time.

If a non-null `inviteUrl` was obtained, the function creates a `<div>` element, assigns it the id `plinko-invite-nudge`, applies inline CSS for a card-style appearance, and sets its `innerHTML` to a template containing a gift emoji header, a "BRING YOUR FRIENDS" heading, a short reward description, and a `<button>` with id `plinko-invite-copy`. The `innerHTML` assignment contains no user-supplied data — all strings are static literals — so there is no XSS concern here. The new element is then appended as a child of `step-5`.

After appending, the function attaches a `click` event listener to the `#plinko-invite-copy` button via `nudgeEl.querySelector`. The listener is itself an async function. Inside the listener, it first tries `navigator.clipboard.writeText(inviteUrl!)` (using the non-null assertion on the closure variable). If that call resolves, it sets `btn.textContent` to `'✓ COPIED!'` and schedules a `setTimeout` for 2500 ms to restore the original label `'📋 COPY INVITE LINK'`; the timeout is fire-and-forget with no stored handle. If `navigator.clipboard.writeText` rejects — for example, when the Clipboard API is unavailable or permission is denied — the catch block falls back to creating a `<textarea>`, setting its `value` to `inviteUrl!`, appending it off-screen to `document.body`, calling `ta.select()` followed by the deprecated `document.execCommand('copy')`, then removing the textarea. After the fallback copy, it sets the same `'✓ COPIED!'` text and schedules the same 2500 ms restore timeout. Both the happy-path and fallback-path produce identical visible feedback to the user. The outer `injectInviteNudge` function does not await the click listener; it returns `undefined` (resolving its promise) immediately after the `addEventListener` call.

## Agent 04

### injectInviteNudge

`injectInviteNudge` is an `async` function that returns `Promise<void>`. It takes no parameters and reads no module-level state.

It begins by querying the DOM for an element with id `step-5` and a second element with id `plinko-invite-nudge`. If either `step-5` is absent or `plinko-invite-nudge` already exists, the function returns immediately with no further action (idempotency guard). Assuming both conditions pass, it enters a `try/catch` block: it dynamically imports `safeRpc` from `../auth.ts` (an `await`ed dynamic import), then calls `await safeRpc('get_my_invite_link', {})`. The returned `result.data` is cast to `{ url?: string } | null` and the `url` property is extracted into `inviteUrl`. If any exception is thrown during the import or the RPC call, the `catch` block silently swallows it and `inviteUrl` stays `null`. After the `try/catch`, if `inviteUrl` is still `null`, the function returns early and writes nothing to the DOM.

If `inviteUrl` is non-null, the function creates a `<div>` element, sets its `id` to `plinko-invite-nudge`, applies inline `cssText` styles directly, and sets its `innerHTML` to a static template string containing a gift emoji, a heading, a descriptor paragraph, and a `<button>` with id `plinko-invite-copy`. The `innerHTML` assignment contains no user-supplied content — all text is hardcoded — so no `escapeHTML` call is present or needed. The `nudgeEl` is then appended as a child of the `step-5` DOM node.

After appending, the function queries `nudgeEl` for the button by id and, if found, attaches an `async` click event listener. When the button is clicked, the listener captures a reference to the button, then `await`s `navigator.clipboard.writeText(inviteUrl!)` inside a `try` block. On success it sets `btn.textContent` to `'✓ COPIED!'` and schedules a `setTimeout` (2500 ms, fire-and-forget) to reset it to the original label. If `navigator.clipboard.writeText` throws (e.g., the Clipboard API is unavailable or permission is denied), the `catch` block creates a `<textarea>`, sets its `value` to `inviteUrl!`, positions it off-screen with fixed styling, appends it to `document.body`, calls `ta.select()`, then calls the deprecated `document.execCommand('copy')` as a fallback. It then removes the textarea from the DOM and applies the same `'✓ COPIED!'` label change followed by the same 2500 ms `setTimeout` reset. The listener does not re-throw or surface errors to the caller in either path.

## Agent 05

### injectInviteNudge

`injectInviteNudge` is an async function that takes no parameters and returns `Promise<void>`. It reads from the DOM and from a remote RPC, writes to the DOM, and registers a click listener.

On entry it calls `document.getElementById('step-5')` and `document.getElementById('plinko-invite-nudge')`. If the `step-5` element does not exist, or if an element with id `plinko-invite-nudge` is already present (idempotency guard), the function returns immediately with no further action. Otherwise it proceeds into a `try/catch` block that dynamically imports `safeRpc` from `../auth.ts` using `await import(...)`, then awaits `safeRpc('get_my_invite_link', {})`. The resolved `result.data` is cast to `{ url?: string } | null` and the `url` property is extracted into `inviteUrl` via optional chaining and nullish coalescing. If the dynamic import or the RPC call throws for any reason, the catch block silently swallows the error (the comment reads "non-blocking") and `inviteUrl` remains `null`. After the try/catch, if `inviteUrl` is still `null` — whether because the RPC returned no URL or because an error was caught — the function returns early, making the entire UI injection conditional on receiving a valid URL.

If `inviteUrl` is non-null, the function creates a `<div>` element, assigns it the id `plinko-invite-nudge`, applies inline CSS (margin, padding, semi-transparent background, border, border-radius, centered text) via `cssText`, and sets its `innerHTML` to a template literal containing a gift emoji, a heading, a description paragraph, and a `<button id="plinko-invite-copy">`. The template literal content does not interpolate any user-supplied data, only static strings and CSS variable references. The nudge element is then appended to `step5` via `appendChild`.

After appending, the function queries the newly appended element for `#plinko-invite-copy` and attaches an async click listener via `addEventListener`. The listener captures `inviteUrl` from the outer scope through closure. On each click it re-queries `#plinko-invite-copy` inside `nudgeEl` and casts it to `HTMLButtonElement`. It then attempts `await navigator.clipboard.writeText(inviteUrl!)` inside its own `try/catch`. If that succeeds, it sets `btn.textContent` to `'✓ COPIED!'` and schedules a `setTimeout` to reset it to `'📋 COPY INVITE LINK'` after 2500 ms. If `navigator.clipboard.writeText` throws (e.g., the Clipboard API is unavailable or permission is denied), the catch block executes a legacy fallback: it creates a `<textarea>`, sets its `value` to `inviteUrl!`, positions it off-screen with `position:fixed;left:-9999px;`, appends it to `document.body`, calls `ta.select()`, calls the deprecated `document.execCommand('copy')`, removes the textarea from the DOM with `ta.remove()`, and then performs the same button text update and `setTimeout` reset as the happy path. The `setTimeout` calls in both branches are fire-and-forget; no return value is produced.
