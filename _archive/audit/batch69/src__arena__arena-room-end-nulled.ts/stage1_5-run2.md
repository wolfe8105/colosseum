# Anchor List — arena-room-end-nulled.ts

1. renderNulledDebate (line 12)

## Resolution notes

- All five agents agreed on `renderNulledDebate` (lines 12-40) as the only top-level function.
- Agents 03, 04, 05 incorrectly listed body statements as top-level — excluded.
- Async arrow callback at line 36 is an inline addEventListener callback — excluded.
