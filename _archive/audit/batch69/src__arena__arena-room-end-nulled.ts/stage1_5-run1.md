# Anchor List — arena-room-end-nulled.ts

1. renderNulledDebate (line 12)

## Resolution notes

- All five agents correctly identified `renderNulledDebate` as the single top-level exported function.
- Agents 03, 04, 05 listed internal function body statements as top-level — excluded.
- Inline async callback at line 36 (addEventListener) is an inner callback — excluded.
