# Stage 2 Outputs — arena-feed-realtime.ts

## Agent 01

### subscribeRealtime (line 38)

The `subscribeRealtime` function is an exported async function that takes a `debateId` parameter (string) and returns a Promise that resolves to void. Entry guards check for a null Supabase client (from `getSupabaseClient()`) or a placeholder state (via `isPlaceholder()`), both triggering early return. The function then retrieves an access token asynchronously via `getAccessToken(client)` and conditionally calls `setRealtimeAuth(client, accessToken)` if a token exists; if no token exists, a console warning is logged but execution continues with anonymous subscription. A private Realtime channel is then created via `createChannel()` with the format `feed:{debateId}`, and three event listeners are chained via `.on()` calls: first a postgres_changes listener for INSERT events on the `debate_feed_events` table filtered to the specific debate_id that calls `appendFeedEvent()` on each received event; second a broadcast listener for heartbeat events that updates the `lastSeen` object by role with the current timestamp; third a broadcast listener for goodbye events that calls `handleParticipantGone()` with the role. The constructed channel is subscribed to via `.subscribe()` and stored in module state via `set_feedRealtimeChannel(channel)`. Finally, `startHeartbeat()` is called to initiate heartbeat sending and staleness checking. The function has no explicit return statement (implicit void return).

### unsubscribeRealtime (line 87)

The `unsubscribeRealtime` function is an exported non-async function taking no parameters and returning void. It retrieves a Supabase client via `getSupabaseClient()` and guards against null client or null `feedRealtimeChannel` (a module-level state variable) via a compound if condition; when both are truthy, it calls `removeChannel(client, feedRealtimeChannel)` and clears the channel state by calling `set_feedRealtimeChannel(null)`. The function has no explicit return statement (implicit void return) and no other branches or side effects.

## Agent 02

### subscribeRealtime (line 38)

`subscribeRealtime` is an async exported function that accepts a `debateId` string parameter and returns `Promise<void>`. It begins with an early return guard checking that a Supabase client exists via `getSupabaseClient()` and that `isPlaceholder()` returns false; if either check fails, the function returns immediately. Next, it calls `getAccessToken(client)` asynchronously and, if a token is returned, passes it to `setRealtimeAuth(client, accessToken)` to establish JWT context; if no token is returned, it logs a warning about anonymous subscription. The function then calls `createChannel(client, `feed:${debateId}`, { private: true })` to instantiate a private Realtime channel, immediately chains three event listeners onto this channel via `.on()` calls: a `postgres_changes` listener that triggers on INSERT events in the `debate_feed_events` table filtered by `debate_id=eq.${debateId}`, calling `appendFeedEvent(payload.new)` for each new event; a `broadcast` listener for `heartbeat` events that extracts the `role` field from the payload and writes to the module-level `lastSeen` object with `lastSeen[role] = Date.now()` when a role is present; and a `broadcast` listener for `goodbye` events that extracts the role and calls `handleParticipantGone(role)` when a role is present. The channel is then subscribed to via `.subscribe()` and stored in the module state via `set_feedRealtimeChannel(channel)`. Finally, the function calls `startHeartbeat()` to initiate heartbeat sending and staleness checking, and returns once the async subscription is complete.

### unsubscribeRealtime (line 87)

`unsubscribeRealtime` is an exported synchronous function with no parameters that returns `void`. It retrieves the Supabase client via `getSupabaseClient()` and reads the current Realtime channel from module state via `feedRealtimeChannel`. If both the client and the channel are truthy, it calls `removeChannel(client, feedRealtimeChannel)` to unsubscribe and clean up the channel, then clears the module state by calling `set_feedRealtimeChannel(null)` to null out the stored channel reference. If either the client or channel is falsy, no action is taken. The function completes and returns immediately.

## Agent 03

### subscribeRealtime (line 38)

`subscribeRealtime` is an exported async function accepting a single `debateId` parameter of type string and returning `Promise<void>`. It begins with two early-return guards: first checking if `getSupabaseClient()` returns a falsy value or if `isPlaceholder()` is true, returning immediately in either case. It then calls `getAccessToken(client)` and conditionally calls `setRealtimeAuth(client, accessToken)` if an access token is obtained; if no token is returned, it logs a warning to console but continues execution. The function creates a Supabase Realtime channel named `feed:${debateId}` via `createChannel()` with `private: true`, chaining three `.on()` listeners: a `postgres_changes` listener for INSERT events on the `debate_feed_events` table filtered by `debate_id`, which calls `appendFeedEvent(payload.new)` on each event; a `broadcast` listener for `heartbeat` events that extracts the role from the payload and conditionally updates `lastSeen[role]` if a role is present; and a `broadcast` listener for `goodbye` events that extracts the role and calls `handleParticipantGone(role)` if a role exists. The channel is then subscribed to via `.subscribe()`, stored in module state via `set_feedRealtimeChannel(channel)`, and finally `startHeartbeat()` is called to begin heartbeat transmission and staleness checking. The function has a single exit path at the end returning undefined (implicit).

### unsubscribeRealtime (line 87)

`unsubscribeRealtime` is an exported synchronous function with no parameters and return type `void`. It retrieves the Supabase client via `getSupabaseClient()` and proceeds only if both the client is truthy and the module-level `feedRealtimeChannel` variable is truthy; when both conditions are met, it calls `removeChannel(client, feedRealtimeChannel)` to unsubscribe from the channel, then sets the module-level `feedRealtimeChannel` to null via `set_feedRealtimeChannel(null)`. The function has a single exit path returning undefined (implicit), with no branches taken if the initial guard condition fails.

## Agent 04

### subscribeRealtime (line 38)

The function `subscribeRealtime(debateId: string): Promise<void>` is an exported async function that sets up a Supabase Realtime channel subscription for a live debate feed. It begins by retrieving the Supabase client via `getSupabaseClient()` and returns early (void) if the client is null or the session is a placeholder (via `isPlaceholder()`). It then retrieves an access token asynchronously via `getAccessToken(client)`; if a token exists, it calls `setRealtimeAuth(client, accessToken)` to authorize the Realtime connection, otherwise it logs a warning that the subscription will proceed anonymously. The function creates a private Realtime channel via `createChannel(client, 'feed:${debateId}', { private: true })` and attaches three event handlers: a `postgres_changes` handler that listens for INSERT events on the `debate_feed_events` table filtered by `debate_id`, invoking `appendFeedEvent(payload.new)` when a new event arrives; a `broadcast` handler for 'heartbeat' events that updates the `lastSeen[role]` object with the current timestamp if a role is present; and a `broadcast` handler for 'goodbye' events that calls `handleParticipantGone(role)` if a role exists. The channel is then subscribed via `.subscribe()` and stored in module state via `set_feedRealtimeChannel(channel)`. Finally, the function calls `startHeartbeat()` to begin sending heartbeat updates and monitoring staleness.

### unsubscribeRealtime (line 87)

The function `unsubscribeRealtime(): void` is an exported synchronous function that tears down the Realtime subscription. It retrieves the Supabase client via `getSupabaseClient()` and checks whether both the client and the module-level `feedRealtimeChannel` exist; if so, it calls `removeChannel(client, feedRealtimeChannel)` to unsubscribe from the channel and clears the module state by calling `set_feedRealtimeChannel(null)`. The function does not call `stopHeartbeat()` anywhere — it only removes the channel subscription and nullifies the stored channel reference.

## Agent 05

### subscribeRealtime (line 38)

An exported async function that accepts a `debateId` string parameter and returns `Promise<void>`. Entry conditions include checking that the Supabase client is available and that the application is not in placeholder mode, with early return if either guard fails. The function then retrieves an access token via `getAccessToken(client)` and conditionally calls `setRealtimeAuth(client, accessToken)` if the token exists, otherwise logging a warning that an anonymous Realtime subscription will proceed. It creates a Realtime channel via `createChannel(client, `feed:${debateId}`, { private: true })` and chains three event handlers: (1) a `postgres_changes` handler listening for INSERT events on the `debate_feed_events` table filtered by `debate_id=eq.${debateId}`, which calls `appendFeedEvent(payload.new)` on receipt; (2) a `broadcast` handler for 'heartbeat' events that extracts the role from the payload and updates the module-level `lastSeen` object with `lastSeen[role] = Date.now()` if a role exists; (3) a `broadcast` handler for 'goodbye' events that extracts the role and calls `handleParticipantGone(role)` if a role is present. The channel is subscribed via `.subscribe()` and stored in module state via `set_feedRealtimeChannel(channel)`. Finally, `startHeartbeat()` is called to initialize heartbeat sending and staleness checking. The function writes to module-level state (`lastSeen`, the Realtime channel via `set_feedRealtimeChannel`) and makes external calls that interact with the Supabase client and channel subscription mechanism.

### unsubscribeRealtime (line 87)

An exported synchronous function with no parameters that returns `void`. It retrieves the Supabase client and checks that both the client exists and the `feedRealtimeChannel` module-level state is not null. If both conditions are true, it calls `removeChannel(client, feedRealtimeChannel)` to clean up the channel subscription and then calls `set_feedRealtimeChannel(null)` to clear the channel reference from module state. The function has no early return if the client is missing or the channel is null — execution simply completes without performing cleanup. Notably, `stopHeartbeat()` is **not called** anywhere within this function; the heartbeat continues running after unsubscribe completes.
