# Stage 1 Outputs — settings.load.ts

## Agent 01
1. comment — block comment (lines 1–4)
2. blank (line 5)
3. import — `getCurrentUser`, `getCurrentProfile` from `'../auth.ts'`
4. import — `getEl`, `setChecked`, `validateTier`, `TIER_LABELS` from `'./settings.helpers.ts'`
5. import — type-only import of `SettingsData` from `'./settings.helpers.ts'`
6. blank (line 9)
7. bind name to function definition — `loadSettings` (exported)
8. blank (line 82)

## Agent 02
1. comment — block comment (lines 1–4)
2. blank — (line 5)
3. import — `getCurrentUser`, `getCurrentProfile` from `../auth.ts`
4. import — `getEl`, `setChecked`, `validateTier`, `TIER_LABELS` from `./settings.helpers.ts`
5. import — type-only import of `SettingsData` from `./settings.helpers.ts`
6. blank — (line 9)
7. bind name to function definition — `loadSettings`, exported

## Agent 03
1. comment — block comment (lines 1–4)
2. blank (line 5)
3. import — `getCurrentUser`, `getCurrentProfile` from `'../auth.ts'`
4. import — `getEl`, `setChecked`, `validateTier`, `TIER_LABELS` from `'./settings.helpers.ts'`
5. import — type-only import of `SettingsData` from `'./settings.helpers.ts'`
6. blank (line 9)
7. bind name to function definition — `loadSettings` (exported)

## Agent 04
1. **comment** — block comment (lines 1–4)
2. **blank** — (line 5)
3. **import** — `getCurrentUser`, `getCurrentProfile` from `../auth.ts` (line 6)
4. **import** — `getEl`, `setChecked`, `validateTier`, `TIER_LABELS` from `./settings.helpers.ts` (line 7)
5. **import** — type-only import of `SettingsData` from `./settings.helpers.ts` (line 8)
6. **blank** — (line 9)
7. **bind name to function definition** — `loadSettings`, exported (lines 10–81)

## Agent 05
1. **comment** — block comment spanning lines 1–4
2. **blank** — line 5
3. **import** — `getCurrentUser`, `getCurrentProfile` from `'../auth.ts'`
4. **import** — `getEl`, `setChecked`, `validateTier`, `TIER_LABELS` from `'./settings.helpers.ts'`
5. **import** — type-only import of `SettingsData` from `'./settings.helpers.ts'`
6. **blank** — line 9
7. **bind name to function definition** — `loadSettings` (exported)
