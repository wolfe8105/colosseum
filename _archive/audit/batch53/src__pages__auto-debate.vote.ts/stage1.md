# Stage 1 Outputs — auto-debate.vote.ts

## Agent 01
showResults (line 10) — (votesA: number, votesB: number, total: number, aiWinner: string, _userVote: string): void
castVoteImpl (line 35) — async (sb: { rpc: (fn: string, args: Record<string, unknown>) => Promise<{ data: unknown; error: { message: string } | null }> }, side: string, getFingerprint: () => string): Promise<void>

## Agent 02
showResults (line 10) — (votesA: number, votesB: number, total: number, aiWinner: string, _userVote: string): void
castVoteImpl (line 35) — async (sb: { rpc: (fn: string, args: Record<string, unknown>) => Promise<{ data: unknown; error: { message: string } | null }> }, side: string, getFingerprint: () => string): Promise<void>

## Agent 03
showResults (line 10) — (votesA: number, votesB: number, total: number, aiWinner: string, _userVote: string): void
castVoteImpl (line 35) — (sb: { rpc: (fn: string, args: Record<string, unknown>) => Promise<{ data: unknown; error: { message: string } | null }> }, side: string, getFingerprint: () => string): Promise<void>

## Agent 04
showResults (line 10) — (votesA: number, votesB: number, total: number, aiWinner: string, _userVote: string): void
castVoteImpl (line 35) — (sb: { rpc: (fn: string, args: Record<string, unknown>) => Promise<{ data: unknown; error: { message: string } | null }> }, side: string, getFingerprint: () => string): Promise<void>

## Agent 05
showResults (line 10) — (votesA: number, votesB: number, total: number, aiWinner: string, _userVote: string): void
castVoteImpl (line 35) — async (sb: { rpc: (fn: string, args: Record<string, unknown>) => Promise<{ data: unknown; error: { message: string } | null }> }, side: string, getFingerprint: () => string): Promise<void>
