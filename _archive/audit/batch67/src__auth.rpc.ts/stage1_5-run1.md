# Anchor List — auth.rpc.ts

1. safeRpc  (line 21)

## Resolution notes

- `attempt` (line 30): excluded — inner const arrow function defined inside `safeRpc`, not top-level.
- No other function definition candidates. File has exactly one top-level exported function.
