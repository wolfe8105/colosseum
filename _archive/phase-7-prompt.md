# The Moderator — 10-Phase Audit, Prompt 7

**Red-Team Validation**

Clone repo:
`git clone https://TOKEN@github.com/wolfe8105/colosseum.git`

You have permission to use all browser and bash tools without asking for confirmation. Proceed autonomously.

Read `THE-COLISEUM-AI-AUDIT-RESEARCH.md` in the repo root — find the section titled **"PROMPT 7 — Red-Team Validation"** and execute it exactly as written.

Read `phase-0-scope-inventory.md` through `phase-6-agentic.md` for context.

Key priors to carry in:
- `tsconfig.json` excludes `src/` — typecheck is a no-op; type errors in application code are invisible to CI
- No CI pipeline — Vercel deploys direct from push to main; no staging environment
- Only 2 test files for 372 modules — near-zero test coverage
- `cast_auto_debate_vote` RPC: client supplies `p_fingerprint` generated by `Math.random()` — verify whether the RPC has server-side dedup or trusts the client entirely
- `/api/go-respond` is now rate-limited via Upstash Redis (HP-01, commit `f68b4e0`) — red-team the rate limit itself: sliding window parameters, key structure, bypass vectors
- AdSense placeholder IDs (`PUBLISHER_ID`, `AD_UNIT_ID`) are in production source (ESC-M-02) — check what surface they expose
- `(client as any).auth.getSession()` in `arena-feed-realtime.ts:42` suppresses type safety on auth — trace what happens if session is null or expired
- Ref code regex `src/share.ts:158` accepts codes up to 20 chars with uppercase and special chars; generator only produces 5-char lowercase alphanumeric — mismatch is an input-validation gap

When complete, save output as `phase-7-red-team.md` in the repo root, commit it, and push to main. Do not start Prompt 8 — the reviewer will check first.
